# Phase 3 cold sweep — v11.9.4 consolidated findings

Synthesis of 4 independent cold-judge passes against the v11.9.4 build (pickers fixed): **baseline** (`phase3_review_v11_9_4.md`), **reconciliation pragmatist** (`_pragmatist.md`), **skeptical QA** (`_skeptic.md`), **exec/oversight** (`_exec.md`). Each judge was context-isolated (no memory, no docs, screenshots only). "Agreement N/4" = how many lenses independently flagged it. Triage tag = warm-pass judgment of where the fix lives. (Domain specifics stripped — generic terms below; "demo instance" = the L2 instance under test.)

## Cross-judge consensus (ranked)

1. **[BLOCKER · 3/4] Daily Statement summary KPIs don't compute / don't tie to the detail.** Debits & Credits read **0** on every populated account while the Posted Money Records table is full of debit/credit rows; **Drift KPI stuck at 0** even where the other tiles make 0 arithmetically impossible (skeptic recomputed on a pool account: closing − (opening + credits − 0) = −992,605, shown as 0); and the Statement's drift=0 **contradicts the Drift sheet** which shows those same accounts drifting. Pragmatist's root-cause hypothesis: the statement isn't pinned to a single `business_day`, so the per-day KPI and the all-days detail table disagree. → **Real dashboard/SQL bug, newly visible only because the picker fix let the statement populate. This is the new keystone — the two numbers you'd open the dashboard for don't compute.** (`r2_ds_*`, `r1_drift_full.png`)
2. **[BLOCKER · 2/4] "Sasquatch National Bank" placeholder on the Investigation (AML) landing.** Exec ranks this #1 — a placeholder bank name on the compliance surface most likely to be screenshared to examiners/auditors. → **Real config bug** (the deploy's identity isn't overriding the shared-ledger template's default name). (`investigation__inv-sheet-getting-started.png`)
3. **[MAJOR · 3/4] Today's Exceptions count integrity.** KPI = 828 vs table footer "1–50 of **935**"; not actually sorted by magnitude (buries the multi-million ledger_drift rows under magnitude-0 rows); rows span multiple days despite "today." → **Mixed: dashboard bug** (count mismatch + sort order) **+ our-seed-artifact** (the magnitude-0 multi_xor/drift rows = the accepted plant residual we already know about). (`r1_todays_exceptions_full.png`)
4. **[MAJOR · 2/4] Parent/pool drift in the millions with no working drill.** Parent-drift KPI in the millions, large ledger_drift on the pool accounts, while leaf drift is tiny (≤~1,290); "right-click → Daily Statement" dead-ends (depends on #1). → **Largely our-seed-artifact** (pool ledger_drift from the independent-leg fan-out modeling — the item flagged in our generator findings), **but** "child postings don't roll up to parent / no drill path" is a real dashboard concern. (`r1_drift_full.png`, drift-timelines sheet)
5. **[MAJOR · 2/4] Executives Transaction Volume math + presentation.** "Average Daily Volume" ≈ off by ~67× vs total/active-days (skeptic); time axis reads like a multi-week outage + an interior gap (exec). → **Dashboard bug** (avg calc) **+ presentation/our-window** (the gap is our short seed window + weekends — annotate). (`exec-sheet-transaction-volume.png`)
6. **[MAJOR · 1/4 skeptic] "count" overloaded on L2 Exceptions** — "Open L2 Violations = 39" directly above a detail row count = 410. → Real dashboard labeling bug. (`l2ft-sheet-l2-exceptions`)
7. **[MAJOR · 1/4 skeptic+baseline] Rail direction / origin string defects** — an ACH credit-side rail tagged direction = Debit (name/direction mismatch); origin value rendered as a mangled non-word. → Mislabel is a real dashboard/data classification issue; the mangled origin is upstream-side (data is clean — see triage). (`r2_ds_*`)
8. **[MAJOR · 2/4] Per-rail bar-chart x-axis is an unreadable label smear** (both exec "Period Total by Type" charts); "stacked by type" charts show no stacking/legend. → Presentation/dashboard. (`exec-sheet-transaction-volume.png`, `*-money-moved.png`)
9. **[MINOR · multi] Ambiguous empty states / no baselines** — Fanout & Anomalies KPIs = 0 at default sliders (Anomalies KPI "0 flagged" contradicts its own populated σ-distribution chart — possible flag-logic bug); Limit Breach = 0 with no as-of stamp ("clean" vs "didn't run"); Unbundled 802 / Supersession with no dollar exposure; clipped y-axis ("0,000,000"); a flagship "money moved" net figure shown with no context. (`inv-sheet-fanout/anomalies`, `l1-sheet-limit-breach/unbundled-aging/supersession-audit`, `exec-sheet-money-moved`)
10. **[MINOR] App Info shows `dialect: sqlite` / dev prefix** — fine for the dev capture; confirm production shows the real engine. (`*-app-info.png`)

## Works well (consensus across judges)
Overdraft / Pending Aging / Unbundled Aging worklists; the **Drift sheet's** leaf stored-vs-computed-vs-drift math (ties to Drift Timelines); **Money Trail** trace + **Account Network** once populated; the **App Info diagnostic canary** (liveness + matview freshness + deploy stamp); **Account Coverage** (audit-ready); Executives volume/coverage legibility (the bar-composition charts excepted).

## Triage routing (warm pass)
- **Real dashboard/upstream bugs:** #1 (DS KPI/day-pinning — top priority), #2 (placeholder bank name), #3 count mismatch+sort, #5 avg-volume calc, #6 count overload, #7 direction mislabel + mangled-origin display, #8 chart axes/stacking, #9 anomalies flag-logic + missing as-of stamps.
- **Our-seed / instance-side artifacts** (don't ship upstream as dashboard bugs): the magnitude-0 exception rows (#3) and the pool ledger_drift millions (#4) = the known plant-residual / independent-leg fan-out modeling; the multi-week empty stretch + weekend gaps (#5) = the deliberate short seed window.
- **Confirm-on-AWS:** #10 (dialect stamp); the empty-on-default AML sliders (#9) — likely render-on-default vs broken.
- **Mangled origin string (#7)** — RESOLVED: the data's `origin` column holds only the two expected clean values. So the mangled rendering is a **display/transform artifact (or a low-res screenshot misread), upstream/dashboard-side — NOT our data.** (The paired direction mislabel is separate and still worth an upstream look.)
