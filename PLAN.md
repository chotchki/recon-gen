# QuickSight Generator — Active Plan


## Phase DMDN - E2E - App2 e2e coverage for DM/DN Daily Statement features (POLICY-2)

POLICY-2 browser coverage for three App2-only Daily Statement features landed on `feature/dm-dn` (cascade narrowing DM.2, cascade clear-on-source-change DM/BR.1, day-availability decoration DM.3). QS cannot do these → App2-targeted tests, structured-triple gap on the QS driver.

- [x] DMDN-E2E.1 - Add driver verbs: `filter_value(label)` + `day_availability(label)` on `App2Driver`; Protocol stubs in `_drivers/base.py`; `NotImplementedError` (structured-triple) on `QsEmbedDriver`.
- [x] DMDN-E2E.2 - Wire `day_availability_fetcher` through `html2_server` + `App2Driver.serving` + `make_live_db_*` harness so the live L1 server decorates the picker.
- [x] DMDN-E2E.3 - `tests/e2e/app2/test_dm_cascade_and_day_availability.py` — cascade narrowing, cascade clear-on-source-change, day-availability decoration; expectations derived from DB/tree.
- [x] DMDN-E2E.4 - Green through `./run_tests.sh up_to=app2` (DuckDB cfg); pyright + typing-smells clean; commit on `feature/dm-dn`. (matrix `--dialects`/`--targets` flags retired post-CB.17.d — dialect comes from the cfg.)


## Phase BX - L2 Editor cold-read + address cycle (provisional)

Same cold-read → triage → design → implement → re-cold-read pattern that BT/BTa established, applied to the L2 Editor surface (the `/` home + `/l2_shape/<kind>/{list, new, edit}` for the six entity kinds + the three singleton pages for instance / theme / persona). Unlike BT (where the design intent was operator-driven from SPEC), the L2 Editor is **discovery-first** — we haven't pointed a first-time persona at it yet, so we don't know what's broken until BX.0.5 fires. BX.1+ implementation cells get enumerated by BX.0.7's REPLAN after operator triage.

**Relationship to in-flight Phase BF:** BF.0-5 + BF.10 address known L2-editor defects (BB.2 sub-form completeness, structured pickers for textareas, AI.2.e subtype banner). BX.0.6 triage may obsolete some of these (if the cold-read says they're fine or finds bigger fish) or keep them in flight. Reconciliation lock in BX.0.7.

**Phase BX total: ~35-50h implementation + ~3h cold-read v2.** Independent of BTa surface except BX.12-16 (side-panel-gated). Sequencing: BX.0.8 mockup → BX.1-5 + BX.17 (in parallel with BTa.1) → BX.6-11 (post-mockup) → BX.12-16 (post-BTa.1) → BX.18 cold-read v2.
**Phase BX total estimate:** highly uncertain until BX.0.5 lands. Floor: ~6-8h (discovery + replan + minimal fixes + re-cold-read if surface is mostly clean). Ceiling: ~25-30h (if cold-read surfaces ~20 issues like BT did).
**Sequencing vs BTa:** BX.0 + BX.0.5 can run in parallel with BTa implementation (different file surface — BTa is ETL Support pages, BX cold-read is L2 Editor pages). BX.0.7 onwards waits until BTa has stabilized (to avoid two simultaneous editor-touching phases).
- [x] BX.0 - **REPLAN — COMPLETE 2026-05-30.** Output: `docs/audits/_archive/bx_0_replan.md`. Locked: (a) two-persona cold-read (ETL Engineer bounce-and-fix + L2 author from scratch); (b) full route inventory across the 6 entity kinds + 3 singletons + diagram surface; (c) field-richness ranking driving screenshot priority (rail → transfer_template → chain → limit_schedule → account_template → account); (d) BF reconciliation rules (BF.1-4 likely subsumed by BX.0.7's design pass, BF.5 + BF.10 keep regardless, BF.0 + BF.6 hold pending BX.0.7); (e) sequencing recommendation — fire BX.0.5b (author flow) now since it's independent of BTa; defer BX.0.5a until BTa.2.4 lands so we cold-read the FIXED bounce flow not the bad-deep-link flow.
- [ ] BX.0.5a - **Cold-read v1a: ETL Engineer bounce-and-fix.** Iterative screenshot loop per `[[feedback_cold_read_iterative_screenshots]]`. Drive Studio against sasquatch_pr.yaml + DuckDB demo (post-CB.8 default). Scenario: operator arrives via a Triage CTA's deep link (BTa.2.4 will fix the link target to a specific entity edit page) — they need to make ONE edit and bounce back. Pages: the specific entity edit page they landed on + back-breadcrumb path. Output: `docs/audits/bx_cold_read_v1a_bounce.md`. Estimated 60-90 min.
- [x] BX.0.5b - **Cold-read v1b: L2 author building from scratch — COMPLETE 2026-05-30.** Output: `docs/audits/_archive/bx_cold_read_v1b_author.md` (455 lines) + 30 screenshots in `bx_cold_read_screenshots_v1b/`. Iterative two-round screenshot loop: 19 initial captures (home + 2 diagram + 6 list pages + 3 singletons + 8 create-form pages) + 10 follow-ups (fill+submit rail, edit existing entities, composite-key URLs, markdown preview, persona fill, delete-in-use, orphan-role create). **Headlines: 5 P1 / 8 P2 / 10 P3.** Top P1 finds: (1) silent-accept of orphan-role account create (most severe integrity gap — no validation, redirect to `/`); (2) delete-in-use rail has no confirm + no reference-check + no warning; (3) Instance singleton is raw YAML textarea (BF-pending; "a consultant won't survive this"); (4) composite-key URLs (`Parent::Child`, `ParentRole::Rail::Direction`) are rename-fragile + URL-encoding-hostile; (5) no dependency-order guidance + no "what's complete" signal on the home/list surfaces. **Genuine wins:** rail subtype-picker page (`14_new_rail_picker`) is the gold standard form-page; limit_schedule's Direction helper copy is excellent; markdown preview on description fields actually works (promote it across all entity description fields). Cleanup done: orphan `test-flobber-1` account removed from local `run/sasquatch_pr.yaml` (fixture under `tests/l2/` is unchanged — `run/` is gitignored). Pending: operator triage → BX.0.6.
- [x] BX.0.6 - **Operator triage of cold-read v1 — COMPLETE 2026-05-30.** Operator inline-commented on all 23 recommendations + added 2 §1-§2 findings. Triage decisions live inline in `bx_cold_read_v1b_author.md`.
- [x] BX.0.7 - **REPLAN with triage — COMPLETE 2026-05-30.** Output: `docs/audits/_archive/bx_0_7_replan_with_triage.md`. Enumerated BX.1-18 cells: 5 ship-as-is + 5 design-needed (mockups via BX.0.8) + 5 BTa.1-side-panel-gated + 1 polish cluster + 2 deferred. Persona-related items absorbed by BXa (P1.1 instance form / P1.4 stakeholders rename / P3.5 / P3.6 — all moot). BF reconciliation locked: BF.0 + BF.1 CANCELLED (superseded by BX.13 + BX.0.5b); BF.2-5 + BF.10 KEEP (orthogonal to BX); BF.6 phase-exit closes on the survivors.
- [x] BX.0.8 - **Agent-driven design mockups** (only the design-heavy fixes flagged in BX.0.7 — BX.6 home / BX.7 top-nav / BX.8 diagram / BX.10 composite URLs / BX.11 Account-vs-template). One mockup doc covers all. Output: `docs/audits/bx_design_mockups.md`. Estimated 60-90 min. **COMPLETE 2026-06-11 (e96749eb).** Shipped as a directory `docs/audits/bx_0_8_design_mockups/` with 5 per-cell docs (`bx_6.md` / `bx_7.md` / `bx_8.md` / `bx_10.md` / `bx_11.md`) + `00_index.md`, not a single doc — workflow output plus operator inline annotations. The role-reframe direction got its own follow-up `bx_6_11_role_reframe.md` once BX.6 + BX.11 collapsed into a shared D1 wrapper-accordion mockup.
- [x] BX.1 - **Delete-confirm inline banner with timed auto-cancel** (P1.3). 5-sec countdown + Cancel button at top of edit/list page; reference-check listing downstream consumers before allowing. NO modal. Estimated 2-3h.
- [x] BX.2 - **Save-success redirects to read card** (P1.5). Edit + Create POST handlers 303 to `/l2_shape/<kind>/<entity_id>` instead of `/`. Estimated 1-2h. **COMPLETE 2026-06-11 (overnight).** Added `_post_save_entity_id(kind, original, new_fields)` + `_read_card_url_for(kind, entity_id)` helpers in `_studio_editor_routes.py`; rewired all four save sites (edit save, create, singleton save, plus rail-picker bounce stays at picker URL) to default to the entity's read card. `?from=` / `_back_from` override preserved (BTa.2 P1.5 one-click loop). 8 new unit tests covering rename-redirects-to-new-id (rail.name + account_template.role + account.id), unchanged-addressing-key redirect, `_back_from` override, open-redirect rejection falls back to read card, create POST redirect, create POST with `_back_from`. Existing tests asserting `location == "/"` rewritten to assert read-card URL.
- [ ] BX.3 - **Rail list table view + group-by-source_role** (P2.5). Toggle between dense table (default) + card grid. Estimated 2-3h.
- [ ] BX.4 - **Read-card visual upgrade matching edit-form sectioning** (P3.1). Estimated 1-2h.
- [x] BX.5 - **Inline currency formatting on cap fields** (P3.2). `currency=True` ColumnSpec on LimitSchedule.cap + similar. Estimated 30-60 min. **COMPLETE 2026-06-11 (cd55ca0d, overnight).** `currency=True` on `LimitSchedule.cap` plus similar money fields surfacing on editor read cards.
- [x] BX.6 - **Home with "Start here" flow + singleton tiles** (P2.1, BX.0.8 mockup gated). Numbered dependency order + completeness checkmarks per kind + singletons promoted out of accordion. Open Q: how to relate to diagram. Estimated 3-4h after mockup. **COMPLETE 2026-06-11 (8bbc2345 + a8921fdb merge + follow-ups 97f91d77 / d58bff59 / fe7ad277 / 77e1d00f / 5d50a285 / 1740cce7 / 759d9099).** Direction D1 from `docs/audits/bx_0_8_design_mockups/bx_6_11_role_reframe.md` shipped: editor-only role reframe via D1 wrapper-accordion + Add Role modal (dedicated picker page per follow-up fe7ad277, matching the rail subtype pattern). Read-card wrapped in full-page chrome (d58bff59). Home page container widened + 3-col responsive grid (1740cce7) then dropped to 1-col so long entity IDs don't wrap (5d50a285). Right-click on role nodes wires through to edit (CF.3.m followup at 77e1d00f). Tailwind CSS rebuild at 759d9099. Version bump 13.14.1 → 13.14.2 at 3985ddec.
- [x] BX.7 - **Top nav BUILD/VIEW split + grouping/color** (P2.2 + §1 inline). Color-code by group with accessible contrast. BX.0.8 mockup gated. Estimated 2-3h after mockup. **COMPLETE 2026-06-11 (449033cf, overnight).** Top nav split into BUILD / VIEW / REFERENCE groups with per-group color underline matching the `docs/audits/bx_0_8_design_mockups/bx_7.md` direction; nav labels renamed to the per-group vocabulary.
- [x] BX.8 - **Diagram node link-to-edit + inline mini-diagram on edit pages** (P2.3, operator walked back to "link not click-jump"). Hover-only "→ Edit" link; per-edit-page inline mini-diagram with self highlighted. BX.0.8 mockup gated. **COMPLETE 2026-06-11 (overnight).** Direction D2 from `docs/audits/bx_0_8_design_mockups/bx_8.md`: hover-revealed `data-role="diagram-edit-link"` anchor injected per editable node in `diagram.js::_injectEditBadge` (suppressed on bundles + shared roles, same rule as right-click menu); keyboard focus reveal piggybacks on `:focus-within` + `tabindex=0`. Inline mini-diagram on edit pages via `_studio_routes.render_mini_diagram_html` + new `mini-diagram.js` shim — reuses `_build_digraph_cached(layer=3, focus_node_id=...)` then a tiny wasm-graphviz render into `#mini-diagram-target`; self-node tagged `class="self"` + `data-role="mini-diagram-self"` for CSS + browser-driver lookup. Neighbor click in mini-diagram navigates to that entity's edit page (operator Q2 lock); `limit_schedule` + singletons (theme, instance) render no mini-diagram per Q3 lock — `_focus_node_id_for_entity` returns None, helper short-circuits, `diagram-svg.css` link suppressed on those pages. 20 new unit tests in `tests/unit/test_bx8_diagram_edit_affordances.py` covering kind→focus_node_id parametrize, badge anchor attrs + click-skip on `.edit-badge` ancestry, CSS hover/focus reveal, mini-diagram fragment shape, limit_schedule skip, singleton skip, error swallowing, edit-page integration (rail includes / limit_schedule omits), and mini-diagram.js inverse-arm parity with diagram.js + Python.
- [ ] BX.9 - **Theme live-preview + section save** (P2.4). Per-color-pair contrast preview; per-section Save button. Estimated 3-4h.
- [x] BX.10 - **Composite-key opaque IDs in URLs** (P2.6, operator: "don't want this bleeding into yaml shape"). URL uses opaque hash; composite key stays in breadcrumb/title; YAML unchanged. BX.0.8 mockup gated. Estimated 4-6h after mockup. **COMPLETE 2026-06-11 (f9e0b983, overnight).** `hash6+slug` opaque URL ID with 301 redirect for the legacy composite-key form, per the `docs/audits/bx_0_8_design_mockups/bx_10.md` direction; YAML shape untouched.
- [x] BX.11 - **Account vs AccountTemplate 1:1-vs-1:N distinction** (§2 inline). Visual + copy surfacing cardinality difference on list pages. BX.0.8 mockup gated. Estimated 2-3h after mockup. **COMPLETE 2026-06-11 (absorbed into BX.6 role-reframe at 8bbc2345 + a8921fdb merge + follow-ups).** 1:1 vs 1:N badge landed inside the D1 wrapper-accordion role reframe rather than as a separate list-page treatment — same direction doc (`docs/audits/bx_0_8_design_mockups/bx_6_11_role_reframe.md`) covers both cells.
- [x] BX.12 - **Vocabulary pass via side-panel definitions** (P2.8, BTa.1-gated). Per-field `[?]` triggers on opaque rail/chain/limit terms; single `GLOSSARY: dict[str, str]` source of truth shared with top-nav glossary (operator's §2.b drift concern). Estimated 2-3h. **COMPLETE 2026-06-11 (4e6936ca, overnight).** `GlossaryAnchor` primitive + 8 `GLOSSARY` entries covering Rail / Chain / LimitSchedule per-field definitions; per-field `[?]` triggers the side-panel surfaced via BTa.1.
- [x] BX.13 - **Per-field "surfaces as:" pointers** (P3.5, BTa.1-gated). Side-panel help per field naming the pages it surfaces on. Estimated 1-2h. **COMPLETE 2026-06-11 (eb59c95d, overnight).** `SurfaceAnchor` primitive + `SURFACES_AS` map naming the pages each field surfaces on; side-panel pointers wired through BTa.1.
- [ ] BX.14 - **Plain-language error messages with SPEC pointers in parens** (P3.9, BTa.1-gated). Validator errors rewrite + side-panel `[?]` for long-form explanation. Estimated 1-2h.
- [ ] BX.15 - **Coverage / Trainer chip tooltips on diagram** (P3.10, BTa.1-gated). `[?]` icons opening side panel. Estimated 30-60 min.
- [ ] BX.16 - **Inline shape-preview on chain form** (P3.8). Tiny parent → child arrow diagram updating as children checkboxes toggle. BX.0.8 mockup decides inline-vs-side-panel placement. Estimated 2-3h.
- [x] BX.17 - **Polish cluster** (P3.3 + P3.4 + P3.7 bundled). Duration picker with common picks + free-form fallback; reference panels default-open only on empty list pages (zero persistence); completion-expression DSL autocomplete. Estimated 3-4h total.
- [ ] BX.18 - **Cold-read v2** (phase exit). Iterative-screenshot pattern; same L2-author persona; assert headline P1s closed. Output: `docs/audits/bx_cold_read_v2_author.md`. Operator iteration + sign-off → archive Phase BX. Estimated 90-120 min.
- [x] BX.new.persona - removal-confirm - **Audit — persona-removal confirmation (BXa absorbed).** Per BX.0.7 replan, BXa absorbed P1.1 (instance form), P1.4 (stakeholders rename), P3.5/P3.6. **COMPLETE 2026-06-11.** 15-min grep audit of `src/recon_gen/common/html/`, `src/recon_gen/common/l2/primitives.py`, and `_studio_routes.py`/`_studio_editor_routes.py` confirms `DemoPersona` is fully removed; no live persona UI surface. Remaining `persona` mentions are: (a) L1-doctrine docstring prose ("persona-blind invariants" — the L1/L2/L3 architecture vocabulary, KEEP); (b) BXa.1 archeology breadcrumb comments (KEEP — intentional); (c) `InvestigationPersona` (distinct concept — Investigation app's vocabulary, not the deleted `DemoPersona` singleton, KEEP). **Two vestigial dead-code references flagged for micro-cleanup** (not removed in audit — operator decides scope): `_studio_editor_routes.py:7395` lists `"persona"` in `_VALID_KINDS: frozenset[str]` (typed as `str` not `EntityKind`, bypasses Literal check; but `SINGLETON_KINDS = {"theme", "instance"}` so `/l2_shape/persona/` routes through the `kind not in _FIELD_SPECS_BY_KIND` 404 branch — behaviorally equivalent to the slug-not-in-`_VALID_KINDS` 404); `_studio_routes.py:889-897` retains `"structured form" if kind in ("theme", "persona")` ternary that's now unreachable (persona never reaches that code path since `_HOME_SINGLETONS` no longer carries persona). Filing as `BX.backlog.persona-dead-code-cleanup`.




- [ ] BX.backlog - BX backlog — Reorder sheets: L1/L2 Exceptions right after Getting Started
  - [ ] BX.backlog.diagram - dashboard-drill-from-node - BX backlog — CF.3.m followup: extend the right-click context menu with dashboard-drill destinations alongside "Edit this entity". Operator request 2026-06-05: `role__X` → L1 Dashboard Transactions filtered to that role's accounts; `rail__X` → L2FT Rails sheet filtered by rail; `tmpl__X` → L2FT Transfer Templates filtered by template (verify the sheet exists first; L2FT today has Rails + Chains). **Open design questions** (parked, not yet answered): (1) Role → which filter? `pL1TxAccount` is account-id-keyed, so a role click needs either a new `pL1TxRole` param + SQL column wired in `apps/l1_dashboard/datasets.py`, or a multi-select of all accounts sharing the role at navigation time. (2) Chains aren't node-clickable today (they're edges in the diagram, drawn as dashed magenta lines); deferring chain-drill until we add edge-contextmenu support OR until the user clarifies whether chain-from-transfer-template counts. (3) Verify dashboard sheet URL shape — `/dashboards/<app_id>/<sheet_id>?param_<name>=…` 404'd on a smoke test, so the routing needs discovery (likely under the App2 server, but the URL key per sheet still needs verification per-app). (4) Should this expand the contextmenu from 1 item to a typed N-item menu? (Currently one `<a>` per popup.) Belongs after CF.3.n design lock, not as a quick polish.
  - [ ] BX.backlog.coverage - merge-broken - BX backlog — Coverage combine job: merge doesn't render, markdown only available via artifact download
  - [ ] BX.backlog.duckdb - pytest-xdist-file-lock - CB scope — DuckDB pytest-xdist intra-cell file-lock contention
  - [ ] BX.backlog.duckdb - memory-per-worker-fixtures - CB scope — Migrate db-tier fixtures to :memory: DuckDB per xdist worker
  - [ ] BX.backlog.runner - browser-probe-target-aware - BX backlog — Runner browser-layer probe over-gates on aws_rds_running for local targets
  - [ ] BX.backlog.session - start-silent-no-op - BX backlog — Studio /training/session-start silent no-op when base schema missing
  - [ ] BX.backlog.studio - plant-oracle-broken - BX backlog — Studio plant flow broken on Oracle (caught manually 2026-06-01)
  - [ ] BX.backlog.qs - tablecellstyle-adapter - BX backlog (ex-CH.x) — QS-side TableCellStyle adapter for per-column right-align + tabular-nums on the deployed QuickSight bundle. ~1 day spike. Not blocking — operator runs against App2 locally. Tracked from Phase CH archive 2026-06-08.
  - [ ] BX.backlog.persona - dead-code-cleanup - BX backlog — Two vestigial `"persona"` references after BXa nuke: (1) `src/recon_gen/common/html/_studio_editor_routes.py:7395` includes `"persona"` in `_VALID_KINDS` (typed `frozenset[str]` not `frozenset[EntityKind]` so Literal-check bypass slips through); functionally inert because `SINGLETON_KINDS = {"theme", "instance"}` so `/l2_shape/persona/` routes to the `kind not in _FIELD_SPECS_BY_KIND` 404 branch instead of the slug-not-in-`_VALID_KINDS` 404 branch — same `_render_unknown_kind_page`. (2) `src/recon_gen/common/html/_studio_routes.py:889-897` retains `singleton_form_kind = "structured form" if kind in ("theme", "persona") else "single YAML block"` — unreachable since `_HOME_SINGLETONS` no longer carries persona. Cleanup: drop `"persona"` from `_VALID_KINDS` + retighten the type to `frozenset[EntityKind]`; drop `"persona"` from the ternary. 15-min cleanup. Surfaced via `BX.new.persona-removal-confirm` audit 2026-06-11.
- [>] BU.5 - MOVED to BV.7 (2026-06-09). Registry-walk `recon-gen docs export` lives in Phase BV now.
- [>] BU.6 - SUBSUMED by BV.4.8 (final cold-read of the dual-prefix Trainer surface lands there; no separate BU exit cold-read).

## Phase BV - Dogfood cap-stone — CLOSED 2026-06-10

Closed at HEAD `00ce44c8` with `./run_tests.sh up_to qs_browser` green across all 6 tiers (run id `20260611T034334Z-00ce44c8`). Full summary + commit trail swept to `PLAN_ARCHIVE.md`. Three sub-items deferred to Backlog (BV.1 / BV.2 / BV.5).

All BV / BV-post backlog items moved to the canonical **# Backlog (not yet phased)** at the bottom of this file.

## Phase BN - sasquatch + AWS variant browser-layer flakes (BM.5 fallout)

**Trigger** (user 2026-05-28 during BM.5 final verification): the sp_pg_aw browser cell PASSED end-to-end with the BM core + 5 root-cause fixes, proving the BM unification works under live AWS PG load. BUT the sasquatch variants (sq_pg_aw, sq_or_aw) + the Oracle/AWS cell (sp_or_aw) continued to fail with shapes that don't trace back to BM-the-mechanism. Per `project_phases_ab_bm_tech_debt_cleanup`: file as a separate cleanup phase rather than block the BM ship.

**Known failure shapes (BM.5 run #6, commit `c874591d`):**

- **sq_pg_aw browser, 7 tests fail:** Limit Breach Detail empty BEFORE any pick on both [qs] + [app2] (matview HAS rows per anchor SQL, visual renders zero — sasquatch's auto_scenario plant at days_ago=4 should land inside the BM default 7-day window, so cust1 mapping or densify_scenario adding rows outside-window is the suspect); same 2-test pair for `test_l1_dropdown_pickers_inverse_excludes_anchor`; L2FT Rails inverse [qs]; L2FT Templates Completion both [qs] + [app2].
- **sp_or_aw browser, rerun storm (50+ R chars):** Oracle + sasquatch under AWS load — connection-pool churn / lock-timeout interaction / matview-refresh timing. Need timings + per-test error shape captured.
- **sq_or_aw browser:** never finished (chain killed mid-run). Repro likely shares shapes with sq_pg_aw + sp_or_aw.

**Why this isn't a BM regression:** sp_pg_aw (spec_example PG/AWS) passes end-to-end with the same BM code. Differential is sasquatch's seed shape AND/OR Oracle/AWS scaling — independent of BM-the-mechanism. Seed-shape side wants `auto_scenario` adjustments (plants on anchor day, not days_ago=N?); Oracle/AWS side wants pool-size tuning + retry-backoff review.

**Done when:** sq_pg_aw + sq_or_aw + sp_or_aw browser layers green on `./run_tests.sh up_to=browser` (no xfail-to-mute per `feedback_no_xfail_to_sweep_under_rug`); sweep to PLAN_ARCHIVE.

- [ ] BN.0 - Triage spike: snapshot per-failure repro shape against sq_pg_aw, sq_or_aw, sp_or_aw. Output: `docs/audits/bn_0_sq_aw_flake_snapshot.md` with per-test root-cause hypothesis + reproduction recipe. Prereq for BN.1+ fixes.

- [ ] Backlog - DD.4.e2e follow-up — full sign_in_via_oidc Dex roundtrip e2e
## Phase CF - v12 + v13.1.1 cold-read defects (carryover + DuckDB regressions)

**Why:** Two cold-read reports, both unaddressed end-to-end. v12 (`docs/audits/_archive/v12_0_0_feedback.md`, 6 judges against sqlite v12.0.2) flagged 4 HIGH (U1-U4) + 8 MED (U5-U12) + 2 LOW (U13-U14). v13.1.1 (`docs/audits/_archive/v13_1_1_feedback.md`, 6 judges against DuckDB v13.1.1 / ~300k tx) confirms the CA DuckDB swap shipped a decisive operational win — U1 (App Info clock), U9 (drift KPI cross-sheet agreement), U12 (Session Start sqlite hang) all fixed; statements tie to the penny; the 60s sqlite sheet renders instantly. BUT most v12 UX HIGHs (U2 exec health rollup, U3 `limit_breach_outbound` plant no-op, U4 Apply status contradicting itself) and most v12 MEDs (U6 L2 hairball, U7 editor scroll-walls, U8 Templates Sankey, U10 blank-state sheets) are still open, AND a HIGH regression landed: `drift` plant joins `limit_breach_outbound` in failing-to-plant on DuckDB. Apply reports "2 plant(s) failed: drift, limit_breach_outbound" with byte-identical clean vs violation dashboards.

**Approach:** Fix the plant regression first (CF.0) since it blocks re-verification of drift-magnitude items. Then sweep the trust-killer HIGHs (CF.1-CF.2). Then polish MEDs (CF.3-CF.6) and minor sweep (CF.7). Re-probe v12's unconfirmed items (U5 plant magnitude scaling, U11 drift→leg drill) during CF.7 since v13.1.1 didn't explicitly evaluate them.

**Done when:** All 6 training plants pass on DuckDB; Apply status banner reflects per-plant truth; exec app has a program-health rollup tile w/ threshold banding; L2 diagram has entity-focus mode + self-loops-off default; editor entity-list pages have search/sort/filter; Templates Sankey is legible at realistic counts; blank-landing sheets prompt for the required picker; minor copy + caption fixes shipped; v13.2.0 cold-read confirms with a clean diff (or surfaces a tight CG followup).

**Artifacts:** `docs/audits/_archive/v12_0_0_feedback.md` + `docs/audits/_archive/v13_1_1_feedback.md` (cold-read sources) + `docs/audits/_archive/v13_1_1_repro.md` (CF.0 root cause confirmed by reviewer 2026-06-04) + `docs/audits/_archive/cf_pre_implementation_audit.md` (9-agent pre-impl audit).

- [x] CF.0 - CF.0 — Picker robustness + post-Apply omission reason. **Fix A SHIPPED** (commit d63e1dbf): `_pick_template` scores AccountTemplates by materializable L1-invariant plant count, tie-break alphabetical; sasquatch_pr + spec_example seeds unchanged; `AlphaCustomerStub` regression fixture added to spec_example. **Fix B SHIPPED** (commit 51003168): post-Apply danger banner ships a `<details>` expander listing each failing kind alongside the first line of its error message from the `trainer_failed_plants` kv. The "1 plant(s) failed, here's why" pain is closed; pre-Apply checkbox-disable for kinds the L2 cannot materialize is split into CF.0a (needs a dry-run picker probe, different code path).
- [ ] CF.0a - CF.0a — Pre-Apply card hint: disable checkbox + show "this L2 cannot demo \<kind\>: missing \<dep\>" copy for kinds whose pickers will throw. Needs a static "can this L2 materialize this kind?" probe run at page render — basically dry-running every picker without executing the plant. Currently the operator only learns the picker-blocked status post-Apply via CF.0 Fix B's banner; this surface prevents the failed Apply visit entirely. Lower priority than the rest of CF since the post-Apply path is now actionable.
- [x] CF.1 - CF.1 — Apply status reflects per-plant truth (HIGH; v12 U4). **SHIPPED** (commit 4b6c78e9): new `trainer_last_apply` kv key, 3-state banner (green/amber/red) keyed off succeeded ∩ failed sets, drops the lying `?status=Apply+done.` URL redirect, kv = source of truth (in-memory `_training_apply_state` stays for in-flight live-tail only — design audit confirmed PLAN line 349's "in-memory summary" was prescriptive, not descriptive; no post-Apply summary ever lived in-memory). Locks honored: amber on partial, persist across /training/* nav, survives Studio restart. App2Driver.trainer_apply migrated to unified `data-test-last-apply-banner` selector so partial-failure scenarios don't time out.
- [x] CF.2 - CF.2 — Exec app program-health rollup tile (HIGH; v12 U2). **SHIPPED v0** (commit 87c0e5a1): new Program Health sheet at index 1, full-width KPI tile bound to `<prefix>_l1_exceptions` (date-windowed, magnitude-filtered) with `KPIValueThresholdBanding(amber_at=1, red_at=20)`. TextBox hyperlink to the L1 Dashboard serves as the cross-app substitute on both renderers (operator-locked QS leg + the CrossAppDrill primitive's first-consumer wiring deferred since QS URL params don't sync sheet controls anyway). **Scope-narrowed** from the design audit: v0 is L1-only because the audit flagged L1+L2FT+Inv counts as "not cleanly summable across pillars" (L1 obeys date picker, L2FT is declared-vs-runtime point-in-time, Inv is σ-threshold continuous). L2FT + Inv expansion deferred until the operator's manual Studio drive surfaces the actual next ask.
- [ ] CF.3 - CF.3 — L2 diagram readability (MED; v12 U6). **LOCKED:** focus picker = typeahead (90+ flat options unworkable); SingleLegRail hide = SERVER-SIDE re-emit (URL is source of state truth); default layer = L1. **META-LOCK from operator:** user explicitly wants more time spent on making very large diagrams legible — scope expands beyond the minimal pass. **CF audit followup 2026-06-05 absorbs:** layer pills inverted active/inactive emphasis (treat as stepper: active=solid, upcoming=outline; never disabled-grey for an included step) — Studio Med. **Diagram design review 2026-06-05** (`docs/audits/v13_1_1_diagram.md`) provides MEASURED prescriptive path — 2-line `constraint=false` change drops L3 crossings 65% (181→63) + canvas height 15% with **zero info loss**. The doc's "Empirically DISPROVEN" list (newrank=true / ordering=out / rank=same/min/max / stacking constraint=false on control+chain / concentrate=true / splines=ortho / rails→edges / non-dot engines / neato/sfdp/fdp/d3-force) is locked **do-not-retry** — every one was measured to regress or NaN-crash. **Sub-tasks below** decompose the doc's "additive stack" into ordered cells; ship CF.3.a first (the 2-liner — that's the headline win), then CF.3.b supersedes 3.a (template recast — same crossing win + bigger node/edge/height reduction), then the additive stack (3.c-3.f) builds on top once measured-safe.
- [x] CF.3.spike - CF.3.spike — **Targeted spike: iterate large fuzz seed + visual results TOGETHER** (operator-reinforced 2026-06-05 across three messages: "very important to use a large seed", "iterate on the custom shapes", "I'll want a visual design iteration if possible", "targetted spike to iterate a large fuzz seed plus the visual results"). Per `feedback_spike_before_locking_implementation` + `feedback_cold_read_iterative_screenshots`. Demo L2 is 14/91/115 nodes for L1/L2/L3 — mid-size. The diagram-feedback doc's measurements (181→63 crossings, -32% additional height from template recast) only hold at the density they were measured against; lock-in without spike risks shipping wins that vanish under real density. **Spike loop:** (1) generate a candidate heavy-density L2 via `RECON_GEN_FUZZ_SEED` (target: 100+ rails / 30+ templates with multi-leg + XOR shapes / 10+ chains / high-fan-out hub roles / external-counterparty cluster); (2) render L1/L2/L3 + measure crossings/height; (3) cold-read screenshots through the operator (or an agent acting as cold-read driver per the iterative screenshot pattern); (4) operator validates "this stresses what we need it to stress" + visual results worth pursuing; (5) iterate on seed + render shape until both converge. **Outputs:** `tests/l2/heavy_density_v1.yaml` (named fixture pinned to a specific commit that locks the seed→yaml mapping) + `docs/audits/cf_3_diagram_spike.md` (baseline measurements + screenshot bundle + visual design iteration notes + custom-shape mockups for CF.3.f port-node and intermediate proposals). **This is a hard precondition gate** — every CF.3.a→.i cell needs the spike's fixture as its measurement target + the spike's visual locks before shipping. Sizing: 2-3 sessions of fuzz tuning + 2-3 cold-read iterations.
- [x] CF.3.a - CF.3.a — **SHIPPED 2026-06-04** (commits `25429a3a` on main). `constraint=false` on the 58 template_member edges + `mclimit` 2.0→10.0. Measured on the spike fixture: sasquatch L3 −94.6 % crossings (1086→59), heavy L3 −99.6 % (139990→542); height +14-17 % (audit predicted −15 % — divergence at scale). Layout +17ms (sub-second). **Ship-today 2-liner:** Touchpoints: `topology.py:1029` (`sub.edge`, non-XOR member) + `:1059` (`xor_sub.edge`, XOR member) + `:745-754` (mclimit bump). Layout-only, invisible to the SVG `<title>` stream `diagram.js` parses — `data-id` / `data-kind` / click-to-focus / coverage / trainer tint all untouched. Cluster containment preserved (82/82 member rail nodes verified inside template bbox). Optional: `group="spine"` on pool/hub roles + `group="ext"` on external roles for stronger left-to-right spine (crossing-neutral polish). **Validate on the CF.3.0 fixture** — confirm the doc's measured wins (≥65% crossing reduction, ≥15% height reduction) hold at the heavier density; no NaN; cluster containment still 100%.
- [x] CF.3.b - CF.3.b — **SUPERSEDED by CF.3.f.** The "drop the triple-render" structural goal (template cluster + redundant inner node + dotted membership edges → one shape) landed inside CF.3.f, but the chosen shape is a custom HTML-`<table>` port-node composite, not an invisible `shape=point` chain-anchor with the cluster preserved. CF.3.f absorbs CF.3.b's structural win (−24 nodes, −58 edges on demo L2) and adds custom shape vocabulary on top. Original CF.3.b text: Each TransferTemplate currently renders THREE times — graphviz cluster (dashed box + title) + `tmpl__<name>` component node with identical label inside + dotted `template_member` edges re-drawing membership the cluster already shows. Represent each template ONCE (the cluster); demote component node to invisible `shape=point` chain-anchor at `topology.py:996`; drop membership edges at `:1028-1035` + `:1059-1065`. **Measured:** 63 crossings (same as 3.a) + height 2497pt→1985pt (-32% additional) + nodes 115→91 (-24) + edges 182→124 (-58). Caveats: keep invisible anchor so chain edges at `:522/570/581` still route (verified 124 edges incl. chains, no NaN); optionally `lhead`/`ltail` to clip chain edges at cluster boundary; relocate `transfer_key` from dropped node label into cluster label/tooltip (`_template_cluster_label:374` is name-only today); re-point the `template_member` "Show templates" + edge-label toggle to cluster visibility. Focus adjacency at `:876/882/889` uses `_template_id` — unchanged.
- [x] CF.3.c - CF.3.c — **SHIPPED 2026-06-05** (commit `d1496843`). Server: `build_topology_graph_per_rail` accumulates an `edge_meta` dict at each `g.edge(...)` site, keyed by the exact `"<src>-><dst>"` form graphviz emits, valued with the typed kind. Stashed on the digraph via setattr. Route includes it in the sidecar JSON. `diagram.js` stamping loop reads `edgeMeta[title]` first; falls back to the heuristic on miss. Pre-existing classification bugs (control_parent ↔ rail_bundle; post-CF.3.f port-anchored edges) bypass the heuristic entirely. Unit test pins the contract. Original lock:
- [x] CF.3.d - CF.3.d — **SHIPPED 2026-06-05** (v0 scope cut: 4 server-side categories; internal/external role scope split stays CSS-toggle for v0.1). `build_topology_graph_per_rail` accepts `show: frozenset[str] | None`; when `None` the layer compat shim derives the set (`role`+`control_parent` at L1; +`rail` at L2; +`template`+`chain` at L3) — prior bookmarks keep working. `_VALID_SHOW_CATEGORIES` + `_categories_for_layer` exported for the route handler. `_studio_routes.diagram` parses `?show=a,b,c`, intersects with the valid set, threads through `_render_diagram_page` → `_build_digraph_cached` (cache key includes `show`). `_qs(...)` URL builder preserves `show` across layer/focus/single-leg navigation; omits the param when the full 5-set is active. Four `<label><input type="checkbox">` chrome rows (rail / template / chain / control_parent) swapped for navigating anchors keyed by `data-show-category=<X>` / `data-show-state=on|off`, accent-pinned when on, href flips the category in/out of the active set — same operator mental model as the layer links + the single-leg toggle. Topology phases gated independently: Phase A on `show_roles`, Phase B template iter on `show_templates`, Phase F chain emit on `show_chains`, Phase G control_parent loops on `show_control_parent`. JS `TOGGLE_KINDS` trimmed to `role-internal` / `role-external` only; the four server-side IDs are gone so the `_wireToggles` `if (!cb) continue;` guard handles the absence cleanly. 12 unit tests pin the contract (`test_cf3d_show_categorical.py`); existing diagram-route + topology + cf3-regression suites all green (68 tests in the topology/diagram/cf3 sweep, baselines unaffected because the layer compat shim preserves byte identity). Original lock text:
- [ ] CF.3.e - CF.3.e — **Focus+context fisheye.** Port `_focus_set` from `topology.py:615-652` into ~15 lines of JS over the stamped `g.edge[data-source][data-target]`; on focus, scale focus subgraph up + shrink the rest to label-less pins + fade off-path edges. **Use scale/opacity only, never `display:none`** — every `data-id` stays addressable so coverage/trainer tint composes. Ambient LoD default pins degree≤2 nodes (the self-loop-rail blob). NB: click-to-focus already relayouts and helps some — fisheye adds the in-place context focus alone lacks.
- [x] CF.3.f - CF.3.f — **SHIPPED v0.1 on `cf-3-f` branch** (commits `6b8a7404` + review-fix `06308d25`). 4-agent design workflow → operator cold-read → 5 directions narrowed to a synthesis (port-record + silhouette-geometric) with operator-locked refinements (smoke-tested `smoke_test_v3_locked.png`). Custom shape vocabulary: cylinder (internal role) / note (external role) / folder (template-role) / cds-family rails (single/double stroke) / cylinder peripheries=2 (aggregating) / HTML-`<table>` composite per TransferTemplate with top header + leg-rail rows underneath, each with `PORT="leg_<rail>"` for east/west chain+rail edge docking. Direction-color edge palette (Dark2: orange Debit / teal Credit / purple Variable / magenta chain). Drops: cluster boundary, inner component node, dotted membership edges, "(debit/credit)" labels on rail edges. **Metrics vs CF.3.a baseline:** sasquatch L3 −25 % nodes / −17 % edges / −28 % height; heavy L3 −29 % nodes / −28 % edges / **−54 % height**. Crossings stable. Adversarial-review-fix pass shipped: transfer_key tuple repr, JS port-suffix stripping (`_stripCompass`), JS edge-kind classification (role↔template-port → rail_bundle), focus-gate on composite leg rows. CF.3.f.b backlog (WCAG palette, composite-aware coverage CSS, ↔ direction marker, etc.) at `docs/audits/cf_3_diagram_spike/cf_3_f_design_directions/07_review_followups.md`. Original text:
- [x] CF.3.f.b - CF.3.f.b — **SHIPPED 2026-06-05** (commit `3c2b08e7` on `cf-3-f`). Operator measured CF.3.f against an upstream graph and locked three follow-on decisions: (1) `rankdir=TB` for layer 3 only (L1/L2 stay LR — width pain is L3-specific); (2) drop `:w`/`:e` compass pins from leg-port edge endpoints (perpendicular to TB flow); (3) XOR-SHORT labels — `(xor i of N)` per edge instead of full sibling list repeated every time (full list stays in typed-model `metadata.xor_siblings` for tooltip/sidecar). Measured on the spike fixture: sasquatch L3 width −52 % (3160→1530pt, ~square 1.3:1); heavy L3 width −68 % (5152→1653pt, tall-vertical). Crossings still slightly down. Same nodes/edges/info — the graph rotates 90° + the wide-ribbon problem is gone. Sasquatch L3 now fits a typical iframe viewport at native zoom.
- [ ] CF.3.f.c - CF.3.f.c — **Hub replication + "Duplicate hubs" toggle** (operator-locked design, 2026-06-04 final). For dense graphs the longest "flying" edges all touch high-degree pool/sweep hubs — operator measured: hub replication 113→**31 crossings (−73 %)**, longest edge 3993→2013px (−50 %). Replicate high-degree hubs (e.g. `>=10` degree, tunable) per-consumer with color-coded copies (green/purple/orange per family); add a "Duplicate hubs" toggle to the existing `?show=` control row (default ON); show a small hub-color legend. Toggle OFF reverts to the honest single-hub topology (use xor-WRAP labels there so names stay legible against fewer-but-longer hub edges). Bigger work than CF.3.f.b — touches topology emit (replica generation + color assignment) + URL state + UI wiring + legend.
- [ ] CF.3.g - CF.3.g — **Typeahead focus picker** (operator lock — 90+ flat options unworkable as dropdown). Typed primitive in `picker_index.py` per the design workflow's spec; consumes the edge-kind sidecar from 3.c for filter-by-kind. Replaces the current dropdown at the diagram chrome.
- [x] CF.3.h - CF.3.h — **SHIPPED 2026-06-05**. `build_topology_graph_per_rail(hide_singleleg=True)` filters STANDALONE SingleLegRails at node, bundle, edge, and chain-endpoint levels. Template-resident single-leg rails stay as port rows in their composite shape (hiding them would break the template's port system). URL param `?hide_singleleg=1` wired in `_studio_routes.diagram`; `_qs(...)` preserves it across layer/focus navigation. Chrome toggle is a navigating anchor (URL = source of state truth, same shape as the layer links — server re-emits cleanly). Unit test `test_topology_graphviz_per_rail_hide_singleleg_drops_standalone_single_legs` pins the contract. Original lock text:
- [x] CF.3.i - CF.3.i — **PARTIALLY SHIPPED 2026-06-05 (Python default)**: `build_topology_graph_per_rail` Python default flipped from `layer=3` to `layer=1` (operator lock — match URL handler default + stop silent test-side landing on L3). The two unit-test call sites that relied on the default now pass `layer=3` explicitly. **Still TODO:** layer pills active/inactive styling fix + stale `_smart_focus_hops` doc string cleanup at `_studio_routes.py:2655-2660`. Both are HTML/copy work, can land in CF.7.
- [x] CF.3.j - CF.3.j — **SHIPPED 2026-06-05** (`tests/unit/test_cf3_diagram_regression.py`). Parametrized over both spike fixtures (heavy_density_v1 + sasquatch_pr); asserts visual-quality metrics don't regress vs the locked baseline metrics.json: exact node + edge counts (rendering shouldn't add/remove), crossings ≤ baseline + 10 % + 5 absolute (improvements welcome; regressions fail), width/height within ±20 % (graphviz layout drift tolerance). Layout time NOT a gate (operator lock). Re-lock by re-running `cf3_spike render` after intentional wins. Original text: (the one-shot spike sizes the right fixture; this cell makes it a permanent measurement gate). After CF.3.spike locks `heavy_density_v1`, wire it into the `RECON_GEN_AI_FUZZ_SAMPLE_N` pool + add a measurement-regression test that fails CI if any CF.3.a→.f change regresses crossings/height vs the post-spike baseline. Catches: template port/record nodes overflowing at scale, focus/LoD heuristics (degree≤2 ambient pinning) mis-pinning at higher degree. **Render time is explicitly NOT a constraint** (operator-locked 2026-06-05: "even if it takes a second to render its worth spending more time and applying caching for the best visual results") — current performance is sub-second, so the regression gate tracks visual quality (crossings + height + label legibility) and we trade layout time freely. CF.3.k absorbs the caching work to keep operator-perceived latency instant.
- [x] CF.3.k - CF.3.k — **SHIPPED 2026-06-05**. Module-level `OrderedDict`-based LRU on `_build_digraph_cached` in `_studio_routes.py`, keyed by `(id(instance), db_table_prefix, focus_node_id, layer, hide_singleleg)`. Bound at 32 entries to handle focus-mode browsing. Studio is single-user / single-process so no Redis layer needed. Cache invalidates NATURALLY when the L2 yaml is saved: `cache.get()` returns a fresh `L2Instance` object → new `id()` → key changes → cache miss → fresh render. 5 unit tests pin the contract (cache hit on identical args, miss on layer change, miss on hide_singleleg change, miss on fresh L2Instance object, bounded size). Original lock:
- [x] CF.3.m - CF.3.m — **SHIPPED 2026-06-05** (operator-driven iteration across 4 dogfood passes). Operator flagged that CF.3.l's diagram chrome — page-local title bar + layer/Coverage/Trainer/focus row + Show/Edge-labels row — ate ~30 % of viewport before the canvas began on the heavy_density_v1 fixture. Replaced all three rows with a left-docked floating `<details id="diagram-sidebar">` overlaying the canvas (z-10, absolute-positioned, max-h calc, scroll-y). Master collapse on the root `<details>` shrinks the sidebar to a 40px strip; canvas extends underneath both states (no relayout). Chevron flips `«` ↔ `»` via tailwind `not-open:`/`open:` arbitrary variants. Four collapsible sections: **Layer** (open) / **Show** (open) / **Edge labels** (collapsed) / **Overlays** (collapsed — Coverage, Trainer, hide-singleleg, Reset). Optional **Focus** section auto-mounts (open) when `?focus=` is set, with focused-id code block + clear-focus link. L2 stem in the sidebar summary strip (always visible) AND prepended to the bottom status line ("`<stem> · N nodes · M edges`") via `data-prefix` attr the JS reads. Show / Edge labels / Overlays / single-leg all render as unified `<input type="checkbox">` rows (`_show_anchor` rewritten — server-side categories navigate-on-change with the flipped show-set URL; CSS-toggle categories use the existing JS wire). Layer buttons relabeled to "Roles & Structure" / "+ Rails" / "+ Chains & Templates"; active button uses a dedicated `bg-primary-fg text-white font-semibold shadow-inner` style (the prior `chrome_button_classes() + bg-accent` overlay lost the cascade fight on mid-blue accent themes, leaving the active pill near-indistinguishable). **`_editor_url_for_focus_node` returns `str | None`** and lands on the **edit** form (`/l2_shape/<kind>/<id>/edit`) per operator lock — list-page fallbacks gone. Resolvable for `rail__X` + `tmpl__X` only; `role__X` (shared) + `rail__bundle_N` (synthetic) return `None` and the caller suppresses the affordance entirely rather than dump the operator on a disambiguation surface. "Open focused entity in editor" button at the sidebar foot only renders when the URL is resolvable. **Right-click on any SVG node** → custom contextmenu with single "Edit this entity" item; mirror `_editorUrlForNode` in `diagram.js` returns null on the unresolvable cases so the browser's native menu fires instead of an empty popup. Dismisses on next click / contextmenu / scroll / Escape. Embed mode (`?embed=1`) drops the sidebar so external host pages render their own chrome. **Tests in `test_cf3m_diagram_sidebar.py`** pin the inverse (7 parametrized cases — 4 None + 2 `/edit` + 1 ambiguous), sidebar markup (root id + 4 section labels + no leaked chrome row classes + "Roles & Structure" first button), chevron-flip variant classes, status-line `data-prefix`, unified-checkbox shape (counts navigate-on-change occurrences), View-in-editor suppression for default/role + present for rail focus, embed-mode omission, and JS/Python inverse parity (every prefix arm + `/edit` suffix + null-defer in contextmenu handler + "Edit this entity" label grepped from `diagram.js`). 425 tests in the studio/diagram/editor/cf3 sweep all green.
- [x] CF.3.l - CF.3.l — **SHIPPED 2026-06-05**. Cold-read on the heavy_density_v1 fixture surfaced enough value in the diagram that it deserves its own top-level surface. `build_top_nav_entries` now emits `Diagram` first in the authoring group (left of L2 Editor). The editor's `_render_home_page` lost its embedded `<iframe src="/diagram?embed=1">` plus all its supporting JS (the X.4.f.7 iframe-cascade-reload bump, the X.4.f.8 iframe-focus filter pipeline `applyFocusFilter`/`refreshFocusFromIframe`/`/diagram/visible` fetcher, and the X.4.f.8.reverse click-card-title iframe-URL mutator `_focusDiagramOnNode`). Per-card focus links in `_render_read_card` rewritten from `<h3 data-focus-node="X" tabindex=0 role=button>` simulated buttons to plain `<h3><a href="/diagram?focus=X">` anchors — browsers get right-click → open in new tab, keyboard focus rings, and screen-reader treatment for free. Tests updated: `test_studio_home_route.py` swaps iframe-presence assertions for `<iframe not in body`, replaces the iframe-cascade-reload + filter-listener + click-listener tests with one combined "iframe JS is gone" test, and rewrites the card-title-focus test to assert `href="/diagram?focus=…"` (with negative assertions that `data-focus-node=` + `role="button"` are gone). Top-nav contract tests pick up the Diagram entry as the leading authoring group member. 440 tests in the studio/diagram/editor/top-nav sweep all green. Operator follow-up CF.3.e (hover-fisheye) layers on top of the now-standalone Diagram surface.
- [x] CF.4 - CF.4 — Editor entity-list search/sort/filter + Studio Med followups (HIGH; v12 U7). **LOCKED 2026-06-05** via `docs/audits/_archive/cf_4_design_review.md`: SERVER-side pagination/search/sort with offset+limit, per-kind URL keys (Q1A); max page_size=200, default=25 (Q2A); per-kind search (Q3A); universal Default/A-Z/Z-A + per-kind sort axes via typed `SORT_AXES_BY_KIND` map (Q4B); collapse-by-default with HTMX expand-on-demand (Q5B); filter auto-opens the collapsed section it hits (Q6A); in-body non-sticky toolbar (Q7A); home page carries kind-namespaced state on `/` (Q1B + Q9A — section `hx-get` URL IS the state truth, mirrors CF.3.m pattern); typed primitive lives in net-new `common/html/_components.py` (Q10A). **Phase CG folded in** — CG.0-CG.4 marked WONT DO since CF.4 IS the first concrete consumer; future generalization happens as CF.4.h+ subcells when additional consumers light up. **Absorbs 3 CF audit followups** (Studio Med): (a) Edit/Delete vocabulary (ghost-outline + danger Delete), (b) editor home `<h1>` + purpose blurb — NO top-level + Add CTA, per-section + Add buttons stay the entry per operator lock, (c) value-column widening + `FieldSpec.render_as` Literal tag for chip-list rendering (Q8c — typed primitive ships in CF.4, not deferred).
  - [x] CF.4.a - **SHIPPED 2026-06-05** — typed list-toolbar primitive at `src/recon_gen/common/html/_components.py`. `ListToolbarState` frozen dataclass (kind / q / sort_axis / page_offset / page_size / total_count / url_prefix) with `__post_init__` invariants: singleton kinds rejected, sort_axis ∈ `SORT_AXES_BY_KIND[kind]`, page_size ∈ [1, 200], page_offset ≥ 0, total_count ≥ 0, q whitespace-stripped. `SortAxis` closed Literal + `SORT_AXES_BY_KIND` typed Mapping (universal Default/A-Z/Z-A + per-kind sentinels: rail_subtype on rail, template_leg_count on transfer_template, chain_parent on chain). `SORT_AXIS_LABELS` for dropdown text. `url_prefix` namespacing supports Q1A (bare keys at `/l2_shape/<kind>/`) AND Q1B (kind-namespaced at `/`). `render_list_toolbar()` emits a `<form>` with search input + sort dropdown + Prev/range/Next pager; htmx wires `hx-get` / `hx-target` / scoped `hx-push-url` (off in embed mode); pager URLs serialize `q` + `sort_axis` + page params so click-Next preserves operator state. 31 unit tests in `test_cf4a_list_toolbar.py` pin: axes coverage per kind, universal-axes-everywhere, per-kind-sentinels-only-on-their-kind, all axes labeled, every invariant rejection, URL key namespacing (bare vs prefixed), pagination math (first/middle/last/empty), rendered markup (search input value, kind-filtered sort options, pager URL state preservation, embed-mode `hx-push-url` omission, empty/no-matches distinction). 482 tests pass in the studio/editor/cf3/cf4 sweep.
  - [x] CF.4.b - **SHIPPED 2026-06-05** — server-side pagination + sort + search on `/l2_shape/<kind>/`. `parse_toolbar_state()` added to `_components.py` (clamps bad query input: negative offsets → 0, oversize page_size → 200 cap, out-of-universe sort_axis → default, non-int values → defaults — risk #6 mitigated at the boundary). `_filter_entities()` (case-insensitive substring on entity_id) + `_sort_entities()` added in `_studio_editor_routes.py`; all axes inlined (per-axis branches with typed key fns) so pyright reads each as a uniformly-typed tuple. Tiebreak on entity_id always so server-side pagination is stable across pages (risk #7). `list_view` now: filter → sort → parse state with post-filter total → slice for page → render toolbar (Q7A: in-body, non-sticky) + paginated cards. `_render_list_page` accepts `toolbar_html` and threads it above the grid (both standalone and `?embed=1` paths). 15 unit tests in `test_cf4b_paginated_list_view.py` pin: parser defaults / whitespace strip / clamping (negative offset, oversize page_size, zero page_size → min, non-int fallback, unknown sort_axis fallback) / url_prefix Q1B namespacing; route renders kind-aware toolbar with rail_subtype option for rails / absent for accounts; search drops non-matches and surfaces "No matches" in empty case; pagination shows "Showing 1–2 of N" and "Showing 3– …" with disjoint card sets across pages; sort_column=name_asc/desc selects the matching `<option>` AND orders the rendered card ids; oversize page_size returns 200 (parser unit tests pin the actual clamp); `?embed=1` returns toolbar + cards fragment only (no `<html>` / `<head>` / `<body>`). 497 tests pass in the studio/editor/cf3/cf4 sweep.
  - [x] CF.4.c - **SHIPPED 2026-06-05** — collapse-by-default per card + HTMX expand-on-demand. `_render_read_card` split into `_render_read_card_summary` (title + Edit/Delete actions, always rendered) + `_render_read_card_body` (heavy `<dl>` rows, lazy-fetched in collapsed mode). New `collapsed: bool = False` flag on `_render_read_card`: when True, wraps summary inside a `<details>` whose `hx-get="/l2_shape/<kind>/<id>?body_only=1" hx-trigger="toggle once"` fetches the body on first expand and swaps a `data-role="card-body"` placeholder. Edit/Delete/focus-anchor inside `<summary>` carry `onclick="event.stopPropagation()"` so clicking them fires the action without toggling the parent `<details>`. `COLLAPSE_THRESHOLD = 10`: `_render_list_page` reads `total_count` from `state` (CF.4.b's already-computed post-filter count) and collapses when total exceeds the threshold. Sasquatch_pr's 7 accounts → eager; 21 rails → collapsed. `read_card` route extended with `?body_only=1` query flag — returns just the `<dl>` fragment (no `<article>`, no header, no actions); same endpoint, no new route to drift against. 8 unit tests in `test_cf4c_collapse_expand_on_demand.py` pin: under-threshold kind (accounts) renders eager inline; over-threshold kind (rails) renders collapsed with the lazy-fetch placeholder + `hx-trigger="toggle once"`; direct unit calls with `collapsed=True/False` produce the right markup; `?body_only=1` returns `<dl>` only (no article/header/actions); 404 propagates for unknown entities; the existing eager card endpoint still works (no regression for post-save flows); collapsed-card action links carry `event.stopPropagation()`. 501 tests pass in the studio/editor/cf3/cf4 sweep.
  - [x] CF.4.d - **SHIPPED 2026-06-05** — home-page kind-namespaced URL state + cascade-reload preservation. `_render_home_page` now takes a `query_params: Mapping[str, str] | None` arg; `landing` handler passes `request.query_params`. Per kind, the home page reads the kind-prefixed keys (`rail_q`, `rail_sort_column`, `rail_page_offset`, `rail_page_size` — Q1B home shape) and translates them into the bare keys (`q`, `sort_column`, ...) the section endpoint's `parse_toolbar_state()` expects (Q1A section shape). Each section's `hx-get="/l2_shape/<kind>/?embed=1&q=foo&page_offset=25"` carries its slice; on `HX-Trigger: l2-cascade-reload` htmx refetches that URL unchanged → state preserved across saves in *other* sections (risk #1 mitigated; same URL-is-state-truth pattern as CF.3.m). Sections with active state auto-open (Q6A); when ANY section is active, the legacy `idx == 0` default open suppresses so unrelated sections don't pop. Unknown URL prefixes (`?bogus_q=…`) silently ignored — the kind list is closed by `_HOME_SECTIONS`. Values URL-encoded via `urllib.parse.quote` so search terms with `%20` / `&` round-trip cleanly. 9 unit tests in `test_cf4d_home_section_url_state.py` pin: no-params uses legacy hx-get + first-section-open default; `?rail_q=external` bakes into `/l2_shape/rail/?embed=1&q=external` (and not into other sections); page_offset + sort_column translate together for templates; multiple kinds coexist; Q6A auto-open + override of legacy first-open; cascade-reload `hx-trigger` still wired on all 6 sections; unknown prefixes leak nowhere; URL-encoding round-trips. **URL-push back to home URL deferred** — the toolbar currently runs with `hx-push-url=false` in embed mode (no browser URL update on toolbar submit); operator can manually craft `?rail_q=…` for bookmarking. CF.4.j cold-read will flag if push-to-home-URL is needed. 510 tests pass in the studio/editor/cf3/cf4 sweep.
  - [x] CF.4.e - **SHIPPED 2026-06-05** — editor home `<section id="home-intro">` carrying `<h1>L2 Editor</h1>` + a one-sentence purpose blurb naming the five entity kinds. NO top-level + Add CTA per operator lock; the per-section + Add affordances stay the entry. Lives between the diagram-status row and the accordion in `_render_home_page` (`_studio_routes.py`). Demo-mode safe: blurb references "per-section affordance" descriptively rather than mentioning the literal "+ Add" string (which `test_studio_demo_mode.py` asserts absent). 2 unit tests in `test_cf4efg_editor_chrome.py` pin: `<h1>` + section id presence + blurb names all 4 multi-instance kinds; intro section emits no `/new` link and no "+ Add" literal.
  - [x] CF.4.f - **SHIPPED 2026-06-05** — Edit + Delete promoted from text links to typed button vocabulary in `_render_read_card_summary` (`_studio_editor_routes.py`). Edit gets ghost-outline (`border-accent text-accent rounded-sm no-underline hover:bg-accent`); Delete gets danger-solid (`border-danger text-danger rounded-sm hover:bg-danger`). Class strings inlined at the call site with `# CI.3 followup` markers so the future typed `Button` primitive has one address to migrate. 3 unit tests in `test_cf4efg_editor_chrome.py` pin: Edit anchor carries accent + ghost-outline classes; Delete anchor carries danger token + still wires `hx-delete`+`hx-confirm`; Edit/Delete class sets don't bleed into each other (anchor-scoped substring check).
  - [x] CF.4.g - **SHIPPED 2026-06-05** — `FieldSpec` gains `render_as: RenderAs` typed Literal (`"text" | "chip_list" | "monospace" | "markdown"`) defaulting to `"text"`. `_render_read_value` branches: `chip_list` + tuple value → flex-wrap of `<span class="bg-link-tint text-accent border border-accent/25 font-mono break-keep">` chips, empty tuple → em-dash; `monospace` → font-mono span. `Rail.bundles_activity` + `TransferTemplate.leg_rails` tagged `chip_list`, so underscored identifiers like `cust_2_treasury_internal_book` no longer split mid-token in the read-card value column (root cause: `break-words` honors `_` as a word-break opportunity; `break-keep` doesn't). 6 unit tests in `test_cf4efg_editor_chrome.py` pin: leg_rails view emits chip wrappers + accent tint + `break-keep`; empty tuple → `—`; XSS escape on chip content; default `render_as="text"`; both tagged fields surfaced through `_FIELD_SPECS_BY_KIND`. 525 tests pass in the studio/editor/cf3/cf4/typing-smells sweep.
  - [x] CF.4.k - **SHIPPED 2026-06-05** — three dogfood-driven followups from a live Studio drive against `run/sasquatch_pr.yaml` on DuckDB. (1) **Search in section header.** Operator: "the search should be integrated into the collapsable section headers." Added `render_summary_search_input()` to `_components.py` — emits a kind-namespaced `<input type="search" name="<kind>_q">` into each home-page `<details>` summary; `oninput="this.closest('details').open=true"` auto-opens the section on first keystroke (Q6A footgun mitigation extended); `onclick="event.stopPropagation()"` so clicking the input doesn't toggle details; htmx wires `hx-trigger="input changed delay:300ms, search"` (debounce + WebKit X-clear button) + `hx-include="this"` + `hx-target="#home-section-body-<kind>"`. The body toolbar's `render_list_toolbar(..., header_owns_search=True)` drops its own search input; pager URLs still carry `q=…` via `_page_url`. (2) **Drop card-title → diagram link.** Operator: "if you click the card headers it takes you back to the diagram, I don't think that makes sense anymore" — the CF.3.l-era reverse-jump was a holdover from before Diagram became its own top-level surface. Title is now plain `<h3>` (no anchor); Edit button is the explicit affordance. Title click inside a collapsed card's `<summary>` toggles the parent `<details>` natively (no `stopPropagation` on the title). Added a rotating `▸` chevron to the left of collapsed-card titles (operator: "note there is no collapse chevron"); `group` on `<details>` + `group-open:rotate-90` for the spin. Deleted the now-orphaned `_focus_node_for_entity` helper (per `[no_compat_shims]`). (3) **Drop sort dropdown UI.** Operator: "the sort doesn't seem to do anything and it makes the header taller, I'd just remove it." Removed the `<select name="sort_column">` widget from both surfaces; body toolbar (embed) is now `<div data-role="list-toolbar">range + pager</div>` — no form, no inputs to submit; standalone toolbar collapses to a single row (search + range + pager, no sort). Server-side `?sort_column=` URL parsing kept in `parse_toolbar_state` + `_sort_entities` so external tooling / shared URLs still work. 12 unit tests in `test_cf4k_summary_search.py` pin summary-search wiring + body-toolbar-drops-search; updated `test_cf4a_list_toolbar.py` / `test_cf4b_paginated_list_view.py` / `test_cf4c_collapse_expand_on_demand.py` / `test_studio_home_route.py` for the dropped diagram link + sort UI. 532 tests pass in the studio/editor/cf3/cf4 sweep.
  - [x] CF.4.l - **SHIPPED 2026-06-05** — four dogfood-driven followups continuing the same live drive. (1) **Pager moved below cards** (operator: "page switching should be at the bottom not the top to match the dashboards"). `render_list_toolbar()` split into `render_list_pager()` (range + Prev/Next with `mt-3` matching `bootstrap.js::table-pager`, below the cards) + `render_list_search()` (standalone-page search form, above the cards). `_render_list_page` rewired: `search_html` ▸ cards grid ▸ `pager_html`. Embed mode emits pager-only since the summary owns search. (2) **Editor list page wears the shared top-nav** (operator: "the editor page header doesn't match the style of the training header, I like the trainer header better"). `make_editor_routes` + `_make_handlers` thread `top_nav_fn`; `list_view` calls `_top_nav_html("/")` so the L2 Editor entry stays the active one (exact-match required since `entry_href == "/"` never wins by prefix). Replaces the bespoke "Studio · editor · <kind>" inline header. (3) **Studio home page chrome unified with trainer.** Operator: "the very top of the page 'Studio qsgen-duckdb' and the deploy changes button should go away. underneath that the 'L2 Editor' block does not match the trainer page style." Removed the `<header>` with `<h1>Studio</h1>` + `font-mono` deployment_name + the `quicksightDeploy()` button + the entire ~60-line JS handler. The home-intro `<section class="px-4 pt-4">` was repromoted to `<header class="px-8 py-4 border-b border-surface-border bg-white" id="home-intro">` mirroring trainer's exact shape (h1 + blurb). (4) **ETL landing page header migrated** (operator: "the etl tab still has that header strip too"). `/etl/` swapped from `<h1>Studio · ETL Support</h1>` + prefix-mono + separate `<section>` blurb → unified `<header class="px-8 py-4 border-b ...">` with `<h1>ETL Support</h1>` + blurb folded in. ETL sub-pages (`/etl/run`, `/etl/triage`, `/etl/probe`) ALREADY used `_render_etl_sub_nav` instead of the legacy strip; left alone. Test updates: `test_home_page_renders_deploy_button_in_header` ▸ asserts deploy GONE; `test_home_deploy_button_hidden_in_demo_mode` ▸ asserts absent in BOTH modes; `test_studio_pages_no_top_nav_when_factory_not_supplied` ▸ asserts via `aria-label="App nav"` not via "L2 Editor" text (h1 collides); `test_card_titles_link_to_diagram_focus_per_kind` ▸ asserts NO `/diagram?focus=` link. 532 tests pass.
  - [x] CF.4.h - **WONT DO 2026-06-05** — operator lock (per the original cell prose): "primitive is opt-in once the consumer set lights up." No consumer currently asks for migration; Training plant cards / Investigation account list have their own typed rendering paths and would gain little from the toolbar primitive. Same wont-do shape as CG.3. Re-open as `CF.4.h.<n>` if a future consumer surfaces a real need.
  - [x] CF.4.i - **SHIPPED 2026-06-05** — `StudioBrowserEditorDriver` (`tests/e2e/_drivers/studio_browser_editor.py`) gained the CF.4 home-page toolbar verbs: `summary_search_locator(kind)`, `set_summary_search(kind, term)`, `clear_summary_search(kind)`, `home_section_card_ids(kind)`, `home_section_pager_range(kind)`, `paginate(kind, direction)`, `expand_collapsed_card(kind, entity_id)`, `card_is_collapsed(kind, entity_id)`. All anchors are `data-role` / `data-kind` / `data-entity-id` per `[feedback_browser_drivers_user_facing_locators]` — never Tailwind classes. 3 browser-tier tests in `tests/e2e/test_studio_cf4_browser.py` against `heavy_density_v1.yaml` (134 entities, paginates rail + template sections, forces COLLAPSE_THRESHOLD): summary-search filters cards + restores on clear; pager Next/Prev cycle stays disjoint with server-side tiebreak; collapsed-card click toggles + `?body_only=1` body fetches + `<dl>` lands. Tier=QS_BROWSER, needs=PLAYWRIGHT, gated by `RECON_GEN_E2E=1`. Cascade-reload-preserves-state and diagram→edit-jump risks (#1, #2) are unit-level covered by `test_cf4d_home_section_url_state.py::test_section_hx_trigger_unchanged_so_cascade_reload_preserves_state` + `test_studio_home_route.py::test_card_titles_are_plain_text_not_diagram_links` — no second copy needed at the browser tier.
  - [x] CF.4.j - **SHIPPED 2026-06-05** — agent-driven cold-read v3 against the post-CF.4.l live Studio (sasquatch_pr / DuckDB). Report at `docs/audits/_archive/cf_4_cold_read_v3.md`: 3 P0 / 11 P1 / 6 P2 + 5 pleasant surprises + 4 open questions for the operator. **All 3 P0s fixed in this cell:** (1) **Persona section orphan.** Home page listed Persona with `<a href="/l2_shape/persona/">Edit</a>`, but BXa.1 deleted the persona routes — link 404'd to a chrome-less "not editable" page. Removed `("persona", "Persona", "persona")` from `_HOME_SINGLETONS` (`_studio_routes.py`); re-add when the persona structured form ships. (2) **Pager `hx-target="#entity-list"` doesn't exist on home.** Home wraps section bodies in `#home-section-body-<kind>`; when embedded, Next/Prev clicks silently no-op'd (caught BY the CF.4.i browser test before the human noticed — page1 and page2 returned identical card sets). `list_view` now picks `swap_target_id = f"home-section-body-{kind}"` when `embed=True`. (3) **Pager URLs double `embed=1`.** `_components.py::_page_url` appended `embed=1` even when `submit_url` already carried it — query parser took the last value, no functional break, but smelled. Guard: `if embed and "embed=1" not in submit_url`. **P1/P2 deferred to CG-series follow-ups** (registered via new BX backlog items per `[no_silent_defer]`): unify the chevron+lazy-load pattern across all 6 kinds (currently only account/rail), add the trainer-style page header to `/l2_shape/<kind>/` dedicated pages, add empty-search in-place message, unify diagram chrome, surface display_name on account cards, fix chain card title shape, scrub `account_template` → "account templates" in aria-labels. Cold-read agent also flagged 4 open questions for next operator session (per-section vs cross-kind search, structured-form pages re-chrome, +Add prominence at scale, eager-vs-lazy card asymmetry intent). 589 unit + 3 browser pass.
- [x] CF.5 - CF.5 — Transfer-Templates Sankey legibility (MED; v12 U8). **PARTIALLY SHIPPED 2026-06-05** (commit 3e0c1e1b): added `items_limit=30` to the L2FT Multi-Leg Flow Sankey — collapses the default-render hairball to top-30 + automatic "Other" bucket, matches Investigation's `_SANKEY_NODE_CAP` convention. Subtitle calls out the cap so operators know to expect rollup. **Lock residual:** first-declared default Template picker selection still TODO (operator-locked: deterministic per-L2 default so the sheet doesn't open blank). Re-open if v13.2.0 cold-read still flags hairball-on-render.
- [ ] CF.6 - CF.6 — Empty-state prompts on picker-driven sheets (MED; v12 U10). **LOCKED:** TextBox always-visible (no auto-hide); Daily Statement gets a SMART DEFAULT — picker auto-selects the first 1:1 Account with a balance instead of just prompting (operator says this recurring pattern wins over "Select an account to begin" copy); enforce invariant via unit test, not `__post_init__`. **CF audit followup 2026-06-05 absorbs:** (a) build an empty-state typed primitive that branches on `selection_required` vs `truly_empty` (audit Dashboards Med #1 — "Pick an account to begin" vs "No data matches"); (b) the empty-state shown twice with mismatched wording (Studio Low). v0 lands the primitive + Daily Statement consumer; other consumers extend in CF.7.
- [ ] CF.7 - CF.7 — Minor sweep + v12 re-probes. **LOCKED:** U11 drift→leg drill = ADD RUNNING BALANCE COLUMN to Daily Statement detail (operator-clearest; supersedes status-badge cheap option); Net Money tolerance = industry-standard fiat thresholds (1%/5%); App Info `latest_date_basis` renders as operator-friendly labels ("end-of-day" / "start-of-day" / "posting time"), not column-name literals. Other sub-items: ETL copy mismatch fix; matview-staleness + business-day caption; U5 plant magnitude re-probe (blocked-by CF.0 Fix B). **CF audit followup 2026-06-05 absorbs the cross-cutting "small": ** (a) value-first KPI cards — lead with one-line lede + value, not multi-line prose above (Dashboards Med #3); (b) tabular-nums everywhere on currency/numeric cells (Dashboards Low — partial; the table-component fix is CH); (c) inconsistent KPI severity glyphs within a row (Dashboards Low); (d) ragged masonry / equal-height marooning a tiny KPI beside a dense table (Dashboards Low); (e) single-category bar charts read as render errors — add a one-row caption (Dashboards Low); (f) active nav item quieter than group labels (Dashboards Low); (g) slider value shown twice — drop one (Dashboards Low); (h) transient success banners persisting + piling up (Studio Low); (i) "i" failing-card error badge looks like a status label not the hoverable affordance — change shape to make hover discoverable (Studio Low); (j) ETL workflow numbered as "5 steps" AND "3" in two components (Studio Low); (k) drop the "[Select all] / [None]" literal bracketed-text shortcuts — promote to actual buttons (Studio Med — partial; control-vocab full pass is CI).
- [ ] CF.8 - CF.8 — Cut v13.2.0 + re-cold-read (same 6-persona shape; sweep CF to PLAN_ARCHIVE on clean diff or file the cross-cutting followups CG-CK below for any residual)

## Phase CI - Control vocabulary + Badge primitive (Studio Med)

**Why:** v13.1.1 design review (multiple Studio Med findings): solid-primary + white-outline + solid-amber + bare text-links + literal `[Select all]`/`[None]` bracket-text shortcuts all coexist with no shared vocabulary. Same audit notes "no shared badge component (state/type/id badges render in 3 idioms)." And: "Three side-by-side buttons in three fill languages with severity contradicting risk — the genuinely destructive 'rebuild from base' looks secondary while a benign action is solid-amber." Severity mismatched to action risk is a real footgun, not just polish.

**Locks (operator to confirm):**
- Pick ONE vocabulary: solid-primary (highest-emphasis safe action), outline-primary (secondary safe action), ghost (tertiary safe action), text-link (in-paragraph navigation), danger-solid (destructive primary), danger-outline (destructive confirmation). Bare bracketed-text shortcuts deleted everywhere.
- Severity must match risk: destructive actions can never look secondary
- Shared `Badge` primitive: ONE state badge shape (state-success / state-warning / state-danger / state-neutral), ONE type/id badge shape (neutral pill w/ monospace text for IDs)

**Done when:** All Studio surfaces use the new button vocabulary; all badges use the `Badge` primitive; the "destructive looks secondary" Studio Med finding closes; visual review confirms one button language across `/training/`, `/l2_shape/*`, `/etl/*`, `/data`.

- [ ] CI.0 - **REPLAN.** Audit every button/badge callsite across studio (`grep` for `bg-accent` / `bg-danger` / `border-accent` and similar in `common/html/`). Group by surface. Decide whether the primitive ships incrementally per-surface or as one big rewrite.
- [ ] CI.1 - **`Button` + `Badge` typed primitives + Tailwind shells.** Land the dataclass + render helpers.
- [ ] CI.2 - **Migrate `/training/` surface.** Highest-traffic Studio surface; "rebuild from base" gets danger-solid.
- [ ] CI.3 - **Migrate `/l2_shape/*` + `/etl/*` + `/data` surfaces.** Per-card Edit/Delete promotion in CF.4 consumes this primitive once it's available.
- [ ] CI.4 - **Cold-read v3 confirmation.** Same persona; assert "destructive looks secondary" finding closes + bracketed-text shortcuts are gone.

## Phase CJ - Color tokens + amber semantics (Color usage)

**Why:** v13.1.1 design review (Color usage section): `accent` is overloaded — it's used for body text, links, table cells, active-tab text, AND KPI big-numbers, which forces brand accent to clear 4.5:1 on white. Most mid-tone brand blues won't. AND: amber is overloaded with three meanings simultaneously — external-counterparty node category (diagrams), neutral descriptive copy (ETL), and caution (buttons/warnings). Audit's fix: derive a separate AA-safe `accent-text` token (auto-darkened from brand accent); reserve amber for caution; move the diagram node-category hue elsewhere; force dark-text-on-amber not white-on-amber (both white-on-amber and amber-on-white fail contrast — dark-on-amber passes).

**Note:** CF audit followup 2026-06-05 already swapped the KPI default state color from `accent` to `text-primary-fg` (commit 3e0c1e1b). That's a partial CJ down-payment but doesn't close the phase — the broader accent-overload still applies to body text, links, active-tab indicators, and table cell links.

**Locks (operator to confirm):**
- New `accent-text` token auto-derives from brand accent (e.g. `oklch(0.4 0.2 hue)`) so it's WCAG-AA against white regardless of how light the brand accent is — keeps brand consistency without contrast failure
- Amber's three roles split: amber-caution (warnings / buttons), node-counterparty (move to a new token like `external-counterparty-node`), neutral-descriptive (move to plain `text-secondary-fg` — the prose doesn't need its own hue)
- Dark text on amber buttons (e.g. `text-slate-900`) — never white on amber
- Audit every `bg-accent` / `text-accent` / `bg-warning` / `text-warning` callsite across the codebase + reclassify against the new tokens

**Done when:** All accent-as-prose surfaces switch to `accent-text` (AA-safe); all amber-as-prose switches to `text-secondary-fg`; all amber-as-button uses dark text; diagram counterparty color moved to its own token; visual regression confirms no surface inadvertently changes color intent.

- [ ] CJ.0 - **REPLAN.** Audit every accent + amber callsite. Decide whether `accent-text` is a separate CSS variable computed at theme-build time, or a Tailwind plugin that derives at render time.
- [ ] CJ.1 - **Token surface — define `accent-text` + audit-replacement targets.** Add `--color-accent-text` to `input.css`; ensure it computes WCAG-AA from `--color-accent` at theme generation.
- [ ] CJ.2 - **Sweep accent-as-prose callsites.** body text, links, active-tab text, table cell links all switch to `text-accent-text`. Theme integration test confirms no callsite uses bare `text-accent` for prose.
- [ ] CJ.3 - **Sweep amber callsites — three-way split.** Audit + reclassify caution/node/neutral occurrences against the new tokens.
- [ ] CJ.4 - **Dark-on-amber rule for button surfaces.** Audit every amber-bg with white text → flip to dark text.
- [ ] CJ.5 - **Cold-read v3 contrast check.** axe-core pass + visual confirm.

## Phase CM - Escheatment tracking sheets (TBD — placeholder)

- Comment: even the name should be considered, escheatment is VERY US centric. Maybe inactive / dormant / unclaimed funds? Pick a neutral term.

**Placeholder filed 2026-06-05.** Banks must identify dormant accounts and, after a regulatory window (typically 3-5 years, varies by state and account type), report and remit unclaimed balances to the state's unclaimed property administrator under each state's escheatment statute. Recon-Gen needs: a way to flag candidate accounts (no activity for N years per the relevant state's rules), surface aging by escheatment-clock, prep the per-state remittance lists, and maintain an audit trail showing why each account did or did not escheat.

**Open design questions (lock before any sub-cells fire):**
- Is escheatment a fifth bundled app (sibling to L1 / L2FT / Investigation / Executives) or a new sheet group inside Investigation?
  - Comment: I think a sheet in investigation would be a good place.
- The "dormancy clock" per account depends on (account_type, state, activity-definition). Where does the policy declaration live — L2 yaml as a new `EscheatmentPolicy` entity? Per-state default tables baked in? Customer-supplied via ETL?
  - Comment: I think we keep it simple to start, defined on account/account template, ISO duration of no transactions but a positive balace (which post CL may be sparse)
- NAUPA II is the de-facto state filing format. Do we generate NAUPA exports here (regulatory-data-out) or stay informational (flag + age, customer files via their existing tooling)?
  - Comment: Informational to keep with the investigation intent.
- Studio Trainer plants needed: "account with no activity for 4 years 11 months" (near-escheatment), "escheated last month" (post-escheatment audit row).
  - Comment: Agreed
- Investigation's existing dormant-account surface — does it absorb or fork from CM?
  - Comment: I don't see that on investigation

## Live BV-post tuning (~150s combined savings)
- **BV-post.qs-browser-skips — QS Browser tier 117 skips / 76 passed (~60% skip rate)** in `up_to qs_browser` run 20260611T034334Z. Operator flagged 2026-06-10 as NOT a release blocker but worth auditing. Per memory `feedback_cheapest_validation_must_fire` — silent drift compounds; verify which skips are intentional (dialect-gated / Need-marker / variant-cell) vs latent gaps.
- **BV-post.app2-wall-tuning — App2 tier wall 1202s vs synthesis estimate of ~340-450s.** Loadgroup pin works (no errors) but per-test wall still ~70s on PG and ~90-200s on Oracle. Snapshot/restore isn't fully amortizing Session Start per the synthesis prediction. Profile + tune; see `runs/20260611T034334Z-00ce44c8/timings/` per-worker `.jsonl` files for cold data.
- **BV-post.snapshot-double-matview-refresh — Drop the duplicate matview refresh in trainer_ready_session._seed_demo_db** (~30s win, s effort). `_seed_demo_db` runs `refresh_matviews_sql` then Session Start's `run_deploy_pipeline → step_4_matviews` runs it again.
- **BV-post.oracle-stuck-asyncio-warning — Oracle stuck_pending/stuck_unbundled asyncio executor warning** — initial cursor-pattern hypothesis FALSIFIED (warning persisted past 8b41b83c). Single clean-run evidence at 20260611T034334Z-00ce44c8 attributes the disappearance to f9123d2a loadgroup pin serializing trainer fixtures onto one worker per dialect (executor-thread thrash from 16 unpinned trainer fixtures, not cursor semantics). Likely the same defect as the (now-dropped) `asyncio executor not joining within 300s` BX backlog item. Confirm-or-drop on the next push-driven CI run; if warning stays absent, drop entirely.
- **BV-post.typing-smell-parametrize — Parametrize `test_typing_smells::test_no_typing_smells` per rule** so AST walks parallelize across workers. Single 12.7s test holds one unit worker hostage. ~11s win, s effort.
- **BV-post.n-12-vs-auto — `-n auto` → `-n 12` on unit + db tiers** (16-core Mac over-provisioned per timing analysis). ~8s win, xs effort. Verify on WSL2 self-hosted CI runner first.
- **BV-post.snap-container-unify — Snapshotter container unification** (loadgroup pin shipped at f9123d2a, app2 layer; gate condition met). ~4s + RAM, s effort. The `recon-gen-snap-test-pg/oracle` dedicated containers may be collapsible to prefix-isolation on the shared CB.17.k containers.
- **BV-post.qs-browser-loadgroup-extension — Extend loadgroup to qs_browser tier** to restore inv/audit producer pins (silent no-ops per the `00ce44c8` stale-comment sync). Currently green so the race hazard is latent; re-validate CB.7-followup marker-deselection × loadgroup cascade hazard first.
## Deferred phase work
- **BK.1 — $0.01 customer-DDA closing balance plant.** Defer: BH.24 cents-vs-dollars cleanup + BH.1 drift formula likely already resolved; no repro in v12/v13.1.1/v13.6.1 cold-reads. Re-file if it surfaces in a future cold-read with a repro.
- **BK.6 — Limit Breach KPI=0 vs 5 rows.** Defer: not reproducible on bundled L2 (sasquatch + spec_example); operator-specific scenario; never reproduced. Re-file if reproducible L2 surfaces.
- **BK.8 — cust-019 / Customer 18 off-by-one.** Defer: needs operator L2 yaml row; never received. Re-file if the L2 yaml arrives.
- **a11y-toolchain-spike** — unblocks CK-deferred.{3,5,6,7} + CJ.5 (the four collapsed items below are gated on this single spike's outcome). Pick the axe-core integration mechanism (npx-via-pytest-subprocess vs Playwright JS-eval vs alternative). Per memory `feedback_rust_influenced_tool_preferences` operator weigh-in needed (axe-core is npm-only). Once the spike resolves, fan out as: entity-list `aria-allowed-role` sweep (~140 nodes); bounding-box overlap sweep (66 remaining; some likely auto-cleared by CG/CH primitives); axe-core CI gate (build-fail on new findings, operator-OK gated); cold-read v3 a11y confirmation (manual screen-reader + axe-core green). Backlogged 2026-06-10.
- **CK-deferred.2 — Training `nested-interactive` fix.** Likely a button-inside-clickable-card issue; pull the inner control out. Carried from Phase CK (archived 2026-06-10) — deferred pending visual repro. Not gated on `a11y-toolchain-spike`.
- **BW deferred-pending-demand — Docs posture (`_kv`-templated deployment-overview page).** Fire only if an operator requests it. D7 already locked at static-default per BV close summary.
- **BV.1 — D3.1 L2 audit redux + per-primitive curriculum doc** ("what's persona for?" deliverable). Operator-judgment on audience (CPA reviewer / dev onboarding / investor / sales), format (single page / multi-doc / mkdocs section), depth, worked-example shape. Backlogged 2026-06-10. (Merged with the separately-deferred D3.1 line.)
- **BV.2 — D3.2 ETL round-trip claim** (test data generator hooked as ETL hook; BT.3's coverage report green per fuzz seed). Likely partially absorbed by snapshot/restore golden-mirror + PlantContract typed invariant; re-scope when re-opened. Backlogged 2026-06-10. (Merged with the separately-deferred D3.2 line.)
- **BV.5 — Agent-based design cold-read against the dogfood + the HUGE test.** Output: `docs/audits/bv_cold_read.md`. Defaults work; just needs target lock (dogfood test surface / Snapshotter abstraction / Trainer UI / all). Backlogged 2026-06-10.
- **CV-followup.1 — per-pair z still flags real biweekly payday clusters.** Filed 2026-06-09 during Phase CV. After CV.2's per-pair PARTITION BY z + min-n floor lands, sasquatch_pr's TRAINER_CLEAN baseline still surfaces ~35 rows at |z| ≥ 4 (down from 52 pre-CV but not 0). Top-5 high-z rows are real customer-payday clusters on busy external counterparties — `ext-payment-gateway-processor → cust-0021-snb` etc. Mathematically per-pair z IS correctly flagging these as anomalies (each cluster day IS the pair's biggest day vs its own history). Differentiating "real anomaly" from "recurring pattern" needs period detection (Fourier / autocorrelation) on top of the z formula. Out of CV's scope. The CV.4 regression test gates on |z| ceiling ≤ 8 rather than count ≤ 0 so the contract is achievable.
- **CV-followup.2 — σ=2 dashboard default needs per-pair calibration.** Filed 2026-06-09 during Phase CV. The dashboard's default `AnomalyView.sigma_threshold` is 3.0; the visual control slider defaults to σ=2. Pre-CV neither was calibrated (per v11_21_0_triage). Post-CV per-pair z has a hard ceiling at N/sqrt(N+1) ≈ 6-7 on the 90-day seed. At σ=2 a large fraction of pairs surfaces; at σ=3 ~100 rows on sasq; at σ=4 ~35 rows. The right default depends on operator intent (deep-dive triage vs systemic-alert) — picking it needs an operator decision + cold-read with both threshold values.
- **CV-followup.3 — InvFanoutGenerator's anomaly side-effect under per-pair z.** Filed 2026-06-09. InvFanoutGenerator's docstring notes it's "NOT registered for AnomalyInvariant: the rolling-window z-score is probabilistic. The boost is deliberately multiplied to push it over." Under CV.2's per-pair z + min-n floor, the fanout plant's recipient may or may not surface anomaly depending on history depth (the boost's 5× multiplier was calibrated against global z). Verify the fanout boost still trips anomaly post-CV; recalibrate if needed.
## CI / release pipeline followups
(none currently — CI-followup.1 + CI-followup.2 shipped per ci.yml flock at `.github/workflows/ci.yml:183` (PG) + `:185` (Oracle) and `@pytest.mark.flaky(reruns=2)` at `tests/unit/test_deploy_pipeline.py:2132`.)
## Other
- **BC.12 deferred: `l2_yaml_raw` / `cfg_yaml_raw` opaque-provenance kv rows.**
- **`no-raw-str-args` AST lint — extend BC.1's D8 family to bare `str` parameters.**
- **Studio / Dashboards rethink under the post-AW DB-projected
- **Q.6 — CLI shape revisit: cfg ⇄ L2 dual-yaml factoring.** Surfaced 2026-05-08 during `Y.2.gate.h.6`. The runner reads `cfg.default_l2_instance` and threads `QS_GEN_TEST_L2_INSTANCE` to subprocesses, making the CLI's dual-arg shape (`-c <cfg.yaml> --l2 <l2.yaml>`) partially redundant. Spike-before-implement (per `feedback_spike_before_locking_implementation`); CLI-surface change touches every operator command + doc example + tests.
- **Dashboards-local L1 dashboard render errors (surfaced 2026-05-10, X.2.g.4 territory, NOT a Y.2.g regression).** With the Y.2.g.2.d pool-lifespan fix landed, `dashboards --app l1_dashboard` starts cleanly + the drift KPI fetches data from the live matview, but other L1 visuals throw render errors in Dashboards (smoke + drift KPI work, broader rendering doesn't). Per-visual coverage in `_tree_fetcher` / `wrap_for_visual` — investigation/L2FT shipped via X.2.g.{2,3} with the same pattern, so the gap is L1-specific visual kinds the renderer hasn't grown arms for yet. Triage: capture the failing visual_ids + renderer error, extend `wrap_for_visual` with the missing arms, mirror the Investigation/L2FT shape. Out of Y.2.g scope (Dashboards visual coverage ≠ pushdown SQL); on the X.2.g roadmap. *(deferred from PLAN 2026-05-19)*
- **Ephemeral AWS infra arc** *(consolidated; six related items, deferred 2026-05-19/05-20 per `[[feedback_ephemeral_aws_infra]]`)*. Six sub-tasks, sequence matters:
- **X.10 — Runner: intra-cell layer DAG (deploy starts right after seed).** The per-cell chain `unit → seed_variant → db → app2 → deploy → api → browser` runs strictly serially today, but `db` / `app2` / `deploy` only depend on `seed_variant` — they're siblings. `deploy` is the long pole (~2 min QS async creation); `db` (~45 s) + `app2` (~30 s) fit inside it. Fan `{db, app2, deploy}` with `asyncio.gather` after seed, then gather `{api, browser}` after `deploy` → ~75 s saved per `aw`-target cell (~6 min off a full-matrix run). The `asyncio` plumbing exists (`Y.2.gate.c.6.async`). Sub-tasks: (a) `cell_chain` returns a `{layer: frozenset[deps]}` DAG; (b) `_run_one_variant` topo-sorts + gathers siblings; (c) failure semantics for in-flight `deploy` (boto3 isn't cleanly cancellable — let it finish, report `db` failure, skip downstream); (d) unit tests for the DAG dispatch (topo/sibling-gather/truncation/failure-skips-downstream, mocked layer dispatch); (e) live wall-clock check, pyright, commit; update CLAUDE.md "Commands" chain description.
- **AA.0 Dashboard UX + exception literacy** *(COMPLETE — shipped v10.1.0a1; full plan archived in `PLAN_ARCHIVE.md` → "Phase AA")*. Six follow-ups still queued from that work:
- **Model-driven docs (drift reduction).** Headline carried forward; design TBD.
- **Mobile / responsive.** Tailwind handles the layout primitives but no explicit mobile-first design pass. Promote when there's a customer story. Note: dashboards are dense by nature; mobile may always be a worse experience than desktop, regardless of effort.
- **Per-table CSV / XLSX export.** Operators expect "export to spreadsheet" on tables (QS has it). Lower priority than feature parity — punt unless it's a small agent task. The audit PDF already covers the "regulator-ready snapshot" case; spreadsheet export is for analyst self-serve.
- **Drop `_oracle_lowercase_alias_wrapper`; emit dialect-natural identifier case from the generator** (was Y.3.f, parked 2026-05-09). DDL is emitted unquoted (PG folds lowercase, Oracle UPPERCASE → divergent storage); `_oracle_lowercase_alias_wrapper` (`common/dataset_contract.py`) bolts an outer `SELECT qs_inner."ACCOUNT_ID" AS "account_id" ...` so QuickSight (which builds `SELECT "account_id" FROM (...)` from its declared lowercase Columns) finds matching aliases. The proper fix — generator emits dialect-natural case in `DatasetContract.to_input_columns()`, QS quotes UPPERCASE on Oracle natively, wrapper gone — is bigger than it looks: QuickSight's analysis-side validation is *case-sensitive* against `Dataset.Columns` (Y.3.f.2's reverted Oracle-deploy probe surfaced 45 column-missing errors), so it requires case-folding ~30+ analysis-side column refs per dialect (visuals / filters / calc-fields / drills), not just the Columns declaration. The original App2 Oracle column-casing bug it would have fixed was instead fixed narrowly by Y.3.f.alt (`wrap_for_visual` quotes its column refs). Re-spike if the dialect-helper count grows past ~60 (SQLite was already dropped from the matrix in CB.8). See `project_qs_analysis_validates_columns_case_sensitive` memory.
- **App2 date picker shows resolved default** — added 2026-05-27 (deferred BL.2.C polish). When the analysis declares a `default_universal_date_range`, the bind layer narrows correctly, but the Flatpickr widget shows "All dates" placeholder — slightly misleading UX. Two paths: (a) pre-fill the visible input's `value` attribute with the default range string (Flatpickr reads it as defaultDate in range mode); (b) emit a `<small>` hint next to the picker showing the resolved range. Skip unless cold-read flags it.
- **BX backlog — Probe name ✓/✗ status badges (deferred from BTa.5)** — added 2026-05-30.
- **BTb backlog — P2/P3 cluster from BTa cold-read v3** — added 2026-05-31.
- **Backlog — test_studio_data_route::test_put_emits_hx_push_url_with_state date flake** — added 2026-05-31.
- **D3.3 Training round-trip claim: every plant kind surfaces correctly per fuzz seed.** — deferred from BV.3 on 2026-05-31.
- **CA.12 — PG pyarrow adapter via adbc-driver-postgresql** — deferred from CA.12 on 2026-06-02. **Why deferred**: Docker-PG probe @ 131k rows showed `psycopg.cursor.copy()` (the right tool here, not adbc) is only 1.95× faster than the existing `cur.execute(big_seed_text)` path (20.89s → 10.69s). PG was never a Python-bound bottleneck the way DuckDB was — psycopg streams big multi-statement INSERTs efficiently and PG's batch planner handles them well. **Revisit if** we step in a PG-perf problem at full prod scale (the COPY-vs-INSERT curve might widen above ~5× as N grows + indexes deepen), or if a user-facing flow (CB.10 QS-against-Docker-PG iteration, a CI seed-apply slowdown) makes the 2× materially valuable. Implementation if revisited: psycopg3 `cursor.copy()`, NOT adbc-driver-postgresql — psycopg is already a core dep and handles type coercion automatically.
- **BX backlog — anomaly/money_trail app2 tests fail on sasquatch_pr L2 (no CustomerSubledger sender)** — added 2026-06-04.
- **App2 KPI binding gap — Distinct Senders + per-account KPI cards render as None** — added 2026-06-04.
- **BX backlog — recreate targeted tests for runner.py post-CB.17.d** — added 2026-06-04.
- **BX backlog — asyncio executor not joining within 300s (thread leak signal)** — added 2026-06-04.
- **BX backlog — Trainer Session Start hangs through browser→studio_server on CI's shared PG/Oracle (CE.4 retreat)** — added 2026-06-04. **DW.5.2 triage update (2026-06-27):** this also reproduces LOCALLY now — the `up_to=agreement` chain hung the `app2` layer twice at ~67% (Playwright greenlet stuck in `run_until_complete`, `Page.wait_for_function` timeout) because the runner set no `RECON_GEN_TRAINER_DIALECTS`, so `test_bv33_trainer_dogfood.py` defaulted to all 3 dialects (`:154`) and spun du+pg+or Studio servers concurrently under `--dist=loadgroup` → memory-pressure starvation. **Mitigated (commit `32b94603`):** the runner's app2 layer now defaults `RECON_GEN_TRAINER_DIALECTS=du` (matching ci.yml — POLICY 1; chain went green, app2 57 passed in 91s). **The ROOT cause is still open + un-rooted:** WHY does the pg/or Studio-server `SessionStart` hang where du doesn't? CE.4 couldn't trace it from logs; du is the only proven-stable dialect, so trainer-dogfood coverage of pg/or Studio deploy is currently sacrificed for stability. Fixing it would let the trainer dogfood run for the chain's actual dialect (pg/or) instead of always du. Same family as [[feedback_strict_engines_surface_isolation_bugs]] + the `pytest-xdist-file-lock` backlog (BX.backlog.duckdb).
- **BX backlog — fetch bare public IP at deploy time for QS data source** — added 2026-06-07.
- **BX backlog — serializer field-emit anti-drift: test gate (A) + auto-derive refactor (B)** — added 2026-06-07.
- **CL.8 followup — Daily Statement source-icon UI plumbing** — added 2026-06-07. Matview rewrite landed (closing_balance_source + closing_carried_from_date + opening_balance_source columns); UI side still needs: (1) typed `KPIValueSourceIcon` primitive in `common/tree/visuals.py` keyed on a column ColumnSpec (3-state enum → glyph map: emitted = no icon, carried_no_activity = ↩, carried_with_activity_gap = ⚠); (2) tree_fetcher reads source column from row + threads to shape_kpi as `source_value`; (3) shape_kpi maps `source_value` → state_icon; (4) Daily Statement Opening Balance + Closing Stored KPIs in `apps/l1_dashboard/app.py` declare the primitive against the new columns; (5) Posted Money Records App2-only empty-state override per CL.0 audit §4.a ("No postings today — balance carried forward from {prior_date}"); (6) QS-side `KPIPrimaryValueConditionalFormatting` parity (audit §2.1 — defer behind the 4-way agreement test).
- **CL.13 followup — Oracle balance_cadence_gap dogfood** — added 2026-06-07. The trainer dogfood for `balance_cadence_gap` passes on PG + DuckDB but returns 0 rows on Oracle 19c after the plant's DELETE. Plant applies cleanly (the `daily_balances` row is gone post-plant), but the `_v_balance_cadence_gap` query reports no row for the targeted `(account, business_day)`. Likely root cause: Oracle matview refresh ordering interaction with the new CL.5 `effective_balances` dependency — the cadence-gap detection works structurally (the spine × LEFT-JOIN-NULL pattern fires on PG / DuckDB) but the Oracle refresh sequence may snapshot the matview before the DELETE took effect, or the matview optimization rewrite picks up a cached predicate. Test is skipped with a typed `pytest.skip` (`tests/e2e/app2/test_bv33_trainer_dogfood.py::test_trainer_dogfood_per_kind[or-balance_cadence_gap]`); demote the skip when the Oracle refresh-ordering issue is debugged. Estimated effort: 1-2 hours of Oracle SQL plan inspection + matview refresh-trace debugging.
- BH.9 - **#9 Today's Exceptions one-bar dominance** — add a click-to-drill-into-check_type left-click drill action, OR log-scale Y-axis, OR top-N + Other bucket. Presentation, not BG scope; tracked here for completeness. *(deferred from phase `BH` on 2026-06-07)*
- BH.10 - **#10 Executives legend overwhelm** — top-N + Other rail bucketing at the dataset SQL layer (limits wire bytes too). Presentation; not BG scope. *(deferred from phase `BH` on 2026-06-07)*
- BH.12 - **#12 Internal Overdraft KPI=0** — investigate `.count()` resolution (COUNT vs COUNT DISTINCT) between QS + App2; verify KPI + table bind the same dataset. BG.3's `test_bg3_overdraft_kpi_matches_matview_count` is the gate. *(deferred from phase `BH` on 2026-06-07)*
- BH.13 - **#13 Pending Aging triple disagreement** — unify the chart + KPI + table on one dataset; if separate datasets are required, add explicit subtitles ("All ageing" vs "Open ageing"). BG.6's triple-identity gate. *(deferred from phase `BH` on 2026-06-07)*
- BH.15 - **#15 Empty-state discipline** — **DEFERRED** for design (not in pre-release cold-read window). Extract L2FT Transfer Templates' empty-state banner into a `common/tree/` helper + apply to Daily Statement / Money Trail / Account Network / Recipient Fanout. Will revisit after next cold-read identifies which sheets are highest priority. *(deferred from phase `BH` on 2026-06-07)*
- BH.19 - **#19 Daily Statement KPI tile text-clipping** — **DEFERRED** (needs browser-render inspection + responsive-CSS work; not in pre-release window). Size-to-content + abbreviation on narrow widths. *(deferred from phase `BH` on 2026-06-07)*
- BH.24.2 - **Sweep + annotate every currency column on every contract** — **DEFERRED 2026-05-25 (housekeeping)**. ~50 ColumnSpec callsites across `apps/{l1_dashboard,investigation,executives,l2_flow_tracing}/datasets.py`. Production behavior is ALREADY correct via the contract-default `storage=DOLLARS` (matches today's pre-converted-in-SQL pattern); the annotation is for documentation hygiene + safety when a future migration introduces raw-cents projections. Defer until BH.24.4 lands (the Cents-typed read boundary will benefit from explicit `storage=CENTS` annotations on the AO.1 storage-shape columns). *(deferred from phase `BH` on 2026-06-07)*
- BH.24.4 - **Type-system reinforcement** (per user 2026-05-25 "smells like a typing issue too") — **DEFERRED to a follow-up cell post-BH** (architectural shift that touches every read-boundary in the codebase). Leverage existing `Cents` (`common/money.py`) at the read boundary: for columns declared `storage=CENTS`, auto-wrap raw cursor values as `Cents.from_db(raw)` before they reach the renderer pipeline. Renderers consume `Cents` and project via `.to_dollars()`. Double-divide becomes a pyright-time type error: `Decimal / 100` doesn't yield `Cents`, so chained converts don't typecheck. Big enough to warrant its own phase; BH.24.6 already makes the BG.7-class bug RAISE LOUDLY at runtime (the runtime gate is in place; pyright gate is the next-level reinforcement). *(deferred from phase `BH` on 2026-06-07)*
- BH.24.5 - **Migration sweep + cleanup** — **DEFERRED** (blocked on BH.24.4). Once Cents-typed read boundaries exist, audit whether the SQL-side `cents_to_dollars_sql` wraps can be dropped. If so, drop the wraps from datasets, mark every currency column `storage=CENTS`, single canonical conversion path. Otherwise document the dual-storage pattern as the new convention. *(deferred from phase `BH` on 2026-06-07)*
- **BX backlog — Handbook route wheel-portability** — added 2026-06-08.
- **BX backlog — L1 Exceptions copy says 10, matview has 12** — added 2026-06-08.
- **BX backlog — DA.7 v1 iterative-screenshot cold-read** — added 2026-06-08.
- **BX backlog — Themed 503 page for PoolReleasedDuringRefresh** — added 2026-06-08.
- **BX backlog — Configurable etl_hook subprocess timeout** — added 2026-06-08.
- **BX backlog — Extend semantic-lock to PG + Oracle dialects** — added 2026-06-08.
- **BX backlog — Wire AsOfFrame.live_from_db into App2 + QS deploy paths** — added 2026-06-08.
- **BX backlog — v13.6.1 finding 4: balance_cadence_gap vs carried_with_activity_gap divergence** — added 2026-06-08.
- **CR.15.followup — Remove dead SQLite plumbing surfaced by no-sqlite-prose lint** — added 2026-06-09.
- **CR.7.a — Sankey node cap operator-visibility (Investigation + L2FT)** — added 2026-06-09.
- **CR.7.b — L1 aging buckets vs rail max_pending_age mismatch** — added 2026-06-09.
- **CS.4.followup — Dynamic Sankey banner with actual node count** — added 2026-06-09.
- **CS.11.followup — Investigate App2 Distinct Senders KPI = None root cause** — added 2026-06-09.
- CS.5 - descoped from phase `CS` on 2026-06-09
- **BX backlog — App2 Anchor parameter pick doesn't trigger /visuals/*/data refetch on CI** — added 2026-06-09.
- [ ] CM.0 - Replan + design — survey state escheatment rule diversity; lock policy-declaration site + filing-output scope (informational vs NAUPA-emitting). SPEC.md update. Output: `docs/audits/cm_0_escheatment_design.md`.





























  Surfaced 2026-05-24 during BC.12 integration. The original design
  said the kv would hold both per-field decompositions AND opaque-raw
  rows (`key='l2_yaml_raw'`, value=<full JSON>) so the operator could
  SELECT the original yaml back from the DB. **Deferred** because the
  Oracle chunked literal shape (`TO_CLOB(c1) || TO_CLOB(c2) || ...`,
  required to dodge ORA-01704's ~4000-char string-literal cap)
  collides with `batch_oracle_inserts`'s quote-aware coalescer —
  the batcher counts quotes per row to detect VALUES tuple boundaries,
  and the chunked TO_CLOB form has multiple quoted segments per row
  that confuse the count.

  Operators retain access via the `--l2 <path>` source file (the yaml
  on disk is the canonical artifact); matview consumption goes through
  the typed projection views. The provenance feature is the loss.

  Fix shape (when prioritized): either (a) teach `batch_oracle_inserts`
  to handle multi-quoted-segment VALUES tuples correctly, OR (b) emit
  the provenance rows as single-row INSERTs that skip the batcher
  entirely (~1KB SQL per deploy event; negligible cost).

  Surfaced 2026-05-24 by user during BC.0 sign-off. The codebase already has
  typed string newtypes (`SheetId`, `VisualId`, `FilterGroupId`, `ParameterName`,
  `DashboardId` in `common/ids.py`; `AccountId`, `RailName`, etc.) but a sweep
  for bare `str` parameters across `src/recon_gen/**` would surface every
  callsite that slipped through. Same shape as D8 (`no-raw-temporal-args`):
  AST-walk function/method signatures, flag any param annotated `str` (or
  `str | None`), require migration to a NewType wrapper OR a
  `# typing-smell: ignore[raw-str-arg]: WHY` escape.

  Expected to be a BIG migration — `str` is everywhere. Whitelist surface
  needs care:
  - SQL fragments + column names (low value to wrap; they're already
    SQL-injected territory if the caller is wrong).
  - User-facing display strings (titles, descriptions, error messages —
    these are content, not identifiers).
  - Dataclass field annotations stay unaffected (point values, not policy).
  - Stdlib-facing seams (Click string opts, env-var reads, file paths via
    `os.PathLike[str]`).
  - The `# typing-smell: ignore[raw-str-arg]: WHY` escape with required
    WHY.

  Staged like D8 — disabled during the migration, enabled at end. Probably
  a dedicated phase rather than a sub-task: scale similar to a full
  Phase BC × N.

  Sequenced after BC + BD land (those validate the AST-lint + named-
  constructor pattern is workable on a smaller migration before scaling).

  L2/cfg.** Surfaced 2026-05-23 after AW completed. AW lifted L2 + cfg
  yaml into `<prefix>_config` as DB-resident JSON; matviews now JOIN to
  it for per-L2 values. That changes a bunch of Studio/Dashboard
  questions that deserve evaluation as a unit, not piecemeal:
  - **Studio editing model.** Studio currently writes the L2 yaml file
    only; deploy regenerates the schema + populates the config table.
    Options: (a) yaml stays the only source of truth; deploy projects;
    (b) Studio also UPDATEs `<prefix>_config` on save for "live-reflect"
    semantics; (c) Studio writes the DB; exports back to yaml on
    operator request. AW makes (b) + (c) feasible; whether they're
    desirable is the open question.
  - **"Deployed-vs-edited" Studio view.** Studio could show the
    DB-resident `<prefix>_config` row alongside the WIP yaml edit — a
    "what's deployed vs what you're authoring" diff surface. Useful for
    "this rail's cap was 5000 in prod; my edit sets it to 7000."
  - **Dashboard pickers from L2 yaml** (the originally-queued item):
    pickers that show L2-DECLARED values (vs dataset-derived which
    shows whatever's in the data); pickers that JOIN to L2 metadata
    (descriptions, types, classifications); pickers that survive
    deploys without re-emitting JSON. Caveat: requires changing
    dashboard JSON's filter shape from `StaticValues` to
    `LinkToDataSetColumn`.
  - **Cross-tabular L2 context in dashboards.** Per-rail
    descriptions / per-account roles surfaced in tooltips, drill
    contexts, etc. — directly JOIN to `<prefix>_config.l2_yaml` from
    the dataset SQL rather than baking at emit time.
  - **Customer ETL access.** Operators / customer pipelines may want
    SQL access to "the same L2 values the matview uses" — DB-resident
    is friendlier than parsing yaml.
  - **Auditability.** `<prefix>_config` could carry timestamps for
    "when did the config last replace?" — Studio could surface "L2
    last updated 3 hours ago" as a deploy-state indicator.
  Scope this as its own evaluation phase (likely audit + spike +
  decision per surface); each Studio/Dashboard surface gets a "use
  DB / don't use DB / hybrid" call. Sized after picking a driver
  goal (analyst-friendliness vs operator ergonomics vs Studio
  iteration speed).



  - **Q.6.0 SPIKE: combined-yaml vs cfg-with-L2-pointer vs status-quo** (LOCKED 2026-05-08; deferred from PLAN 2026-05-19). Output `docs/audits/y_11_cli_shape_spike.md`. Four candidates: **(A)** status quo + `--l2` defaults from `cfg.default_l2_instance` (smallest delta, mostly additive); **(B)** single combined yaml (eliminates dual-yaml friction but env-only fields co-mingle with institution-flavor — Q.5 separation existed for a reason); **(C)** cfg-with-L2-pointer + `--l2` removed entirely (forces multi-instance operators to duplicate cfg); **(D)** `--l2 <name>` indexed against an `l2_instances:` registry in cfg + `default_l2_instance:` (named ergonomics, one indirection layer). Constraints: (1) no-args `json apply --execute` deploys the default L2; (2) multi-L2 operators don't copy cfg files; (3) existing `--l2 <yaml>` keeps working or has a documented migration; (4) doc examples shrink; (5) tests pass without env-var passthrough. Likely outcome: A or D — A smallest delta, D cleanest if multi-L2-per-cfg becomes common.
  - **Q.6.1–Q.6.3 implement per spike result.** Updates `cli/{json,schema,data,audit}.py`, `cli/_helpers.py::resolve_l2_for_demo`, every CLAUDE.md / README / handbook example, every `runner.invoke([..., "--l2", ...])` test, every CI workflow YAML that uses `--l2`. Migration warning ≥1 minor version. Sweep memory entries + docs for stale `--l2 <yaml>` refs. Update CLAUDE.md "Commands" block to show the new shape as canonical; keep explicit `--l2` form as the multi-instance/override sub-pattern.
  - **Q.6.4 bump version (breaking CLI change — post-v9.0.0) + RELEASE_NOTES** (deferred from PLAN 2026-05-19) — entry highlighting the simplification + migration recipe.
  1. **Cleanup-scope bugs (3)** — CI/release cleanup currently targets the wrong scope (sweeps `database-2` + QS resources from other runs). Harmless to release-correctness, leaks resources.
  2. **IAM widen** — `rds-start-stop` inline policy → also allow Create/Delete on `recon-gen-local` + `Github_e2e_testing` (DBCluster + DBInstance + DBSubnetGroup + `iam:PassRole` for monitoring + `ec2:` SG/subnet describe+authorize).
  3. **CI fresh-DB-per-run** — create the DB at run start, apply locked seed, drop at end. No snapshots (seeds churn). Runner `up`/`down` lifecycle.
  4. **Local scripts** — same pattern locally; ephemeral per-run DB pointed at the one standing QS account.
  5. **Per-run cleanup verification** — `tests/e2e/test_cleanup_completeness.py` asserts deploy→teardown leaves no DB row or QS resource tagged with the run's `deployment_name`.
  6. **Verify + close** — confirm idle RDS = ~$0 between runs in Cost Explorer.
  - **AA.A.10 (stretch) Tree-walk picker→column derivation.** `PickerSpec.column` still hand-mapped; the tree carries the wiring formally (`ParameterControl.parameter → Parameter.mapped_dataset_params → (dataset, dataset_param_name) → SQL <<$p>> site → column`). Either parse the dataset SQL or annotate `DataSetParameter` with a `narrows_column` field at construction. Spike before locking annotation-vs-parse.
  - **AA.A.l2ft rails-inverse.4 Type-encode the `table_rows()` invariant.** `table_rows()` for narrowing-assertion sites is a smell — picker-row-survival is about SQL row count, not DOM visibility. Deprecate `len(table_rows())` for assertion use, or rename to `dom_visible_rows()`.
  - **AA.A.daterange.3 Structural refactor: single DATE_RANGE control.** Replace each sheet's `(DateTimePickerControl from + to + TimeRangeFilter)` triplet with one `FilterDateTimePickerControl(Type="DATE_RANGE")`. Closes "from > to" footgun; aligns L1 / L2FT / Exec with Investigation. **Wall:** L1's multi-dataset-per-sheet model needs a sharing mechanism (Investigation's filter-bound widget binds to ONE filter on ONE dataset). Options: (a) consolidate L1 datasets, (b) one widget per dataset per sheet, (c) QS mechanism driving multiple parameter-bound filters via intermediates. Spike before locking.
  - **AA.A.daterange.4 App2 renderer for widget-bound DATE_RANGE.** Already proven for Investigation; extends to the new L1/L2FT/Exec range controls. Follows .3.
  - **AA.A.daterange.5 Test infra.** `apply_anchor_to_pickers` becomes "set the range to span anchor's date ±1 day" instead of separate from/to. Single picker spec. Follows .3.
- [ ] backlog - multi-select clear_button visual verification (no real picker to test)

## Phase DB - QS-to-App2 parity audit + completeness gate (App2 ⊇ QS)

**Why:** Phase DA (closed 2026-06-12, v13.15.0) uncovered a hidden parity gap of recognizable shape: a tree-level feature (`conditional_formatting`) emitted to QS JSON, NOT extracted by `_VisualPlan`, silently dropped by App2's renderer. The DA gap sat in production for months unnoticed because the existing parity tests checked tree→fetcher derivation but not renderer-side honoring. Same shape almost certainly lives in ≥1 other place — most likely candidates:

- **KPI conditional formatting beyond the three indicators we plumb.** `_VisualPlan` carries `kpi_zero_is_healthy` / `kpi_inflow_is_healthy` / `kpi_threshold_banding`; QS's `KPIConfiguration.ConditionalFormatting` also accepts arbitrary background/icon CF.
- **BarChart / LineChart per-series color encoding.** `_ChartMeta` covers axis labels + stacking but probably not the `colors=` field-well or per-category color overrides.
- **`SameSheetFilter` highlight-without-narrowing.** Operator docs deprecate this for filter intent but it's still a Visual-level construct QS would honor and App2 wouldn't render.
- **Filter-group date widening on cross-sheet drills.** Tested at JSON layer; renderer-landing parity not explicitly covered.

**Goal (operator-locked at DB filing 2026-06-12):** **App2 is a SUPERSET of QuickSight.** Every QS-emit-able feature renders correctly on App2 (DA-shape gap = bug); App2-only features stay enhancements (handbook `?` button, ⋯ row menu, xlsx export, markdown prose, Studio L2 editor, ETL hooks — see `parity/breaks.py` PARITY_BREAKS registry). The audit explicitly measures coverage in the QS→App2 direction. The completeness gate enforces it forward.

**Locks (operator to confirm at DB.0 exit):**
- **Audit direction is QS → App2** (every QS-emit field has an App2 consumer). The reverse direction (App2 features not in QS) is tracked as ENHANCEMENT in `PARITY_BREAKS` and stays out of scope here.
- **Completeness gate fails at construction time** (per `[[feedback_invariants_in_types]]`), not at deploy or render. A tree-level visual attribute that lands in the QS JSON without a registered App2 consumer raises `ValueError` at App / Visual construction.
- **Hard-divergence and workaround entries in `PARITY_BREAKS` are exempted.** Some QS features (`count_distinct_quirk_bl1` etc.) are deliberate workarounds, not parity to chase; the registry is the authority.

**Done when:** every QS-emit feature on every Visual kind has a registered App2 consumer OR a `PARITY_BREAKS` entry explaining why not; the construction-time completeness gate fails any new mismatch at the wiring site; cold-read v5 visual parity across all 4 apps confirms no visual divergence beyond the ENHANCEMENT set.

- [ ] DB.0 - **Audit.** For each Visual kind in `common/tree/visuals.py`, walk `emit()` and inventory the QS JSON fields it sets. Walk `_VisualPlan` extraction (`_table_column_meta`, `_chart_meta`, `_kpi_*`) + `shape_*` adapters + the d3 renderers to inventory honored fields. Diff. Output: `docs/audits/db_0_qs_to_app2_parity_audit.md` with a per-Visual coverage table. Operator-confirm scope before sub-cells fire.
- [ ] DB.1 - **Close the DB.0 gaps.** Per-feature scope (may split if many). Each gap closes via a `_VisualPlan` field + extraction + `shape_*` forwarding + renderer consumption (the same 4-layer pattern DA used). Each gap also gets an anti-regression unit + JS test mirroring DA.6's shape.
  - [x] DB.1.1 - DB.1.1 BarChart color_label + orientation parity
  - [x] DB.1.2 - DB.1.2 Sankey items_limit + (others) rollup
  - [x] DB.1.3 - KPI Sparkline `Visibility=HIDDEN` (don't plumb data source yet — operator-locked 2026-06-12; tree's hardcoded VISIBLE was schema-validation appeasement only, no actual trend data wired. Future enhancement: App2 ⊇ QS richer KPI graphics with TrendGroups + trend renderer.)
  - [x] DB.1.4 - DB.1.4 LineChart Type=AREA/STACKED_AREA parity
- [x] DB.2 - **Completeness gate.** At `App` (or `Analysis`) construction, walk every Visual + cross-check emitted-attribute set against a registry of "App2-consumed attribute keys". Mismatch raises `ValueError` with the offending Visual / attribute / path. The registry is the typed source of truth; adding a Visual attribute later forces the author to add a registry entry (so the gap can't sneak back in).
- [x] DB.3 - **Cold-read v5 parity verify.** Visual side-by-side (QS embed + App2) of every Visual kind across 4 apps; confirm no divergence beyond the ENHANCEMENT set in `PARITY_BREAKS`. Output: `docs/audits/db_3_parity_verify.md`.
- [ ] DB.4 - **Phase exit + v13.16.x release cut.** Bundle DB into one release notes entry. Bump 13.15.x → 13.16.0 (minor if any user-visible visual rendering changes; patch if only the gate lands without behavior change). Operator authorize-at-cut per `[[feedback_always_ask_before_release_cut]]`. Sweep DB to PLAN_ARCHIVE.md.

## Phase DC - HTTPS support for studio / dashboards (draft 2026-06-12)

**Why:** Operator-flagged 2026-06-12 — `recon-gen studio` + `recon-gen dashboards` currently serve plain HTTP via uvicorn. Deployed copies need HTTPS for: (1) QuickSight embedded analytics URLs (QS embed sessions require https origin), (2) modern browser security posture (HSTS / Secure cookies / mixed-content blockers on the docs site mount), (3) credential handling in any future auth surface (login forms over HTTP get a browser warning + degrade UX).

**Locks (operator to confirm at DC.0 exit):**
- TBD. Open questions:
  - **Cert provisioning surface.** uvicorn `--ssl-keyfile`/`--ssl-certfile` flags (operator pre-provisions PEM files), or generate self-signed at boot with a stable local CA (mkcert-style), or expect a reverse proxy (Caddy/nginx) to terminate TLS?
    - Comment: cert and key should be pem files referenced by path in the cfg.yaml
  - **Local dev experience.** Self-signed cert means browser warning on first hit; mkcert locally-trusted CA fixes that but requires `brew install mkcert && mkcert -install`. Acceptable?
    - Comment: self signed is fine
  - **CLI shape.** `recon-gen studio --tls` (auto-generate) vs `--tls-cert <file> --tls-key <file>` (explicit) vs both?
    - Comment: neither, fields in the yaml
  - **Deployed-mode default.** Assume reverse proxy terminates TLS (the wheel-served Studio listens on HTTP behind it), or assume in-process TLS termination?
    - Comment: in-process TLS termination unless the base library doesn't support it, if so, let's discuss
  - **QS embed implications.** QS embed URLs are HTTPS-only; does App2's QS-embed driver path need any change beyond the origin protocol?
    - Comment: QS should have no changes, this impact App2 only
  - **HTMX + browser cache interactions.** `Secure` cookies on HTTPS — does anything in App2 set cookies?
    - Comment: Right now I don't think we set any cookies, if we do, let's talk

- [x] DC.0 - **Spike + lock cert provisioning + CLI surface.** Output: `docs/audits/dc_0_https_spike.md`. Operator redirected (2026-06-14) from mkcert-self-signed to DNS+LE via Cloudflare for test/local-dev/CI; 4 DNS names per env (loopback + dynamic public IP, both under `<env>.recon-gen.hotchkiss.io`); cert/key paths live in `cfg.app2.tls.{cert_path,key_path}` (per DC.1) — runner-internal helper, no user-facing CLI verb. Downstream `recon-gen.exe` operators bring their own PEMs (out-of-scope for the coordinator).
- [x] DC.1 - **CLI plumbing + uvicorn TLS wiring.** `cfg.app2.tls.{cert_path,key_path}` → uvicorn `ssl_certfile`/`ssl_keyfile`. Click `--tls-cert`/`--tls-key` flags layer over the cfg. Unit-tested.
- [x] DC.2 - **Run-internal TLS coordinator under `src/recon_gen/_dev/tls/`.** Modules: `storage.py` (XDG state + advisory fcntl renew lock + atomic cert+key writer), `public_ip.py` (cloudflare_trace pattern), `cloudflare_api.py` (zone discovery + cached zone-id + A/TXT record CRUD), `acme_client.py` (DNS-01 state machine via `acme` lib), `ensure.py` (the `ensure_dev_env(env, *, cert_path, key_path, account_email, acme_directory_url)` orchestrator). 37-unit-test suite (1.7s, all mocked; real PEM minting via `cryptography`). Env tuple locked: `Env.DEV` → `localdev`/`dev.recon-gen.hotchkiss.io`; `Env.CI` → `localci`/`ci.recon-gen.hotchkiss.io`. Token read via typed `RECON_GEN_CLOUDFLARE_TOKEN` registry; absent ⇒ ValueError with run/secrets.env hint. Deps (`acme`, `cryptography`, `dnspython`, `requests`) under `[dev]` extra only — NOT shipped in the published wheel.
- [x] DC.3 - **Runner integration.** Cfg extension (`account_email` required, `env: dev|ci` default-dev, post-init validates) + `TLS_TOUCHING_LAYERS = ("app2", "qs_api", "qs_browser")` + `_ensure_tls_if_configured` helper wired into `cmd_up_to` before `_setup_thin_chain_environment`. 8 unit tests pin the 4 branches + ci-env mapping + tilde expansion in cert paths.
- [x] DC.4 - **Operator onboarding doc + ci.yml wire-up.** `docs/operations/tls-setup.md` covers the full setup (token mint → run/secrets.env paste → cfg block → first-run expectations → troubleshooting → token rotation). `ci.yml` reads `RECON_GEN_CLOUDFLARE_TOKEN` from `secrets.CLOUDFLARE_TOKEN`. The cfg-overwrite heredoc's `app2.tls:` block lands in DC.5 (held until operator confirmed both secrets present, to avoid painting CI red between merge and secret-paste).
- [x] DC.5 - **Live verify + 3 bug fixes surfaced by first live LE call.**
  - [x] DC.5.fix.deadline - `acme_client._deadline` returned offset-aware datetime; the acme library's `poll_authorizations` compares against naive `datetime.now()` → TypeError mid-poll. Fixed to naive. Regression-pinned.
  - [x] DC.5.fix.conflict - `_register_account` caught `messages.Error` for the "account already exists" benign case, but the library raises `acme.errors.ConflictError`. Caught the right exception class. Regression-pinned.
  - [x] DC.5.fix.regr - Post-ConflictError catch left `client.net.account` unset → "No Key ID in JWS header" on next request. Reconstruct `RegistrationResource` from ConflictError's `location` URL and assign. Regression-pinned.
  - [x] DC.5.verify - Live `ensure_dev_env(Env.DEV, ...)` minted a real LE cert for `(localdev, dev).recon-gen.hotchkiss.io`, 90d validity, key 0600. Stored under XDG (`~/.local/share/recon-gen/tls/dev/`).
  - [x] DC.5.heredoc - `ci.yml` cfg-overwrite heredoc gained the `app2.tls:` block (env: ci, account_email from `secrets.TLS_ACME_EMAIL`).
  - [ ] DC.5.merge - Operator merges feature/dc-2-tls to main; bump to v14.0.2 + tag + sweep DC to PLAN_ARCHIVE.md.

## Phase DF - L2 Editor essentials / test-data block (draft 2026-06-12, locks 2026-06-12)

**Why:** Operator-flagged 2026-06-12 alongside DE — the L2 Editor (`/l2_shape/<kind>/<id>/edit` Studio routes) shows every field flat, with production-required values mixed inline with knobs that only matter for fuzz / training / scenario plant generation. Operators on a real customer L2 squint past 20+ fields to find the 5 that matter for shaping their real flows. Same UX win the BX9 theme editor shipped — essentials at top, optional knobs in a collapsed `<details>` block underneath — should apply to every L2 entity kind. **Not a data-shape change.** No field renames, no inheritance, no schema migration; pure UI grouping over the existing field specs.

**Locks (operator-confirmed 2026-06-12):**
- **Pattern: clone the BX9 theme block.** Top section "Essentials" with the production-required + commonly-tuned fields; `<details>` block below with the test-data-generation / fuzz knobs. Mirror `_theme_editor_bx9.py::_render_theme_essentials_section` + `_render_advanced_details_open` — auto-opens when any non-default value is present in the collapsed block. Likely shared HTML helper between theme + L2 editor (extracted in DF.1 if it isn't already). (Source: operator "I think we'll be doing what we did with the theme block. Here are the main values, here is the stuff needed to produce good test data (probably in a collapseable block)".)
- **Scope: UI grouping only.** No field renames, no schema change, no inheritance. The per-kind field-spec module (likely `_editor_field_specs.py` / `_l2_form_schema.py`) gains a small bucket annotation per field (`essential` vs `test_data`); the renderer routes each into the right section. Operator-visible URL + form post wire shape unchanged.
- **Per-kind buckets locked at DF.0 exit, not DF entry.** Each entity kind (Account, Rail, TransferTemplate, Chain, plus singletons) gets its own essential/test-data split during the DF.0 audit — likely obvious for most kinds (essential = name + identity + role + relationships; test_data = anything that only shapes plant flow shape). Operator confirms before DF.1 sets it in stone.
- **DE independence.** DF doesn't depend on DE shipping first. If DE's cfg-side block primitive ends up reusable for the L2 form, DF.1 can adopt it; if DE ships later, DF can use the existing theme-editor helper directly.

**Scope is largely mechanical** — bucket annotation per field + renderer wiring + browser-driver verb to expand the collapsed block in e2e tests. One new driver verb (`expand_details_section(name)` or similar) added to the appropriate driver protocol per `[[feedback_build_verbs_not_skip]]` so e2e tests can verify the test-data fields are reachable when needed.

- [ ] DF.0 - **Audit + per-kind essential/test-data bucketing.** Output: `docs/audits/df_0_editor_field_buckets.md`. Inventory every entity kind's field spec; propose per-kind essential vs test_data split (one bullet list per kind). Operator-confirm before DF.1.
- [ ] DF.1 - **Bucket annotation + renderer wiring.** Add the per-field bucket annotation to the field-spec module. Extract the theme-editor essentials/advanced helper into a shared HTML primitive (or directly reuse if already shared). Renderer routes fields into the two sections. No field renames; no behavioral changes to form post.
- [ ] DF.2 - **Reuse `open_home_section` for L2-editor test-data block.** `tests/e2e/_drivers/studio_browser_editor.py::open_home_section(kind)` already opens `details[data-kind="<kind>"]` — scoped to home-page sections today but the locator pattern is general. Either (a) ensure DF.1's test-data `<details>` carries `data-kind="test_data"` so the existing verb works untouched, OR (b) split out a kind-agnostic `open_details_section(name)` helper if the home-section scoping is too tight. Either way: minor generalization, not a from-scratch verb. Per `[[feedback_build_verbs_not_skip]]` — extend the existing affordance rather than skip the e2e param.
- [ ] DF.3 - **Cold-read verify.** Iterative-screenshot loop per `[[feedback_cold_read_iterative_screenshots]]` against the most-field-heavy entity kind (likely TransferTemplate or Chain). Confirm operator can scan production-required fields without the test-data knobs in their face.
- [ ] DF.4 - **Phase exit + release.** Sweep to PLAN_ARCHIVE.md.

## Phase DG - CI database hygiene + persistent-container debris cleanup (draft 2026-06-13)

**Why:** v13.15.1 release-gate CI run (`gh run 27454069073`) failed with `psycopg.errors.DiskFull: could not resize shared memory segment ... No space left on device` on two `tests/e2e/test_inv_filters.py` cells. Pattern matches the multi-commit CI red streak on main going back at least 6 pushes (d09edd36 / f62b8f2c / a27492ec / 49a94a01 all failing on `CI` workflow). Root cause: `tests/e2e/_isolation.py::isolated_cfg` (lines 117-162) is a **module-scoped fixture** that creates a per-(file, worker) prefixed schema then runs a **best-effort silent drop** on teardown (`except Exception` + `print` to stderr, no fail-loud). The CI step "Ensure shared PG + Oracle are up (reuse persistent containers when present)" reuses the same containers across runs, so any teardown failure leaves debris that accumulates across builds. ~150 e2e files × N workers × M runs = hundreds of stale prefix sets, indexes, matviews, and tablespace consumption that never gets reclaimed. Eventually PG's shared-memory segment can't fit the working set + DiskFull.

**Oracle has the same bug class.** The teardown code path doesn't dialect-branch the drop (only the DuckDB `if` short-circuits) — Oracle runs the same `emit_schema_drop_sql → execute_script → swallow-on-error` flow. Hasn't tipped over yet because (a) Oracle's tablespace headroom is larger by default, (b) Oracle's `ORA-*` failure modes surface louder than PG's silent disk pressure, (c) Oracle tests fire less frequently in the full chain. Same root cause, just buying time.

**Locks (operator-confirmed 2026-06-13):**
- Operator: "disk full says we're not cleaning up databases or tables on fixture tear down" — confirmed via inspection of `tests/e2e/_isolation.py:158` swallow-and-print.
- Operator: "I bet oracle has the same problem" — confirmed by the dialect-uniform teardown path.

**Open questions (locks at DG.0 exit):**
- **Fail loud or fix the swallow?** Today's `except Exception: print(...)` is the silent-failure smell that produced the multi-week red streak. Options: (a) re-raise → first failing teardown fails the test session loudly; (b) collect failures + report at session end; (c) keep best-effort but add a session-scoped sweep that audits leftover `<base>_<suffix>` schemas after the run.
- **Container-boot sweep.** Add a runner step that drops every `_<suffix>`-named schema BEFORE the first test fires — the "scorched earth at start" pattern. Side-steps the cumulative-debris compounding regardless of per-test teardown reliability.
- **VACUUM / tablespace reclaim.** PG's `DROP SCHEMA CASCADE` reclaims pages lazily — under heavy churn the working set can stay too big even after a clean drop. Run `VACUUM FULL` after the teardown sweep? Or DROP DATABASE entirely if cheap?
- **Per-worker DBs instead of per-suffix prefixes.** Today every worker shares one DB and namespaces by table prefix. Alternative: every worker gets a separate database (`CREATE DATABASE recon_worker_<n>` at boot, `DROP DATABASE` at teardown). Cleaner isolation, but every test connect has to know the right DB.
- **Same fix shape for Oracle?** Oracle's equivalent of `DROP SCHEMA CASCADE` is per-user-schema (`DROP USER ... CASCADE`); container-boot sweep needs Oracle-specific SQL but same concept.

- [x] DG.0 - **Audit + lock fix shape.** Output: `docs/audits/dg_0_db_hygiene_audit.md`. Per `[[feedback_no_silent_defer]]` + `[[feedback_cheapest_validation_must_fire]]`: measure the per-cell schema accumulation in a fresh-container baseline, propose the loud-fail + scorched-earth-at-boot pattern, lock the PG and Oracle SQL. Per `[[feedback_spike_before_locking_implementation]]` — prototype the boot sweep against a stale container before committing.
- [x] DG.1 - **Fail-loud teardown.** `tests/e2e/_isolation.py::isolated_cfg` teardown re-raises (or collects + reports at session end) instead of `print + swallow`. Per `[[feedback_no_xfail_to_sweep_under_rug]]` — no more silent skips on the bookkeeping path.
- [x] DG.2 - **Container-boot scorched-earth sweep.** Runner step that drops every `_<suffix>` schema (PG) + every `recon_*` user (Oracle) before the test layer fires. Idempotent; safe to re-run.
- [x] DG.3 - **Triage the 12 v13.15.1 failures.** With hygiene fixed, re-evaluate which of the 12 cells were genuine bugs vs DiskFull-cascade. Particular focus on `test_bo_1_daily_statement_picks_reconcile_per_role[qs]` (ZBASubAccount / WireSettlementSuspense dropdown miss) + `test_inv_dashboard_structure_matches_tree[qs]` (Recipient Fanout tree mismatch) — those look pre-existing-real, not cascade.
- [ ] DG.4 - **Phase exit + release.** Sweep to PLAN_ARCHIVE.md. CI green is the exit gate.

## Phase DH - QuickSight resource hygiene — dataset title-prefix + full-coverage cleanup (draft 2026-06-13)

**Why:** Operator-flagged 2026-06-13 while logged into the QS console to inspect Sankey hover behavior. Two related QS-resource-hygiene gaps surfaced:

1. **Datasets don't carry the `(<deployment>)` suffix in their Name.** Analyses + dashboards already do — e.g. analysis Name is `"L1 Reconciliation Dashboard (qsgen-postgres)"`, dashboard Name is `"L1 Reconciliation Dashboard (qsgen-postgres)"`. But all 53 datasets emit with names like `"Executives Account Summary"`, `"App Info -- Liveness"`, `"Executives Program Health"` — no deployment suffix anywhere. When the operator views the QS datasets list with multiple deployments present (e.g. `qsgen-postgres` + `qsgen-oracle` + an ephemeral CI deployment), the list shows N copies of the same dataset Name with no human-visible way to map each row to its deployment without clicking through.

2. **`recon-gen json clean` only iterates 5 QS resource kinds: dashboards, analyses, datasets, themes, datasources.** Per `src/recon_gen/common/cleanup.py` paginator inventory (lines 110-161). Resources known to exist but NOT swept: **folders** (QS organizational containers), **refresh schedules** (dataset-attached, may orphan when datasets recreate), **templates** (we don't use them today, but unmanaged-template debris from operator experiments could accumulate), **topics** (Q-feature; unused but reachable), **groups** + **users** (out of scope — managed by IAM). Operator observation: "our aws clean up isn't nuking everything in quicksight."

These are related concerns — both about end-to-end resource lifecycle hygiene — distinct enough that DH has two parallel sub-tracks (DH.1 title prefix, DH.2 cleanup coverage audit + sweep).

**Open questions (locks at DH.0 exit):**
- **Title-prefix suffix format.** Match the analysis/dashboard convention exactly: `"<base name> (<deployment_name>)"`? Or operator-visible different (e.g. leading `[<deployment>]` prefix for grouping)? Match-existing is the safest default.
- **Cleanup coverage scope.** DH.2 audits the full QS API surface, but does the sweep IMPLEMENT every kind, or just inventory what's leaking + fix the highest-impact one (likely folders + refresh schedules)? Recommend inventory + prioritize on impact.
- **Folders ownership.** Do we currently create QS folders anywhere? If not, the cleanup gap is about debris from operator manual creation, not the generator. Different fix — maybe out of scope for DH.
- **Refresh schedules.** Datasets in the recon-gen tree use Direct Query (no SPICE per `[[project_direct_query]]`), so refresh schedules shouldn't exist on our datasets. If they DO exist, it means someone is attaching them outside the generator — also a debris-from-manual-action case.
- **Tag-filter strictness.** The cleanup tag filter is `ManagedBy: recon-gen` + `Deployment: <name>`. Are there resource kinds where these tags don't propagate from the resource we created (e.g. refresh schedules inherit parent dataset's tags but the list_tags_for_resource call may not work on them)?
- **DG dependency.** DH is independent of DG; can land anytime after the active DG.3 triage settles.

- [ ] DH.0 - **Audit + lock fixes.** Output: `docs/audits/dh_0_qs_resource_hygiene.md`. Inventory every QS API list_* paginator vs every kind we touch. Confirm dataset title-suffix is the only Name-side gap (or surface other asymmetric kinds). Lock title format + cleanup-coverage scope. Operator-confirm before DH.1.
- [ ] DH.1 - **Dataset Name `(<deployment>)` suffix.** Apply the locked title format to every dataset emit site. Per-app `apps/<app>/datasets.py` already constructs the Name from app + dataset-kind; add the suffix at the model level (`DataSet` / `DataSetSummary` emit) so every emit path inherits it. Unit test: every dataset JSON in `out/datasets/` carries the suffix. Drift gate against a fixture if needed.
- [ ] DH.2 - **Cleanup coverage sweep.** Per DH.0 scope: add `list_folders` / `list_refresh_schedules` / etc. paginators to `cleanup.py`, filter on `ManagedBy: recon-gen` + `Deployment: <name>`, delete the stale set. Add per-kind tests against a mocked QS client (similar shape to existing cleanup tests).
- [ ] DH.3 - **Phase exit + release.** Sweep to PLAN_ARCHIVE.md.

## Phase DJ - Tech debt sweep (post-v14.2.0 cleanup) (draft 2026-06-15)

**Why:** Three independent cleanup items surfaced during DD.4 + the v14.2.0 cut. None blocks a feature; all reduce drift / silent-failure surface. Bundling into one phase keeps the bisect-trail clean. Operator-authorized overnight autonomous run 2026-06-15 (all three + direct-to-main push per [[feedback_autonomous_run_boundaries]]).

**Locks (operator-confirmed 2026-06-15):**
- **All three sub-phases land in v14.3.0** (post-cut, minor bump for the POLICY 1 net-new + JS security fixes).
- **Each leaf is its own commit** so morning review is granular + bisect-friendly.
- **Direct-to-main push per leaf.** Pre-push hook gates on full unit tier locally; CI runs autonomously per push.
- **No tag/release cut overnight.** v14.3.0 cut happens in operator's morning session after they review the diffs.

- [x] DJ.1 - **Retire `RECON_GEN_E2E` env-var gate (#79).** Remove `RECON_GEN_E2E=1` gate at `tests/e2e/conftest.py::pytest_collection_modifyitems` + the env-var registration in `env_keys.py` + the runner's per-layer `RECON_GEN_E2E=1` sets + all 30+ `RECON_GEN_E2E.get_or_none()` callsites. POLICY 1 divergence fix: operator-confirmed 2026-06-14 there's no marginal AWS cost (QS subscription is fixed-cost). Pre-removal: full unit suite + chain to verify no test silently skipped only because the gate was hiding it.
- [x] DJ.2 - **DD.4 adversarial-review polish (5 sub-items).** All bounded fixes from the wrxjfgldy workflow's 16 minor findings:
  - [ ] DJ.2.tempdir - _cleanup - register an `atexit` sweeper (or context-manager hook) that `shutil.rmtree`s `cfg_dir` tempdirs created by `ensure_dev_idp` after the container's lifetime ends. Stops accumulated 0o600 LE-key copies under `/tmp/recon-gen-dex-cfg-*` from piling up indefinitely. Adversarial-review's most operationally-painful follow-up.
  - [ ] DJ.2.name - _threading - `_dex_logs_tail` thread `name` parameter from `get_or_start_dex_container`'s signature (or drop the parametrization at the caller — pick whichever is cleaner). Today's hardcode of `DEX_SHARED_CONTAINER_NAME` is the classic "parametrized at the boundary, hardcoded internally" smell.
  - [ ] DJ.2.windows - _guard - `os.getuid()` / `os.getgid()` in `container.py` AttributeErrors on Windows. Add `sys.platform != 'win32'` guard with a friendly `RuntimeError("Dex test container fixture requires Linux/macOS")`. _dev/ is excluded from the wheel so the constraint is test-only, but the failure shape should be operator-friendly.
  - [ ] DJ.2.adopt - _mount_check - On the adopt path, inspect `existing.attrs['Mounts']` and force-recreate when the bind-mount source doesn't match the current `cfg_dir`. Closes the "adopted container serves stale cert / config" gap that the b298ca26 docstring honestly named but didn't fix.
  - [ ] DJ.2.adopt - _status_explicit - Adopt path: match `existing.status in ('exited', 'dead')` for force-recreate rather than `!= 'running'`. Avoids destroying `created` / `restarting` / `paused` mid-flight containers in races.
- [x] DJ.3 - **CodeQL XSS-through-DOM fix (6 high-severity warnings).** Validate URL protocol via `new URL(url).protocol in ('http:', 'https:')` before assigning to `window.location.href` at all 6 flagged sites:
  - [ ] DJ.3.bootstrap - _608 - `src/recon_gen/common/html/assets/js/bootstrap.js:608` (row-drill URL).
  - [ ] DJ.3.bootstrap - _768 - `src/recon_gen/common/html/assets/js/bootstrap.js:768` (row click → drill URL).
  - [ ] DJ.3.bootstrap - _773 - `src/recon_gen/common/html/assets/js/bootstrap.js:773` (Enter/Space → drill URL).
  - [ ] DJ.3.mini - _diagram_154 - `src/recon_gen/common/html/_studio_assets/mini-diagram.js:154` (edit URL click).
  - [ ] DJ.3.diagram - _533 - `src/recon_gen/common/html/_studio_assets/diagram.js:533` (edit-badge href).
  - [ ] DJ.3.diagram - _534 - `src/recon_gen/common/html/_studio_assets/diagram.js:534` (edit-badge xlink:href).
- [ ] DJ.4 - **QS MUI Autocomplete noOptions-race fix in `narrow_dropdown_options_by_query`.** Triaged from CI run 27533230182 (`test_cq_4_e_l2ft_picker_finds_known_value[qs-Rails-Rail]` failing with `Advertised (first 10 of 0): []` while DB has 30 rails). Same class as task #68 (Transactions-Transfer sheet-mount), #69 (Anchor DOM shape), #70 (Anchor MUI noOptions). After the `wait_for_selector(option | noOptions)` resolves, re-poll `_OPTION_SELECTOR` for ~2s when noOptions won the initial race + the listbox isn't yet populated. Lets a late-arriving listbox win; still returns `[]` for genuine empty results. Closes task #70 by construction.
- [ ] DJ.5 - **Phase exit + v14.3.0 release cut** (operator-driven morning session — release notes + version bump + tag are NOT in scope for the overnight run per the autonomous-boundary rule).

## Phase DL.3 - reopen — Drill destination integrity (2026-06-16)

**Why:** CI run 27586777150 on commit 6ffd69c1 revealed DL.3 + DL.4 shipped without runtime verification on the drill destinations. The post-CI drift-plant work (DM.1 / `ee17db80` local-only) makes the drift cells COLLECT but exposes two real bugs in the drill seam plus three test-infra issues. POLICY 2 — no deferred failures: all six leaves land in one commit chain before v14.4.1.

**Locks (operator-confirmed 2026-06-16):**
- **Daily Statement remains the destination shape (balance + transactions)** — fix the drift PLANT to emit the matching tx, not the destination contract. Option A path.
- **DM.1 stays local** (`ee17db80` — drift plants + 4 sibling helper fixes + re-locked semantic snapshots). Will push as the head of the DL.3-reopen chain, not standalone.
- **All six subtasks ship in one commit chain** before v14.4.1 — no scope-dropping.

- [x] DL.3.1 - **Bug 1: drift→daily-statement destination empty.** `DriftGenerator` + `LedgerDriftGenerator` emit balance rows for synthetic drift accounts but no matching transaction rows on the same account+day. Daily Statement's WHERE filters tx by `(account_display, business_day_start)` → 0 rows → empty destination, despite DL.3's `account_display` shape fix being correct. Extend the plant generators to emit at least one matching tx row (small magnitude is fine — the destination just needs to be non-empty + bound to the drilled value). Verify via `up_to qs_browser --only "Leaf Account or Parent Account"`.
- [x] DL.3.2 - **Bug 2: pL1TxTransfer drills land but don't narrow.** 3 cells write a specific `transfer_id` (Stuck Pending Detail / Stuck Unbundled Detail / Transactions Audit) but the destination Transactions sheet shows completely different transfer_ids (50/50 row mismatch). DL.4's static audit was incomplete — the destination control's `source_parameter_name` or the dataset's WHERE clause isn't actually bound to `pL1TxTransfer` at runtime. Diagnostic + wire fix.
- [x] DL.3.3 - **Runner `--only` rejects pytest -k expressions with spaces.** `_normalize_only_expr` (`src/recon_gen/_dev/runner.py`) rewrites visible-name substrings carrying whitespace into AND-joined identifier substrings (`"Leaf Account or Parent Account"` → `"(Leaf and Account) or (Parent and Account)"`) so pytest -k's identifier-only tokenizer accepts the expression — pytest substring-matches each AND-joined identifier against parametrize ids like `[Leaf Account Drift]`. Wired through `_options_from_args` so every layer's `-k` invocation gets the normalized form. Quoting / escaping was the obvious-but-doesn't-work fallback (pytest rejects `"Leaf Account"` as `string literal`).
- [x] DL.3.4 - **Runner xdist crashes with exit=3 when `--only` filters out all items in an earlier layer.** `_is_only_no_match_exit(exit_code, only)` (`src/recon_gen/_dev/runner.py`) classifies BOTH rc=5 (pytest NO_TESTS_COLLECTED) AND rc=3 (xdist INTERNAL_ERROR from `assert not crashitem`) as the no-match shape when `--only` is set. The existing `cmd_up_to_thin` arm uses the helper to mask the same "this layer has nothing to do" condition under either exit shape; rc=0/1/2/4 still surface loud. Constants `_PYTEST_NO_TESTS_COLLECTED = 5` + `_PYTEST_INTERNAL_ERROR = 3` document the cross-tool exit-code grammar.
- [x] DL.3.5 - **QS drift cells fast-skip at 5.7s (table cells don't render in time).** Even with planted drift data (`db_counts.txt` shows drift=17, ledger_drift=18), the QS embed iframe doesn't paint Leaf Account Drift's first row in time for `_read_source_row_values()` to capture it. Same renderer-timing class as task #68 (Transactions-Transfer sheet-mount), #69 (Anchor DOM shape). Operator suspects a matview is needed to pre-aggregate the drift visual so QS can render fast enough. Investigate: probable shape is a `<prefix>_drift_summary` matview projecting `(account_display, leaf_drift_amount, parent_drift_amount, business_day)` so QS hits a tabular shape directly. **Landed**: `<prefix>_drift_summary` matview UNIONs `<prefix>_drift` + `<prefix>_ledger_drift` with pre-computed `account_display` + `abs_drift` + `account_class` discriminator. Wired into refresh order, snapshotter, BV.6 UNIQUE-index test list. `build_drift_dataset` + `build_ledger_drift_dataset` SELECT from the new matview filtered by `account_class`. Same dataset IDs + contracts — visual rewire is transparent.
- [x] DL.3.7 - **Overdraft → Daily Statement destination-empty on CARRY days.** DL.3.6 fixed the contextmenu paint timeout that was blocking this cell; the next-layer bug is structural. `<prefix>_overdraft` reads from `effective_balances` (CL.5 carry-forward), so a single planted overdraft balance row surfaces on EVERY business day from its emit day forward until the next emit or window end — but DL.3.1's marker-tx pattern emitted exactly ONE marker on the plant's emit day. Drilling from any of the 5-15 carry days landed on the Daily Statement with an empty Posted Money Records table (the drill writes the carry day; the marker-tx WHERE filter only matches the emit day). Same destination-empty CLASS as DL.3.1's drift fix; different mechanism (CL.5 carry-forward rather than account_name shape). **Landed**: `OverdraftGenerator.as_of_day: date | None` field; `emit()` sweeps one zero-amount marker tx per day from `anchor_day` through `as_of_day` inclusive when set, falling back to DL.3.1 single-marker shape when None (the in-process unit-test path). `_adapt_overdraft` threads `plant_window.end` through as `as_of_day` so the production seed pipeline gets full-window coverage. Marker tx id includes BOTH the plant anchor day AND the coverage day so overlapping densified plants don't PK-collide on `_current_transactions.id` (densify factor=5 plants at days_ago=6/13/20/27/34 all on `cust-0002-snb` overlap on every shared carry day). Amount=0 preserves all L1 invariant identities byte-identically; semantic locks unchanged. Two new unit tests in `tests/unit/test_spine_overdraft.py` pin the sweep shape + the overlapping-plants no-collision contract.
- [ ] DL.3.6 - **Phase exit + v14.4.1 release cut** (operator-driven — bundled hotfix release including the entire DL.3-reopen chain).

## Phase DM - Daily Statement App2-only cascade + day-availability picker (draft 2026-06-16)

**Why:** App2's server-rendered model can dynamically query "what data exists for this account" cheaply per request; QS can't (dataset-parameter cross-wiring hits `[[project_qs_url_parameter_no_control_sync]]`). This is the right kind of symmetry break — driven by renderer capability, not arbitrary preference. Per `[[feedback_qs_convention_origin]]` ("App2 may break the rule when a better affordance exists"), this is the second authorized App2-over-QS divergence.

**Principle (prominent in DM.0):** App2 cascades on search/filter; the DATA stays consistent with QS. The symmetry break is UX-of-finding-the-row, not what-the-row-shows. Motivation: every tester gets burned by QS's cascade-that-doesn't (cascading dataset parameters silently failing, control-sync quirks).

**Locks (operator-confirmed 2026-06-16):**
- **Picker order**: Role → Account → Day (top-to-bottom in sidebar; left-to-right in horizontal layout). Reads as a natural drill-down progression.
- **Role → Account cascade** via the existing BR.1 cascade-refresh endpoint pattern.
- **Account → Day filter — DECORATION not RESTRICTION.** Sparse accounts mean any day up to `as_of` stays valid as a pick target. Flatpickr's `onDayCreate` callback adds CSS classes (`.has-transactions`, `.has-balance`, `.has-both`) based on a server-supplied date map. No `enable:`/`disable:` arrays — full date space stays clickable.
- **QS keeps Account + Day pickers, no work, just document the divergence** via structured triple (quirks.md + memory file).
- **Renderer-gate primitive: strongly typed in core trees** (per operator preference, not a registry side-table; tree-walking already pays off in DL.1's `iter_cross_sheet_drills` + DA.5's `Table.__post_init__` type-system gate). If no primitive exists, add `app2_only: bool` field to relevant tree primitives (Filter / ParameterControl) + branch in QS emitter walk to skip those nodes — that's DM.0.5 (prereq).
- **One commit per leaf**; release ships as v14.5.0.

- [x] DM.0 - **Audit + design lock** at `docs/audits/dm_0_daily_statement_app2_cascade.md` (shipped overnight, commit `03769610`; 43KB doc covers the principle, cascade chain, BR.1 cascade-endpoint contract, day-availability endpoint contract, decoration-not-restriction rationale, empty-state UX, and the DM.0.5 renderer-gate scout). Box was left untick'd by the overnight run.
- [x] DM.0.5 - **Renderer-gate primitive** (if scout shows none exists) — add typed `app2_only: bool` field to relevant tree primitives (Filter / ParameterControl), branch in QS emitter walk to skip them. Tree-walking unit test asserts QS-side surface count vs App2-side surface count when any `app2_only` nodes exist.
- [x] DM.1 - **Add Role picker to Daily Statement** (App2-only via the renderer-gate primitive). Wire as third picker between Account and Day.
- [x] DM.2 - **Role → Account cascade** via the existing BR.1 cascade-refresh endpoint. Reuse `dropdown-options/...` route pattern.
- [x] DM.3 - **Day picker decoration**: server endpoint returns `{date: ['transactions'|'balance']}` map; Flatpickr `onDayCreate` adds CSS classes; CSS rules for the markers (dot / underline / bullet — pick at design time in DM.0).
- [x] DM.4 - **Document the QS divergence** (structured triple): append to `docs/reference/quicksight-quirks.md` (`2e3bc6f2`) + file `project_qs_no_searchfilter_cascading.md` memory cross-linking the rationale. Plus DM polish this session: day markers fill=transactions/ring=balance + legend (`c635f603`), KPI fit-to-width + bottom-align (`a613398c`), cascade clear-on-source-change (`64396f49`), pre-existing biome/typing-smell debt cleared (`e358104c`/`162c7671`). gl-1010 weekend-balance question → Backlog.
- [ ] DM.5 - **Tests**: unit (cascade SQL emit, day-availability projection, empty-state hint); E2E App2-only (pick role → account narrows; pick account → day decorations match seed; pick account with no data → empty-state hint shown). QS branch is a no-op assertion that Role picker doesn't exist on QS.
- [ ] DM.6 - **Phase exit + v14.5.0 release cut** (operator-driven — release notes + version bump + tag at operator's discretion).

## Phase DN - Running balance on Daily Statement (both renderers, placeholder 2026-06-16)

Operator flagged 2026-06-16 in the DM design discussion: adding a running-balance column to Daily Statement (balance carried forward across transactions on a given account-day) would benefit BOTH QS and App2 — it's a parity feature, not a symmetry break. Parked as a separate phase from DM because DM is specifically the UX-of-finding-the-row work; running balance is the row-shape work.

Sub-leaves enumerated by `docs/audits/dn_0_running_balance.md` (DN.0 lock — SQL window function, no matview).

- [x] DN.0 - Audit + design lock at `docs/audits/dn_0_running_balance.md`.
- [x] DN.1 - Contract update + dataset SQL. Add `ColumnSpec("running_balance", "DECIMAL")` between `amount_direction` and `status` in `DAILY_STATEMENT_TRANSACTIONS_CONTRACT`. Extend `_daily_statement_transactions_sql` with `SUM(tx.amount_money) OVER (PARTITION BY tx.account_id, business_day ORDER BY tx.posting, tx.id ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)` wrapped via `cents_to_dollars_sql` — single SQL across PG / Oracle / DuckDB. Unit test asserts the projection on every dialect.
- [x] DN.2 - Posted Money Records visual: add running_balance column on both renderers between amount_direction and status, `currency=True`.
- [x] DN.3 - Carry-forward fix for sparse-account opening_balance KPI (`_daily_statement_summary_sql` source change from `business_day_start = picked` to `business_day_start < picked ORDER BY business_day_start DESC LIMIT 1` — strict `<` matches the prior LAG semantic; opening = EOD of the prior emit, never the picked day's own EOD). COALESCE-to-0 on the no-prior edge case.
- [x] DN.4 - Audit PDF extension: `DailyStatementTransaction` dataclass gains `running_balance: Decimal`; `_query_daily_statement_walks` projects via the same window function (`SUM(amount_money) OVER (PARTITION BY account_id, business_day ORDER BY posting, id ROWS BETWEEN UNBOUNDED PRECEDING AND CURRENT ROW)`, cents→dollars at the `_cents_to_dollars` read boundary like amount_money). PDF + markdown renderers gain a "Balance" column between Amount and Status.
- [x] DN.5 - Tests: cross-dialect SQL-emit (json tier `test_daily_statement_transactions_projects_running_balance`, PG/Oracle/DuckDB fragments) + DuckDB Python-accumulate equivalence + multi-account PARTITION isolation (`tests/data/test_dn5_running_balance.py`); closing-arithmetic agreement (`running_balance(last_leg) == matview closing_balance_recomputed - opening_balance`); sparse-account carry-forward owned by `tests/data/test_dn3_sparse_account_opening_balance.py`; audit-PDF parity (`_query_daily_statement_walks` running_balance == dataset SQL == Python accumulate); E2E `[qs, app2]` column-present + arithmetic stub added at `tests/e2e/test_l1_account_filters.py::test_dn5_posted_money_records_running_balance` (needs a qs_browser run to verify behaviorally — not exercised in this unit/audit run).
- [ ] DN.6 - Phase exit + release cut (operator-driven; version depends on DM ship order).

## Phase DP - qs_browser regression sweep (overnight DL.3/DK fallout, 2026-06-16)

Main went red at the v14.4.0 cut: the qs_browser layer hung (stdout-stuck >900s → SIGKILL → exit 247) on top of picker/drill failures. Root: DL.3.1/DL.3.7 `_spine_plant` zero-amount marker tx (recent `posting` via the carry-day sweep) won picker anchors + led drill-source rows. Operator decision 2026-06-16: plants ARE user-facing errors and won't round-trip cleanly; tests must exercise VALID rows; pickers selecting errors is a backlog enhancement.

- [x] DP.0 - Picker-anchor fix: exclude `_spine_plant` from the L1 Transactions + L2FT Rails additive-picker anchors (anchor a valid transfer). Resolves the 4 picker failures + the exit-247 hang. Committed `96bae2ab`; verified on a narrowed qs_browser run (31 passed, hang gone).
- [x] DP.1 - QS driver hardening: convert the BO.1 one-shot 800ms empty-state re-check into a bounded condition-poll (`_visual_settled_empty`) on both `table_row_count` + `table_rows`, fixing the flaky `dropdown_pickers_inverse_excludes_anchor[qs-*]` restore step (DOM paint lag > 800ms under concurrent-worker load). Verified: every `dropdown_inverse` test passes (incl. the previously-hard-failed `[qs-Pending Aging]` `assert 0==1` and the formerly-flaky `[qs-Drift]`/`[qs-Transactions]`), no reruns needed.
- [ ] DP.2 - Drill guardrail `Stuck Unbundled Detail → Transactions` (app2). **Root cause (triage):** the drilled transfer is the oldest stuck one (legs at the window start, 2026-03-18); App2's `_serialize_table_row_drills` DROPPED the drill's `DrillStaticDateTime` date-widen (its premise "App2 date filter defaults to all rows" went false at the DK data-anchor refactor), so the old legs fell outside the destination's as_of window → empty (confirmed: wide date → 4 rows). **Fix:** emit static-`value` drill params (render.py) + `rowDrillUrl` uses them (bootstrap.js) — fixes every Pending/Unbundled/Supersession → Transactions date-widen drill. JS unit test added; e2e verify pending in the full run.
- [ ] DP.3 - Drill guardrail `L2 Violation Detail → Chains` (app2): row 0's entity_a is `_spine_plant` (the DL.3.1 marker rail surfacing as an L2FT violation) → "View in Chains" writes a non-existent chain → empty. **Operator decision 2026-06-16: keep the marker visible (plants ARE errors); test-side exempt** — the guardrail `pytest.skip`s the populated/narrowed contract when row 0's drilled value carries `_PLANT_ERROR_SENTINEL` (`_spine_plant`), mirroring the picker-anchor exemption. e2e verify pending in the full run.
- [x] DP.5 - App2 trainer-apply timeout bump (120s → 300s). The full-chain run aborted at the app2 layer on `test_trainer_dogfood_per_kind[or-expected_eod_balance_breach]` — `trainer_apply`'s `_trainer_wait_until_finished` hit the 120s ceiling on Oracle's matview refresh (heavier since DK added the `data_anchor` matview; the docstring already said "oracle ~few min" but defaulted to 2 min). Passed in CI, timed out locally (slower Oracle). Operator-directed: it's a timeout we control → raise it (sibling `trainer_start_session` already uses 10 min). Unrelated to the DP picker/drill fixes.
- [x] DP.4 - Full `up_to=qs_browser` green + push the batch — pushed (origin/main `46523d05..6224a83b`), CI green (run 27637281875), cut **v14.4.1**. Backlog filed: enhance pickers to select planted-error values. Open follow-ups: DP.5-revert (timeout was the wrong instinct; real cause was the stale Oracle container), DG container-recycle gap, Phase DQ (typed dependency order).

## Phase DQ - Strongly-typed database-object model (dependency order + typed column refs) (planned 2026-06-16)

Operator directive 2026-06-16 (surfaced by the DP Oracle-trainer near-miss): database objects (base tables, Current* views, matviews, v-overlay) should declare their dependencies AND columns in a strongly-typed model, so two latent-bug classes become unrepresentable:
- **Refresh/drop order is DERIVED** (topological sort of the dependency graph), not three hand-maintained order lists (`schema.refresh_matviews_sql`, the drop-order list, `snapshotter._V_OVERLAY_MATVIEW_SUFFIXES`) that can silently diverge. "Is drift_summary after its parents?" stops being an eyeball check. (The DP sweep verified the lists are *currently* consistent — that they CAN diverge is the footgun.)
- **Column references are TYPED** — the same per-object declaration eliminates the implicit STRING column references scattered across dataset SQL, matview SELECT bodies, and dashboard field refs (a renamed/dropped column fails at runtime, not construction). Extends the existing `DatasetContract` / typed-ID / `ColumnShape` patterns down to the schema layer. Operator note: the dependency-graph model "would also solve our implicit column references all over the codebase" — same root.

- [ ] DQ.0 - **Audit + design lock.** Output: `docs/audits/dq_0_typed_db_objects.md`. Inventory every DB object + its upstream deps + emitted columns; survey the current string-column-reference surfaces. Propose the typed model (one declaration per object = deps + columns), the topological-order helper, and how column refs become typed attrs. Decide incremental vs big-bang + which footgun closes first. Operator-confirm before DQ.1.
- [ ] DQ.1 - **Dependency graph + derived order.** Replace the hand-maintained matview order lists with a declared dependency graph + topological sort; `refresh_matviews_sql` / drop order / snapshotter suffixes all derive from it. Construction-time validation: a cycle or missing dep fails loudly. Test: derived order matches today's (correct) order; an inverted dep raises at construction.
- [ ] DQ.2 - **Typed column references (incremental).** Per DQ.0, migrate the highest-value string-column-reference surfaces to typed attrs so a renamed/dropped column fails at construction. Likely starts at the matview/dataset boundary the DP work touched.
- [ ] DQ.3 - **Phase exit + release.** Sweep to PLAN_ARCHIVE.md.

## Phase DR - Supersession Audit detection fix + transaction-id navigation (planned 2026-06-17)

Design lock: `docs/audits/dr_0_supersession_design.md`. The Supersession Audit "flags everything" — root-caused to TWO independent defects, both from `entry` being a global insert-order serial (not per-id): (A) the no-reason FLAG (`entry > 1`) fires on every original row; (B) the SELECT (`COUNT(*) OVER (PARTITION BY id) > 1`) over-selects — on the full baseline it returns 77 rows but only 10 are a genuine supersession; the other 67 are densified-plant legs whose deterministic `id` omits the day, so 5 replicas collide on one `id`. Both fixed detection-side in the dataset SQL (DR.1 — the correctness bug shipped in v14.5.1; lands first, standalone). DR.2-DR.5 add the operator's transaction-id navigation (search + crosslink on the logical `id` the audit is keyed on, vs `transfer_id`). DR.4 carries one open decision-gate (crosslink target — option (b) same-sheet trail filter recommended).

### DR.7 - Post-review fixes (adversarial review `wszrtv8bf`: 4 confirmed findings)
Empirically confirmed on the densified seed: the Supersession Audit shows 10 rows / **1 distinct id** / **4 false no-reason flags**. Root cause: `replicate_super` densifies 5 same-account SupersessionPlants across 5 days, but `SupersessionGenerator.transaction_id`/`transfer_id` OMIT the day, so 5 independent daily trails collapse onto one id; `entry > min_entry AND supersedes IS NULL` then flags the 4 later-day originals (DR.1 fixed the SELECT predicate but not the no-reason FLAG's collision-blindness). Per `[[feedback_production_honest_invariants]]` the fix is seed-side, not matview-side.
- [x] DR.1 - Supersession detection fix (flag + SELECT narrowing, both datasets)
- [x] DR.2 - ColumnShape.TRANSACTION_ID + contract tagging
- [x] DR.3 - Transaction-id search on the L1 Transactions tab (both renderers)
- [x] DR.4 - Crosslink → transaction id (option (b): same-sheet trail filter)
- [x] DR.5 - "(No reason)" filter on the Supersession Audit (depends on DR.1.a)
- [x] DR.6 - Visible Transaction ID picker on the Supersession Audit



- [x] DR.7.a - Day-unique supersession plant id (`transaction_id` + `transfer_id` include `anchor_day`) + `_adapt_supersession` honors `days_ago` (it dropped the day-spread, a pre-BC.4 latent bug) → confirmed on clean reseed: no-reason KPI 4→0, 5 distinct ids.
- [x] DR.7.b - Semantic locks verified current — the green unit run (incl. `test_spine_semantic_lock`) confirms DR.7.a's day-unique change does NOT shift the L1-invariant violation-sets the locks capture (supersession is an audit concern, not a locked L1 invariant), so no re-lock was needed. Refreshed the stale `datasets.py` "5 replicas reuse one id" comment (the replicas are day-distinct post-DR.7.a; the `has_supersede` guard stays correct on its own terms).
- [x] DR.7.c - e2e: replaced the tautological `all(== target)` with real narrowing (pre-set >1 id guard, STRICT `focus_count < full_count`, post-set == {target}) on both DR.4 self-filter + DR.6 dropdown.
- [x] DR.7.d - json static guard: `build_l1_supersession_tx_ids_dataset` SQL asserted to share all three `_SUPERSESSION_*` predicate constants with the audit dataset.
- [x] DR.7.e - DR.6 dropdown Oracle+App2 ORA-00904 fixed at the wrap path: `_picker_search_sql_wrap`/`_picker_seed_sql_wrap` used `column_name()` (unquoted→UPPER on Oracle) against base_sql's `_oracle_lowercase_alias_wrapper` quoted-lowercase surface. Switched to `_quote_col()` (the validated BV.3.3.e/m.5.d pattern) — no-op on PG/DuckDB, fixes Oracle. Unit-locked.
- [x] DR.7.f - Harden the `day_availability` App2 driver: it returned a premature stable-`{}` when the async onDayCreate fetch landed after its 450ms stability window — the DM.3 "empty markers" flake, surfaced when DR.7.a's seed shift moved the `carry_days DESC` selection onto a heavier-fetch account (gl-1830). Now only a stable NON-EMPTY snapshot settles; genuinely-empty still returns `{}` at the deadline. (app2 re-run confirmed the failure was non-deterministic; sole caller asserts non-empty.)
- [ ] DR.7.g - Phase exit + v14.6.0 release cut. **Gate fixes** (2 driver read/click races surfaced by the first green-blocked `qs_browser` run; commit `acb582ca`): App2 `drill_from_first_row` did a center-of-row click that landed on the transfer_id `cell-accent-menu` cell (stopPropagation→ctxmenu, never wrote `pL1SaTransaction`) → drive the row's keyboard Enter handler; QS `table_rows` caught the stale empty-state overlay before a cross-sheet drill repainted (screenshot showed 4 rows, read got 0) → route through the `_visual_settled_empty` paint-poll the other QS reads already use. **Full gate GREEN 2026-06-18** (unit 5420 / db 104 / app2 91 / qs_api 45 / qs_browser 204 — 0 failed). `__version__`→14.6.0 + RELEASE_NOTES staged on `feature/dr-supersession`. **REMAINING (operator):** merge → main + tag `v14.6.0` + push, held per [[feedback_always_ask_before_release_cut]]. After the cut: tick this box + sweep DR to PLAN_ARCHIVE.md.

## Phase DS - Math-invariant ↔ code parity (canonical residual + oracle-equivalence gate) (draft 2026-06-17)

Operator directive 2026-06-17: each math invariant's law is currently encoded 3-4 times — the Python sim (`ledger_simulation` / `account_simulation` / `expected_eod`), the detector matview SQL (`schema.py`), the generator's planted magnitude (`spine/*`), and the audit-PDF walk — with nothing forcing them to agree. Two latent-bug classes become unrepresentable:
- **Generator↔detector magnitude drift** — a generator plants a hardcoded δ calibrated against one version of the detector's formula; the formula moves; the plant no longer trips its own detector. Live instance: CV-followup.3 (`InvFanoutGenerator`'s 5× boost calibrated against global z, may not surface under per-pair z). Fix: generators plant *to a residual the detector computes*, not a literal magnitude.
- **Detector↔oracle semantic drift** — the matview WHERE-clause silently diverges from the canonical math (a `<` where the law says `=`). DQ's typed-DB model can't see this; only oracle-equivalence over generated states can. DN.5 already proves the pattern for running_balance (`python accumulate == dataset SQL == matview closing_balance_recomputed - opening_balance == audit-PDF`); DR generalizes it to every conservation invariant.

Single source of truth: each invariant becomes a pure signed-residual fn `State -> Money` (zero = satisfied). Detector reads it, generator plants to it, healthy asserts zero. Conservation invariants (drift, ledger_drift, overdraft, expected_eod_balance_breach, limit_breach, money_trail) get exact-residual contracts; probabilistic invariants (anomaly / per-pair z) get a SEPARATE tolerance-band contract — do not force exact-δ on the statistical bucket.

Gated on DQ.2 (typed column refs) so residuals reference typed columns and "detector reads exactly the columns its invariant is defined over" becomes a construction-time assertion. The residual + hand-derived KAT work (DS.0–DS.1) can start in parallel; column-typing wires in after DQ.2.

**Pre-DS.0 recon (workflow `wb2da0bdk`, 2026-06-17 — agent-derived, VERIFY file:line at lock).** An adversarial 6-mapper sweep over sim / matview / generator / audit-PDF surfaced four corrections that should reshape DS.0 before it locks:
1. **Taxonomy needs THREE residual kinds, not two.** Of the 13 `ALL_INVARIANTS`, only ~4-5 are clean `residual(state)->Money` Money-conservation laws (drift, ledger_drift, overdraft, expected_eod). FOUR are integer CARDINALITY constraints (chain_parent_disagreement, xor_group_violation, fan_in_disagreement, multi_xor_violation) where `->Money` is a category error (natural residual = count delta). TWO are wall-clock TIME thresholds (stuck_pending, stuck_unbundled) whose matview recomputes `CURRENT_TIMESTAMP` every refresh (non-deterministic — no `residual(state)->Money` exists for a value that changes with zero state change). money_trail has no predicate (a derivation); anomaly is the probabilistic bucket. → DS.0 must split Money-residual / cardinality-residual / threshold-residual, or the residual type lies. The PLAN's two-bucket (conservation/probabilistic) framing above is too coarse.
2. **DN.5 does NOT establish single-source-of-truth.** It's a 4-way EQUALITY GATE over four INDEPENDENTLY-coded copies of the running-balance law on ONE fixture (the opening-anchor subquery is literally duplicated dataset-SQL ↔ audit-walk; matview uses a third formula). There is no `running_balance_residual()` any path calls. DS is therefore proposing something STRONGER than DN.5 (extract the law into one pure fn the paths call), not "generalizing" it — correct the framing in DS.0 so the goal is explicit.
3. **CV-followup.3 is largely a PHANTOM** — `InvFanoutGenerator.intended` returns a `CoverageObservation`, NOT a `RuleViolation`, and is explicitly NOT registered for `AnomalyInvariant` (registry ~201-208). Verify before citing it as the DS.2 motivating drift instance; the magnitude-drift class is real but this specific example may not be.
4. **DQ.2 gating confirmed correct.** DS.0-DS.1 (residuals on `State` dict values + hand-math KATs) genuinely need nothing from DQ.2 → real parallel-start. DQ.2 is a HARD prereq only for DS.3's construction-time "detector reads exactly the columns its invariant is defined over" check.

- [ ] DS.0 - **Audit + design lock.** Output: `docs/audits/ds_0_invariant_parity.md`. Inventory every math invariant × where its law currently lives (sim / matview / generator magnitude / audit-PDF). Classify into the THREE residual kinds above (Money / cardinality / threshold) + the probabilistic bucket — NOT the two-way conservation/probabilistic split. Propose the `MathInvariant` enum + per-kind residual signature + the KAT format — test vectors derived BY HAND from the written law, not lifted from current code (per `[[feedback_cheapest_validation_must_fire]]`: the audit bridge can't be the code auditing itself). Verify the pre-DS.0 recon's file:line claims as part of the lock. Operator-confirm before DS.1.
- [ ] DS.1 - **Canonical residuals — conservation bucket.** One pure `residual(state) -> Money` per conservation invariant = the single home of that law. KATs pinned to hand-computed vectors. No I/O, no detection logic copied in — the matview stays the detector; the residual is the spec it's checked against.
- [ ] DS.2 - **Generators plant to residual, not magnitude.** Refactor the drift-prone generators (start `InvFanoutGenerator` — closes CV-followup.3) to plant to a target residual the detector reads. Kills the calibration-drift class structurally; recalibration stops being a recurring backlog item.
- [ ] DS.3 - **Oracle≡matview parity gate.** Generalize DN.5: Hypothesis over generated states asserting `python_residual == matview_residual` per invariant, named by `MathInvariant` id so a red test points at the exact law; healthy-state ⟹ residual zero across ALL detectors (no-false-positive gate).
- [ ] DS.4 - **Probabilistic bucket — tolerance contract.** Distributional / tolerance-band assertion for anomaly-class invariants (not exact δ). First consumer: recalibrate + pin the fanout boost under per-pair z (absorbs CV-followup.3's recalibration tail).
- [ ] DS.5 - **Completeness gate at construction.** Every `MathInvariant` member has residual + matview ref + ≥1 generator + KAT; a missing edge raises at import (extends `[[feedback_invariants_in_types]]` / completeness-gate-fails-at-construction down to the invariant layer).
- [ ] DS.6 - **Phase exit + release.** Sweep to PLAN_ARCHIVE.md.


## Phase DT - Documentation voice pass — rewrite AI-voiced docs in chotchki's voice

Rewrite the reader-facing prose so it reads as chotchki, not generic-AI. Source of truth: `/Users/chotchki/workspace/career-portfolio/voice_profile/chotchki-voice-profile.md`. The README + QS-quirks catalog are named IN that profile as Claude-written-under-his-guidance — exactly the AI-voice → raw-voice conversion this phase makes. His own SPECs / design-thoughts are voice SOURCES, leave them untouched.

**Scope (operator-locked 2026-06-25):** everything reader-facing — README.md, the mkdocs handbook (`src/recon_gen/docs/**`), the 37 in-app per-sheet help snippets, CLI `--help`, the handbook vocabulary/invariant/diagram prose. OUT: `docs/audits/**` (dev-facing), `CLAUDE.md` (agent instructions), PLAN trackers, RELEASE_NOTES history (forward-style note only), any chotchki-authored SPEC.
**Cadence (operator-locked):** README first as calibration → operator red-pen → fold corrections into the voice checklist → fan out the rest with his fixes baked in.
**Hard constraints every rewrite honors:** preserve jinja / mkdocs-macros + HandbookVocabulary tokens; keep persona-neutrality (`test_docs_persona_neutral.py`); don't trip handbook drift / liveness / parity gates (`test_sheet_handbook_liveness.py`, `test_handbook_invariants.py`, `test_bu2b_registry_anti_drift.py`, `test_etl_examples.py`, App2↔QS parity); preserve technical meaning + every fact / number / link; NO Oxford commas; ALL-CAPS + parens for emphasis, never bold.

- [x] DT.0 - Setup + voice-rewrite guide + provenance / drift-gate map. Branch `feature/dt-doc-voice` (off `feature/dr-supersession` HEAD — rebase onto main later). Write `docs/audits/dt_0_voice_rewrite_guide.md`: repo-specific lint distilled from the profile, the hard constraints above, the his-authored-vs-AI-authored provenance classification per in-scope file, and the doc-content test-gate map.
- [x] DT.1 - **README.md calibration rewrite** [CALIBRATION GATE]. Hand-draft against the guide, then run an adversarial perspective-diverse voice-lint workflow (one critic per anti-pattern lens — maxim-headers / triads / Oxford / marketing-adjectives / "load-bearing" / windup) plus a meaning-preservation checker vs the original; fold findings; present the diff for operator red-pen.
- [x] DT.2 - **Fold red-pen → voice checklist v2.** Run a voice-redpen analysis over chotchki's README edits; fold the surviving corrections into `dt_0_voice_rewrite_guide.md` so the fan-out inherits his fixes.
- [x] DT.3 - **Handbook core fan-out.** mkdocs prose: index, concepts/, for-your-role/, handbook/ app guides, reference/ (install, self-host, CLI, quirks log), scenario/. Workflow pipeline draft → voice-lint → correctness per file; operator review at the section boundary.
- [x] DT.4 - **Walkthroughs fan-out** (28 guides, ~6.6k lines — largest section). Same pipeline; operator review at boundary.
- [x] DT.5 - **In-app per-sheet help snippets** (37 files, ~3k lines — render LIVE in QS TextBoxes + App2). Pipeline + drift / liveness-gate guard (token preservation is the thing that breaks everything else if dropped); operator review.
- [x] DT.6 - **CLI `--help` + handbook vocabulary / invariant / diagram prose** (in-code: `cli/**`, `common/handbook/**`). Pipeline + App2↔QS parity / drift guard; operator review.
- [x] DT.7 - **RELEASE_NOTES forward style note.** Add a short "new entries in chotchki's voice" note; leave the 14.6.0 → 13.14.4 history untouched.
- [ ] DT.8 - **Phase exit.** Full mkdocs build clean + all doc-content tests green + persona-neutrality + App2↔QS handbook parity + link check; operator final review; RELEASE_NOTES entry (ask before cut per [[feedback_always_ask_before_release_cut]]); sweep DT to PLAN_ARCHIVE.md.

## Phase DU - Logical→physical column terminology alignment

Drop the v3 logical-name hold-over (`signed_amount` / `posted_at` / `balance_date` / `balance` / `transfer_type` / `account_type` / `parent_transfer_id` / `transaction_id` / `amount`-absolute) from reader-facing docs + CLAUDE.md; align to the v6 physical columns. Decisions (2026-06-26): SPEC.md stays the conceptual/logical layer (untouched); fix prose only — the Investigation matview `*_account_type` output aliases stay (no code/parity churn). Context-aware: base-table column refs → physical; matview OUTPUT columns + SPEC concepts → untouched.

- [x] DU.0 - Mapping + scope + decisions. Authoritative map from the v6 DDL (`schema.py:1959`/`2022`) + rename comments (`transfer_type`→`rail_name` Z.B; `account_type`→`account_role` `:3984`; `posted_at`→`posting`; `balance_date`→`business_day_start`; `balance`→`money`; `signed_amount`→`amount_money`; `parent_transfer_id`→`transfer_parent_id`; `transaction_id`→`id`; `amount`-absolute → `ABS(amount_money)`).
- [x] DU.1 - Fan-out: align the ~30 reader-facing docs to physical base-table column names (context-aware; keep matview output aliases + SPEC). rewrite → verify → fix + the doc/handbook/parity gates.
- [x] DU.2 - CLAUDE.md Domain Model section align to physical (the source of the hold-over).
- [x] DU.3 - Verify (full unit + docs + build + parity gates) + commit; operator review.

## Phase DV - QuickSight soft-deprecation: optional `[quicksight]` extra + drop the shim publish (v15.0.0 — major)

Make QuickSight a fully opt-in renderer without deleting a line of QS code. boto3 moves out of the default install into a dedicated `[quicksight]` extra; the one eager boto3 import goes lazy; the QS CLI verbs fail with a friendly "install the extra" message; QS test legs `importorskip` cleanly; the legacy `quicksight-gen` rename shim stops publishing (retired with v15); deprecation notes land in the DT voice. **The `aws:` cfg block becomes optional — the system MUST run with zero AWS keys in the config** (operator hard requirement). Reversible de-risking pass: proves App2 + PDF + DB stand alone (no boto3, no AWS keys) BEFORE Phase DW deletes anything. Ships as **v15.0.0** (boto3-out-of-`[prod]` is a breaking extras change → major) with a loud RELEASE_NOTES migration note. Soaks **one release cycle** before DW.

- [x] DV.1 - Make the one eager boto3 import lazy: move `import boto3` + `from botocore.exceptions import ClientError` (`common/deploy.py:18,20`) inside the functions, mirroring `cleanup.py`'s already-lazy pattern (the only top-level boto3 import in `src/`). After this, `import recon_gen.common.deploy` is safe with no boto3.
- [x] DV.2 - Guard the lazy QS callsites in `cli/json.py` (`build_datasource` :122, `deploy` :137, `run_cleanup` :185, `probe_dashboard` :251): a `ModuleNotFoundError: boto3` raises `click.UsageError("recon-gen[quicksight] is required …")`, not a traceback. Confirm `json apply` WITHOUT `--execute` stays pure-python (deploy import is inside the `--execute` branch).
- [x] DV.3 - Add the `[quicksight]` extra to `pyproject.toml` (`boto3>=1.34` + `botocore[crt]>=1.34` — un-collapse the BS.6-retired `[deploy]` bucket; cite the BS.6 + AWS-deploy comments).
- [x] DV.4 - Remove `boto3` + `botocore[crt]` from `[prod]` (`pyproject.toml:80-81`) so `recon-gen[prod]` (App2 + PDF + DB) resolves with ZERO AWS SDK; re-point `[dev]`/`[e2e]` to self-reference `recon-gen[quicksight]` (pin in one place); keep `boto3-stubs` in `[dev]` while QS tests run.
- [x] DV.5 - **AWS-keys-optional cfg (operator hard requirement):** make the `aws:` cfg block optional — `load_config` tolerates a missing/empty `aws:` section; only the QS deploy path (`json apply --execute` / `clean` / `probe`) requires + validates `aws.account_id` / `region` / `deployment_name`, loud-failing THERE with the install-extra + set-aws-cfg hint. Coordinate with the pending DE v14 cfg redesign.
- [x] DV.6 - QS test legs skip cleanly on a no-quicksight venv: `pytest.importorskip("boto3")` in the parametrized driver fixtures + the `qs_deployed` autouse fixture (`tests/e2e/conftest.py`), so `up_to=app2` on a boto3-less install COLLECTS + PASSES with qs params skipped (today they skip only on missing `RECON_E2E_USER_ARN`).
- [x] DV.7 - Drop the quicksight-gen PyPI publish: remove the 'Sync shim version' (`release.yml:338-347`) + 'Build quicksight-gen shim' (:349-350) steps (publish-* glob `dist/*`, so no publish-job edit needed). Retire `quicksight-gen-shim/` IN THE SAME CHANGE (the sync step seds its pyproject). Stops publishing with v15.
- [x] DV.8 - Clean the stale shim coverage ref (`ci.yml:729` + its `--ignore-errors` band-aid) + drop `boto3-stubs[quicksight]` from the smoke job (`release.yml:395`); keep smoke as the bare-install-imports-clean gate.
- [x] DV.9 - Deprecation notes across docs + CLI, **preserving the DT voice** (sentence-level review + adversarial voice-lint + red-pen — [[feedback_voice_rewrite_review_everything]]):
  - [x] DV.9.1 - README: demote QS from first renderer; soften the QS-Enterprise prerequisite + $250/mo warning to "only if you opt into `[quicksight]`".
  - [x] DV.9.2 - `click.echo` a one-line deprecation notice atop `json apply --execute` / `clean` / `probe`.
  - [x] DV.9.3 - Deprecation admonition atop the 4 QS-centric docs (quicksight-quirks, configure-the-deploy, run-my-first-deploy, disable-tagging).
  - [x] DV.9.4 - CLAUDE.md: reword the QS renderer bullet to "optional/deprecated renderer"; `install.md`: move boto3 to the `[quicksight]` extra row.
- [x] DV.10 - **Acceptance proof:** a CI job (or documented invocation) on a `uv sync --extra dev` venv WITHOUT `[quicksight]` AND with a cfg carrying NO `aws:` keys runs unit + db + app2 + pdf green with QS skipped; `recon-gen dashboards/studio/audit/schema/data/docs` + `json apply` (emit, no `--execute`) all work. Keep ci.yml's main runner at `up_to=qs_browser` UNCHANGED (POLICY 1: QS stays covered for extra-installers during the soak).

## Phase DW - Remove QuickSight: delete QS code/tests/CI/extra, rewrite QS docs, re-establish the App2 backstop (after DV soaks one release)

After DV ships (v15.0.0) and soaks one release cycle, excise QuickSight entirely: the boto3 deploy, the `to_aws_json` emitter, the datasource/cleanup/probe modules, the `json` CLI group, the qs_api/qs_browser test tiers + driver, the `[quicksight]` extra, the QS-centric docs — keeping the shared tree, App2, and PDF fronts intact. **Migrate-before-delete** so POLICY 2 (no red collection, no deferred failures) holds at every commit. The App2↔QS parity gate degrades to **3-way: scenario plants ⊆ direct-DB matview recompute == App2 (== PDF for drift)** — the direct-DB leg was always QS-independent; QS was a third corroborator, never the truth anchor.

> **AUTONOMOUS-RUN ASSESSMENT (2026-06-27, operator away):** DW.3–DW.8 are the ENTANGLED, POLICY-2-atomic QS-deletion sequence — NOT executed blind unattended; flagged for operator coordination. Findings: **(1)** the agreement validators already run **3-way at runtime** when QS is absent — the `qs_available:false` guard (DV.6) skips the `_QS` comparison, so the gate is NOT red today, it just no-ops the QS leg. **(2)** Rename `qs_browser`→`agreement` cascades through: the `Tier` enum (`Tier.QS_BROWSER`→new `Tier.AGREEMENT`), the `Need.AWS_QS` drop, the runner layer name + `AWS_TOUCHING_LAYERS`, the `@inputs(...)` nodeid strings (they hardcode `tests/e2e/qs_browser/...`), and `conftest.py`'s tier/needs marks. **(3)** Mutual blocking: DW.4 (delete QS producers) must land ATOMICALLY with the `_QS`-input drop (else `@inputs` resolves a deleted producer); DW.7 (delete QS source) is blocked while `cli/_app_builders.py` still calls `emit_*`; DW.8 (strip the emitter) is blocked until DW.4/5/7 remove every `to_aws_json`/`emit_*` caller. **(4)** Verifying any of this needs the **live agreement layer** (DB + App2 + PDF producers via `up_to=agreement` + a container) — runnable, but heavy + risky to leave half-done. **Recommendation: drive DW.3–8 as one coordinated sequence with the agreement layer UP and operator eyes. The safe verifiable prefix (DW.0 / DW.1 / DW.2) is the autonomous-run deliverable.**
> **Duck spike — REVERTED + backlogged 2026-06-27** (full re-applicable detail in the Backlog "parameterize the agreement layer over dialects" entry; DW.3 proceeds on PG). The spike's findings, for reference: added `duckdb` to the agreement matrix — UNCOMMITTED working-tree changes in 4 files: `tests/e2e/_agreement_helpers.py` (`_DIALECT_CONFIG_PATHS["duckdb"]` + the cfg-dialect `duckdb` detection branch), `tests/e2e/db/test_audit_direct.py:95` + `tests/e2e/app2/test_audit_invariants_app2.py:58` (`params=["duckdb","postgres","oracle"]`), + this note. The inv producers already follow the cell dialect (no change needed). **BLOCKED:** the app2 agreement producers FAIL on duck — `common/db.py:252` opens the per-worker duck file READ-ONLY (`RECON_GEN_DB_READ_ONLY`) but on `popen-gw2` the file was never seeded (seed-write + App2-read split across xdist workers → `IOException: ... demo.duckdb does not exist`). The single-producer files (`inv_anomaly_app2`, `inv_money_trail_app2`) fail too, so it's NOT a simple `xdist_group` pin (per the db producers' "one-producer-per-scope = no race" rule). New variable vs the pg/oracle path: the App2 server is a SECOND duck-opener (read-only) alongside the seed. The runner's `--parallel 1` did NOT serialize (gw2 present). The db direct/PDF leg appeared to advance (chain reached app2). **Decision needed (operator's DuckDB-xdist territory — [[feedback_strict_engines_surface_isolation_bugs]] + the pytest-xdist-file-lock backlog):** (a) fix the app2-producer duck isolation (per-worker seed-before-App2-read, or pin the producer+App2+read to one worker); (b) run agreement-on-duck single-process (`-p no:xdist`) as the local fast path, keep xdist for pg/ora; (c) keep agreement pg/oracle-only, defer duck-parameterization to a focused isolation task. Tree is GREEN at `dw1-complete`; revert the 4-file working set if dropping 1a. The validators stay put under the renamed dir, just QS-stripped; the `agreement` layer sits ABOVE `app2` in the chain (unit → db → app2 → agreement), consuming the App2 renderer + DB-direct + PDF producers, no AWS.
> **Open questions for DW → all promoted into the DW.0 decision spike** (resolved there, with a locked `docs/audits/dw_0_decisions.md`, before DW.1 starts). See DW.0.1 (auth disentanglement + direction), DW.0.2 (non-drift backstop), DW.0.3 (`out/*.json` consumers), DW.0.4 (PyPI hygiene), DW.0.5 (AWS-footprint / CI-OIDC fork — gates DW.11).
- [x] DW.0 - **DECISION SPIKE — gates DW.1+ (operator directive 2026-06-27: "resolve all our technical decisions before we keep charging ahead").** ALL FIVE locked + operator-confirmed; `docs/audits/dw_0_decisions.md` written. Deliverable: `docs/audits/dw_0_decisions.md`, one section per decision below, each with a LOCKED verdict + rationale (spike-before-locking, [[feedback_spike_before_locking_implementation]]). DW.1 does not start until DW.0 lands. Promotes the former "Open questions" blockquote into resolvable tasks.
  - [x] DW.0.1 - **AUTH — disentangle the THREE "OIDC" things + lock the direction (operator 2026-06-27: "we still need auth, IdP is standard, fully-local wise too, I LOVE passkeys; okay if IdP/auth stays as-is + future phase").** recon-gen has THREE distinct auth concerns my prior framing wrongly merged: (a) **App2 user-login OIDC** (`common/html/auth.py` authlib + `JwtCookieMiddleware` JWT-cookie session, Dex in tests) — recon-gen's OWN renderer auth, AWS-INDEPENDENT, the nuke never touched it; (b) **`cfg.auth.aws` QS-embed STS signing** (`config.py::AuthAwsConfig` + `resolve_qs_user_arn`) — QS-coupled, DIES with QS; (c) **CI GitHub→AWS OIDC role** (the nuked GitHub secrets) — infra auth = DW.0.5, NOT app auth. **VERDICT: App2 OIDC stays AS-IS through DW (no change).** SURGICAL delete in DW.8/DW.13: drop ONLY `cfg.auth.aws` (`AuthAwsConfig` + `resolve_qs_user_arn`), KEEP `cfg.auth.{oidc,session}` + `common/html/auth.py` + the auth unit/e2e tests intact — add a DW guard that `test_oauth_login_flow.py` + the route-gate contract stay green post-removal. Passkeys/WebAuthn + a fully-local AUTHENTICATED mode (today: OIDC-or-no-auth-dev — no local-password/passkey path, zero WebAuthn code) → NEW future "Auth" phase (backlog stub), explicitly OUT of DW.
  - [x] DW.0.2 - **Non-drift backstop (was open-q 1):** is `plants ⊆ direct-DB matview == App2` set-equality enough, or extend PDF recompute from drift-only to ALL L1 invariants for a 2nd independent recomputer? **VERDICT (operator-confirmed): 3-way is enough** — the direct-DB matview recompute is already a QS-independent recomputer (different query path than App2's projection), so the set-equality is a real cross-check, not circular. The PDF-all-invariants extension is defense-in-depth, not a DW blocker → backlog.
  - [x] DW.0.3 - **External `out/*.json` consumers (was open-q 2):** does anything consume the QS-API emit shape? **VERDICT (operator-confirmed): no consumers — and ONLY the QS-API JSON emit retires.** It's an AWS-QS-API dict shape, meaningless without QS; this is chotchki's tool and the DV soak was the migration window. The `json apply` emit retires with the `json` CLI group (DW.7). **The SQL emit for schema/data + the audit PDF emit were never QS-coupled and STAY** (operator: "sql is still emittable for schema/data/etc.") — DW.7 must scope its deletion to the `json` CLI group ONLY, not the destructive-default-to-emit behaviour that schema/data/audit share. No compat shim ([[feedback_no_compat_shims]], pre-stable).
  - [x] DW.0.4 - **PyPI hygiene (was open-q 3):** yank published `quicksight-gen`, or just stop uploading (DV.7 already stopped)? **VERDICT (operator-confirmed): don't yank, but `quicksight-gen` is NEVER published again — permanent + structurally guarded.** Don't yank — breaks anyone pinned + rewrites published history, against [[feedback_no_tag_rewriting]] (history stays immutable); leave the published versions as the v15 escape hatch (DW.15). DV.7 already deleted the shim-publish steps + retired `quicksight-gen-shim/`, so the republishable definition is gone; DW.14's grep-zero lock EXTENDS to assert `quicksight-gen` appears in zero workflow/pyproject publish surface, so a future edit can't silently resurrect the shim (operator: "we must never publish it again").
  - [x] DW.0.5 - **AWS footprint after QS — the consequential fork (was open-q 4, operator-flagged):** keep ANY AWS access post-QuickSight? The nuked GitHub→AWS OIDC role authed CI for BOTH the QS deploy AND the `_aw` RDS-target variants (real managed PG/Oracle — `_aw` caught real managed-service divergence before, e.g. [[project_drcp_on_aws_oracle_dead_end]]). Until decided, CI is red on every AWS-touching layer. Three paths: **(a) KEEP `_aw` in CI** — re-enable the GitHub→AWS OIDC + secrets, DW.11 strips ONLY the QS leg, `_aw` + RDS stop/start lifecycle stay standing (accept the cost + re-opened footprint); **(b) FULLY-LOCAL** — drop `_aw` entirely, tear out OIDC + creds + RDS lifecycle, AWS footprint → ZERO (simplest, aligns offline-first, loses real-RDS coverage permanently); **(c) HYBRID** — CI default fully-local (zero STANDING footprint + the home-network port-forward hole stays closed), but KEEP the `_aw` variant definitions + RDS scripts as operator-run OPT-IN (manual `--targets=aw`, gated on LOCAL creds, never CI secrets) for pre-release managed-RDS validation. **VERDICT (operator 2026-06-27): FULLY-LOCAL (path b).** Drop `_aw` entirely, tear out the GitHub→AWS OIDC + creds + the RDS stop/start lifecycle; AWS footprint → ZERO, the home-network port-forward stays closed, no CI secrets survive. Reframe in the operator's words: running recon-gen against a cloud-managed DB — AWS RDS / Azure / GCP Cloud SQL — is "100% on the end user … still easily possible, just not our problem." The portability contract is portable PG 17 / Oracle 19c SQL reached via a connection string; validating a specific managed-service's quirks is the operator's job against their own infra, NOT a standing coverage burden this project carries. So DW.11's strip is now UNCONDITIONAL + wider than the QS leg alone (QS + `_aw` + RDS lifecycle + CI OIDC all go).

- [x] DW.1 - PRE-WORK (migrate-before-delete): re-point the mixed `tests/json` app-structure gates off `to_aws_json` onto a tree-walk (test_l1_dashboard / investigation / l2_flow_tracing(+_matrix) / executives / dataset_parameters / kitchen_app / text_box_safety / table_column_headers / bar_chart_axis_labels / drill / app_info / cross_sheet_drill_date_widening) — assert against the tree object graph ("Tree IS the source of truth"). **Approach (scouted + exemplar proven 2026-06-27):** `test_app_info.py` is the 100%-tree-walk precedent (its one stray `emit_analysis` fn migrated to `sheet.text_boxes`, green — `app.analysis.sheets` → `sheet.visuals`/`.text_boxes`/`.parameter_controls`/`.filter_controls`, no emit). Three categories: (1) PURE STRUCTURAL (l1_dashboard / investigation / l2_flow_tracing(+matrix) / executives / kitchen_app) → straight tree-walk; (2) EMIT-MECHANISM but renderer-agnostic INTENT (table_column_headers / bar_chart_axis_labels / text_box_safety) → RE-TARGET to the shared truth source (tree field-refs → dataset-contract `human_name`, what App2 reads), do NOT weaken to circular tree-only checks; retire to App2 coverage ONLY where `test_html_table_parity` provably covers it (verify first — no silent gap); (3) DONE/TRIVIAL (app_info ✅, drill = no emit coupling leave-as-is, cross_sheet_drill = hybrid via `iter_cross_sheet_drills`). SCOPE BOUNDARY: ONLY the named app-structure gates — the other emit-coupled files the scout found (test_deploy / test_probe / test_cli_json / test_emit_cross_reference_consistency / test_screenshot_harness) test QS-emit MACHINERY that DIES in DW.5/7/8, deleted downstream NOT migrated here. Staged commits, one per migrated file, each green (bisectability — [[project_cb6_design_locks]]). **COMPLETE 2026-06-27: 12/12 migrated, `tests/json` FULLY emit-free (438 passed).** 8 via the type-driven sweep + the 4 judgment-heavy: the label pair (table_column_headers / bar_chart_axis_labels) RETARGETED to the contract `human_name` (catches a snake_case `display_name` override — the residual value NOT covered by `test_html_table_parity`'s label==human_name pin); dataset_parameters RETIRED 9 QS-`DataSetParameter`-wire-shape tests (no App2 consumer, die with the QS model); text_box_safety → `sheet.text_boxes`; drill needed no change. Surfaced + fixed: `emit_analysis` bundled the 8-step validation walk (ran only at emit time) → extracted public `App.validate()` (emit delegates; DW.8.2 pulled forward) so the validation canary survives emit deletion. Two adversarially-reviewed workflows (5+4 files) caught + fixed real defects a blind sweep would have shipped: a tautological `len==len`, the dropped validation canary, a kitchen drill silent-skip, an overstated bar-chart vacuity guard. **TYPE-DRIVEN method (operator 2026-06-27 — [[feedback_invariants_in_types]]):** grep UNDERCOUNTS the surface — emitted-model fixtures hide consumers (e.g. `test_executives::exec_analysis` returns `emit_analysis()`, consumed 21× via `.Definition.Sheets`, but grep finds only the 6 emit-call sites). METHOD: flip the fixture/call to return the TREE node, run pyright — it flags every consumer using model attrs (`.Definition`/`.SheetId`/`.Name`) absent on the tree (`.sheets`/`.sheet_id`/`.name`); fix each, verify green. The type system is the completeness oracle, not grep. Same probe scopes DW.4/5/6: nuking `_drivers/qs.py` / `QsEmbedDriver` surfaces ~24 test files + 5 shared-infra modules (`_drivers/{base,__init__,_lifecycle}` + conftest + `_capture`) → shared ones get surgical edits, qs_browser/qs_api producers get deleted, the `[qs,app2]`-parametrized app2 tests get de-parametrized.
- [x] DW.2 - Repoint the one non-QS emit caller: `cli/docs.py` screenshot builder used `emit_analysis()` for its auto-ID side effect — switched to `app.resolve_auto_ids()` (the narrow primitive). The other src `emit_*` callers are all in `cli/_app_builders.py` (the QS `json`-group emit path, deleted in DW.7). So `emit_analysis`/`emit_dashboard` now have zero non-QS, non-test callers.

- [x] DW.3 - Collapse the 4-way agreement gate → 3-way + extract it into its own AWS-free `agreement` layer. **COMPLETE 2026-06-27 (commits `ce4c316d` collapse + `ddb503ab` extract; verified on PG: unit 5397 / db 104 green, agreement layer = 6 audit validators PASS against real producer output + 2 inv validators skip-clean).** Two commits: (A) dropped `_QS` from `@inputs` + deleted the `qs_available` blocks + deleted the 3 QS agreement producers ATOMICALLY (collection-time `@inputs` check couples them) + renamed validators off the now-wrong `_four_way`/`_three_way` suffix. (B) `git mv qs_browser/ → agreement/` (the dir held nothing BUT the 3 validators post-collapse, so the move IS the rename), new `Tier.AGREEMENT` (no AWS/Playwright needs — pure artifact reader), new runner `agreement` LAYER slotted AFTER app2 / BEFORE qs_api so `up_to=agreement` is AWS-free; command collects db/+app2/ alongside agreement/ (so `@inputs` resolves) then `--tier=agreement` runs only the validators. **LATENT-GAP FIX (this is the "tests diverged" finding — see new backlog item):** the validators were NEVER running — `tier(qs_browser)` + no `browser` mark → the qs_browser layer's `-m browser` selector silently deselected all 8; the autonomous-run assessment's "already run 3-way at runtime" was WRONG. `--tier=agreement` makes them run for the first time. **L2-optional inv skip:** anomaly/money_trail producers `pytest.skip` on sasquatch_pr (it doesn't declare the roles — backlog #239); validators now skip-clean via `_agreement.artifact_exists` (safe: a producer FAILURE goes red upstream + halts the chain before agreement, so a missing artifact here can only be a legitimate skip). The qs_browser runner-LAYER STAYS for now (hosts the root `[qs,app2]` `-m browser` tests); DW.7 removes it after DW.5/6 de-parametrize those to app2-only. **Duck-parameterization stays DEFERRED** (Backlog "parameterize the agreement layer over dialects"); built on PG.
- [x] DW.4 - Delete the QS agreement/render producers (`qs_browser/{test_audit_invariants_qs, test_inv_anomaly_qs, test_inv_money_trail_qs, test_l1/inv/exec_dashboard_renders}.py`); keep the App2 + DB-direct/PDF producers. **COMPLETE 2026-06-27** — the 3 render-smoke tests in `f6a976b6` (the safe independent slice), the 3 QS agreement producers in `ce4c316d` ATOMICALLY with the `_QS` `@inputs` drop (DW.3). All six gone; App2 + DB-direct/PDF producers retained.
- [x] DW.5 - Delete the QS e2e tier: `tests/e2e/qs_api/` (+conftest), `tests/e2e/_drivers/qs.py`; remove `qs_driver_or_none` + `qs_deployed`/`_qs_pre_warm` fixtures. **`qs_browser/` is NOT deleted — it's RENAMED → `agreement/` (DW.3), retaining the App2/DB/PDF agreement validators after the QS producers come out (DW.4).** KEEP `common/browser/{helpers,screenshot}.py` (App2Driver uses `webkit_page`). **COMPLETE 2026-06-27 — DW.5.1 (driver nuke) + DW.5.2 (qs_api tier + fixtures + runner-layer rename).**
  - [x] DW.5.1 - **DONE 2026-06-27 (`7afb5576`, type-driven sweep).** Nuked the QS BROWSER driver: deleted `_drivers/qs.py` (QsEmbedDriver + WS tracker, 1261 LOC) + `_drivers/_lifecycle.py` (qs_driver_or_none) + the unit tests for the deleted internals (`test_qs_typeahead_filter`, `test_qs_ws_activity_tracker`); dropped the `qs_driver` fixture + `QsEmbedDriver` export/import + `_AWS_DEPENDENT_FIXTURE_NAMES["qs_driver"]`; rewrote `test_failure_capture_fixture_wiring` to app2-only (coverage kept). pyright clean, e2e collects 407, unit suite green. Net -2116 LOC.
  - [x] DW.5.2 - **DONE 2026-06-27 (Option 2: retier-in-place + rename layer; operator-chosen via AskUserQuestion).** Deleted `tests/e2e/qs_api/` (6 tests + conftest) + `test_db3_parity_snaps.py` (QS-vs-App2 parity capture, dead post-QS) + `test_qs_deployed_fixture.py` (the type-driven sessionstart gate surfaced it — 310 LOC testing the deleted fixtures). Stripped 15 QS fixtures from `tests/e2e/conftest.py` (`qs_deployed`/`_qs_pre_warm_dashboards`/`qs_client`/`account_id`/all `*_dashboard_id`/`*_analysis_id`/`*_dataset_ids`, AST-removal, −309 lines) + emptied `_AWS_DEPENDENT_FIXTURE_NAMES` (→ `_session_needs_aws` always False, fully-local). **Runner-layer removal pulled forward from DW.7 (atomicity — `pytest tests/e2e/qs_api/` would hit a missing dir):** removed BOTH `qs_api` + `qs_browser` from `LAYERS` (the latter already dangling post-DW.3); the root `tests/e2e/test_*.py` browser tests (~22 files, app2-only since DW.6) **retiered in place** `tier(Tier.QS_BROWSER)`→`tier(Tier.APP2)` + dropped `Need.AWS_QS`; the `qs_browser` runner layer **renamed → `app2_browser`** (terminal tier, `pytest tests/e2e/ -m browser --ignore={app2,db,agreement}` so the 6 `browser`-marked app2/ files don't double-run; AWS deps stripped → `frozenset({docker})`, kept in TLS_TOUCHING). `AWS_TOUCHING_LAYERS = ()`; `_is_aws_touching_layer` always False; qs_user_arn derivation gated off; `_infer_layer` root-files → `app2_browser`. Updated `test_runner_triage.py` (62 pass), `tree_validator.py` (dropped dead `_is_qs` branch), `ci.yml` + `run_tests.sh` (`up_to=qs_browser`→`up_to=app2_browser`, POLICY 1). **Verified on PG:** pyright sessionstart gate green; app2_browser collection = 105 tests / **0 `[qs]` callspecs / 0 app2-subdir leaks**; agreement collection = 8 validators; composition rules pass; `up_to=agreement` chain run. **DW.5/6 debris item (2) CLOSED** (`test_dashboard_driver.py` retiered). **Deferred to DW.7:** gated-off dead machinery (`_resolve_qs_user_arn`/`always_derive_qs_user_arn`/`_build_deploy_command`); `_marks.py` `Tier.QS_API`/`QS_BROWSER` enum + `tests/conftest.py` composition rules (dead branches); ci.yml AWS-OIDC/QS-subscription setup steps (vestigial); env_keys `RECON_GEN_QS_CONFIG`/`SKIP_QS_DEPLOY` (still referenced by runner QS-cfg plumbing).
- [x] DW.6 - De-parametrize the `[qs, app2]` driver fixtures to app2-only. **DONE 2026-06-27 (`517ccafa`, landed BEFORE DW.5.1 so the driver deletion hit dead code — cleaner than the planned same-change).** Flipped the 4 `<app>_dashboard_driver` fixtures `params=["qs","app2"]` → `["app2"]`, collapsed `_parametrized_dashboard_driver` to the App2 branch, dropped the AWS-fixture deps (region/account_id/`*_dashboard_id`) so the app2 leg is AWS-free. 0 `[qs]` callspecs remain; test↔driver split kept. **Surfaced (backlogged below):** `test_inv_drilldown::test_account_network_table_walk_rerenders_table` now ALWAYS skips — its only non-skipped leg was QS; App2 can't yet (backlog #331). Flip params to `['app2']`; the QS `NotImplementedError` structured-triples retire with the driver. **KEEP the test↔driver split (operator directive 2026-06-27 — [[feedback_keep_test_driver_split]]):** single-renderer ≠ inline-the-driver. `DashboardDriver` (base.py) + `App2Driver` STAY a separate layer; tests keep speaking renderer-agnostic verbs (`pick_filter`/`table_rows`/`drill_from_first_row`/…), never raw Playwright; the `no-playwright-leak` AST lint stays. The abstraction's value is READABILITY, not just parametrization.
- [x] DW.7 - Delete the QS source modules + dead scaffolding. **COMPLETE 2026-06-27 — DW.7a (json CLI, prior session) + DW.7-rest (7 green staged commits A–G; full unit tier 5279 passed / 115 skipped).** Done: `common/{probe,datasource,deploy,cleanup}.py` deleted (A–D, each with its `tests/json/test_*.py` + tag-constant detangle — the 3 tag-key constants inlined as test-locals in `test_models`/`test_config_proxy_views` with why-comments; the `create-tags` typing-smell lint retired with deploy.py); `_dev/cleanup.py` + the runner `sweep` verb (`cmd_sweep`) + triage-down's QS-sweep (`_triage_qs_sweep` + `--keep-qs`) removed (E); `_resolve_qs_user_arn`/`always_derive_qs_user_arn`/`_build_deploy_command` + the runner `RECON_E2E_USER_ARN` import + the QS-auth `--help` epilog + the `qs_arn` probe ripped out (F); `Tier.QS_API`/`QS_BROWSER` + `Need.AWS_QS` + their `tests/conftest.py` composition rules dropped (G). `cli/json.py`/`_app_builders.py` + json-group unregister were DW.7a. **Type-driven method paid off again:** deleting deploy.py surfaced a hidden `CreateTagsCheck(files=[deploy.py])` inclusion (FileNotFoundError at the lint, NOT a grep hit) — the test run, not pyright, caught it ([[feedback_invariants_in_types]]). **DEFERRED (correct scope boundaries, NOT skipped — [[feedback_no_silent_defer]]):** (1) `models.py` AWS dataclasses (DataSource/Theme/ResourcePermission/…) + `to_aws_json` left ORPHANED-but-present → **DW.8.1** (its own surgical task); test_models's `TestDataSourceSerialization`/`TestConfigDatasourceArnDerivation` + the tag tests stay green meanwhile. (2) `cfg.aws.*` surface (`tags()`/`prefixed()`/`account_id`/`datasource.arn`/`qs_disable_pg_ssl`/`principal_arns`/`resolve_qs_user_arn`/`quicksight_user_arn`) + `RECON_E2E_USER_ARN` EnvVar def + `browser/helpers.get_user_arn` → **DW.8.4 (helpers) + config cleanup** (helpers still reads the env var; config.py `resolve_qs_user_arn` + `test_config_v14` stay green). (3) runner `RECON_GEN_QS_CONFIG`/qs.yaml two-cfg plumbing + the `AWS_PROFILE` injection + `_probe_aws`/"aws" probe + ci.yml AWS-OIDC/QS-subscription/`--extra quicksight` + `release.yml::e2e-against-testpypi` + env_keys `RECON_GEN_QS_CONFIG`/`SKIP_QS_DEPLOY` → **DW.11** (the workflows + AWS-footprint + port-forward teardown phase explicitly owns the two-cfg contortion). (4) typing-smells `boto3-direct` stale never-match exclusions (deploy.py/cleanup.py paths) → **DW.14** (no-boto3 lint rework).
  > **⚠️ DW.7 ↔ DOCS COUPLING (found 2026-06-27, attempted DW.7a + reverted).** Deleting the `json` CLI group is GATED by `tests/unit/test_docs_cli_invocations.py` — it auto-discovers every `recon-gen <verb>` in `src/recon_gen/docs/**/*.md` fenced bash blocks and asserts the verb resolves against the live Click tree. There are **57 `recon-gen json` references across ~20 docs** (incl. whole QS-deploy-centric pages: `walkthroughs/customization/how-do-i-run-my-first-deploy.md`, `how-do-i-configure-the-deploy.md`, `reference/self-host.md`, `reference/disable-tagging.md`, the handbook `l1/l2_flow_tracing/executives/customization.md`, `for-your-role/integrator.md`). So **DW.7 cannot land green until the json-citing docs are rewritten (DW.9–13)** — they're one coupled unit. SEQUENCE: do the docs rewrite (DW.9–13) FIRST (or in the same chain), THEN delete the CLI. **The DW.7a mechanical sweep IS proven + cheap to redo** (it was verified green except the docs gate): delete `cli/json.py` + `cli/_app_builders.py` + `tests/json/{test_cli_json,test_emit_cross_reference_consistency,test_cli_smoke}.py`; rewrite `cli/__init__.py` (drop `_json_group` import + `add_command` + docstring/help mentions); AST-excise the json-CLI tests (`TestCli` class in `test_executives`/`test_l1_dashboard`; `test_json_apply_writes_investigation_{files,app_jsons}` in `test_investigation`; `test_cli_json_apply_*` in `test_l2_flow_tracing`) + their now-unused imports (`CliRunner`/`main`/`Path`) + the orphaned `_write_min_config` helper in test_investigation. Scratchpad scripts: `strip_qs_fixtures.py` + `strip_json_cli_tests.py` (the AST-removal pattern). The `common/{deploy,cleanup,datasource,probe}.py` deletion ALSO needs tag-constant surgery: `MANAGED_TAG_KEY`/`MANAGED_TAG_VALUE`/`DEPLOYMENT_TAG_KEY` live in `common/cleanup.py`, consumed (in src: cleanup.py only; in tests: `test_models`, `test_config_proxy_views`, `test_dev_tls_ensure`, `test_cleanup`) — they're QS-resource tags, dead post-QS, so excise the tests that use them (or relocate the constants if any non-QS use surfaces). `cmd_sweep` (the `sweep` verb) + `_dev/cleanup.py` (`sweep_qs_resources_by_tag`) are now pointless (no QS resources) — remove the verb + run_tests.sh `sweep` mention. **NOTE:** `config.py:989` `_derive_qs_user_arn` (boto3 STS/ListUsers) + the `quicksight_*` ARN fields are config-shape QS surface — out of DW.7's module-deletion scope; left for a config cleanup (becomes orphaned-dead once the runner `_resolve_qs_user_arn` wrapper goes).
- [ ] DW.8 - SURGICALLY strip the QS emitter from the SHARED files (edit, NOT delete — load-bearing for the tree + App2). **SCOPE FINDING (2026-06-27): DW.8.1 + the test_tree migration is the BIGGEST single piece of the whole DW phase — bigger than the subtask framing reads, comparable to DW.1.** The QS-emission layer isn't just `to_aws_json` on a few resource dataclasses — it's `.emit()`/`.emit_definition()`/`.emit_declaration()` on EVERY tree node (Sheet/Visual subtypes/Param/FilterGroup) producing models.py dataclasses, PLUS the AWS-resource dataclasses, PLUS `to_aws_json`. `test_tree.py` (~3200 lines) is ~half emission-testing (`emits_minimal_sheet_definition`, `emits_kpi_visual`, `StringParameterDeclaration.Name`, `emit_definition_carries_sheets`, …). **The hard precondition is a dataclass-by-dataclass AUDIT:** which models.py dataclasses does App2 (`html/render.py`, `html/_tree_fetcher.py`) + `_sql_executor` actually READ (→ KEEP, strip only `to_aws_json`) vs which are QS-emit-only intermediates like `SheetDefinition`/the emit-definition wrappers (→ DELETE with their `.emit()` producers). Get the audit wrong and you either break App2 (deleted a read dataclass) or leave dead QS cruft. **RECOMMENDED TREATMENT (operator call): the DW.1 playbook — fresh context + a workflow with adversarial review** (Ultracode + [[feedback_agent_driven_design_works]]); DW.1's own experience proved a blind sweep on exactly this work ships subtle defects (tautological `len==len`, dropped validation canary). Do the App2/_sql_executor read-audit FIRST (it's the type-driven completeness oracle — [[feedback_invariants_in_types]]), then migrate/delete test_tree.py per-test, then remove the emit methods + AWS dataclasses bottom-up.
  - [x] DW.8.1 - `common/models.py`: remove `to_aws_json()` + the AWS resource dataclasses + the QS-emit-only definition/field-well/visual/param/filter/control wrappers; KEEP only what App2/`_sql_executor` read. **AUDIT COMPLETE + VERIFIED 2026-06-27 → `docs/audits/dw_8_models_audit.md` (the execution map).** **STAGES 1–5 DONE + committed green (`DW.8.1 (1..5/n)`):** the ENTIRE emit graph (visual/field/filter/control/param/action/text-box/theme `.emit()` + the App/structure emit methods + `build_theme`, ~120 classes) is gone; the four leaf-`.emit()` domain invariants relocated to `__post_init__`/`validate()`; the dead `app2_only` gate removed. **REMAINING = DW.8.1.b (DataSet graph, ONE entangled sub-stage the audit MIS-MAPPED as clean-delete — see audit doc "scope correction"):** `build_dataset` RETURNS a `DataSet` that production `app.py` reads (`aws.DataSetId` → tree-Dataset ARN, all 4 apps + app_info) AND ~6 `tests/json/` files + e2e/db validate (`.PhysicalTableMap`/`.CustomSql`/`.DataSetId`). So `DataSet`/`CustomSql`/`PhysicalTable`/`LogicalTable*`/`InputColumn`/`DataSetUsageConfiguration` + `Tag` (via `config.tags()`) + `dataset_permissions` + the DataSource graph CAN'T delete until `build_dataset` is refactored to return the `dataset_id` (str) + the dataset-SQL test surface migrates to `get_sql`/`get_contract`. Recipe in the audit doc. Produced by the `dw8-models-audit` workflow (10 agents: inventory + 5 per-consumer read-classifiers → synthesis → 3-way adversarial verify; 155 dataclasses, 0 uncertain, 0 conflicts). **Headline: App2 reads almost NOTHING from models.py** — it reads the tree NODES directly; the only models.py shapes on App2's runtime path are the **`DatasetParameter` family (9)** (`_sql_executor.py:110-137`) + `DateTimeDefaultValues` (`DateTimeParam.default` field type) + the tree-node `TimeRangeFilter` (name-collides with emit-only `models.TimeRangeFilter`). **139 confirmed DELETE** (the whole emit graph: Visual/FieldWell/Configuration/`*Declaration`/`*Control`/`AnalysisDefinition`/`SheetDefinition`/`Layout`/Theme-graph-12/DataSource-graph-7/`Tag`/`ResourcePermission`) **+ 11 already-dead** (PieChart family/FreeFormLayout*/TableOptions/…). **5 COUPLING TRAPS the adversarial pass caught** (a class-only delete NameErrors App2 boot — full detail in the audit doc): (1) `fields.py` module-level `_USD_FORMAT`/`_integer_format` build the NumberFormat graph at import → remove constants + the `FormatConfiguration=` kwargs WITH the classes; (2) `dataset_contract.py::build_dataset` tail builds `InputColumn`/`PhysicalTable`/`DataSet`/`Tag` on App2 boot (registry side-effects kept, DataSet discarded) → refactor the tail; (3) `config.py::tags()` builds `Tag` (overlaps deferred cfg.aws.* config cleanup); (4) `TimeRangeFilter` collision — delete the model/alias, keep the tree node; (5) `DateTimeDefaultValues` = genuine KEEP. **Execution: top-down cascade** (test_models teardown → test_tree migration → `emit_analysis`/`emit_definition`/`Sheet.emit` → tree-node `.emit()` removal → theme → build_dataset tail → models.py delete + `to_aws_json` strip), pyright + unit tier as the per-stage oracle. **DW.8.1.b DONE 2026-06-28 (2 green commits `0242e3f4` + `9cf971fa`):** `build_dataset` returns a `BuiltDataset` snapshot struct (fields, NOT read-through-registry properties — the registry is last-write-wins by `visual_identifier`, which the app_info per-dialect probe caught red); ~19 test files migrated off `.CustomSql.SqlQuery`/`.DatasetParameters`/`.Columns`; DataSet + DataSource model graphs DELETED (pyright src+tests = 0 dangling). **SECOND audit mis-map found:** the recipe grouped `Tag`/`ResourcePermission`/`config.tags()`/`dataset_permissions` with the DataSet graph as "clean delete" — but `Tag`/`ResourcePermission` are field types on Theme/Analysis/Dashboard (alive), so they're COUPLED to the emit-graph gut → moved to DW.8.1.c. Full verified cascade map in `docs/audits/dw_8_models_audit.md` ("DW.8.1.b EXECUTION" + "REMAINING DW.8.1" sections).
  - [x] DW.8.1.c - **The emit-graph gut (the REST of DW.8.1, NOT .b — this is what makes `grep to_aws_json src/` → 0).** After DW.8.1.b the 3 surviving `to_aws_json` are Theme/Analysis/Dashboard; deleting them cascades to the whole Visual/Filter/Control/Layout/FieldWell/ParameterDeclaration/SheetDefinition graph (~120 classes). **Verified dead-producer-first order (type oracle):** (1) DELETE `common/aging.py` + `test_aging.py` (dead AR/PR-app QS-emit, zero src callers) → frees BarChart*/Axis*/Dim/Measure/ColumnIdentifier/Visual; (2) DELETE `tree/_helpers.py::title_label`/`subtitle_label` (zero Python callers — the helpers.py greps are JS selector strings) → frees VisualTitle/SubtitleLabelOptions; (3) **DELICATE** `drill.py::cross_sheet_drill`/`set_drill_parameters` (build `models.VisualCustomAction`, zero src callers, tested by `test_drill.py` for the K.2 shape-validation DOMAIN invariant [[project_drill_param_shape_typing]]) — re-point test_drill's coverage at the live `field_source`/`Drill.resolve_source_shapes` path BEFORE deleting (KEEP `DrillStaticDateTime`/`DrillParam`/`DrillSourceField`/`DrillResetSentinel`/`field_source` — render.py + actions.py consume them); (4) `config.py::tags()`+`Tag` and `dataset_contract.py::dataset_permissions`+`ResourcePermission` die WITH Theme/Analysis/Dashboard (retire `test_config_proxy_views` tags test); (5) models.py mass-delete down to the DatasetParameter family (9) + `DateTimeDefaultValues` + `_check_static_values_cap`/`_strip_nones`, pyright per-delete + retire test_tree/test_models emit-shape consumers; (6) `grep to_aws_json src/` → 0 + module-docstring rewrite + full suite green. **Fits the DW.8 box's "fresh context + workflow + adversarial review" recommendation** (drill K.2-invariant delicacy + broad test-retirement surface). **DONE 2026-06-28 — 3 staged green commits (`e24369b9` relocate K.2 → `de767051` delete 5 consumers → `8fae0da2` models gut, 1402→141 lines, `grep to_aws_json src/`=0).** Ran the DW.1 playbook the box recommended: a 6-agent pre-flight verification workflow THEN a 4-agent adversarial-review workflow. **Pre-flight caught the recipe's CRITICAL miss** — the K.2 shape-check (`can_assign_to`) lived ONLY in the dead `set_drill_parameters`, NOT in the kept types (the live `resolve_source_shapes` resolved each source's shape but discarded the param and never compared); deleting the helper would've SILENTLY DROPPED the invariant. So (1/3) RELOCATED it (+ empty-writes + duplicate-param guards) to `Drill.resolve_source_shapes` (the live `validate()` walk, gates every renderer incl App2) BEFORE any deletion. Two more recipe corrections: `_strip_nones` is DEAD not kept (only the 3 `to_aws_json` called it); `_DATASET_PARAM_STATIC_VALUES_CAP` joined the keep-set explicitly. The "multi-round dead-producer cascade" framing was WRONG — the emit graph was ALREADY orphaned (tree stopped importing models.py phases ago), so it's ONE mass-delete after removing the 5 consumers (aging.py, the 2 drill funcs, the 2 `_helpers` label builders, `config.tags()`, `dataset_permissions`). **Adversarial review CLEAN on all 4 lenses** — K.2 `can_assign_to` direction verified NOT reversed; zero deleted class carried a `__post_init__`/raise (no domain rule beyond K.2 to relocate); all 131 deleted class names' surviving refs resolve to self-defined TREE classes (name collisions, not dangling); grep-zero confirmed (exit 1). Full record: `docs/audits/dw_8_models_audit.md` DW.8.1.c section. **RESIDUE HANDOFF (review lens 4, all `info`) → later DW/DE stages, captured so nothing is silently dropped:** config cleanup (later DW/DE — DW.8.4 is taken): `cfg.aws.theme_arn()` DEAD (0 callers); `extra_tags`/`tagging_enabled` DEAD-config; `qs_disable_pg_ssl` QS-only dead (drop AFTER the runner qs.yaml-cell retires in DW.11); **`cfg.aws.dataset_arn()` + tree `Dataset.arn` is DEAD-PAYLOAD** — every app builder still populates `.arn` but nothing reads it post-`to_aws_json` (the self-hosted render path never touches `.arn`); a meaty cross-app sub-task (drop the field + ~19 call sites + the `dataset_arn`/`theme_arn`/`partition`/`principal_arns` chain together), NOT a config one-liner. DW.11: `resolve_qs_user_arn` + the boto3 quicksight `ListUsers` derive (`config.py:931-985`) is the LAST live boto3 — only `tests/unit/test_config_v14.py` consumes it now → remove it + `cfg.auth.aws.quicksight_user_arn`. DW.10: with that gone, the `[quicksight]` pyproject extra has no src anchor. DW.13 prose: incidental "QS"-as-renderer-stand-in mentions survive in `tree/date_view.py` / `tree/parameters.py` / `apps/l2_flow_tracing/datasets.py` docstrings.
  - [x] DW.8.2 - `common/tree/structure.py`: remove `emit_analysis`/`emit_dashboard`. **DONE 2026-06-27 (DW.8.1 stages 2+3).** Non-test-tree callers migrated earlier (app emit-shims + side-effect callers → `validate()`); then `test_tree.py` migrated via a 5-reader workflow classification + AST transformer (84 emit-shape tests deleted, 28 `emit_analysis()` → `validate()`, the drill-source shape check relocated to `validate()`), and `App.emit_analysis`/`emit_dashboard`/`Analysis.emit_definition`/`Sheet.emit`/`GridSlot.emit`/`_permissions`/`_theme_arn`/`_used_datasets` deleted.
  - [x] DW.8.3 - `common/theme.py`: remove `build_theme` + QS Theme imports; KEEP `DEFAULT_PRESET`/`ThemePreset`/`resolve_l2_theme` (App2 uses). **DONE 2026-06-27 (DW.8.1 stage 5):** `build_theme` + the 12 Theme-graph imports + ResourcePermission + Config gone; test_theme_presets keeps the 4 DEFAULT_PRESET palette checks.
  - [x] DW.8.4 - `common/browser/helpers.py`: excised `get_user_arn` + `generate_dashboard_embed_url` + the lazy boto3 + the QS-identity-region docstring; KEPT `webkit_page` + failure-capture. **DONE 2026-06-27.** Also handled the QS-screenshot fallout (operator decision 2026-06-27 — "leave the placeholders so the App2 version slots in"): `browser/screenshot.py` (100% QS embed — `ScreenshotHarness` + `capture_deployed_app`) → a single `capture_app_dashboards(...)` PLACEHOLDER that raises `NotImplementedError` with the App2 recipe in its docstring (spin `recon-gen dashboards` → webkit localhost → per-sheet screenshot; `#p.` embed-hash → `?param_*` query). The `recon-gen docs screenshot` command + ALL its scaffolding (app-slug map, date-param resolution, output layout, DB warmup) KEPT + rewired to the placeholder; dropped the aws_account_id/region guard (App2 needs no AWS). Deleted `tests/json/test_screenshot_harness.py` (tested the removed `ScreenshotHarness`); added `tests/unit/test_screenshot_placeholder.py` (pins the NotImplementedError — FAILS when the App2 build lands, so the placeholder can't outlive its gap). Stripped `get_user_arn` tests + the no-hardcoded-ARN guard from `test_browser_helpers.py`; dropped the two QS helpers from the `no-playwright-leak` allowlist. `RECON_E2E_USER_ARN` EnvVar def stays (config-shape, deferred). **App2 screenshot capture → BACKLOG (DX fit).**
  - [x] DW.8.5 - `common/drill.py`: NOT a separable "QS-emit branch" — drill.py builds tree `VisualCustomAction` objects that BOTH renderers consume (QS via `to_aws_json`, App2 by reading the fields). Folds into **DW.8.1**: keep the action dataclasses (App2 decoration-map reads them), strip their `to_aws_json`. No standalone drill.py edit. **DONE 2026-06-28 via DW.8.1.c** — the App2-consumed action types live on the TREE (`tree/actions.py` Drill + `drill.py` DrillParam/DrillSourceField/DrillResetSentinel/DrillStaticDateTime/field_source, all KEPT); the QS-emit `models.VisualCustomAction` graph + the `cross_sheet_drill`/`set_drill_parameters` helpers that built it were deleted in DW.8.1.c (2/3), K.2 invariant relocated to `Drill.resolve_source_shapes` first (1/3).
- [x] DW.9 - Re-establish the App2 backstop: **DONE 2026-06-28 — 3 commits (`0c9a34c2` + `cee60f23` + `9b70ff1c`).** The split held up: the field-disposition gate (`app2_parity_registry.py`) is the keeper — reframed to a single-renderer completeness gate; the divergence registry (`common/parity/`) is the casualty — retired, its knowledge already preserved in `quicksight-quirks.md` (DW.12). pyright clean src+tests; unit+docs 4474 passed.
  - [x] DW.9.1 - Reframe `common/tree/app2_parity_registry.py` + `test_app2_parity_gate.py` from "App2-vs-QS" → "App2-vs-tree" (the field-disposition gate keeps its value; rewrite the QS-only cross-refs). **DONE 2026-06-28 (`0c9a34c2`)** — the gate now catches "author adds a Visual field, App2's renderer silently drops it" for the single renderer. `ByDesign.parity_break: str` (cross-ref into the retired PARITY_BREAKS) → `ByDesign.reason: str` (inline); the 3 chart-`actions` entries carry the justification inline (App2 = row-level table drills only). Deleted `_HardcodedEmit` + `HARDCODED_EMIT_INVENTORY` (catalogued `emit()` literals — emit() is gone) + its test. `App2Consumed`/`TreeOnly`/`ByDesign` three-way disposition over dataclass fields is the whole surface now.
  - [x] DW.9.2 - Scrub QS comments from `test_html_table_parity.py` (already pins to `contract.human_name` — keep). **DONE 2026-06-28 (`cee60f23`)** — docstring + inline header-assertion comment + the decoration-map test's stale `Drillable.emit`/`App2 ≡ QS` reference reframed onto the contract → App2 path; test logic unchanged. (Residual `arn="arn:aws:quicksight:..."` fixture strings left for the `Dataset.arn` dead-payload cleanup — they're fixture values, not comments.)
  - [x] DW.9.3 - Decouple `structure.py` from `common/parity/`; retire `common/parity/{breaks,emitter}.py` + `test_parity_breaks.py` (the App2-vs-QS divergence registry is meaningless with one renderer). **DONE 2026-06-28 (`9b70ff1c`)** — deleted the whole package (`__init__`+`breaks`+`emitter`; `render_markdown`/`render_handbook_section` had ZERO callers), `test_parity_breaks.py`, and the orphan generated doc `docs/reference/qs-parity-breaks.md` (not in mkdocs nav, no inbound links). structure.py's only coupling was a stale CN.5 comment clause ("QS embeds carry the description alone, registered as handbook_help_panel in breaks.py") — dropped. No real import existed.
- [x] DW.10 - Drop the QS deps + extra from `pyproject.toml`. **DONE 2026-06-28 (`94132ecc`)** — deleted the `[quicksight]` extra (boto3+botocore[crt]) + `boto3-stubs` from `[dev]`; `uv lock` dropped the whole AWS SDK subtree (jmespath/s3transfer/types-awscrt/4× mypy-boto3-*/botocore). Keywords `quicksight`/`aws`/`dashboards` → `self-hosted`/`audit`; description leads with self-hosted HTMX + PDF. Stale DV.4 boto3-extra comments replaced with the DW.10 v15-pin note; the acme/cryptography "boto3 drags them in transitively" claim corrected. Full unit tier (incl. sessionstart pyright over src+tests) green with `boto3-stubs` gone. Unblocked by DW.11-code (`8fdbdbf3`, removing `resolve_qs_user_arn` — the last live boto3). ~~COUPLING MAPPED 2026-06-28 (DW.9 follow-on, not executed — operator paused DW after DW.9): DV.4 already moved boto3 RUNTIME out of `[dev]`/`[e2e]` into the `[quicksight]` extra — only `boto3-stubs[quicksight,rds,sts,s3]` remains in `[dev]` (for pyright). So DW.10's real work = delete the `[quicksight]` extra (boto3+botocore[crt]) + `boto3-stubs` from `[dev]` + keywords + description + the stale DV.4 explanatory comments. BLOCKER: dropping `boto3-stubs` while `config.py::resolve_qs_user_arn` still does `import boto3` (line ~957) makes pyright fail (`reportMissingImports`). So the LAST live boto3 (`resolve_qs_user_arn` — the `session.client("quicksight")` ListUsers ARN-derive, ZERO src callers, only 3 `test_config_v14.py` tests that all short-circuit before boto3) MUST be removed first — that's DW.11's code-part. Order: DW.11-code (remove `resolve_qs_user_arn` + import + the 3 tests) → DW.10 (deps). The metadata half (keywords + description) is uncoupled + safe anytime. Batch the field/spec cleanup WITH it (next note).**~~
- [x] DW.11 - Strip QS from the workflows, keeping ci.yml↔release.yml parity ([[feedback_ci_release_workflow_parity]]). **DONE 2026-06-28 (code + workflows + network teardown + dead-config sweep all complete; `up_to=agreement` verified green on PG — all 5 layers).** **CODE + WORKFLOWS DONE 2026-06-28 (6 commits `8fdbdbf3` code + `0587e9d2` reorder + `d2a7d429` runner-strip + `4efa5057` env_keys + `66fab0b0` workflows). Operator-chosen chain shape (AskUserQuestion): `unit → db → app2 → app2_browser → agreement` — agreement is the TERMINAL layer (the DW.11-prose "agreement on top" intent, NOT DW.5.2's app2_browser-terminal; browser tier keeps its own layer + rerun/Oracle-cap/timeout handling). Done: (a) `8fdbdbf3` removed `resolve_qs_user_arn` + the 3 `test_config_v14` tests — the last live boto3, unblocking DW.10. (b) `0587e9d2` reordered LAYERS + flipped the two chain-shape tests (semantics swap: `up_to=app2_browser` stops before agreement, `up_to=agreement` is the full chain). (c) `d2a7d429` ripped the dead AWS/QS runner plumbing: `_write_qs_cfg_for_thin`+`_swap_url_host`+`_QS_FORWARD_HOST` (the qs.yaml two-cfg + hotchkiss.io-forward machinery), `_probe_aws`+"aws" probe, `AWS_TOUCHING_LAYERS`+`_is_aws_touching_layer`+both dirty-tree deploy gates+`--allow-dirty-deploy`, the `AWS_PROFILE` injection, the `RECON_GEN_QS_CONFIG` plumbing + the triage state's `qs_cfg_path`/`deployed`, the up/down `aws`/`all` scope choices (no-compat-shims: removed the always-False seam DW.5.2 left "for DW.11 to finish"). (d) `4efa5057` deleted the dead env-keys QS family (`RECON_GEN_QS_CONFIG`/`RECON_GEN_SKIP_QS_DEPLOY`/`RECON_E2E_USER_ARN`/`RECON_E2E_IDENTITY_REGION`) + their test surface; kept the Playwright timeout knobs (App2 uses them). (e) `66fab0b0` ci.yml: dropped the OIDC perm + Configure-AWS-credentials + `--extra quicksight` + the per-run QS-side-cfg generation (the `aws:` block + `quicksight_user_arn`/`principal_arns` reading `RECON_E2E_USER_ARN`) + the `json clean` sweep step; chain target → `up_to=agreement`; the runner-cfg materialize keeps only db + App2 Dex OIDC/session/TLS (App2's own auth, DW.0.1); `nightly-cost-sweep.yml` deleted whole (both jobs pure-AWS no-ops); QS scripts deleted (`nuke_quicksight.sh`/`disable_quicksight_genai.sh`/2× `cb10_qs_*`/`spike_x2r_qs_event_capture.py`); the broken `no-quicksight-install` job (imported deleted `common/deploy`+`cli.json`) repurposed → `clean-install-smoke` (boto3-absent + clean-import guard, runtime complement to DW.14's lint); conftest dropped the `AWS_PROFILE` derive; run_tests.sh reframed CLOUDFLARE_TOKEN as App2 TLS. **VERIFIED: full unit tier green (5112) at every checkpoint; `up_to=db` green on PG (stripped runner spins container + dispatches + tears down). Full `up_to=agreement` integrated run pending as the final pre-push gate.** POLICY 1 holds (release.yml gates on ci.yml's run, never re-ran the chain). **REMAINING (split out): (1) NETWORK TEARDOWN — operator's physical hands: close the `hotchkiss.io:<port>` inbound forward + revoke its TLS cert + drop the QS-egress IP-whitelist. **DONE 2026-06-28 — operator confirmed "I've torn it all down."** All in-repo code that USED the forward was already gone. (2) DEAD-CONFIG FIELD SWEEP — **DONE 2026-06-28 (4 staged commits `7c1389e8` A + `6e3223d7` B + `584d4d67` C + `ccc2e8f1` D; operator chose full-cascade-now via AskUserQuestion).** `cfg.aws` gutted to `{deployment_name}` + `prefixed()`: (A) deleted the tree `Dataset.arn` dead-payload (field + 18 app-builder write sites + 21 fixtures + 2 retired QS-collision tests; the inline-loop builders keep `build_all_*` for registry side-effects, drop the redundant pairing); (B) gutted AwsConfig (account_id/region/principal_arns/extra_tags/tagging_enabled/qs_disable_pg_ssl/pg_cluster_id/oracle_instance_id/datasource/partition/dataset_arn/theme_arn) + deleted DatasourceConfig/Config.partition/_partition_from_arns/AuthAwsConfig+auth.aws, rewrote __post_init__ + _build_aws_nested + _build_auth_nested, fixed diagrams.py + make_test_config + ~14 caller sites, retired test_config_partition.py whole + the partition/datasource/ARN/auth.aws tests across proxy_views/v14/models (the 253-error test migration ran via a single sequential subagent + was diff-reviewed for over-deletion); config.py −279 lines; (C) deleted the 6 dead AWS env vars (account_id/region/datasource_arn/principal_arns/pg_cluster/oracle_instance) + their orphaned regexes, kept RECON_GEN_DEPLOYMENT_NAME; (D) recorded the removal in the de_0 spec. Full pyright clean + full unit tier green (5074) at every stage. **SCOPE NOTE:** this was DW.8.1.c-scale (~8x the "~30 tests" estimate — a config-loader gut + 253-error migration), NOT a config one-liner. (3) `test_typing_smells.py::NoDirectBoto3Client` stale allowlist → DONE in DW.14 (replaced by the strict `no-boto3` lint). (4) PRE-EXISTING (not DW.11): `--dialects`/`--targets` stale in CLAUDE.md (fixed in DW.13) + the pre-push hook docs.** ~~DW.0.5 verdict = FULLY-LOCAL, so this strip is UNCONDITIONAL + wider than the QS leg alone — the entire AWS footprint goes:** ci.yml runner → `up_to=agreement` (the chain's new top layer: unit → db → app2 → agreement, all AWS-free), remove the OIDC perm + Configure-AWS-credentials + `quicksight_user_arn` injections + the QS-sweep step; **drop the `_aw` RDS-target variants from the matrix + tear out the whole RDS stop/start lifecycle + the `up local|aws`/`down`/`status` AWS-infra scaffolding** ([[feedback_ephemeral_aws_infra]] — nothing AWS remains to boot/stop); nightly-cost-sweep → delete the QS-nuke job + `scripts/nuke_quicksight.sh` + the RDS-stop step (the whole cost-sweep job goes — zero AWS left to sweep); update local default + CLAUDE.md to `up_to=agreement` (POLICY 1). **Tear down the QS-to-Docker port forward** (the `hotchkiss.io:<port>` → dev-box Postgres forward + its TLS cert + the QS-egress-IP whitelist — [[project_cb10_qs_to_docker_pg_constraints]]): no QS deploy means no embed needs the local DB, so the inbound network hole + the whole two-cfg `qs.yaml` contortion go away (operator-flagged DW benefit 2026-06-27 — closing an externally-reachable port into the home network). **MAPPED 2026-06-28 (DW.9 follow-on, not executed) — the CODE-PART that unblocks DW.10 + its tendrils, for the dedicated session: (1) `config.py::resolve_qs_user_arn` (~931-986) + the lazy `import boto3` + the `session.client("quicksight")` ListUsers derive — the LAST live boto3 in src, ZERO src callers. Remove it + the 3 `test_config_v14.py` tests (`explicit_override`/`no_profile_returns_none`/`cache_hit` — all short-circuit before boto3, so none actually exercise the AWS path). (2) `cfg.auth.aws.quicksight_user_arn` field becomes dead-config once the derive goes — batch its removal with the other dead QS-config (`extra_tags`/`tagging_enabled`/`theme_arn()`/the `dataset_arn()`+tree-`Dataset.arn` dead-payload/`principal_arns`+`partition`/`qs_disable_pg_ssl` — full list in the DW.8.1.c residue handoff). (3) `docs/audits/de_0_cfg_redesign.md` documents `quicksight_user_arn` as a boto3-DERIVED field (lines 42/91/120) — update the spec when the derive is removed (cross-phase: it's the DE config-redesign source). (4) `test_typing_smells.py::NoDirectBoto3Client` allowlist (~50-53) is ALREADY STALE — names deleted files (`common/deploy.py`/`cleanup.py`/`browser/helpers.py` QS bits); refresh the allowlist as part of this strip. (5) `tests/unit/test_deploy_pipeline.py` still exists despite deploy.py being gone — audit it (residual QS-deploy test surface) as part of the DW.11 test removal. NOTE: `resolve_qs_user_arn` does NOT trip the boto3-direct lint (it's `session.client`, not `boto3.client`, and config.py isn't in the allowlist).**~~
- [x] DW.12 - Rewrite the QS-centric docs, PRESERVING THE DT VOICE ([[feedback_voice_rewrite_review_everything]]). **DONE 2026-06-27 — farmed out via the `dw12-docs-qs-removal` workflow (34 doc agents, each read the voice profile + surgically removed QS preserving the doc's established house style; SPEC.md done by hand — it's a pure-him voice corpus source). Verified: doc gates green (112 passed — test_docs_cli_invocations / test_docs_serve / test_handbook_invariants / test_main_macros / tests/docs/), 0 dangling links, `recon-gen json` fully out of docs (so DW.7 won't trip the gate). I reviewed README + quirks-header + spot-checked self-host: the README preserved prior red-pen wins ("We help you implement", "shape-locked"), the quirks reframe nails the balanced DW.15 framing.** RESIDUAL (one genuine, doc-correct keep → tracked for DW.14): `reskin.md:55` + a few docs quote `theme_name: "QuickSight Gen Theme"` verbatim from `tests/l2/spec_example.yaml:623` — renaming the doc alone would misrepresent the source, so the FIXTURE-branding rename (spec_example.yaml + mirrors + byte-identity locks) is the real fix; the doc follows it. Backlogged below.
  - [x] DW.12.1 - **KEPT `quicksight-quirks.md`** — header reframed to "QuickSight is GONE" + v15-pin pointer (`pin recon-gen==15.x`) + the balanced why-we-left record (QS is a legit call for some orgs; we left over the recurring AWS cost-risk, not because QS is wrong); entire quirk-catalog body intact. DELETED + dropped from the mkdocs nav: how-do-i-configure-the-deploy.md, how-do-i-run-my-first-deploy.md, disable-tagging.md (dangling links to them repointed to self-host.md or dropped).
  - [x] DW.12.2 - REWRITTEN (QS-default → self-host-primary): self-host.md, install.md, customization.md, l1/l2_flow_tracing deploy sections, etl.md Info-canary, reskin/test/swap walkthroughs, SPEC.md, compliance-analyst/executive/integrator role pages.
  - [x] DW.12.3 - KEPT handbook/audit.md's "no QuickSight in the loop" (PDF-independence FEATURE claim) — preserved, reads clean.
  - [x] DW.12.4 - Swept the incidental Info-canary "QuickSight is healthy" → "the renderer is healthy" across the per-sheet handbook + app-info.md.
  - [x] DW.12.5 - README: removed the QS mermaid node + the Generate-and-deploy section + the QS-Enterprise prereq + the $250-fee warning + boto3/AWS extras + qs_api/qs_browser test legs; reframed architecture/CLI/agreement (4-way → 3-way) around self-hosted dashboards + audit PDF; kept the `quicksight-gen` shim-retirement note.
- [x] DW.13 - Strip the dev-facing QS prose from CLAUDE.md + remove the `docs/audits/db_3_parity_verify/` QS↔App2 PNG suite. **DONE 2026-06-28 (`d821f38c` PNG suite + `ea29f936` CLAUDE.md).** CLAUDE.md: front matter (two fronts, 3-way gate), the qs.yaml two-cfg block + hotchkiss.io rationale, the `recon-gen json` deploy/clean examples (json group gone), the QS-user-ARN derivation (auth + POLICY 1), the QsEmbedDriver conventions + `[qs, app2]` parametrization (App2Driver sole impl; DashboardDriver split STAYS per [[feedback_keep_test_driver_split]]), the QS_GEN_E2E api/qs layers, the "QuickSight can fail silently" footgun — all stripped/reframed to the new chain (`unit → db → app2 → app2_browser → agreement`, agreement terminal). Fixed refs to DELETED code surfaced along the way: `emit_analysis`/`emit_dashboard` → `App.validate()`; the probe list (Docker-only); the tags surface (no AWS resources → dead-config); `to_aws_json` dataset decision; the pre-push hook (actually `up_to=unit`, CLAUDE.md had it wrong). The annotation-surface POLICY 2 carve-out keeps the structured-triple discipline, repointed at App2 gaps (QS exemplars preserved in the kept quirks doc). PNG suite: 20 PNGs (10 sheets × qs/app2) + the index `db_3_parity_verify.md` removed as a unit (machinery 100% deleted across DW.5/9, not in nav, no inbound links). Residual cfg.aws field refs (deployment_name/prefixed/account/region) left as accurate dead-config for the config sweep. **PRE-EXISTING staleness fixed in passing:** CLAUDE.md's `--dialects`/`--targets` examples (CB.17.d removed the matrix sub-flags; dialect comes from cfg).
- [ ] DW.14 - Final sweep + lock. **STRUCTURAL LOCK + STRIPS DONE 2026-06-28 (4 commits `e66d13dc` no-boto3 lint + `eb60eee9` deleted-symbol strip + `da5fd8fc` theme rename + `3a5c3c65` docstring reframe).** Done: (a) **`no-boto3` AST lint** — replaced `boto3-direct` (which allowed boto3 in 5 now-deleted wrappers) with a strict no-boto3 check: ANY boto3/botocore import or `boto3.<x>` reach anywhere in `src/recon_gen` fails (mirrors no-playwright-leak). Passes clean — zero boto3 in src. This is the enforceable structural guard. (b) **deleted-symbol strip** — `_apply_cfg_aws_profile_to_env` deleted (last AWS_PROFILE-for-boto3 plumbing); the `emit_analysis()`/`emit_dashboard()` comment refs repointed to `App.validate()`/`resolve_auto_ids()`; stale boto3 comments (tagging_enabled, studio /deploy, test_l2 peek) reframed. (c) **theme_name fixture rename** `"QuickSight Gen Theme"` → `"Recon Gen Theme"` across the 6 live sites (DEFAULT_PRESET + test + 3 byte-identical spec_example mirrors + reskin doc); the feared semantic-lock/byte-identity cascade evaporated (theme_name isn't in the L1-violation snapshots, QS-JSON emit is gone). (d) **wrong-module-docstring reframe** — `__init__`/`config.py`/`ids.py`/`rich_text.py` headers that claimed the module IS QuickSight-generation machinery. (e) `recon-gen --help` has no `json` group ✓; `grep quicksight-gen .github/ pyproject.toml` = zero on the publish surface ✓ (only the historical shim-drop comment in release.yml). **LITERAL grep-zero on `quicksight\|QuickSight` is NOT achievable as specced + was REINTERPRETED (operator FYI):** explaining a removal names it (the test_models/models.py deletion docstrings, the runner's "QuickSight removed" notes), the kept quirks doc is a deliberate historical record (DW.12), the DatasetParameter QS-API-constraint comments document why the kept 32-cap/UUID-Id exist, and the `cfg.aws` QS fields (`quicksight_user_arn` + the dataset_arn/theme_arn ARN synthesis) are dead-config DEFERRED to the config-cleanup sweep. So ~150 `QuickSight` refs remain in src — all historical/dead-config, none live. **REMAINING: (1) a strict QuickSight-grep-zero LOCK TEST is best built AFTER the dead-config sweep removes the cfg.aws QS fields (else it needs an allowlist that churns); for now no-boto3 is the lock. (2) `up_to=agreement` green — the integrated POLICY 1 proof for the DW.11 reorder+strip, pending as the final pre-push run. (3) The `quicksight-test-oracle-` Docker container reuse prefix (`runner.py::ORACLE_REUSE_CONTAINER_PREFIX`) is QS-branded but local-only; rename to `recon-test-oracle-` deferred (one-time container-adoption miss).**
  > **DW.14 known residual (from DW.12):** `theme_name: "QuickSight Gen Theme"` is QS branding baked into the persona fixtures (`tests/l2/spec_example.yaml:623` + the sasquatch mirror + every doc that quotes them verbatim — e.g. `reskin.md:55`). Renaming it is a FIXTURE-DATA change that cascades into the byte-identity / semantic-lock snapshots (`tests/data/_semantic_locks/*.json`) + the src/ mirrors — re-lock required. Rename to a renderer-neutral name (e.g. `"Recon Gen Theme"` / `"Default Theme"`) as part of the grep-zero lock; the doc quotes follow the fixture. Also sweep the `QSG`-prefixed code identifiers (`QSGSystemSignature` pyHanko field, `QSG_SIGNING_PASSPHRASE` env var in `common/config.py`) if grep-zero is meant to catch the `QSG` stem (they don't spell "QuickSight" so may be out of scope — operator call).

- [x] DW.15 - **Graceful exit (the loud-v15-warning → graceful-v16-exit arc). DONE 2026-06-28 (`4f8372a3`).** (a) **v15 escape hatch** — the "QuickSight shipped through v15.x; pin `recon-gen==15.x`" pointer now lives in all three surfaces: README "Where it runs" note (added), the `quicksight-quirks.md` header (DW.12.1), and a new v16.0.0 RELEASE_NOTES entry. (b) **Balanced framing** — the v16 entry + README + quirks header all carry the "QuickSight is a legitimate call for some orgs (managed BI / embed-in-the-console); we left over recurring AWS cost-risk, NOT because it's wrong" framing. v15 keeps its LOUD deprecation tone as shipped; v16 carries the calm removal narrative (Removed: renderer + emit graph + extra + AWS footprint; Changed: agreement-terminal chain + 3-way gate + App2-as-THE-renderer) in full DT voice. **NOTE: the v16.0.0 RELEASE_NOTES entry is DRAFT — `__init__.py` stays 15.0.0; the version bump + tag is the release cut, which needs explicit operator go-ahead ([[feedback_always_ask_before_release_cut]]).**


## Phase DX - App2 Studio user docs + walkthrough staleness sweep + quirks reorg + link hygiene

The self-hosted App2 Studio (`recon-gen studio` — the unified diagram + L2 editor + data-shaping panel + Deploy-changes) has ZERO user-facing documentation; a user who runs it gets no guide (only incidental mentions in `etl-hook.md` / `self-host.md`, no handbook page, no mkdocs nav entry). Two manual doc-reviews during the v14.7.0 cut also surfaced stale content the DT/DU audit missed (a fictional `account_role` enum, broken `Schema_v6.md` anchors) plus ~47 non-clickable bare `See it live:` URLs. This phase closes the Studio-docs gap (the lead deliverable), systematically re-audits the customization/etl walkthroughs for staleness, and fixes the link hygiene — all in the DT operator voice. **Coordinate with Phase DW (QS removal) if it lands first** — DW rewrites the QS-centric docs, so the staleness sweep must not re-introduce QS framing. It also reorganizes the quirks log — splitting QS-specific quirks from the harness/renderer ones (prepping DW's clean deletion of the QS file) and consolidating the scattered silent-failure-detection saga into one coherent quirk. Acceptance: every Studio tool documented + in the mkdocs nav; the walkthrough staleness sweep green against the code + real L2; the quirks log split so the QS file is deletable while the harness quirks + the detection-pattern lesson survive; zero bare `See it live:` URLs.

- [ ] DX.1 - **Studio user docs (the gap — lead deliverable)** — a new handbook section + mkdocs nav entry for the `recon-gen studio` interactive surface:
  - [ ] DX.1.1 - The unified diagram — the L2 topology view + click-to-edit badges: what it shows, how to read it, the edit affordances.
  - [ ] DX.1.2 - The L2 editor — editing the L2 YAML in-browser; validation feedback; how changes persist (`.studio-state.yaml`, preserving the cfg's operator-authored comments).
  - [ ] DX.1.3 - The data-shaping panel (trainer knobs) — densify / plants / the seed-mutating knobs: what each does + persistence.
  - [ ] DX.1.4 - Deploy-changes — applying L2 edits to the demo DB from Studio; the schema → data → refresh chain it runs.
  - [ ] DX.1.5 - The `/dashboards/` surface inside Studio (the 4 apps rendered live) + the `etl_hook` seam for real ETL.
- [ ] DX.2 - **Walkthrough staleness sweep** (the DT/DU gap, two manual finds):
  - [ ] DX.2.1 - Kill the fictional 6-value `account_role` enum (`gl_control`/`dda`/`merchant_dda`/`external_counter`/`concentration_master`/`funds_pool`) in `handbook/seed-generator.md` + `walkthroughs/customization/{how-do-i-extend-canonical-values, how-do-i-map-my-database}.md` — replace with the live-inspect pattern (`load_instance(...)` → real roles `DDAControl` / `ConcentrationMaster` / `CashDueFRB` / …).
  - [ ] DX.2.2 - Fix/repoint the broken `[Schema_v6.md → canonical account_role values]` anchors (no such section; `account_role` is free-form `VARCHAR(100)`). Reconsider the "canonical values" filename/framing across the 4 walkthroughs (index, map-my-database, test-my-customization, extend-canonical-values).
  - [ ] DX.2.3 - SYSTEMATIC re-audit: every customization/etl walkthrough re-checked against the code + `sasquatch_pr.yaml` for stale concepts / columns / anchors (DT/DU under-covered this tree). Consider an ultracode workflow sweep given 2 bugs found by hand.
- [ ] DX.3 - **Link hygiene** — fix the ~47 bare `See it live:` URLs: prefer adding `pymdownx.magiclink` to `mkdocs.yml` (render-audit for over-linking first) else sed-wrap as `[See it live](https://recon-gen-spec.hotchkiss.io/)`.
- [ ] DX.4 - **Quirks log split (QS vs harness — preps DW)** — `docs/reference/quicksight-quirks.md` mixes QS-specific quirks (§1–5: silent rendering, drill/param-write, control/widget UX, data-type/shape, backend/refresh) with ONE non-QS section (§6 "Local browser-test-harness quirks", e.g. the macOS WebKit IOSurface/WindowServer crash). Extract §6 into its own file (`reference/test-harness-quirks.md` or `reference/renderer-harness-quirks.md`) with its own mkdocs nav entry, leaving `quicksight-quirks.md` purely QS so it becomes a clean, self-contained QuickSight REFERENCE that **DW KEEPS** (reframed for v15 users — operator directive 2026-06-27), while the harness quirks survive in their own file. Update cross-links + the CLAUDE.md quirks-log pointer + the structured-triple references that point at the quirks doc.
- [ ] DX.5 - **Consolidate the silent-failure-detection saga into one quirk** — the "crazy extent we went to to detect QS errors" is scattered today: the "worst footgun" intro, §1 (silent rendering failures + the diagnostic ladder), the App Info canary mentions (~line 322), the AA.H.6 failure-capture (~line 356). Write ONE coherent entry capturing the full machinery: QS gives NO error signal (datasets describe-clean, analyses dead, spinner-forever, no banner) → so we built (a) the **App Info canary** (every dashboard's last sheet = real-query KPI + per-matview row-count + deploy stamp — "if Info shows a number QS is healthy"), (b) the **diagnostic ladder** (hit the SQL via psycopg2 → verify `describe_data_set` SUCCESSFUL → fresh incognito → assume QS itself is broken), (c) the **QsEmbedDriver SQL-exception scanner**, (d) the **AA.H.6 eager capture suite** (screenshot/dom/errors/console/network/ws/db_counts/trace on detection). The QS-silent-failure half stays in the QS file (**KEPT by DW** as the v15 reference); but **extract the transferable lesson** — active canaries + multi-layer detection for any renderer that fails silently — into the surviving harness file, since the App Info canary lives in the SHARED tree (App2 has it too) and the lesson is the same theme as the App2 server-overload-signal backlog item.
- [ ] DX.6 - Verify (docs build + per-sheet snippet liveness gates green) + operator review.

## Phase DY - Test-coverage integrity + DuckDB parameterization (close the missing/skipped/diverged tests DW surfaced)

> **SHIPPED in v16.0.0 (2026-06-28):** Phase DW (QuickSight removal) + DY.0 (audit) + DY.1 (reconciliation gate + importorskip lint + the finished `--tier` migration, 11 re-enabled orphans green) + DY.4-prose (stale-QS-docstring sweep). Tagged `v16.0.0`, published to PyPI, GitHub Release cut. **Remaining DY → v16.x point releases:** DY.2 (DuckDB agreement), DY.3 (#331 App2 Anchor-refetch), DY.4-markers (the 2 stale xfail strips), DY.5 (#239 L2-robustness), DY.6 (DuckDB db-tier `:memory:`), DY.8 (spine `set_trace` re-enable), DY.9 (screenshot capture build), DY.10 (the 3 real App2 bug xfails). These + the Backlog items below are the resume point — the reconciliation gate now guarantees no test silently falls out of the run-set while they're worked. v16's release notes list DY.3/DY.5/DY.10 as documented Known Issues.

Removing QuickSight (Phase DW) lifted a rock: collapsing the `[qs, app2]` parametrization to app2-only exposed tests whose only non-skipped leg WAS QuickSight (so they now always-skip), DW.3 surfaced a whole release-credibility gate (the agreement validators) that had silently run NOWHERE since the CB.5 tier-dir migration, and the "build agreement on duck" spike proved both that the agreement layer was pg/oracle-pinned by vestigial QS coupling AND that DuckDB has real cross-database divergences + xdist single-writer isolation failures. chotchki (2026-06-28): "all the missing / skipped tests we discovered and that duckdb doesn't work for stuff." This phase is the cleanup: make "a test that runs nowhere" structurally unrepresentable, get the agreement layer (and the local fast path) running on DuckDB, and resolve every QS-orphaned always-skip per POLICY 2 (build the App2 verb or retire the test — no silent always-skip). The detailed re-applicable specs already live in the Backlog entries this phase promotes (the agreement-over-dialects 5-step enablement; the tier-mark divergence; the App2 parity gaps); DY is where they get executed, not re-derived.

> **SESSION PROGRESS 2026-06-30 (uncommitted working tree — operator review before commit/cut).** Landed + verified: **DY.5** (broadened `anomaly_pair_for_l2` to STRUCTURAL role-discovery — preference head keeps spec_example→CustomerSubledger deterministic, falls back to any internal-leaf role, skips only when the L2 has NO leaf-money account; CORRECTS the plan's premise — sasquatch has ZERO internal-leaf accounts so it NEVER exercised these validators, the "sasquatch runs" note was wrong), **DY.8** (10 spine pushdown-surface tests re-enabled via `tests/unit/_spine_sql_capture.py::record_sql` — a type-transparent recording-connection proxy that captures BOTH the `fetch_all` cursor path AND the `conn.execute()` duckdb shim; unit tier green 5128 passed + pyright clean), **DY.4-markers** (both stale `strict=False` xfails stripped), **DY.3** (always-skip removed — CT.1 says local-pass, only the retired WSL2 CI runner timed out; app2_browser verify gated on DY.6 below). **DY.2 parameterization DONE + producers green on duck:** fixed the THREE duck-blind spots in `_agreement_helpers.py` (THE key bug was `Dialect.ORACLE if ... else Dialect.POSTGRES` silently coercing `duckdb`→POSTGRES — now `Dialect(name)`; plus the `_DIALECT_CONFIG_PATHS` duckdb entry + the cfg-filename detect branch), added `read_only=False` at 6 seed sites, added `duckdb` to the 2 audit parametrize lists. **db layer GREEN on duck (104 cells)** incl. the `limit_breach` cell I root-caused: NOT a duck SQL bug (the matview + audit are correct single-process, `count=2`), the `audit_pdf` fixture wasn't threading the ISOLATED DuckDB URL to the `audit apply` CLI (per-file isolation — the prefix env alone routes PG/Oracle on a shared DB but can't redirect duck across files; the CLI fell back to the runner's ambient demo-seeded cell DB) → fixed by adding `RECON_GEN_DEMO_DATABASE_URL: cfg.db.url` to the fixture env (no-op on PG/Oracle). app2 agreement producers wrote their duck artifacts (drift/overdraft/limit_breach). **DY.2 VALIDATORS on duck are GATED on DY.6** (below) — the full `up_to=agreement` duck chain fails at [app2] on the trainer dogfood, upstream of the agreement layer. **DY.6 db-tier CONFIRMED green under xdist** (per-file isolation via `_isolate_cfg` — `:memory:` NOT needed, answers the "unifying fix" question: per-worker FILE is the fix). **DY.6 DEEP FINDING (the remaining work): `RECON_GEN_DB_READ_ONLY=1` is VESTIGIAL under per-file isolation AND actively breaks WRITERS** — it's a shared-file-era hack (runner.py:508-512), obsolete now that each worker gets its own `.duckdb` file. It forced the `read_only=False` workaround at all 7 seed sites (6 DY.2 + `test_bv33_trainer_dogfood::_seed_demo_db`, which the DY.0 scout missed), AND it blocks the trainer dogfood's Studio-server writer: `_studio_routes.py::_run_session_start`'s DELETE fails `InvalidInputException: ... attached in read-only mode` → the v_* overlay matviews never build → `CatalogException` downstream. **DECIDED (operator, 2026-06-30) — NARROW fix, keep tests DB-AGNOSTIC.** First-pass "drop read-only for duck" was REVERTED: read-only is NOT purely vestigial — it's LOAD-BEARING for the shared-base-demo readers (`dm_cascade` / `html2_executives` / `cq_picker` read the runner's ONE pre-seeded base file via the raw cfg; DuckDB cross-process shared read across xdist workers REQUIRES read-only, where PG/Oracle get concurrent shared read FREE via MVCC — so ALL dialects share the base pre-seed and the model is uniform / DB-agnostic). Dropping the env broke those readers (`IOException: Could not set lock ... conflicting lock held in PID …`) AND would diverge duck from PG/Oracle. So: KEEP the runner env for READERS, thread `read_only=False` through the ISOLATED WRITERS only. Seeds already pass it (7 sites); the trainer's in-THREAD Studio server (an isolated writer on its own per-worker file — NOT cross-process) now gets it via `studio_server(cfg, read_only=False)` → `make_connection_pool(..., read_only=False)` → `_AsyncDuckdbPool` (explicit wins over env), WITHOUT a global env-clear (which could leak onto a shared-base reader sharing the xdist worker). Memory: [[project_duckdb_test_isolation_is_per_file]]. **RESUME POINT:** DY.6 read-only fix → unblocks DY.2-validators + DY.3/DY.4 app2_browser verify + DY.7-duck; then DY.10 (a+b picker races are the SAME class as DY.3 — likely just marker-strip + local verify; c exec date-bind: the active-accounts SQL DOES narrow by date single-process, suspect App2 date-param threading — needs live/SQL-bind check), DY.9 (screenshot build — non-trivial, `capture_app_dashboards` signature-mismatched vs the cfg+instance server-construction path), then PG `up_to=agreement` + DY.7.
> **DY.7.3 STATUS (2026-07-01, executed).** Server-side DI LANDED in `deploy_pipeline.py` exactly per spec (`_close_if_owned`/`_open_deploy_conn` helpers, 5 steps threaded + 3 SQL-builder pass-throughs, `run_deploy_pipeline` owns one read-write conn + Stage-A/B release around `step_1`'s bracket). pyright clean; 132 deploy/etl_hook unit tests pass. **TWO-LAYER FINDING:** the DI fixed the SERVER-side conflict, but a SECOND conflict layer surfaced — the trainer TEST's OWN direct-read helpers (`_v_matview_signatures`/`_diagnose_v_state`/`_v_matview_account_ids` @ test_bv33:493/538/633) opened `connect_demo_db(cfg)` read-only (env) to verify matview state, conflicting with the running read-write pool. Fixed with `read_only=False` (isolated-writer reads matching the pool; DB-agnostic no-op on PG/Oracle). NOT touched: `_studio_deploy_helpers.apply_schema_to` (dead — no callers) + `.row_count` (not in trainer path; shared helper → would need a param if ever used by a shared-base reader). `apply_plants._run` in v_overlay is a separate already-one-conn writer (untouched, correct). **THIRD LAYER (server-side reads the DI missed):** with the conflict cleared at Session Start, the trainer proceeded and hit a NEW duck failure — the Apply button stayed disabled → `Page.click` 30s timeout ×17×rerun (22 min). Root cause: `_studio_routes.py::_v_overlay_exists._probe` opened `connect_demo_db(cfg)` READ-ONLY, conflicted with the read-write pool, and its `except: return False` SWALLOWED the exception (→ 0 conflict signals in logs) → `v_overlay_exists=False` → button disabled. Fixed `read_only=False` on it + the plant-apply writer (`_do_plant` @5455) + the two overlay-apply writers (`pipeline_overlays.py` 109/150). `_db_fetcher`/`_role_cardinality` default factories confirmed OUT of the studio path (it renders via the async pool `_sql_executor`). **DONE + VERIFIED:** PG canary PASS (no regression — DI is dialect-agnostic, read_only no-op on PG) + **full trainer dogfood 17/17 GREEN on duck in 90s** (was 22-min timeout marathon), 0 conflict signals, pyright 0 errors. **DRIVER HARDENING (operator-requested, DZ-ish):** the `App2Driver` now fails LOUD+FAST on a disabled target instead of eating the 30s Playwright timeout — added `_scream_if_disabled` + baked it into 3 interaction primitives `_click`/`_check`/`_fill` (the only enabled/editable-requiring interactions the driver uses; select_option/type/tap/etc. unused), routed ALL ~15 click + 2 fill + 1 check sites through them. DY.7.3 = DONE; full-chain `up_to=agreement` verification tracked under the DY.7-exit task.
>
> **DY.7-EXIT PROGRESS (2026-07-01, duck `up_to=agreement`).** Chain now clears **unit (5128✓) → db (104✓) → app2 (57✓ — trainer suite GREEN on duck)** and fails only at **app2_browser**. Failures triaged (NONE from the DI/driver work — 0 disabled-screams, 0 conflict signals): **(1) 12× `fetch_anchor_row: unsupported dialect DUCKDB`** — a test helper (`tests/e2e/_picker_anchor.py`) hardcoded `psycopg.connect` + a PG/Oracle-only fence. FIXED DB-agnostic: routed through `connect_demo_db(cfg)` (env-default read-only — it's a SHARED-BASE reader, the app2_browser dashboard pool + sibling xdist workers all hold the one pre-seeded base file read-only; `read_only=False` would grab a write lock + block them — caught + reverted an initial wrong `read_only=False`), DuckDB `LIMIT 1` like PG, `d.name`→`d[0]` (DuckDB description rows are plain tuples, no `.name`). Cleared 11/12 → **18 passed**. **(2) 2 REMAINING real App2-renderer failures (PG-classification RUNNING to split duck-specific vs general):** `test_drill_guardrail[exceptions/Exception Detail/Narrow Drift to this account→drift]` — BS.3-class picker-bypass: the drill writes `pL1FilterAccount=gl-2010-dda-control` but the Drift destination shows OTHER accounts (`acct-ledger-drift-parent-dN`), param not narrowing (only THIS drill param fails; the other 3 drills narrow correctly). `test_inv_drilldown::test_account_network_table_walk_rerenders_table` (the DY.3 re-enabled always-skip) — Anchor typeahead option `'Juniper Ridge LLC'` didn't land in ts.options in 60s; explicitly "Option-load failure, NOT a refetch timeout" (so NOT the #331 refetch gap); ran on sasquatch (account exists) so not an instance mismatch. **(3) 2 xfailed** = the DY.10 picker races (test_l1_additive_pickers 443/542 — expected-fail, don't block, DY.10 fixes). NEXT: classify (2) via the PG run → duck-specific SQL/param fix (DY.2-class) OR general App2 bug (DY.10/#331 territory). Then PG full-chain + the DY.7.1 report-only gate.
>
> **PG-CLASSIFICATION RESULT (2026-07-01, sasquatch on PG).** Ran both remaining failures on PG-sasquatch to split duck-specific vs general: **`drill_guardrail[exceptions→Drift]` PASSES on PG → DUCK-SPECIFIC.** Root (static analysis, unconfirmed live): `gl-2010-dda-control` is a `DDAControl` CONTROL account (`parent_role` NULL → NOT in the leaf-only Drift matview); Exception Detail's amount-DESC sort has no stable tiebreaker, so DuckDB's row-0 diverges from PG's (picks the control account), and drilling "Narrow Drift to this account" on a no-drift control yields the un-narrowed shape. Likely fix = stable ORDER BY tiebreaker on the Exception Detail dataset (DY.2-class determinism), BUT the "14 rows shown, not narrowed" needs LIVE TRIAGE to confirm (a narrowed no-drift account should → 0 rows, not 14). **`inv_drilldown::test_account_network_table_walk` FAILS on PG too → GENERAL App2 gap** (the #331 Anchor typeahead option-load; DY.3 re-enabled it). Build-or-retire per DY.3 — substantial App2-renderer work, not duck. **AUTONOMOUS-STRETCH SEQUENCING (operator asleep 2026-07-01):** both App2-renderer bugs DEFERRED to focused triage (documented here + tracked under DY.10/#331/task#2; NOT swept — they need the live system). Proceeding with the tractable high-value autonomous work: DY.7.1 report-only gate (generates the skip-inventory review sidecar) → DY.7.2 (sasquatch template-role) → PG full-chain baseline. The 2 App2 bugs + the auth-skip surfacing are for operator-present sessions.
>
> **DY.7.1 REPORT-ONLY GATE LANDED (2026-07-01, `tests/conftest.py`).** The `example_l2` skip/xfail gate is in — folded into the existing `pytest_runtest_makereport` wrapper (BEFORE the call-phase early-return so setup-phase marker-skips are caught), REPORT-ONLY (logs categorized JSONL to `runs/<id>/example_l2_gate/<layer>-<worker>.jsonl`, does NOT flip outcome yet). Marker `@pytest.mark.example_l2_exempt(reason="fuzz:"|"engine-limit:")` registered; non-prefixed reason → `invalid-exempt` (still surfaced). Categories: `suspect` (the gaps) / `config-absence` / `infra-import` / `exempt` / `invalid-exempt`. pyright-strict clean (needed `cast("tuple[object,...]")` on the skip-longrepr + `cast` import). **SCOPING FIX (caught in validation):** first pass gated the whole unit prelude (env-unset defaulted to spec_example) → flooded with 103 docs-liveness skips ("No recon-gen invocations in index.md" — NOT L2-coverage). Fixed: (a) env-unset → NOT gated, (b) `_EXAMPLE_L2_GATED_LAYERS = {db, app2, app2_browser, agreement}` — only the per-L2 e2e tiers where the runner dispatches an instance count as "a spec/sasquatch test." Unit-tier "spec_example lacks chains/limit_schedules" feature-absence skips are a separate L2-fixture-completeness concern, deliberately out of this gate's e2e-coverage scope. Validated: unit sidecar now empty (docs noise gone); db-tier duck-spec_example sidecar empty too (its 31 skips are dialect deselects/infra). **NEXT for DY.7.1:** run `up_to=app2_browser` on spec_example AND sasquatch to populate the `suspect` review list (the app2/app2_browser data-conditional + xfail skips the fork inventoried), triage them, THEN flip report-only → hard-fail. Fork design + classified inventory: agent a94275f2.
>
> **DY.7.2 PLAN (ready to implement).** Confirmed: sasquatch `instance.accounts` has ZERO leaf accounts (parent_role) — all leaves are template-generated (`account_templates`: CustomerDDA/MerchantDDA/ZBASubAccount, each scope=internal + parent_role). So `anomaly_pair_for_l2` (scans `instance.accounts`) skips for sasquatch. **KEY SIMPLIFIER:** the anomaly generator (`spine/anomaly.py:247`) uses ONLY `sender.parent_role`/`recipient.parent_role` from the `find_internal_with_role` return — it synthesizes its OWN account_id (`acct-anomaly-sender-<role>`). So the fix doesn't need a real seeded account, just an Account carrying the template's role/scope/parent_role. TWO edits: (1) `tests/e2e/_agreement_helpers.py::anomaly_pair_for_l2` — also scan `instance.account_templates` for internal-leaf roles (returns CustomerDDA for sasquatch). (2) `src/recon_gen/common/spine/_emit_helpers.py::find_internal_with_role` — when `role` not in `instance.accounts`, check `account_templates`; if a matching template (scope=internal; parent_role present for must_be_leaf), synthesize + return `Account(account_id=<template_instance_ids(t)[0] or role>, role=t.role, scope=t.scope, parent_role=t.parent_role, ...)`. `template_instance_ids` at `auto_scenario.py:1649`. Verify: `test_inv_anomaly_app2` + `test_inv_money_trail_app2` + the two `*_agreement` validators RUN (not skip) on sasquatch. money_trail may also need a CHAIN-role discovery (check `spine/money_trail.py`'s find_internal_with_role usage).
>
> **DY.7.2 LANDED (2026-07-01) — THREE edits, not two.** (1) `spine/_emit_helpers.py::find_internal_with_role` — template fallback (synthesizes `Account(id=template_instance_ids(t)[0], role, scope, parent_role)` from a matching `account_template` when the role isn't in `instance.accounts`; lazy-imports `template_instance_ids`). (2) `_agreement_helpers.py::anomaly_pair_for_l2` — scans `account_templates` too. (3) **`tests/e2e/db/test_inv_direct.py::_pick_internal_leaf_role`** — the db-tier producer had its OWN `instance.accounts`-only skip check (with a STALE comment claiming "sasquatch is structurally unsupportable"); scans templates now. money_trail confirmed covered — `spine/money_trail.py:122` uses the same `find_internal_with_role(must_be_leaf)` reading only `parent_role`. All pyright-clean. **Verified unit-level:** `find_internal_with_role(sasquatch,"CustomerDDA",leaf)` → `cust-0001-snb | CustomerDDA | internal | DDAControl`; `anomaly_pair` → CustomerDDA. **First e2e run (before the db-producer fix):** old skip GONE (0 hits), app2 producers RAN (5 passed), but agreement validators still skipped because the DB producer (`_pick_internal_leaf_role`) still skipped → no `anomaly_direct_meta` artifact → agreement `pytest.skip`. The db-producer fix (edit 3) closes that. **CONFIRMED DONE (re-verify bak3382xq):** sasquatch-duck `up_to=agreement --only "anomaly or money_trail"` — db producers 2-skipped→**5 passed**, agreement validators 2-skipped→**2 passed**, chain PASS. Task #10 ticked. **RESIDUAL (separate, NOT DY.7.2):** app2_browser had "2 skipped" — a data-conditional (anomaly slider needs planted z-score rows, fork inventory `test_inv_filters:75`), which the DY.7.1 gate will flag as `suspect` → fix by planting.
>
> **DY.7.3 EXECUTION SPEC (2026-06-30, post-fork-map af966b21 — operator will compact then start fresh).** Root smell: the sync Studio-deploy path opens ~28 fresh `connect_demo_db(cfg)` (read-only via the runner env) that CONFLICT with the async read-write pool on duck (DuckDB: "same database file with a different configuration than existing connections"). PG/Oracle mask it (MVCC). Fix = inject ONE owned read-write conn. SCOPE IS BOUNDED to `common/l2/deploy_pipeline.py` — the v_overlay writers (`session_start._run`, `apply_plants._run`, `cleanup._run`) + the read_* are ALREADY one-conn-each (fine). Only `run_deploy_pipeline`'s 5 step-functions each open fresh.
>
> **FACTS (fork):** deploy DB steps are strictly SERIAL (each `await asyncio.to_thread(_run)` completes before the next) → one injected conn is thread-safe (serial cross-thread OK; only simultaneous is not). Two read-write handles (pool + injected) COEXIST in-process (DuckDB shares the same-config instance). Trainer path: `step_1_etl_hook` is SKIPPED (cfg.app2.etl_hook is None → returns 0, NO subprocess) → subprocess-release hazard ABSENT → trainer greens with Stage A alone. Order in `run_deploy_pipeline`: step_2_wipe(1095) → [bracket: step_1_etl_hook(1124)] → step_3_generator(1150) → step_3_5_derive_balances(1153) → step_4_matviews(1175) → step_5_reload(1176, no DB).
>
> **EDITS (deploy_pipeline.py):** (1) add `SyncConnection` to `from recon_gen.common.db import (...)` (line 44). (2) FIVE step fns — `step_2_wipe`@253, `step_3_generator`@419, `_covered_rail_names`@656, `step_3_5_derive_balances`@696, `step_4_matviews`@830 — add kwarg `injected_conn: "SyncConnection | None" = None`; change `conn = connect_demo_db(cfg)` (330,460,668,758,858) → `conn = injected_conn if injected_conn is not None else connect_demo_db(cfg, read_only=False)`; guard `finally: conn.close()` (383,475,683,817,888) to close ONLY when `injected_conn is None` (indents: 12-sp at 383/475/888 inner `_run`; 8-sp at 683/817 module-level). `step_3_generator` calls `_covered_rail_names` internally → thread injected_conn to it too. (3) `run_deploy_pipeline`@993: add `injected_conn=None`; own a read-write conn in a try/finally spanning the body (cover halt-return@1137); pass `injected_conn=owned` to step_2_wipe/step_3_generator/step_3_5/step_4_matviews. **Stage A** (trainer-green): hold across the no-op bracket. **Stage B** (production etl_hook): close owned conn BEFORE `async with bracket`@1123 + reopen AFTER (real subprocess needs exclusive file; no-op on trainer). session_start passes NO conn (run_deploy_pipeline owns its own; session_start._run is a separate already-one-conn op).
>
> **VERIFY (order):** (1) `RECON_GEN_CONFIG=run/config.duckdb.yaml ./run_tests.sh up_to=app2` → trainer-du green (was 17 errors). (2) etl_hook: `test_studio_etl_run.py` + `test_cz_studio_deploy_etl_hook_gate.py` (Stage B). (3) PG no-regression (`RECON_GEN_CONFIG=run/config.postgres.yaml ./run_tests.sh up_to=app2`). (4) full duck `up_to=agreement`. Surface (fork §8): test_deploy_pipeline.py, test_session_start_base_schema_probe.py, test_bv4_training_v3.py, test_bv49_diff_apply.py, test_studio_deploy_route.py, test_bu_1_11_trainer_clean_no_false_positives.py + 20 others.
>
> **LANDED (uncommitted — DO NOT re-do):** DY.2 (`_agreement_helpers.py` 3 duck fixes + `read_only=False` at 5 seed sites [test_audit_direct/test_audit_invariants_app2/test_inv_direct(replaced os.environ.pop)/test_inv_money_trail_app2/test_inv_anomaly_app2] + `duckdb` in 2 audit `params=` + `RECON_GEN_DEMO_DATABASE_URL: cfg.db.url` in test_audit_direct::audit_pdf); DY.5 (`anomaly_pair_for_l2` structural + `SCOPE_INTERNAL` import); DY.8 (`tests/unit/_spine_sql_capture.py` + 10 spine files); DY.4 (2 xfail markers stripped); DY.3 (skip removed in test_inv_drilldown.py); v_overlay.py (8× read_only=False + comment); db.py (`_AsyncDuckdbPool`/`make_connection_pool` gained `read_only` param); _studio_deploy_helpers.py (`studio_server` gained `read_only` param); test_bv33_trainer_dogfood.py (`_seed_demo_db` read_only=False + `studio_server(cfg, read_only=False)`); runner.py (read-only-for-duck KEPT with DY.6 comment). **RECONCILE after DI:** pool/studio_server `read_only` param + trainer `studio_server(read_only=False)` STAY (async read pool must be read-write to coexist). v_overlay 8× read_only=False may stay (explicit writer-intent) or revert once injected conn covers them — low priority. **STILL OPEN in DY:** DY.7.1 (example_l2_exempt gate), DY.7.2 (sasquatch template-role — `inst.account_templates` has CustomerDDA leaf; ALSO needs find_internal_with_role to resolve template-generated accounts), DY.10 (a+b picker races ~= DY.3 class, c exec date-bind), DY.9 (screenshot build), then PG+duck `up_to=agreement` for DY.7.
- [x] DY.0 - **AUDIT — enumerate every skip / xfail / xpass + classify.** **DONE 2026-06-28 — 4-agent read-only Workflow → `docs/audits/dy_0_skip_inventory.md`.** Headline: **(spine) 11 tests run NOWHERE** — `test_dashboard_driver.py` (9 App2-driver smoke) + `test_studio_deploy_browser.py` (2 — the Deploy→pipeline→dashboards integration test, real coverage dark); `app2_browser` is the sole un-migrated `-m browser` selector, every other layer is dir/tier-driven → DY.1. **(always_skip) 12** = 10 spine `*_detect_does_not_cross_a_sql_pushdown_surface` (`set_trace_callback` SQLite-only, no DuckDB equiv → new DY.8) + #331 (K.4.8 now ZERO coverage → DY.3). **(xfail) 5** = 2 stale QS-only to strip (DY.4) + 3 REAL App2 bugs parked behind xfail (picker race ×2 + exec date-filter KPI → new DY.10). **(driver gaps) NONE** — all 11 `NotImplementedError` are precondition-guards / abstract stubs with concrete overrides; `[qs,app2]→[app2]` collapse VERIFIED complete (no `qs` callspec anywhere). **(prose) ~15** stale present-tense QsEmbedDriver framings → DY.4 sweep. **(#239 CORRECTED)** not "skip-on-sasquatch" (sasquatch has `CustomerDDA`, runs); skip fires only when an L2 declares neither role → L2-robustness, DY.5. **(screenshot)** DW.8.4 placeholder confirmed → new DY.9. ~75 other `pytest.skip` + 5 `skipif` all conditional-legit, no action. **Posture: fix-and-enable ([[feedback_dy_more_testing_fix_and_enable]]) — expect DY.1's re-enablement to surface real failures; in-scope, not deferred.**
- [x] DY.1 - **Finish the CB.6 `--tier` migration + add the coverage-reconciliation gate (make "runs nowhere" unrepresentable).** **DONE 2026-06-28 (shipped in v16.0.0).** Built the gate (`tests/unit/test_layer_coverage_reconciliation.py`, RED→GREEN) + the importorskip lint (`test_importorskip_declared.py`); deleted `test_studio_deploy_browser.py` as superseded (legacy `/data` deploy UI; modern deploy is `/etl/`+`/training/` — backlogged); finished the migration (new `tests/e2e/app2_browser/` dir + auto-marking conftest, 21 root files moved, `-m browser` + `--ignore` hacks + `browser` marker + `qs_api/` all retired). The 9 re-enabled `test_dashboard_driver` orphans run GREEN; full `up_to=app2_browser` chain green on PG. Surfaced+fixed its own move-breakage (relative imports, a unit→e2e cross-import, `__file__`-relative fixture paths). Promotes the "tests that silently stopped running / diverged" Backlog entry. ROOT CAUSE: the CB.6 "migrate layer selection from `-m <mark>` to `--tier=<tier>`" never finished, so the test-DIR (`tests/e2e/<tier>/`, the source of truth for the tier) and the runner SELECTOR (`-m browser` / `-m api` legacy marks) drifted — a tier-marked-but-not-mark-marked test falls through the gap and runs NOWHERE (this is how the 8 agreement validators went un-run since the CB.5 migration). DY.0 named the orphans precisely: `tests/e2e/test_dashboard_driver.py` (9) + `tests/e2e/test_studio_deploy_browser.py` (2). **Recommended sequencing — gate FIRST (TDD-red), then migrate:** **(1)** Build the reconciliation gate as a UNIT-tier test that drives `runner._layer_command` directly (can't drift from the real runner): for each e2e layer assert `collect-only(tier-dir) == collect-only(layer's real argv)` + a global `union(all layers' ran) == collect-only(tests/e2e/)`; print the difference set on failure. It FAILS today naming the 11 orphans — that red is the spec. Collect-only is cheap (no DB/browser/Docker) → lives in the `unit` prelude, gates every push. **(2)** Finish the migration to turn it green: create `tests/e2e/app2_browser/` with an auto-marking conftest (adds `tier(APP2)`+`needs(PLAYWRIGHT)` to every item — adding a test = `touch` a file, no mark to forget), move all 22 root files in, rewrite the selector to `pytest tests/e2e/app2_browser/`, delete every `pytest.mark.browser` (root + the 6 redundant `app2/` files) + the three `--ignore` hacks + the empty `tests/e2e/qs_api/` dir + the `browser` marker registration — `agreement` (DW.3) is the dir-set template, go one step further to pure-dir like db/app2. **(3) Triage the re-enabled orphans** — once the 11 run, expect some to FAIL (real bugs); fix-and-enable per [[feedback_dy_more_testing_fix_and_enable]], don't re-skip. **(4)** Sweep `tests/e2e/` for any other tier-mark-without-run-mark shape ([[feedback_cheapest_validation_must_fire]] + [[feedback_invariants_in_process]] — unrepresentable, not fixed-once).

- [ ] DY.2 - **DuckDB-parameterize the agreement layer + the 2D agreement matrix ("duckdb doesn't work for stuff," part 1).** Promotes the "parameterize the agreement layer over dialects" Backlog entry (which carries the FULL 5-step re-applicable enablement spec built in a DW.3 spike + reverted to keep QS removal moving). The agreement layer is currently PG/Oracle-only — vestigial QS coupling, not a real constraint. The blocker is DuckDB-specific: the App2 agreement producer is a SECOND duck-opener (read-only, `RECON_GEN_DB_READ_ONLY=1`) alongside the seed, and on a non-seeding xdist worker the per-worker `.duckdb` file was never created → `IOException: ... does not exist`. The spec's fix (re-applicable verbatim from the Backlog entry): (1)-(4) the `_DIALECT_CONFIG_PATHS` + cfg-dialect-detect + the `dialect_enum` `else Dialect.DUCKDB` map fix (THE key bug — duckdb silently coerced to POSTGRES → every duck param skipped) + the producer `params=["duckdb","postgres","oracle"]`; (5) the bespoke-seed `connect_demo_db(..., read_only=False)` at the 4 seed sites so the iso DB gets created before the read-only App2 opener hits it. Operator decision still open (from the duck spike): (a) fix the app2-producer duck isolation properly (per-worker seed-before-App2-read), vs (b) run agreement-on-duck single-process (`-p no:xdist`) as the local fast path + keep xdist for pg/ora. Verified during the spike: `test_audit_invariants_app2.py::...[duckdb-drift]` = 1 passed via triage, so the producers DO run green on duck for most invariants once seeded. **ISOLATION CHOSEN 2026-07-01: option (a) — per-worker seed-before-App2-read, file-based (NOT single-process `-p no:xdist`).** Core implemented in-tree: the `Dialect(name)` coercion fix (duck was silently → POSTGRES → every duck param skipped), `read_only=False` at the seed sites, `_picker_anchor` made DB-agnostic, `duckdb` added to the audit/agreement `dialect_cfg` params. Pending the DY.7 matrix verify.
- [ ] DY.3 - **Resolve the QS-orphaned always-skips — build the App2 verb or retire (POLICY 2).** Promotes the "App2 parity gaps surfaced by DW.5/6 de-parametrization" Backlog entry. Headline: **backlog #331** — `test_inv_drilldown::test_account_network_table_walk_rerenders_table` ALWAYS skips now; App2's Anchor parameter pick fires `setValue` but the bound table doesn't re-fetch (the QS leg that used to carry it is gone). This is a REAL App2 renderer gap, not a test artifact. Per [[feedback_build_verbs_not_skip]] + POLICY 2 (no always-skip): either fix the App2 refetch-on-Anchor-pick (preferred — it's a genuine renderer capability) or retire the test with a written rationale. DY.0's inventory feeds this — any class-(c) entry lands here. Cross-check DY.0 for other App2-can't-yet always-skips beyond #331.
- [x] DY.4 - **Strip the stale QS markers + prose (cosmetic, the de-parametrization debris).** **PROSE DONE 2026-06-28 (shipped v16.0.0)** — the ~25 `[qs, app2]`/`QsEmbedDriver`/`qs_driver` present-tense framings swept across base.py (~30 verb docstrings) + ~24 tests/audit+tests/e2e files + 2 tests/unit stragglers; the dead `_capture.py` `._page` branch removed; only correct past-tense annotations + historical links remain. **REMAINING (carry to a point release): strip the 2 stale xfail MARKERS** (`app2_browser/test_l1_cross_sheet_drill_date_widening.py` + `app2_browser/test_l1_filters.py` — `strict=False` xfails that only applied to the deleted QS leg, so they XPASS harmlessly every run now). Their docstrings were already made honest; removing the `@pytest.mark.xfail` themselves + a browser-tier run to confirm they pass clean is the leftover (prose-only agents couldn't touch code markers). The 2 stale xfails DY.0 named — `test_l1_cross_sheet_drill_date_widening.py:58` + `test_l1_filters.py:90` — are `strict=False` markers that only ever applied to the now-deleted QS leg (their reasons say "the app2 leg xpasses / doesn't share the flake"), so they XPASS every run now → remove the markers ([[feedback_no_xfail_to_sweep_under_rug]] — a passing xfail lies about state). Plus the ~15 stale present-tense QsEmbedDriver framings: `_drivers/base.py` (module docstring "Two impls", the `dialect` attr, ~15 verb docstrings "App2-only — QsEmbedDriver raises NotImplementedError", ~25 lines QS-vs-App2 comparison prose), `tests/audit/_dashboard_extract.py` + `test_dashboard_extract.py`, `test_inv_filters.py`, `test_inv_drilldown.py`, `_capture.py` (+ strip the dead `._page` branch), and the stragglers (`app2/test_dm_cascade_and_day_availability.py`, `test_parameter_anchored_sheets.py`, `_picker_anchor.py`, `test_l1_account_filters.py`, `app2/test_html2_executives.py`, `test_drill_guardrail.py`). LEAVE the correct past-tense annotations ("QS was removed in DW") — only the live-impl framings are stale. **MARKERS STRIPPED 2026-07-01 (in tree):** both `@pytest.mark.xfail` decorators removed from `test_l1_cross_sheet_drill_date_widening.py` + `test_l1_filters.py`; the tests now run unmarked. Browser-tier confirm folds into DY.7.
- [x] DY.5 - **inv-producer L2-role robustness (backlog #239) — framing CORRECTED by DY.0.** NOT "skip-on-sasquatch": post-CS.12 `anomaly_pair_for_l2` returns `CustomerDDA` for sasquatch, so the anomaly producer (`app2/test_inv_anomaly_app2.py:104`) + validator (`agreement/test_inv_anomaly_agreement.py:50`) DO run on sasquatch. The `#239` skip fires only for an L2 declaring NEITHER `CustomerSubledger` NOR `CustomerDDA` (and the money_trail pair on a missing CHAIN role). So this is an L2-robustness gap — `_build_anomaly_generator` / `_build_money_trail_generator` are brittle on role shape — not a default-L2 coverage hole. Per the fix-and-enable posture: broaden the generators' role-discovery so fewer L2s fall through (preferred), OR confirm the remaining skip is a genuine L2-optional data-conditional (the invariant doesn't apply when the L2 models no such role) and document it. Lower priority — sasquatch already exercises both validators.
- [ ] DY.6 - **DuckDB db-tier xdist isolation ("duckdb doesn't work for stuff," part 2).** Fold in the standing `BX.backlog.duckdb` items (pytest-xdist intra-cell file-lock contention; migrate db-tier fixtures to `:memory:` DuckDB per xdist worker) — they're the same single-writer-lock family as DY.2's agreement-producer failure ([[feedback_strict_engines_surface_isolation_bugs]] — the lock is surfacing a real isolation bug, fix per-worker isolation, don't relax the engine). Decide whether `:memory:`-per-worker (no file, no lock) is the unifying fix for both the db-tier and the agreement-producer duck paths. **DECISION 2026-07-01 (chotchki): RATIFY file-based per-worker isolation — REJECT `:memory:`.** The tree already fixed the real isolation bug the file-based way (per-worker `.duckdb` via `_isolate_cfg`, explicit `read_only=False` at every writer site, seed-before-read WITHIN one worker) and it's green (db 104 cells, trainer dogfood, sasquatch-duck agreement chain). `:memory:` is strictly WORSE here: the App2 second reader is a SEPARATE async-pool connection and DuckDB `:memory:` is per-connection, so going `:memory:` would mean threading an owned handle through every test fixture + extending `make_connection_pool`'s public API — a rearchitecture that LOSES the cross-connection reach the file path already has, for zero gain. DY.6 reduces to: (a) confirm no residual db-tier file-lock contention under `-n auto` (`--dist=loadgroup` keeps a module on one worker, so seed+read stay co-located), (b) document the decision. No `:memory:` migration.
- [x] DY.8 - **Re-enable the 10 spine `*_detect_does_not_cross_a_sql_pushdown_surface` always-skips (the `set_trace` debris).** All 10 (`test_spine_money_trail/stuck_unbundled/expected_eod/limit_breach/anomaly/stuck_pending/overdraft.py` + `test_au0_overdraft/as0_drift/at0_anomaly_full_spine.py`) share the verbatim skip "set_trace_callback was SQLite-only; DuckDB has no equivalent. CB.8 backlog #set_trace." They assert the detector computes in-process (doesn't push the invariant down to a SQL surface) — SQLite's `set_trace_callback` recorded executed SQL; DuckDB has no equivalent hook. Fix-and-enable: find a DuckDB-equivalent query-capture (a recording cursor wrapper that logs `execute()` SQL, or `EXPLAIN`/`PRAGMA` introspection) and re-express the invariant against it, OR re-express the assertion to not depend on SQL-trace at all. POLICY 2 — no dangling backlog deferral.
- [ ] DY.9 - **Build the App2 dashboard screenshot capture (DW.8.4 placeholder → real).** `src/recon_gen/common/browser/screenshot.py::capture_app_dashboards` is a `NotImplementedError` whose docstring carries the full recipe; `tests/unit/test_screenshot_placeholder.py` pins it (fails when the build lands — the signal). Build per the recipe: start the `recon-gen dashboards` Starlette app on an ephemeral localhost port → `with webkit_page(...) as page: page.goto("http://127.0.0.1:<port>/dashboards/<slug>/")` → walk `app.analysis.sheets` (`click_sheet_tab` → settle → `page.screenshot(full_page=True)`) → move `url_params` from QS's `#p.<name>=` embed-hash to App2's `?param_<name>=` query string. Replace the placeholder-pin test with real capture coverage. Natural DX fit (App2-docs).
- [ ] DY.10 - **Fix the 3 REAL App2 bugs parked behind xfail (fix the bug, not the marker).** Distinct from DY.4's stale-marker strip — these are live-renderer bugs: **(a+b)** `test_l1_additive_pickers.py:446 + :543` — the App2 picker race on the L1 Exceptions sheet (visual `/data` responses fire before `expect_response` registers its listener; SQL is fast, not perf) → fix the App2 picker's response sequencing (or the test's listener-before-trigger ordering if the race is test-side). **(c)** `test_html2_executives_live.py:339` — the date filter doesn't narrow Active Accounts / Net Money Moved (bind not reaching, OR the matviews encode as-of-now vs as-of-`:window` semantics) → triage which, fix the SQL bind or the matview date semantics. May be silently XPASSing now (`strict=False`) — confirm current state first. Same fix-and-enable posture: a current-renderer bug behind an xfail is a POLICY-2 deferral to resolve, not preserve.
- [ ] DY.7 - Verify: full chain green on PG AND on DuckDB (`up_to=agreement` both dialects), the coverage-reconciliation gate (DY.1) green, zero unexplained always-skips (every skip is a documented data-conditional or permanent gap), zero stale xpass, the screenshot-placeholder pin replaced. Operator review of `dy_0_skip_inventory.md` final state.
  - [ ] DY.7.1 - example_l2_exempt gate — hard-fail un-exempted skip/xfail on spec/sasquatch
  - [x] DY.7.2 - Sasquatch anomaly/money-trail: discover roles from account_templates so it runs (not skips)
  - [x] DY.7.3 - Inject one owned connection through session_start/deploy_pipeline/v_overlay (kill fresh connect_demo_db in the Studio-server path)


## Phase EA - CI back to a cloud-hosted runner (AWS-free unlocks it)
- [ ] EA.1 - Confirm the self-hosted-runner rationale is now moot (post-QS/AWS removal)
- [ ] EA.2 - Spike the full chain on a GitHub-hosted ubuntu runner
- [ ] EA.3 - Resource/perf: Oracle container fit + xdist sizing + minutes cost
- [ ] EA.4 - Flip ci.yml runs-on to cloud; keep release.yml in parity
- [ ] EA.5 - Decommission the WSL2 self-hosted runner + update prose

## Backlog (not yet phased)

- **App2 parity gaps + stale marks surfaced by DW.5/6 de-parametrization (2026-06-27)** — collapsing `[qs, app2]` → `[app2]` exposed tests whose only non-skipped leg was QuickSight, plus marks/prose that outlived the driver. None are red (skips, not failures), but each is a [[feedback_no_silent_defer]] loose end: **(1) Backlog #331 (now dangling — referenced in code, absent from this file):** `test_inv_drilldown::test_account_network_table_walk_rerenders_table` ALWAYS skips — App2's Anchor parameter pick fires `setValue` but the bound table doesn't re-fetch (the QS leg that used to carry it is gone). Real App2 renderer gap; fix the refetch or retire the test (POLICY 2 dislikes always-skip). **(2) CLOSED (DW.5.2):** `test_dashboard_driver.py` retiered in place `tier(Tier.QS_BROWSER)`→`tier(Tier.APP2)` + dropped `Need.AWS_QS` (along with all ~22 root e2e browser files); now runs AWS-free in the `app2_browser` layer. **(3)** Prose-only stale `QsEmbedDriver` mentions in `_drivers/base.py` (the DashboardDriver protocol docstrings still describe the deleted QS impl + "App2-only — QsEmbedDriver raises NotImplementedError"), `tests/audit/_dashboard_extract.py`, `test_inv_filters.py`, `_capture.py` — DW.7-era docstring sweep; the protocol verbs are now unconditionally App2. Pairs with the "tests diverged over time" item below — both are de-parametrization debris.
- **App2 dashboard screenshot capture (DW.8.4 placeholder → build; DX fit)** — added 2026-06-27 (operator decision: "leave the placeholders so when we build the app2 version it slots in"). DW.8.4 removed the QS embed-URL screenshot path (`generate_dashboard_embed_url` + `ScreenshotHarness` + `capture_deployed_app`) but KEPT the `recon-gen docs screenshot` command + all its renderer-agnostic scaffolding (app-slug map, `--date-from/--date-to` resolution, output layout, DB warmup). The capture engine is `common/browser/screenshot.py::capture_app_dashboards`, currently a `NotImplementedError` placeholder whose docstring carries the full recipe. **Build:** (1) start the self-hosted dashboards server for the app (the `recon-gen dashboards` Starlette app on an ephemeral localhost port) instead of minting a QS embed URL; (2) `with webkit_page(headless, viewport) as page: page.goto(f"http://127.0.0.1:<port>/dashboards/<slug>/")`; (3) walk `app.analysis.sheets` — `click_sheet_tab` → settle → `page.screenshot(full_page=True)` (the renderer-agnostic per-sheet loop the QS engine already used); (4) move `url_params` (`--date-from/to`) from QS's `#p.<name>=` embed-hash to App2's `?param_<name>=` query string. `tests/unit/test_screenshot_placeholder.py` pins the NotImplementedError and FAILS when this lands — the signal to replace it with real capture coverage. Natural DX fit (the App2-docs phase). Until built, existing doc PNGs are untouched but not auto-regenerable.
- **Audit — tests that silently stopped running / diverged over time (chotchki, 2026-06-27: "so many tests that have diverged over time")** — DW.3 surfaced a whole GATE that wasn't executing: the agreement validators carried `tier(qs_browser)` but no `pytest.mark.browser`, and the runner's qs_browser layer selects via `-m browser`, so `pytest tests/e2e/ -m browser` silently deselected all 8 — the multiway-agreement release-credibility check had NOT run since the CB.5 tier-dir migration. Root cause: the CB.6 "migrate layer selection from `-m <mark>` to `--tier=<tier>`" never finished (see the stale comment in `runner.py` LAYERS + `tests/conftest.py::pytest_addoption`), so the test-DIR (`tests/e2e/<tier>/`, source of truth for the tier) and the runner SELECTOR (`-m browser`/`-m api`) drifted apart — tier-marked-but-not-mark-marked tests fall through the gap and run NOWHERE. **The pattern, not just this instance:** selection-by-mark vs selection-by-tier is a divergence surface; the same fall-through could be hiding other silently-deselected tests. Tasks: **(1)** finish the CB.6 `--tier` migration — make every runner pytest layer select by `--tier=<tier>` (collect the dir, deselect by tier) instead of `-m <legacy-mark>`, so the tier-dir IS the run set; retire the `pytest.mark.browser`/`pytest.mark.api` legacy selectors. The `agreement` layer (DW.3) already does this — it's the template. **(2)** Add a coverage-reconciliation gate: for each tier, assert `collected(tests/e2e/<tier>/) == ran(--tier=<tier>)` so a tier-marked test that no layer runs SCREAMS (mirrors [[feedback_cheapest_validation_must_fire]] — "pair zero-hit checks with planted-fixture smoke tests" + "trace red-CI back to why the cheaper validation didn't fire"). **(3)** Sweep the rest of `tests/e2e/` for the same tier-mark-without-run-mark shape (anything in a tier-dir whose layer still selects by legacy mark). Same family as the inv-producer skip-on-sasquatch (backlog #239) — both are "the test exists but doesn't exercise what it claims." [[feedback_invariants_in_process]] — make "a test that runs nowhere" unrepresentable, not just fixed once.
- **Studio deploy browser coverage — modern `/etl/` + `/training/` flow (2026-06-28, DY.1)** — `test_studio_deploy_browser.py` was DELETED as superseded: it tested the legacy `/data` "Deploy changes" button (`#deploy-btn`), which is pre-buildout dead UI — deploy moved to `/etl/` + `/training/` in the X.4.f.7 unified-home buildout, and `/data` carries NO top-nav entry (`build_top_nav_entries` = Diagram / L2 Editor / ETL Support / Training) + no rendered `href="/data"` anywhere, so no operator reaches that button. Its contracts are already covered: deploy pipeline → `test_deploy_pipeline.py` (X.4.g, every step, event-shape); poller auto-reload → `tests/js/test_data_generation_poller.py` + `test_studio_deploy_route.py`; modern trainer deploy in a browser → `test_bv33_trainer_dogfood`; dashboards render → the app2 browser tier. (En route I built then BACKED OUT a `deploy_changes()`/`attached()` verb on `StudioBrowserEditorDriver` — it only drove the dead button.) **Two follow-ups:** **(1)** if we want a BROWSER test of the modern `/etl/run` (streamed) deploy → data-generation bump → dashboard auto-reload end-to-end (today unit + JS-covered, not browser), build it against `/etl/` via a driver verb on the operator-discoverable nav path. **(2)** the legacy `/data` deploy controls (`#deploy-btn` + the `/deploy` POST route + the data-shaping deploy section, JS `quicksightDeploy()`) are dead UI — confirm `/etl/run` doesn't delegate to `/deploy`, then strip them. Both are Studio-cleanup, not blocking.
- **Vestigial `e2e` / `api` pytest marks (2026-06-28, DY.1 follow-on)** — DY.1 retired the `browser` mark (the app2_browser layer now selects by directory). The sibling `e2e` ("requires deployed AWS resources" — AWS is gone) + `api` (the removed api tier) marks are the same shape: still applied by ~10 e2e files (`pytestmark = [pytest.mark.e2e, ...]`) but NO runner selector reads them (every layer is dir/tier-based). Strip them from the files + drop the two `markers =` registrations in pyproject, same as `browser`. Low-risk mechanical sweep; deferred from DY.1 to keep that change focused on the browser-selector migration.

- **App2 server overload signal — make load failures self-reporting (chotchki, 2026-06-26)** — added during the v14.7.0 cut while triaging the `test_cq_4_e[app2-Rails-Rail]` 60s refetch timeout. A blind `expect_response` timeout is ambiguous: server-overload vs client-side (the actual cause was a `pick_filter` race — the pick fired no refetch). **Idea:** the App2 `visual_data` route tracks in-flight count (it already `run_in_threadpool`s the blocking fetch); when in-flight / threadpool-queue-depth / per-request latency crosses a threshold, return **`503` + `{error: "overloaded", in_flight, queue_depth}`** + `Retry-After` and LOG it. `App2Driver` detects the 503 → raises `App2ServerOverloaded(...)` with the server log attached, instead of an opaque refetch timeout. **Diagnostic win:** disambiguates the two failure modes for free — if it fires, it's load (tune capacity/parallelism with proof); if it stays silent and the refetch still never lands, it's client-side. (This round it would have stayed silent — the server returned all 200s — which is itself the "look at the client" verdict; confirmed only after a 105MB artifact download. The signal makes the *next* one self-report.) Same triple could surface a client-side analog: `pick_filter` already raises a clear option-load error (commit `83eb7319`) — extend that pattern so "the pick fired no refetch" is a named failure, not a 60s hang.
- **Revert (or justify) the DP.5 trainer-apply timeout bump (120s→300s, commit `531dd020`, shipped v14.4.1)** — added 2026-06-16. The bump was a wrong-diagnosis fix: the Oracle `test_trainer_dogfood_per_kind[or-*]` failures' real cause was an 11h-stale Oracle container (DG-phase debris), not apply slowness — a fresh container ran 17/17 green. Confirm a fresh-Oracle `trainer_apply` finishes under 120s; if so revert to 120s, else keep 300s as legitimate headroom (the docstring already says "oracle ~few min"). Low-priority cleanup; harmless either way.
- **Persistent local DB container max-age / staleness-recycle** — added 2026-06-16. An 11h-stale local `recon-gen-snap-test-oracle` container caused intermittent Oracle trainer-apply failures that masqueraded as a code bug and cost significant debugging during the DP/v14.4.1 sweep (chased ordering + matview hypotheses before a container restart fixed it). DG-phase hygiene (DG.2 schema/user scorched-earth) doesn't cover broader staleness (Oracle process / resource state). Add a max-uptime recycle or a pre-run staleness probe for the persistent `recon-gen-snap-test-*` containers so a stale local container can't intermittently flake the suite and misdirect debugging.
- **date-model.plant-days_ago-bounded — replace plant days_ago: int with days_into_window bounded type** — added 2026-06-12.
- **QS Anchor account picker MUI Autocomplete shows noOptions despite 139 options known** — added 2026-06-13.
- **Encode QS parameter-commit sequence as a typed wrapper (Tab-required)** — added 2026-06-15.
- **Future "Auth" phase — passkeys/WebAuthn + a fully-local authenticated mode (chotchki, 2026-06-27)** — App2 auth today is OIDC-against-an-external-IdP (`common/html/auth.py` authlib + `JwtCookieMiddleware`, Dex in tests) OR no-auth dev mode — there is NO local authenticated path (no local users/password, zero WebAuthn code; confirmed grep-clean). Operator wants both kept: (a) IdP/OIDC as the standard enterprise path (already shipped, stays as-is per DW.0.1) AND (b) a fully-local option so a self-hosted deploy doesn't REQUIRE an external IdP, with **passkeys/WebAuthn (FIDO2)** as the preferred local-first mechanism ("I LOVE passkeys"). Scope sketch: a `cfg.auth.webauthn` (and/or `cfg.auth.local`) block + a WebAuthn registration/assertion flow (e.g. the `webauthn` py lib — Rust-core options welcome per [[feedback_rust_influenced_tool_preferences]]) wired to mint the SAME `recon_gen_session` JWT cookie the OIDC callback already mints, so the session layer is shared and the IdP becomes optional rather than mandatory. Explicitly deferred OUT of DW (operator: "okay if IdP/auth stays as-is and we address in a future phase"); NOT a QS-removal blocker. Decision recorded in DW.0.1.
- **Future phase — purge QuickSight-isms from the SQL + code now that App2 is the sole renderer (chotchki, 2026-06-27)** — DW deletes the QS *code*; this follow-on phase removes the *complexity QS imposed* on code that survives. Lead candidate: the dataset SQL's parameter-pushdown contortions that exist purely for QuickSight — the `<<$paramName>>` literal-substitution placeholder, the 32-element `DataSetParameter.DefaultValues.StaticValues` cap (`common/models.py` `__post_init__`) and the `('<sentinel>' IN (<<$pX>>) OR <col> IN (<<$pX>>))` match-all shape built to work AROUND that cap (`apps/*/datasets.py` `_data_value_clause`/`_match_all_in_clause` + `L1_ALL_SENTINEL`/`L2FT_ALL_SENTINEL`). Post-QS, App2 binds `:param` natively, so the cap + sentinel dance MAY collapse to plain binds. **CAVEAT (operator): "it may just be a helper" — AUDIT FIRST, don't presume it's a burden.** The `<<$param>>` placeholder is the renderer-agnostic contract App2 already reads; if it's a clean helper it STAYS. Spike-then-decide ([[feedback_spike_before_locking_implementation]]). Other QS-ism sweep candidates: the drill left/right-click convention ([[feedback_qs_convention_origin]] — App2 may break it for a better affordance), QS-CLASSIC theme fallbacks, any SQL shaped by QS quirks rather than the dialect (pairs with [[feedback_sql_dialect_convergence_preferred]]). NOT a DW blocker — DW deletes QS; this simplifies after.
- **Future phase — parameterize the agreement layer over dialects + the 2D agreement matrix (chotchki, 2026-06-27; deferred from DW.3 to keep QS removal moving — "getting qs [removed] is more important")** — DW.3's "build agreement on duck" surfaced the layer was pg/oracle-pinned (vestigial QS coupling) AND that DuckDB has real cross-database divergences. Three parts:
  - **(A) Structural enablement (built in a DW.3 spike, REVERTED to unblock 1b — re-applicable from these exact edits, all in `tests/e2e/`):** (1) `_agreement_helpers.py::_DIALECT_CONFIG_PATHS` += `"duckdb": Path("run/config.duckdb.yaml")`. (2) `_agreement_helpers.py::load_dialect_cfg` cfg-dialect detect (~L166) += `else "duckdb" if "duckdb" in low`. (3) **THE KEY BUG** — `load_dialect_cfg`'s `dialect_enum` map (~L216) was `Dialect.ORACLE if ... else Dialect.POSTGRES`, silently coercing `dialect_name="duckdb"` → POSTGRES → the cfg's `Dialect.DUCKDB != POSTGRES` skip fired → EVERY duck param skipped (a no-op that masked everything for 3 debugging rounds). Fix: add `else Dialect.DUCKDB if dialect_name == "duckdb"`. (4) producer params: `db/test_audit_direct.py:95` + `app2/test_audit_invariants_app2.py:58` → `params=["duckdb","postgres","oracle"]` (the inv producers already follow the cell dialect via `isolated_cfg`). (5) **bespoke-seed read-only fix** — the duck tier sets `RECON_GEN_DB_READ_ONLY=1` (App2 = 2nd duck reader); bespoke seed fixtures NOT using the shared `_apply_isolated_seed` (which passes `read_only=False`) open read-only → can't create the iso DB → `IOException: ... does not exist`. Add `connect_demo_db(..., read_only=False)` to the SEED connects at `db/test_audit_direct.py:148`, `app2/test_audit_invariants_app2.py:95`, `app2/test_inv_anomaly_app2.py:91`, `app2/test_inv_money_trail_app2.py:88`. (`db/test_inv_direct.py:186` already `os.environ.pop("RECON_GEN_DB_READ_ONLY")` — fine.) With (1)-(5), the agreement producers RUN green on duck for most invariants (verified: `test_audit_invariants_app2.py::...[duckdb-drift]` = 1 passed via triage).
  - **(B) The real bug it caught — `limit_breach` diverges on duck:** the `<prefix>_limit_breach` matview returns 0 rows on duck where pg/oracle plant 2 (`db/test_audit_direct.py:336`). NOT the JSON quirk (matview uses `json_value()` / `schema.py:319`), NOT cap-type (the `_v_config_limit_schedules` view casts `cap` numeric / `schema.py:2396`). Drop is in `config_kv → _v_config_limit_schedules view → cap LEFT JOIN (ls.cap*100) → matview WHERE cap IS NOT NULL AND outbound_total > cap`. PINPOINT: `RECON_GEN_CONFIG=run/config.duckdb.yaml ./run_tests.sh triage "tests/e2e/db/test_audit_direct.py::test_l1_invariant_direct_extract[duckdb-limit_breach]"`, then pdb-query row counts at each stage (config_kv → view → matview). Audit ALL invariants on duck — the chain stopped at limit_breach; others may diverge too. [[feedback_strict_engines_surface_isolation_bugs]] — duck is the strict engine surfacing a real correctness gap.
  - **(C) The 2D agreement matrix (chotchki's insight) — "check agreement across ALL axes":** the gate today checks RUNTIMES within ONE database (`plants ⊆ direct-DB == App2 == PDF`, e.g. on pg). The duck divergence is a SAME-RUNTIME-ACROSS-DATABASES disagreement (App2-on-duck ≠ App2-on-pg) the gate never checks. Full credibility grid is 2D: {runtimes: App2 / PDF / direct-DB-recompute — QS removed post-DW} × {databases: pg / oracle / duckdb} — every (runtime, database) cell must agree. = multi-runtime agreement PER database AND multi-database agreement PER runtime. THIS is what auto-catches a dialect bug like limit_breach-on-duck.
- **DD.4.e2e follow-up: full sign_in_via_oidc Dex round-trip e2e** — added 2026-06-15. `tests/e2e/app2/test_oauth_login_flow.py` exercises the JwtCookieMiddleware contract + 4 of 6 OIDC verb paths, but not the full Dex code-flow round-trip. Requires `html2_server` to bind to `cfg.auth.oidc.redirect_uri`'s exact `host:port` over HTTPS via `cfg.app2.tls` — current ephemeral-port HTTP server doesn't match the Dex-registered redirect_uri. Add `bind_host` / `bind_port` / `ssl_certfile` / `ssl_keyfile` kwargs to `html2_server` + thread through `App2Driver.serving`, then unskip the `sign_in_via_oidc` full-flow test guarded by `cfg.app2.tls` + `dex_container_url`. Operator triage step: spin Dex via `./run_tests.sh triage tests/e2e/app2/test_oauth_login_flow.py::<new_test>` + pdb-inspect the Dex login form selectors against `password.html` v2.40.0.
- **App2 table sort arrow appears backwards (operator-reported, code-trace looks right — need repro path)** — added 2026-06-16.
- **How should planted errors surface in the app? (+ enhance pickers to SELECT them)** — added 2026-06-16. Two coupled questions surfaced by the DP qs_browser sweep: (1) **Surfacing/semantics** — the `_spine_plant` zero-amount drill-scaffolding marker tx (DL.3.1) leak across apps: they win L1 picker anchors, lead L1 drill-source rows, AND surface in **L2FT as a violation whose `entity_a` is a rail, not a chain** (so a chain-drill from that row can't round-trip). Decide whether scaffolding markers should be distinguishable from real errors, whether they should create L2FT violations at all, and how error rows should be presented (badge? grouped? non-leading sort?). The DP fixes (anchor-skip `_spine_plant`, drill-guardrail `_PLANT_ERROR_SENTINEL` exempt) keep the TESTS honest but punt this product question. (2) **Selectability** — once (1) is decided, pickers/dropdowns should let an operator who SEES an error row filter to it (the dropdown universes advertise only "valid" declared values today). Operator note 2026-06-16: it's not fair to make the test assert error-round-trip; this is the app's job to figure out.
- **Confirm gl-1010 weekend daily_balances are intended (not a seed quirk)** — added 2026-06-16.
- **Fix stale CLAUDE.md test-runner docs (post-CB.17.d variant-matrix retirement)** — added 2026-06-16.
- **app2-layer test hangs on an Oracle seed when Oracle is unavailable locally** — added 2026-06-16.- **test_snapshotter_pg restore-SLA test is load-flaky in the pre-push hook** — added 2026-06-17.
