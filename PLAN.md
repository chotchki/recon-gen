# QuickSight Generator — Active Plan

**Where we are.** Phase W shipped (v8.6.0); the v8.3.x → v8.6.x cumulative bug sweep settled at v8.6.13. We're out of the heat of phase-driven development — new work runs as targeted minor features + bug sweeps in the **Sustainment & minor features** queue below. Historical detail for every phase prior to v8.0.0 lives in `PLAN_ARCHIVE.md` and `RELEASE_NOTES.md`. This file tracks **forward-looking** work only.

## Greater Plan
X.2 - add the non quicksight renderer
  - Solves testing limitations
X.3 - add sqlite as a database dialect
  - Does not support materialized views but shouldn’t matter due to the local nature of the db
X.4 - yaml editor - How do you handle the yaml? Aka the shape of the institution?
X.5 - etl helper
X.6 - Stop the documentation lying.
- [ ] Make the core domain model the source of the documentation site just as the yaml for the shape today. No duplication, use it to build the doc from the models.
X.7 Cloud cost optimization


---

## Phase history (one-line per shipped phase)

- **Phase N** (v6.1.0) — Investigation + Executives ported onto L1/L2 tree primitives; theme moved to L2 YAML attribute; preset registry dropped. Full detail: `PLAN_ARCHIVE.md`.
- **Phase O** (v6.2.0) — Unified docs render pipeline with mkdocs-macros + `HandbookVocabulary`; per-app handbooks render against any L2 instance.
- **Phase P** (v7.x cumulative) — Dialect-aware schema + dataset emission; Postgres + Oracle CI matrix; Phase R seed pipeline foundations.
- **Phase Q.1** — Dashboard polish: USD currency formatting via `Measure(currency=True)`, universal date-filter sweep, Oracle case-fold wrapper for `ORA-00904`.
- **Phase Q.2** — Doc IA cleanup; Shape C audience-first home; persona-leak sweep across handbook + walkthroughs.
- **Phase Q.4** (v7.3.0) — Persona-neutral docs release; new `persona:` block on L2 YAML; CI gate for persona-token leakage.
- **Phase Q.5** — Persona-neutral docs full L2-driven substitution; Investigation walkthroughs split into mechanics + worked-example admonitions.
- **Phase Q.3.a** (v8.0.0) — CLI redesign: four artifact groups (`schema | data | json | docs`); each `apply`/`clean` defaults to emit, `--execute` opts in to side effects; `cli_legacy.py` deleted; bundled JSON emit (no per-app filter).
- **Phase R** (v7.2.0) — 90-day per-Rail healthy baseline + embedded plant overlays (densify×5, broken×15, inv-fanout×5); Volume Anomalies signal real on the seed; lognormal amount distribution.
- **Phase S** — Research: drop the system `dot` binary. Spiked Mermaid+ELK (failed eyeball — self-loops floated, layout fidelity poor) and graphviz WASM via `@hpcc-js/wasm-graphviz` (passed — byte-identical to graphviz/dot). Verdict written into RELEASE_NOTES + git log; Phase T executed the migration.
- **Phase T** (v8.1.0) — Every diagram now renders client-side via `@hpcc-js/wasm-graphviz`. `render_*` helpers return DOT strings; `<template class="qs-graphviz-source">` blocks inside `<figure>` wrappers; ~50-line JS shim does the WASM render. 5 `apt-get install graphviz` lines deleted across CI / Release / Pages workflows.
- **Phase U** (v8.2.0) — Audit Reconciliation Report. Fifth artifact group (`audit`) ships `apply` / `clean` / `verify` / `test` verbs that emit a regulator-ready PDF (cover + exec summary + per-invariant violation tables + per-account Daily Statement walks + sign-off + provenance appendix) bound by a four-input SHA256 fingerprint (`tx hwm + bal hwm + l2 yaml + code identity`); optional pyHanko auto-sign. Release-gate U.8.b's three-way contract (`expected == PDF == dashboard`) verified live across **6 invariants × 2 dialects = 12/12 PASS**. Closed the L1 dashboard's stuck/supersession `[today-7, today]` date scope that hid current-state matview rows from the dashboard's view.
- **Phase V** (v8.3.0) — Cleanup + tooling: `config.yaml` ↔ L2 institution YAML strict split (env-only allowlist on `load_config`, hand-built `Config(...)` literals collapsed to `make_test_config(**overrides)`); `docs apply --portable` (offline static-site builds with inlined wasm-graphviz + no fetched fonts — ship-on-USB-stick workflow); App Info sheet enhancements (`__version__` deploy stamp, per-matview `latest_date` + base-table comparison rows, ETL stale-matview troubleshooting page); R.6.e baseline tune-up (limit_breach noise drop on customer outbound; intermediate-clearing overdraft credit cascades on aggregating-rail / TT-leg / MerchantPayout / ZBA patterns); reference-nav regroup (App handbooks / Data contract / Operations); pip → uv migration with `uv.lock` committed.
- **Phase W** (v8.6.0) — Browser e2e in GitHub Actions. Three e2e jobs (`e2e-pg-api` push:main, `e2e-pg-browser` nightly cron, `e2e-oracle-api` push:main) against operator-owned Aurora/Oracle via OIDC role assumption + dedicated `ci-bot` QS user; per-run resource isolation via `qs-ci-${run_id}-{pg|oracle}` prefix; workflow-level always-cleanup (`cleanup-pg` / `cleanup-oracle`); `e2e-against-testpypi` release-pipeline gate holds prod publish on a live AWS run against the just-published TestPyPI wheel; per-job `pg_stat_statements` / `v$sqlstats` top-queries dump as a markdown artifact; unified Hynek-pattern coverage report posted to GHA Step Summary + republished to the `badges` branch (no Codecov); `docs-portable-install` CI regression guard. Docs now ship inside the wheel so `quicksight-gen docs apply` works from a plain PyPI install.
- **v8.3.x → v8.6.x cumulative** — Post-Phase-V/W bug sweeps + small features (no phase number — graduated as a sustained release stream): independent-system bug sweep + per-prefix cleanup isolation (v8.4.0); plain-English column headers + BarChart axis labels (v8.5.0 / v8.5.5 / v8.6.1); cross-sheet drill date widening (v8.5.7); L2FT metadata cascade write-back (v8.6.5); Oracle 19c JSON_VALUE functional-index skip (v8.6.6); L2FT Transfer Templates SingleLegRail plants + dropdown perf indexes (v8.6.7 / v8.6.8); rich-text card padding 12px (v8.6.9); L2 theme CSS injection on docs site + relative logo/favicon paths (v8.6.10); `tagging_enabled` config override for IAM-restricted environments (v8.6.11); coverage uplift to ~82% via 33 new unit tests (v8.6.12); `json clean --all` purge mode for full-deploy teardown (v8.6.13).

_Phase S / T / U / V / W sub-task detail removed during post-phase cleanup. RELEASE_NOTES `v8.{1,2,3,4,5,6}.x` carry the per-phase + per-release narratives._

---

## Phase X — e2e testing expansion + cloud CI cost optimization

### Parallelism map

Phase X has tracks that genuinely don't touch each other. Identifying them up front so they can be farmed to isolated agent worktrees (or to humans on side branches) without redundant merge pain.

**Hard sequential — finish before fanning out:**
- **X.2.a → X.2.b → X.2.f** — architecture cleanup → REST surface → real data fetcher. These set the foundation; every parallel track that touches App 2 depends on them. Lock these first, on a single branch, with a single owner.
- **X.2.h, X.2.j, X.2.k** — depend on the body of X.2 being done.

**Parallel candidates, in descending isolation:**

1. **X.3 (SQLite dialect) — strongest parallel candidate.** Touches `common/sql/`, `common/l2/schema.py`, new locked seed file. Zero overlap with X.2 HTMX work. Can run in a worktree-isolated agent end-to-end through X.3.f. Skip X.3.g (CI cells) until X.2.h lands so the matrix cells line up. **Integration risk:** the locked seed file at `tests/data/_locked_seeds/spec_example.sqlite.sql` will need a re-lock if the seed pipeline shifts during X.2 — small risk, easy to detect via `data lock --check`.

2. **X.2.g per-app builds** (Executives / Investigation / L2FT / L1) once X.2.c (renderers) + X.2.f (data fetcher) land. Each app is its own `apps/<app>/` world. **Integration risk:** the shared renderer (`common/html/render.py`) is the contention surface — one app discovers it needs a feature the others haven't asked for, the renderer changes mid-flight. **Mitigation:** sequence X.2.c fully BEFORE fanning the app builds, AND have a single owner gate-keep `render.py` edits during X.2.g. App tracks PR their `apps/<app>/` changes; renderer changes go through the gate-keeper.

3. **X.2.c per-visual renderers** (KPI / Table / BarChart / LineChart). Each is one `case` arm in bootstrap + one JS function. All touch one file (`render.py`) so either sequence them or have agents open per-renderer PRs the human merges in order. The merge cost per renderer is small — sequencing is probably easier than coordinating.

4. **X.5 backend** (`etl.yaml` schema + loader + `--end-date` CLI) can start parallel to X.4 — the UI piece blocks on App 1 existing but the data plumbing doesn't.

**Integration pain mitigations (because integration IS always pain):**
- **Frequent merges back to the integration branch (X.2 main-line) — don't let parallel tracks drift for more than 2-3 days.** Long-lived branches accumulate conflict surface logarithmically.
- **Lock the foundation before fanning.** X.2.a/b/f decisions made AFTER agents fan out cost N times the cost made before.
- **Per-track CI** that runs against the integration branch's tip, not just the track branch — catches "my track passed in isolation but doesn't compose."
- **Single owner per shared file.** `render.py` and the data fetcher are the contention hotspots; everything else is per-app or per-dialect and naturally parallel.
- **Track owners write a brief integration note when their work lands.** "I added the Table renderer's `?sort_column` query param — the Sheet builder downstream needs to wire it" type thing. Captures the cross-track ripple before it bites.

### X.1 — e2e fixes + auto-screenshot foundation

Wedge: land auto-failure-screenshot first so all subsequent browser-test investigations get visual evidence. Then use that evidence to root-cause the two known browser-leg defects (L2FT cascade test reading 0 rows; Sasquatch L1 render flake). Then sweep the layered (query+render) pattern across the rest of the suite. Pre-warm Rails is queued behind these; it may become unnecessary depending on what X.1.b finds.

- [x] **X.1.a — Auto-failure-screenshot hook in `webkit_page`.** Shipped v8.6.14 + extended with JS console capture (X.1.a v2) and network response trace (X.1.a v3) during the X.1.b investigation. Every browser test failure now produces `_failures/<test_id>.{png,_console.txt,_qs_errors.txt,_network.txt}` in the GHA artifact.

- [x] **X.1.b — Diagnose L2FT cascade test.** Diagnostic phase complete. Findings:
  - Console + network capture (X.1.a v3) identified `[pageerror] Sample values not found` paired with 4 specific 404 URLs. 3 of the 4 are `tenK-sample-values-V2` calls for the Rail / Status / Bundle CategoryFilter dropdowns; the 4th is `GetThemeForDashboard` (theme).
  - Replacing the Metadata Value `LinkedValues` ParameterDropdown with a `ParameterTextField` shipped (added the new tree primitive). Eliminated 1 of 4 fetches. Visual still empty.
  - Tried statically encoding the 3 CategoryFilter dropdowns via `add_filter_dropdown(selectable_values=...)`. Reverted — AWS rejects `FilterDropdown + CategoryFilter(FILTER_ALL_VALUES) + StaticValues` with `InvalidParameterValueException: doesn't support SELECT_ITEMS control with the given properties`. Helpers (`declared_rail_names`, `transaction_status_values`, `bundle_status_values`) stayed in `apps/l2_flow_tracing/datasets.py` for the proper restructure (X.1.g).
  - Comment: I want full browser testing on everything which is fine if X.1.d is expanded and X.1.b just covers the filter chaining problem.

- [x] **X.1.f — Theme `GetThemeForDashboard` 404 — root-caused + fixed.** Cause (ii) from the X.1.f hypothesis list was right: the L1 Dashboard + L2 Flow Tracing per-app generators in `cli/_app_builders.py` called `build_theme(cfg, ...)` BEFORE stamping `cfg.l2_instance_prefix`, while their `build_*_app(cfg, l2_instance=...)` calls stamp the prefix internally. Result: `theme.json` shipped with id `<resource_prefix>-theme` but every dashboard's `ThemeArn` referenced `<resource_prefix>-<l2>-theme` — a dangling binding. QS's embed session called `GetThemeForDashboard` looking for a theme that didn't exist → 404. Investigation + Executives generators stamped the prefix correctly, so their dashboards had matching themes; the L1 / L2FT mismatch is what produced the 404s X.1.b's diagnostic bundle captured. Fix: stamp the prefix in both broken generators before `build_theme`, plus add a structural guard in `build_theme` that raises if `cfg.l2_instance_prefix is None` so a future per-app generator can't silently regress to the same shape. Verified locally: `describe-theme` resolves cleanly post-deploy + Rails browser e2e passes (deployed dashboard now sees its theme).

- [x] **X.1.g — Convert L2FT CategoryFilter dropdowns to `ParameterDropdown(StaticValues)` shape.** All 7 dropdowns migrated:
  - **Rails**: rail_name / status / bundle_status.
  - **Chains**: parent_chain_name / completion_status.
  - **Templates**: template_name / completion_status (with `cross_dataset="ALL_DATASETS"` to keep tt-instances + tt-legs in lockstep).

  Each control: a multi-valued `StringParam` defaulting to all declared values + `add_parameter_dropdown(StaticValues, MULTI_SELECT)` + `CategoryFilter.with_parameter` (analysis-side filter). Centralized in `_populate_param_filter_dropdown` helper at `apps/l2_flow_tracing/app.py`. New datasets helpers: `chain_completion_status_values`, `tt_completion_status_values`. Per-dropdown browser e2e tests added: `test_l2ft_{rails,chains,templates}_dropdowns.py` (7 tests, walks every advertised option, asserts table doesn't go empty — guards both X.1.g param-bound CategoryFilter narrowing and the broader "advertised dropdown value with no seed data" bug class). Local e2e against deployed sasquatch_pr passed 5 / 7; 2 failures are pre-existing data-coverage gaps, queued as X.1.i (Status `Failed` + open-set enum) and X.1.j (Chain validator: reject zero-Required-children). Cascade test re-skipped, queued as X.1.g.11 follow-up — Metadata Value is now a text field, the original cascade-source regression class is structurally unreachable on that shape. Index cleanup (v8.6.8 template_name + transfer_parent_id) deferred until X.1.i / X.1.j land.

- [x] **X.1.i — Status enum: open-set + plant Failed transactions.** L2FT Rails Status dropdown's `Failed` enum value replaced with `Other` sentinel (covering every status outside `Pending`/`Posted` per the open-set schema). L2FT Rails dataset SQL gained `CASE WHEN status IN ('Pending','Posted') THEN status ELSE 'Other' END AS status` projection. New `FailedTransactionPlant` dataclass + `_emit_failed_transaction_rows` emitter; auto-scenario plants one Failed leg per scenario via the existing `pending_rail` pick. Bug caught + fixed during local verify: `densify_scenario` / `boost_inv_fanout_plants` / `add_broken_rail_plants` were dropping the new field on reconstruct — patched all 3 to pass it through. Re-hash-locked sasquatch_pr + spec_example seed SHA256s; bundled `_l2_fixtures/` + `_default_l2.yaml` synced. Verified locally: deployed Status dropdown e2e passes against sasquatch_pr (1 Failed seed leg confirmed in DB; QS narrowing through the projected `Other` value works).

- [x] **X.1.j — L2 validator: reject Chain with zero Required children AND zero XOR groups.** Added validator rule C5 at `common/l2/validate.py::_check_chain_parent_has_required_or_xor`: every chain parent MUST have at least one `required=True` child OR at least one `xor_group`-tagged child. Dropped the `'No Required Children'` enum value from `chain_completion_status_values()` and the matching CASE branch from the chain-instances dataset SQL. Updated the fuzzer (`tests/l2/fuzz.py::_build_chains`) to pin the first plain entry per parent to `required=True` so generated instances satisfy C5. Verified locally: deployed Chains Completion e2e passes against sasquatch_pr (only `Completed` / `Incomplete` advertised, both have data).

- [x] **X.1.h.A — Build out real ETL example patterns (the etl.md hallucination fix).** Pre-X.1.h `apps/investigation/etl_examples.py::generate_etl_examples_sql()` returned a single placeholder line claiming the patterns lived in deleted-since-M.4 `payment_recon/etl_examples.py` + `account_recon/etl_examples.py` files. New `common/etl_examples.py` ships 10 canonical INSERT-pattern blocks against the v6 schema covering: single-leg Posted, two-leg paired, force-posted (`origin='ExternalForcePosted'`), Pending → Posted lifecycle, TechnicalCorrection rewrite, bundled transfer (`bundle_id`), chained transfer (`transfer_parent_id`), daily balance row, daily balance with limits JSON, and metadata extension. Each block carries `-- WHY:` + `-- Consumed by:` headers per the handbook contract. Output is deterministic (no random IDs, no clock-dependent timestamps). 11 unit tests guard the structural contract.

- [x] **X.1.h.B — CLI cross-reference checker (Track B v0).** Custom pytest test at `tests/unit/test_docs_cli_invocations.py` walks `src/quicksight_gen/docs/**/*.md`, extracts every `quicksight-gen <cmd> <flags>` invocation from fenced bash blocks, and asserts the subcommand chain + every flag exist in the live Click tree. Catches the "doc cites a removed CLI verb / renamed flag" hallucination class. Handles bash line continuations, env-var prefixes (`QS_GEN_E2E=1 quicksight-gen ...`), positional args (`audit verify report.pdf -c config.yaml`), and rejects false positives like `pip install quicksight-gen`. 12 docs cited the CLI; all 12 pass against the v8.6.18 surface. Backlog items split out: X.1.h.B.2 (Python doctest infra w/ fixtures), X.1.h.B.3 (SQL block execution), X.1.h.B.4 (schema column cross-reference, e.g. `transaction_id` → `id` drift in populate-transactions walkthrough).

- [x] **X.1.k — Unify the seed-hash lock surface; key the CLI off config.yaml, not the L2.** Shape (c2) shipped: `data hash` renamed to `data lock`, takes `-c config.yaml --l2 path/to/L2.yaml`, derives dialect from `demo_database_url`, and writes the locked SQL to `tests/data/_locked_seeds/<instance>.<dialect>.sql`. `--check` re-emits and shows a unified diff on drift (50-line cap). `emit_full_seed` stamps a `-- SHA256: <hex>` header on every emit so saved-to-disk SQL self-identifies. Deleted: the four old hash sites — YAML `seed_hash:` blocks (6 files), `seed_hash` field on `L2Instance`, `_load_seed_hash` loader, two `test_full_seed_hash_lock_*` Python constants, `_BROAD_MODE_HASHES` dict + `test_broad_mode_hashes_match_lock` parametrize. New auto-discovering test at `tests/data/test_locked_seeds.py` parameterizes over `_locked_seeds/*.sql`. Locked spec_example at both dialects (~1.5MB each); skipped sasquatch_pr (would have been 55MB+ each, way too much repo bloat — spec_example is the canonical contract and catches every change to the seed pipeline; sasquatch_pr is a flavor instance whose L2-shape changes are obvious in the YAML diff at PR review time). 1706 unit tests passing. Today the hash lock has four distinct sites and one CLI that updates one of them: (i) `seed_hash.postgres` in each L2 YAML, (ii) `seed_hash.oracle` in each L2 YAML, (iii) the Python full-seed hash constants in `tests/data/test_l2_baseline_seed.py`, (iv) `_BROAD_MODE_HASHES` in `tests/data/test_l2_seed_contract.py`. `quicksight-gen data hash --lock` only writes (i) because dialect was hardcoded to postgres. A partial re-lock during X.1.i ⇒ CI red on every commit between 2026-05-04 21:58 and the fix-CI commit (`ff40c8e` … `3b113ed`); the docs-templating commit `ff40c8e` added 3 unrelated failures on top, masking that the lock surface was the real problem.

  **Reshape:** the hash is a function of `(L2 instance, dialect)`. Dialect lives on `config.yaml` (via `demo_database_url`). So the CLI should take `-c config.yaml` instead of an L2 path, derive the dialect, and write to the matching L2 slot — config.yaml is what knows the deploy target, the L2 just carries the scenario. New invocation: `quicksight-gen data hash --lock -c config.postgres.yaml` and `... -c config.oracle.yaml` together cover both dialects for the L2 referenced by each config.

  **Storage shape (decided):** the hash IS the seed's identity, not an input — so it doesn't belong in the L2 YAML or in Python constants. **Lock the emitted SQL itself.** Per `(instance, dialect)` write the canonical-anchor seed to `tests/data/_locked_seeds/<instance>.<dialect>.sql` (checked into git). Test recomputes SHA256 of a fresh emit and compares against SHA256 of the locked file; failure surfaces the SQL diff so reviewers see what shifted, not "trust me, the new hash is correct." `emit_seed` also stamps `-- SHA256: <hash>` as a header comment (computed against the post-strip body) for human-readability of one-off SQL outputs. Sites (i)-(iv) (postgres / oracle YAML `seed_hash:` blocks, `test_l2_baseline_seed.py` Python constants, `_BROAD_MODE_HASHES` dict) all get deleted. Test parametrizes over auto-discovered `_locked_seeds/*.sql` so adding a new (instance, dialect) pair = drop a file, no Python constant to maintain. CLI rename: `data hash` → `data lock` (with `--lock` / `--check` flags) — what we're doing is byte-snapshotting the SQL, not just hashing it.

  **CLI shape:** `quicksight-gen data lock -c config.postgres.yaml` writes / refreshes the locked SQL for the L2 referenced by config (dialect derived from `demo_database_url`). `quicksight-gen data lock --check -c config.postgres.yaml` exits non-zero if the fresh emit doesn't match the locked file. Run once per (config.postgres.yaml, config.oracle.yaml) covers both dialects. Reviewer cost: bigger PR diffs (~hundreds of KB of SQL per shift) — which is a feature for a hash-lock that exists specifically to catch unreviewed seed drift. Seed SQL compresses well in git.

  **Architectural smell (deferred):** the per-artifact emit / lock surface (schema | data | json | docs | audit each independently emittable, each with their own contracts + locks) is what made today's partial re-lock possible. A unified "deploy bundle" view — emit all five together, hash all five together, lock the bundle — would collapse the failure mode. Counter-argument: independence is what gives `json apply` (re-deploy dashboard without re-running schema) its ergonomic appeal; a unified bundle would force every change through the slowest emitter. **Hold for now.** If a second bug of this class shows up (one artifact's hash drifts while siblings stay locked, or cross-artifact id consistency breaks again like the X.1.f theme binding), revisit as Phase Y or similar.

  **Gate:** add `data lock --check` as a CI step before the unit suite, run once per `(config.postgres.yaml, config.oracle.yaml)`. Failure shows the SQL diff inline so reviewers don't have to chase down which of 7+ scattered hash-mismatch assertions is the real signal.

  Must land before X.1.c / X.1.d so a future seed-shape change in either phase doesn't repeat the partial-lock incident.

- [x] **X.1.c — Sasquatch L1 dashboard 'flake' was table virtualization.** Diagnosed + fixed in `09e8f16`. NOT a flake — deterministic data-density-dependent assertion bug. The harness's `_active_sheet_text` reads `inner_text()` of every visual on the active sheet to check whether a planted account_id surfaces; QS tables virtualize at ~10 DOM rows so `inner_text()` only sees the rendered window. Sasquatch_pr's denser seed pushed `cust-0001-snb` below the visible 10 — spec_example's sparser seed kept it visible. The candidate fix list (widen wait / screenshot diff / tighter days_ago plant) was off-target; the real root cause was the assertion mechanism, not timing or sheet state. Fix: new `expand_all_tables_on_sheet` helper bumps every paged table to page-size 10000 before reading text. Verified live `2026-05-04`: `./run_e2e.sh --harness` passes 15/15 across all 3 L2 instances in 13:47, including the previously-flaking sasquatch_pr Limit Breach assertion.

- [x] **X.1.d — Layered (query+render) pattern: helpers shipped, sweep deferred.** X.1.d.1 lifted the harness's `assert_l1_matview_rows_present` pattern into a reusable module at `tests/e2e/_layer1_query.py` (`query_matview_rows`, `matview_row_count`, `assert_matview_has_row`, `assert_account_in_matview` — all dialect-aware, 14 unit tests). The originally-planned `.d.2/.d.3` sweep across active browser tests (`test_l1_filters`, `test_l1_sheet_visuals`, `test_inv_filters`, `test_inv_sheet_visuals`, `test_exec_sheet_visuals`) was assessed as low value: those tests assert *structural* claims (visuals present, filter narrows row count, dropdown opens) rather than *specific row presence*, so Layer 1's "is the row in the matview" check doesn't have a natural site to slot in. The active row-presence tests that would benefit (`test_inv_drilldown.py`, `test_l2ft_metadata_cascade.py`) are both `@pytest.mark.skip`'d (dependent on URL-hash anchor pre-seed and on a removed dropdown, respectively). The harness — already layered — remains the canonical row-presence test. **Going forward**: new e2e tests that assert specific row presence should call into `_layer1_query.py` to gate the Layer 2 render assertion.

- [ ] **X.1.e — Pre-warm Rails sheet (perf hardening — reassess after X.1.b).** Originally proposed as a fix for the L2FT cascade test failure, but X.1.b's investigation will reveal whether the actual root cause was perf-related. If X.1.b resolves the failure without needing pre-warm, this item drops from scope. If perf was a contributing factor: visit Rails once during dashboard warm-up, navigate away, then re-enter for the actual assertion (cache is hot the second time).

### X.2 — App 2: self-hosted dashboard renderer (QuickSight as one dialect, not THE frontend)

**Reframe.** Same way `Dialect.POSTGRES`/`ORACLE` are SQL dialects projected from one schema model, the dashboard tree (`common/tree/`) becomes a **dashboard model** with three renderers: QuickSight JSON (today), audit PDF (today, proves the pattern works), and a self-hosted HTML renderer (this phase). The tree's job description sharpens — it IS the dashboard, not a description of QS dashboards.

**Stack** (all proven via spike): Python backend (Starlette) + HTMX + Tailwind v4 (via `pytailwindcss` standalone CLI) + d3 from CDN. No bundler, no TS, no npm. Read-only surface, no auth in X.2 (auth deferred to backlog).

**Browser support.** Modern evergreens only — Chrome / Edge / Firefox / Safari, current minus 2 versions. No IE, no legacy mobile webview support. The HTMX + d3 + Tailwind v4 stack we're building on doesn't push beyond standard ES6 / modern SVG / CSS variables, so this is essentially "browsers people actually use today" — no exotic features being demanded.

**Why it's worth building (not just an annoyed diversion):**
- **RESTful filtering as the killer USP.** Every (sheet, filter-set) is a bookmarkable URL — "click this URL to see what I saw on May 5 at 3pm." QS embed URLs are signed, single-use, don't carry filter state. Pairs with the audit PDF — both become reproducible-view artifacts.
- **Server-side cache keys are trivial.** `(sheet, filter-set)` → SQL result. URL == cache key. SPICE-equivalent for the Python backend, with edge / browser caching free via Cache-Control.
- **Bypasses the QS quirks log entirely.** URL parameter sync, dropdown click-target weirdness, spinner-forever footgun, render flakes — none of these surface for the HTMX dialect.
- **No QS per-user license fees.** Real cost story for customers.
- **Same e2e harness gates both dialects.** Layer 1 (matview check) is renderer-agnostic; Layer 2 swaps the driver. Bug-parity falls out of the harness shape already in place.

**Hard parts (still real, but smaller post-spike):**
- **Sheet count + visual richness.** L1 dashboard alone is 11 sheets × 5–15 visuals each. Each app's surface needs the full visual catalog wired (KPI / Table / BarChart / LineChart / Sankey + filters).
- **Embedding context.** QS dashboards embed via signed URLs; HTMX dialect needs its own iframe story when phase.2 lands. Out of scope here.
- **Maintenance.** Two render implementations of the same surface — Layer 2 e2e parity (the dialect-comparison gate) is what keeps them honest.

**Spike sequence — proven on `phase-x-2-htmx-renderer` branch (Apr-May 2026):**

- [x] **X.2.spike.1** — `emit_html(app, sheet)` projects tree → HTML. Pure projection, takes in-memory tree node, never touches disk. spike.1.fix promoted `App._resolve_auto_ids` to public + threaded App through `emit_html` so `data-visual-id` attributes resolve before render.
- [x] **X.2.spike.2** — Starlette server + HTMX swap + d3 hydration on swapped fragments. Pluggable `DataFetcher` callable so spike tests stay DB-free. `python -m quicksight_gen.common.html` is the smoke runner.
- [x] **X.2.spike.3** — Layer 2 (Playwright + WebKit) against the local Starlette server. Positive case asserts SVG rect/path counts match Layer 1 fetcher's promise; negative case (route-intercepted empty body) catches the regression class. Same shape as the QS harness's Layer 2, different selectors. Dialect-comparison thesis proven.
- [x] **X.2 capability experiments** — clickable Sankey via d3 + `htmx.ajax` (anchor click pattern), dev-log + HTMX event forwarder, Tailwind v4 CSS via pytailwindcss CLI (styles HTML + d3 SVG), ForceGraph visual via d3-force (proves new visual kinds add as one `case` arm + one render function).

Architecture decisions captured in `docs/x_2_design_thoughts.md` (App 1 vs App 2 split, all-GET REST surface, plural nouns + nested resource paths, query-param state, cascade-rename via `HX-Trigger`, force-directed as shared primitive across three surfaces).

**Test matrix expansion (X.2 + X.3 woven together).** As renderers + DB backends multiply, the regression surface multiplies. The matrix that needs to stay green to call this "parity":

|                 | Postgres | Oracle | SQLite (X.3) |
|-----------------|----------|--------|--------------|
| **Layer 1** (matview check, renderer-agnostic — `_layer1_query.py`) | ✓ today | ✓ today | ✓ X.3.g.1 |
| **L2 QS JSON dialect** | ✓ today (`e2e-pg-api/browser`) | ✓ today (`e2e-oracle-api`) | N/A — QS doesn't read SQLite |
| **L2 Audit PDF dialect** | ✓ today (`tests/audit/`) | ✓ today | ✓ X.3.g.2 |
| **L2 HTMX dialect (this phase)** | X.2.h | X.2.h | X.3.g |
| **Cross-tool agreement** (U.8.b becomes 4-way: `expected == PDF == QS == HTMX`) | X.2.j | X.2.j | X.3.g |

Eight cells worth gating on (3 renderers × 3 DBs minus QS-on-SQLite). Five exist today; X.2.h adds two (HTMX × {PG, Oracle}); X.3.g adds three (Layer 1 + Audit PDF + HTMX, all on SQLite); X.2.j wraps it with the 4-way agreement contract that catches "all the renderers passed but they disagreed." Cost shape (which cells need AWS, which run on Docker / locally) lives in X.7.

**Parity-as-much-as-possible principle.** Some cells are physically unsupportable — QS×SQLite is the obvious one (QuickSight can't read SQLite; no native datasource type for it). Future combos may surface similar gaps (e.g. a renderer that doesn't support a particular visual kind, a DB feature missing on one dialect). When that happens: mark the cell N/A in the matrix with a one-line reason, run every other combination in the row + column. The principle is parity wherever the combination is technically possible — not parity everywhere, regardless of physics.

**Theme parity — its own 4-way test.** Distinct concern from data parity. The L2 YAML's `theme:` block (`common/l2/theme.py::ThemePreset`) is the single source of truth for accent / primary_fg / link_tint / etc. Today QS dashboards, audit PDF, and the docs site all consume it (Phase U.1 cover, v8.6.10 docs CSS injection). App 2 (X.2) currently uses hardcoded Tailwind classes — needs to consume the same theme. Once it does, the four visual surfaces below should render the same accent for the same theme spec:

| Surface | Status |
|---------|--------|
| QS dashboard | ✓ today (`build_theme(cfg, theme)` → QS Theme JSON) |
| Audit PDF | ✓ today (Phase U.1 cover) |
| Docs site | ✓ today (v8.6.10 CSS injection) |
| App 2 HTMX | X.2.l |

Test: change one accent value in the L2 YAML, all four surfaces shift in lockstep. Becomes a release-gate visual smoke (one screenshot per surface; pixel-diff or eyeball — eyeball acceptable since the gate is "do they all match the spec," not "are they pixel-identical").

**Scope to feature parity + e2e tests passing:**

- [ ] **X.2.a — Architecture cleanup post-spike.** Promote the experimental code into the phase shape:
  - Lift `tests/spike/test_html_layer2.py` → `tests/e2e/_harness_html2.py` (Layer 2 against HTMX dialect, fixture pattern matches the QS harness).
  - Replace the smoke runner's stub fetcher with a real DB-backed `DataFetcher` factory; keep the stub callable for unit tests.
  - Move the `python -m quicksight_gen.common.html` entry point under a Click subcommand (`quicksight-gen serve` group: `app2 apply`, mirroring the existing `schema | data | json | docs | audit` shape).
  - Branch consolidation pass — squash / rebase the spike experiments before further work, get the diff reviewable.
  - **JS tooling — same standalone-binary pattern as pytailwindcss:** add `biome` (Rust binary, single executable — replaces eslint + prettier + a minifier) via `biome-py` on PyPI or a 30-line downloader if no current wrapper. Adds to `[dev]` extras. CI step: `biome lint` + `biome check --write` (format) + `biome --apply --write --max-diagnostics=0` to gate merges. Minify in the build pipeline alongside the tailwindcss build. **JS unit tests:** extend the existing Playwright pattern — small HTML fixtures under `tests/js/` load the function under test in a real browser context, exercise it, write results to `window.__test_result` for Playwright to assert on. No jest / vitest / Node dependency. Trade-off: each test costs ~50ms Playwright launch overhead; we accept that because the JS is DOM/HTMX-coupled enough that real-browser tests catch the right class of bugs.

- [ ] **X.2.b — All-GET REST surface per design doc.** Drop `POST /visual/{id}/data`. New routes (plural nouns, nested resources):
  - `GET /` — landing page, lists `/dashboards`.
  - `GET /dashboards` — fixed list of the four apps for the served L2 instance (L1 Dashboard + L2 Flow Tracing + Investigation + Executives). One App 2 process serves one L2 instance — multi-tenancy is the auth use case (deferred to phase.2).
  - `GET /dashboards/:id` — redirect to first sheet OR render dashboard chrome + first sheet inline.
  - `GET /dashboards/:id/sheets/:id` — full sheet content.
  - `GET /dashboards/:id/sheets/:id/visuals/:id` — single visual (with chart data inline). Filter values via query string.
  - Bookmarkability: `hx-push-url="true"` on every swap so URL stays in sync with filter / sort / page state. Browser back/forward replays.
  - Cache-Control headers on data routes (Money Trail data scoped to a date range becomes cacheable per-tuple).
  - `POST /log` (dev-only) is the only POST that survives — for the dev-log forwarder.

- [ ] **X.2.c — d3 renderers for the remaining visual primitives.** Currently Sankey + ForceGraph proven. Add one `case` arm + one `renderXxx` JS function each:
  - **KPI** — a single number + delta arrow. ~10 lines of d3 (mostly text styling).
  - **Table** — `<table>` with sortable headers (each `<th>` is a link to `?sort_column=col:desc`). Pagination via `?page_offset=N&page_size=50`. Tailwind classes for striped rows + sticky header.
    - Comment: for the pagination, a big win over quicksight would be to give a total row count with the pagination (prefer 0-50 of x over page 1 of xx). this will make the e2e testing way easier
  - **BarChart** — d3 bars with axis labels (the plain-English ones added in v8.5.5 carry over via the tree).
  - **LineChart** — d3 line + axes + legend.
  - Per-renderer Layer 2 unit test: stub fetcher, assert SVG / DOM shape matches Layer 1 promise (rect counts, path counts, etc.).

- [x] **X.2.d — Filter primitives wired.** Currently date range form. Add:
  - **ParameterDropdown** — `<select>` driven by L2 / dataset values; change fires a swap with the new value in `?param_<name>=...`.
  - **CategoryFilter** — multi-select check group; change fires `?filter_<col>=v1,v2,v3`.
  - **NumericRange** — two inputs or slider; fires `?min_<col>=N&max_<col>=M`.
  - All values flow into URL query params per X.2.b. No form state.
  - **Status:** `ParameterDropdownSpec` / `CategoryFilterSpec` / `NumericRangeSpec` frozen dataclasses in `common/html/render.py`, exported from `common/html/__init__.py`. `emit_html(..., filter_specs=[...])` threads them into the form. Server-side: existing `visual_data` route already passes all query params through to the data fetcher, so no server changes needed (X.2.f's real fetcher consumes the prefix-keyed dict). CategoryFilter's checkboxes are unnamed — a hidden `filter_<col>` input is what HTMX serializes; `wireCategoryFilters` (bootstrap.js) keeps it in sync as a comma-joined string. 23 tests: 16 in `tests/unit/test_html_filter_primitives.py` (render + TestClient round-trip) + 7 in `tests/js/test_filter_primitives.py` (CategoryFilter checkbox sync).

- [x] **X.2.e — Sheet structure + cross-sheet + cross-app navigation.** Sheet tabs across the top of `/dashboards/:id`. Click switches sheet via `hx-get` + `hx-push-url`. Cross-sheet drills (currently QS `CustomActionURLOperation`) become plain `<a href>`s in the rendered HTML — `/dashboards/:id/sheets/:other-sheet?param_account_id=...`. URL-as-state means the QS URL-param-doesn't-sync-controls quirk doesn't surface.
  - **Cross-app drills (QS-blocked, App 2 unblocks).** L1 → Investigation drills are dropped under QS (`project_qs_url_parameter_no_control_sync` memory: K.4.7 — URL fragment sets parameter but doesn't push values into bound controls). App 2's URL-as-state model makes this trivial — `<a href="/dashboards/investigation/sheets/money-trail?param_anchor=...">` works. Wire the previously-deferred K.4.7 cross-app drills back in for the App 2 dialect; QS dialect stays as-is. Drill destinations need to know the OTHER app's URL pattern — resolve at render time via a small `app_for(visual)` helper that maps tree references to dashboard ids.
  - **X.2.g.1.c — Visual-level aggregation wrapping.** ✓ — App2's tree fetcher now wraps the dataset SQL with the visual's declared aggregation before executing. Without this, KPI(values=[count(...)]) rendered one card PER DATASET ROW (the screenshot bug). New `common/html/_visual_sql.py::wrap_for_visual(base_sql, visual)` produces: KPI → `SELECT <agg(col)> FROM (base_sql) sub`; BarChart → `SELECT <cat>, <agg(val)> FROM (base_sql) sub GROUP BY <cat>`; LineChart → same + `ORDER BY <cat>`; Table / Sankey / ForceGraph → unwrapped pass-through. Maps Measure.kind ("sum"/"max"/"min"/"average"/"count"/"distinct_count") to the SQL aggregation function. Dialect-portable (SQL-92 aggregations every dialect supports). Tree fetcher unit tests updated to use row-grain dataset SQL.
  - **X.2.g.1.b — Cross-dialect filter wiring via SQL templating.** ✓ — `build_dataset(..., app2_sql=...)` accepts an optional App2 SQL variant; QS SQL stays as-is for `CustomSql.SqlQuery`. Each app's dataset author writes ONE SQL template with a `{date_filter}` slot — QS gets `""` (its filter comes from the analysis-level FilterGroup), App2 gets the bind-clause snippet from `common/sql/app2_filters.app2_date_filter("col_name")`. The snippet uses `NULLIF(:date_from, '') IS NULL OR ...` to handle PG's `''` vs Oracle's `''`-as-NULL vs SQLite's `''`-as-text. Executives' two datasets (`exec_transaction_summary`, `exec_account_summary`) ported to the templating pattern — no SQL duplication. The same pattern lifts to L1 / L2FT / Investigation when their X.2.g.{2,3,4} wiring lands. **Architecture note:** this is the latest example of the broader "one logical model, projected per target runtime" pattern: SQL placeholders (PG `%s` / Oracle `:1` / SQLite `?` / App2 `:name` / QS `<<$name>>`), theme tokens (CSS vars vs QS Theme JSON), filter primitives (URL params vs ParameterControl), all converging.
  - **Status:** Sheet tabs + sheet route shipped. `_render_sheet_tabs(dashboard_id, sheets, active_sheet_id)` renders plain `<a href="/dashboards/:d/sheets/:s">` per analysis sheet (active tab carries `bg-accent`); single-sheet analyses get an empty tab strip (suppressed). New `/dashboards/:d/sheets/:s` route in `server.py::sheet_view` resolves any analysis sheet by id (404 themed). `visual_data` route widened to accept any analysis-sheet id, not just the served (default landing) sheet's. Tabs use full-page reload (no HTMX swap) — Tailwind/HTMX/d3 are cached so only chrome reloads, and URL stays trivially bookmarkable. Decision was plain-anchor over hx-get to avoid the fragment-vs-full-page distinction. 13 tests in `tests/unit/test_html_sheet_nav.py`. **Deferred to X.2.e.2:** cross-sheet drill rendering (per-row anchors derived from the tree's `Drill` primitive — needs renderer-per-visual-kind work in bootstrap.js or per-row server fragments) and cross-app drill rendering (the K.4.7 re-enable — needs an `app_for(visual)` resolver that lands when X.2.g wires the 4 apps). The plumbing this phase shipped is what those follow-ons sit on top of.

- [x] **X.2.f — Real data fetcher.** Replace the stub `DataFetcher` callable with an implementation that:
  - Takes the visual's dataset SQL (already in `apps/<app>/datasets.py`).
  - Substitutes filter values into the SQL (parameterized — never string-format user input).
  - Executes against the configured DB (Postgres / Oracle, dispatched via `Dialect`).
  - Returns rows shaped per visual kind (Table → list of dicts, Sankey → nodes/links, KPI → number, etc.).
  - Per-visual `data_shape()` helper on each tree primitive that documents the JSON shape its d3 renderer expects.
  - **Status:** Infrastructure shipped. `common/html/_sql_executor.py` provides `execute_visual_sql(connection_factory, sql, url_params, dialect)` — accepts `:name`-style placeholders in dataset SQL, rewrites to `%(name)s` for Postgres (Oracle/SQLite stay native), collects bind params from the URL-keyed filter dict, executes against any DB-API 2.0 connection, returns `(rows, columns)`. Unreferenced URL params are silently dropped (forms serialize every input on every Refresh; per-visual SQL only references the filters it cares about). `common/html/_data_shape.py` provides `shape_kpi` / `shape_table` / `shape_bar_chart` / `shape_line_chart` / `shape_sankey` adapters matching the bootstrap.js `renderXxx` JSON contracts exactly, plus `shape_for_kind(kind, rows, columns, **opts)` dispatcher keyed off `type(visual).__name__`. ForceGraph stays projector-driven (no SQL). Wiring into `make_db_fetcher` per app lands when X.2.g builds out the 4 apps — X.2.f is the renderer-agnostic infrastructure those per-app fetchers consume. 31 tests in `tests/unit/test_html_data_shape.py` (17) + `tests/unit/test_html_sql_executor.py` (14); end-to-end SQLite round-trip in the executor tests.

- [ ] **X.2.g — Build out the 4 apps.** Order by dependency / risk, lightest first to surface architecture gaps early:
  - **X.2.g.0 — Generic per-tree DataFetcher factory** ✓ — `make_tree_db_fetcher(tree_app, cfg)` walks visuals, looks up dataset SQL via the new `get_sql(visual_identifier)` registry (populated by `build_dataset` → `register_sql`), executes via X.2.f's `_sql_executor`, shapes via X.2.f's `_data_shape`. 10 tests in `tests/unit/test_html_tree_fetcher.py` (build-time visual indexing + dispatch + filter substitution + multi-sheet + loud-fail on missing SQL).
  - **X.2.g.1 — Executives** (5 sheets: Getting Started + Account Coverage + Transaction Volume + Money Moved + Info; KPI + Table + BarChart visuals). ✓ — `quicksight-gen serve app2 apply --app executives` builds the real Executives tree, calls `build_all_datasets(cfg)` to populate the SQL registry, constructs the fetcher via `make_tree_db_fetcher`, and serves through the existing App2 server. 4 wiring tests in `tests/unit/test_html_executives_wiring.py` pin sheet count + assert the build-time SQL lookup succeeds for every visual. Operator verifies end-to-end against live PG before X.2.g.2 lands. Fastest end-to-end signal that the renderer carries a real app.
  - **X.2.g.2 — Investigation** (5 sheets: Money Trail Sankey already proven via spike, Account Network already a force-directed shape).
  - **X.2.g.3 — L2 Flow Tracing** (4 sheets: Rails, Chains, Templates, Hygiene — heavy on ParameterDropdown + cascading filters from X.1.g).
  - **X.2.g.4 — L1 Dashboard** (11 sheets — biggest surface, do last when the renderer is settled).
  - Per-app smoke: render every sheet, walk every drill, exercise every filter.
  - **Discipline (pre-X.6 docs work):** every new sheet's `description` and every visual's `subtitle` is the single source of truth for the corresponding docs page. CLAUDE.md already mandates "every visual has a subtitle" — extend the rule to "the subtitle IS the docs source of truth." X.6 will read these strings to auto-generate walkthroughs; if they live in tree primitives now, X.6 has nothing to refactor.
  - **App Info sheet parity.** Every QS app has the `Info` canary as its last sheet (CLAUDE.md mandate; renders App version + per-matview latest_date + deploy stamp). App 2 serves the same Info sheet — same diagnostic value when a sheet renders empty: glance at Info first to tell "App 2 is healthy, the data/SQL is the bug" from "App 2 itself broke." Renderer treats the Info text-box visual the same as a KPI card.

- [ ] **X.2.h — Layer 2 e2e against the HTMX dialect.** Lift `tests/spike/test_html_layer2.py` pattern across the harness. Same fixture shape as the QS harness, same Layer-1 + Layer-2 assertions, different selectors:
  - **Status: X.2.h.1 (Executives stub) + X.2.h.2 (Executives live PG) shipped.** `tests/e2e/test_html2_executives.py` (stub fetcher, 6 tests) covers: sheet tabs render, TextBoxes render, visuals auto-load on DOMContentLoaded, filter changes refetch, filter form suppression on text-box-only sheets. `tests/e2e/test_html2_executives_live.py` (live PG, 2 tests) covers: KPI renders with real data (catches "wrong L2 prefix → relation does not exist"), date filter doesn't error when applied (caught the PG OR-short-circuit gotcha that X.2.g.1.b's COALESCE+sentinel-date pattern fixes). Live-PG variant gates on `QS_GEN_TEST_L2_INSTANCE` env var pointing at the L2 YAML matching the seeded DB; skips cleanly when unset. Shared assertion helpers (`wait_for_kpi_value`, `wait_for_table_rows`, `make_live_db_fetcher_for_app`) extracted into `_harness_html2.py` so X.2.h.{2,3,4} for Investigation / L2FT / L1 ports are mechanical — same shape per app, only sheet IDs / visual IDs / matview names change.
  - Add an `HTMX_HARNESS=1` env gate alongside `QS_GEN_E2E=1`.
  - Re-use `_layer1_query.py` (already renderer-agnostic per X.1.d.1).
  - Mirror every existing browser test (test_l1_*, test_inv_*, test_exec_*, test_l2ft_*) against the HTMX dialect.
  - **Per-DB cells.** HTMX runs against PG + Oracle now (Docker, no AWS — HTMX dialect doesn't need QuickSight). SQLite cell lands when X.3 ships. Two new GHA jobs: `e2e-htmx-pg`, `e2e-htmx-oracle` (+ `e2e-htmx-sqlite` post-X.3).
  - **Performance budget gate.** New `tests/perf/test_swap_latency.py` against the local Starlette server: assert P95 swap latency under a budget (initial: 100ms server-side per swap; revisit after the 4-app surface lands). Pass/fail in CI. Catches the day someone introduces an N+1 query that ruins the iteration UX. The "we run circles around QS" claim becomes measurable, not aspirational.
  - The dialect-comparison gate: same Layer 2 assertion catches a render bug in both dialects = parity proven.
  - [x] **X.2.h.6 — Triage + fix `test_html2_money_trail.py` (LOCKED 2026-05-07).** All 3 tests fixed. **Root cause** (caught immediately once `c.11.app2` traces landed): trace.trace showed `Frame.click("button[hx-get]")` timing out after 30s. The button no longer exists — X.2.g.1.a polish made App2 visuals auto-load on `DOMContentLoaded` via `<div hx-trigger="load, refresh from:body">`. `trigger_initial_swap(page)` (which clicks `button[hx-get]`) was dead code from the pre-X.2.g.1.a era. **Fixes**: (1) drop `trigger_initial_swap` calls; (2) `test_layer2_initial_load_renders_sankey` waits for `networkidle` after goto (HTMX auto-fetch completes during that wait — same pattern test_html2_executives.py already uses); (3) `test_layer2_click_pivots_sankey` wraps `page.goto` in `expect_response` so the auto-fetch is captured; (4) `test_layer2_catches_missing_chart_data_bug` just relies on the route intercept catching the auto-fetch. **Secondary issue** also surfaced: smoke app has TWO visuals (smoke-sankey + smoke-force) that both auto-fetch on load, so `captured_urls[1]` was the ForceGraph fetch, not the Sankey click pivot. Switched the assertion from positional to predicate: filter to `/visuals/smoke-sankey/` URLs + assert at least one carries `anchor=`. **Cleanup**: deleted `trigger_initial_swap` from `_harness_html2.py` (was the only consumer). **Live-verified**: `./run_tests.sh up_to=app2 --variants=local-pg` now green (9/9 app2 + 3 skipped); total wall-clock 50s vs 150s previously. Pyright clean.

- [ ] **X.2.i — Embed MkDocs into App 2.** `Mount("/docs", StaticFiles(directory=mkdocs_build_dir))` on the Starlette app. One process, one cache, one auth front (when phase.2 lands). Per the design thoughts. Removes the "docs live on Pages, dashboards live in QS, audit PDF lives wherever" three-surface story.

- [ ] **X.2.j — Production validation + 4-way cross-tool agreement + backward-compat.** Deploy App 2 against real Aurora across all 4 apps with sasquatch_pr seed. Visual eyeball + Layer 2 e2e green = the "feature parity with QS, minus the bugs" claim verified. Cross-tool agreement extends U.8.b's three-way contract (`expected == PDF == QS dashboard`) to four-way (`expected == PDF == QS dashboard == HTMX dashboard`). Per DB. Becomes a release-gate test in `e2e-against-testpypi`. Catches the "all renderers individually passed but they disagree on a violation row" failure class — the only test that really earns the parity claim.
  - **Backward-compat assertion.** Existing QS deploys keep working through X.2-X.5 ship. Both local CI (`pytest tests/json/`) and `e2e-against-testpypi` already exercise the QS surface — the assertion is "those tests stay green for the entire phase X, no exceptions." If a tree primitive change for App 2 breaks QS emit, that's a regression to be fixed, not a tradeoff to accept.

- [ ] **X.2.l — Theme integration: L2 YAML drives Tailwind rendering.** Currently App 2 hardcodes Tailwind classes (`bg-slate-50`, `fill-blue-500`, etc.) — same source-of-truth violation as hand-written docs. Wire L2 theme through:
  - `common/l2/theme.py::ThemePreset` is already the canonical theme. App 2's page shell injects per-instance CSS variables in `<head>` (`--color-accent: #...; --color-primary-fg: #...; ...`) read from the resolved theme.
  - Tailwind v4's `@theme` block in `input.css` declares semantic color tokens (accent, primary-fg, link-tint, surface, surface-muted) that resolve to the CSS variables. Tailwind generates `bg-accent`, `text-accent`, `fill-accent`, etc. utilities that consume them.
  - Replace hardcoded utility names in `render.py` (`bg-slate-50` → `bg-surface`, `fill-blue-500` → `fill-accent`, etc.) so the Tailwind output is theme-token-driven, not literal-color-driven.
  - d3 SVG class strings in the bootstrap JS get the same treatment — `fill-accent` instead of `fill-blue-500`.
  - **4-way theme parity test** (per the matrix above): single screenshot per surface (QS dashboard, audit PDF, docs site, App 2 HTMX) generated with the same L2 theme spec. Eyeball comparison gate at release. Fast — runs against the existing demo deploy.
  - Honors the silent-fallback contract from N.4.k: when L2 has no `theme:` block, App 2 falls back to a built-in default Tailwind palette, same as QS does CLASSIC.
  - **Accessibility rule (carries across all four surfaces).** Color SHALL NOT be the only indicator for status / category / severity. Use texture / icon / shading / position alongside. If the operator picks a low-contrast theme that's their call (theme is their input), but the visual encodings the renderer chooses must remain interpretable in monochrome. Phase X.2 enforcement is a code-review rule; future phase could add an a11y CI lint.

- [x] **X.2.m — Error handling: 5xx pages + transient toasters.** Today's spike returns raw stack traces on dataset failure / DB down / sheet-not-found. Production needs:
  - **Status:** themed 5xx + 404 pages (`emit_error_page` reuses the X.2.l theme injection so the per-dashboard `ThemePreset` cascades through; `DEFAULT_PRESET` fallback when L2 has no theme) + HTMX toasters via `htmx:responseError` / `htmx:sendError` listeners in `bootstrap.js` (toast at top-right, `bg-danger` / `text-accent-fg` semantic tokens, auto-dismiss 5s, stacks). Production mode hides tracebacks; `dev_log=True` carries them inside `<details>`. Tests in `tests/unit/test_html_error_handling.py` (16) + `tests/js/test_error_toasts.py` (7). Structured server logs + health check endpoint deferred — they land alongside whichever later phase introduces the logging stack.
  - **Full-page errors** (404 sheet not found, 500 dataset SQL crashed, 503 DB unreachable) — render a styled error page that fits the page shell + dev-log + theme. Includes a "report this" surface that captures URL + request-id + timestamp for the operator's logs.
  - **Toasters for transient on-page errors** — a swap returned 5xx but the rest of the page is fine; show a dismissable toast at the top of the viewport ("Couldn't refresh Money Trail — retrying" with a manual retry button). HTMX has `htmx:responseError` → catch in the dev-log forwarder pattern, render the toast via OOB swap.
  - **Structured server logs** — every 5xx writes a structured log line (timestamp / route / SQL / params / exception class + message) so prod operators can grep without guessing. Plain Python `logging` module to stderr; ALB / log aggregator picks it up downstream.
  - **Health check endpoint** — `GET /healthz` returns 200 if the data fetcher can `SELECT 1` against the DB. ALB liveness / k8s readiness consume it.

- [ ] **X.2.k — Release v8.x.0 / v9.0.0.** RELEASE_NOTES, README update reflecting the 4 apps now ship in two dialects, CLAUDE.md updates for the new `serve` CLI group + the architecture, tag + push.

**Deferred to backlog (post-X.2):**
- **Multi-user OIDC auth** (was X.2.phase.2). ALB + IAM Identity Center / Cognito + JWT in `X-Amzn-Oidc-Data` + per-request STS `AssumeRoleWithWebIdentity`. Read-only collapses authorization to a `(identity → [allowed L2 prefixes])` lookup. ~50 lines + ALB config; revisit when there's a customer asking for it.
- **Per-customer caching layer** (CDN / Redis in front of the data fetcher).
- **Embedding context** (X-Frame-Options, signed-URL handoff for iframe contexts).

### X.3 — SQLite as a database dialect (integrator-local persona)

**What.** Add `Dialect.SQLITE` alongside Postgres + Oracle. SQLite is the integrator persona's local-iteration backend per `docs/x_2_design_thoughts.md` — "did I design my YAML right?", run 100% local, no remote DB setup, no Docker, no AWS.

**Why.** The integrator's iteration loop today requires either a live remote DB or a Docker-PG container. Both are heavyweight for "I want to see if my YAML edits look right." SQLite collapses that to a single file in `~/.quicksight-gen/` (or in-memory). App 2 (X.2) becomes the local viewer; SQLite becomes the local store.

**Scope.**

- [x] **X.3.a — Dialect.SQLITE enum + connection plumbing (LOCKED 2026-05-08; landed implicitly via X.3.g.1).** `Dialect.SQLITE` is in `common/sql/dialect.py:69` (alongside POSTGRES + ORACLE; module docstring §X.3 covers the SQLite branch contract). Connection via stdlib `sqlite3.connect()` in `common/db.py::connect_demo_db()` (lines 137–152), with `PRAGMA foreign_keys = ON` + `_register_sqlite_aggregates()` (lines 160–201) registering `STDDEV_SAMP` (matview SELECTs depend on it). `sqlite_path(url)` (lines 70–96) parses `sqlite:///path/to/db.sqlite` and `sqlite:///:memory:` URLs. Async pool via `_AsyncSqlitePool` (lines 552–575) wraps `aiosqlite`. `Config.dialect` accepts `Dialect.SQLITE` and `load_config()` parses `dialect: sqlite` from YAML + `QS_GEN_DIALECT` env. `execute_script(cur, sql, dialect=SQLITE)` uses `cur.connection.executescript(sql)` for multi-statement scripts.
- [x] **X.3.b — SQLite schema emit (LOCKED 2026-05-08; landed implicitly via X.3.g.1).** `common/l2/schema.py::emit_schema(instance, dialect=Dialect.SQLITE)` works end-to-end: 45KB of DDL emits cleanly + applies green against an in-mem SQLite. Per-dialect helpers in `dialect.py` already branch on SQLite for every primitive (`serial_type` → `INTEGER PRIMARY KEY AUTOINCREMENT` (single-col), `boolean_type` → `INTEGER` (0/1 convention), `text_type` / `json_text_type` / `varchar_type` → `TEXT`, `decimal_type` → `NUMERIC` (storage affinity), `cast` / `typed_null` → `CAST(... AS <_sqlite_type_alias>)`, `to_date` → `DATE(expr)`, `date_literal` → `'YYYY-MM-DD'` (plain text — `DATE 'literal'` is rejected by SQLite, see project memory + module docstring), `json_check` → `CHECK (col IS NULL OR json_valid(col))` (JSON1 built-in), `epoch_seconds_between` → `(julianday(later) - julianday(earlier)) * 86400`, `interval_days` / `range_interval_days` / `order_by_day_expr` adapt for SQLite's numeric-only RANGE frames + julianday projection, `date_trunc_day` → `datetime(expr, 'start of day')`, all `drop_*` → `DROP <thing> IF EXISTS` (no CASCADE — SQLite has no FKs in our schema), `with_recursive` → `WITH RECURSIVE`, `dual_from` → `""`). `_entry_column_decl(SQLITE)` returns `INTEGER PRIMARY KEY AUTOINCREMENT` (single-column auto-incrementing); composite `(id, entry)` collapses to `UNIQUE` (per-id supersession contract preserved by the matview's `WHERE entry = MAX(entry) WHERE id = ...` projection).
- [x] **X.3.c — Materialized views as truncate-and-select-into (LOCKED 2026-05-08; landed implicitly via X.3.g.1).** `dialect.py::matview_create_keyword(SQLITE)` returns `"CREATE TABLE"`; `matview_options(SQLITE)` returns `""`. So `CREATE TABLE <prefix>_X AS SELECT ...` (matview-as-table). `refresh_matviews_sql(instance, dialect=SQLITE)` in `common/l2/schema.py` branches to `_emit_sqlite_matview_refresh(instance)` which re-runs DROP + CREATE blocks for every matview-as-table, dependency-ordered. `drop_matview_if_exists(name, SQLITE)` collapses to `DROP TABLE IF EXISTS name;`. Same data contract + column shapes as PG / Oracle; preserves dialect-comparison parity. End-to-end probe (matview refresh against in-mem SQLite seeded with 864 transactions + 2,285 daily_balances) lands green.
- [x] **X.3.d — Seed pipeline against SQLite (LOCKED 2026-05-08; landed implicitly via X.3.g.1).** `cli/_helpers.py::build_full_seed_sql(cfg, instance, anchor=date(2030,1,1))` emits 1.4MB of dialect-aware INSERTs against `cfg.dialect = SQLITE` and applies clean. `seed.py::_sql_timestamp_literal(iso, SQLITE)` strips the timezone offset and converts `T` → space for SQLite's `datetime()` parser. `wipe_demo_data_sql(instance, dialect=SQLITE)` uses `DELETE FROM <table>` + `DELETE FROM sqlite_sequence WHERE name='<table>'` to reset AUTOINCREMENT counters (matches PG's `RESTART IDENTITY` semantics). Hash-locked SQL: `tests/data/_locked_seeds/spec_example.sqlite.sql` (1.4MB, byte-deterministic against the c.13.1 density=1.0 default). Locked-seed determinism test in `tests/data/test_locked_seeds.py` covers all three dialects.
- [ ] **X.3.e — App 2 reads from SQLite.** The X.2.f data fetcher dispatches by dialect; SQLite arm uses `sqlite3` rows. Smoke runner gets `--sqlite` shorthand for the integrator's local-iteration flow.
- [ ] **X.3.f — Documentation.** Handbook addition: "the integrator's local loop" — install, point at SQLite, edit YAML, see it. Probably one walkthrough page.
- [ ] **X.3.g — CI cells (the SQLite column of the X.2 matrix).** **Status: X.3.g.1 (Layer 1) and X.3.g.2 (Audit PDF) both shipped. Layer 1: 8 tests in `tests/unit/test_layer1_query_sqlite.py` exercise `query_matview_rows` / `matview_row_count` / `assert_matview_has_row` / `assert_account_in_matview` against an in-memory `sqlite3` connection. Audit PDF: 8 tests in `tests/audit/test_pdf_sqlite.py` exercise `_query_drift_violations` / `_query_overdraft_violations` / `_query_limit_breach_violations` / `_query_stuck_pending_violations` / `_query_stuck_unbundled_violations` / `_query_supersession` / `_query_executive_summary` against an in-memory SQLite seeded with the L1 invariant matview shapes. The 8 inline date-literal sites (15 lines) in `cli/audit/__init__.py` were routed through the new `date_literal(value, dialect)` helper in `common/sql/dialect.py` — Postgres + Oracle keep the SQL-standard `DATE 'YYYY-MM-DD'` form (byte-identical between them); SQLite gets a plain `'YYYY-MM-DD'` text literal (the `CAST('YYYY-MM-DD' AS DATE)` form coerces to INTEGER 2030 in SQLite, silently breaking comparisons). HTMX cell still pending — lands when X.2.h ships.** Three new jobs (or three matrix-axis entries on existing workflows):
  - `e2e-sqlite-layer1` — Layer 1 matview-check unit suite parametrized over the SQLite dialect. Mirrors what `_layer1_query.py` already does for PG / Oracle.
  - `e2e-sqlite-audit` — Audit PDF generation against SQLite seed; same `tests/audit/` shape as PG / Oracle audit tests.
  - `e2e-htmx-sqlite` — HTMX dialect Layer 2 against SQLite (the third DB cell of the X.2.h matrix; lights up when both X.2.h and X.3 land).
  - Cheapest cells in the matrix — SQLite needs no DB instance, no AWS, no Docker. Runs anywhere, finishes fast. The integrator-local persona's iteration loop becomes the CI's iteration loop too.
  - Updates `e2e-against-testpypi` to include the SQLite arm of the 4-way cross-tool agreement (`expected == PDF == HTMX` for SQLite — QS column drops because QS doesn't read SQLite).

**Notes on portability:**
- **Recursive CTEs.** Investigation matviews use `WITH RECURSIVE`; SQLite supports this since 3.8.3 — should port cleanly.
- **CONCAT operator.** PG / Oracle / SQLite all use `||`. Same.
- **Window functions.** SQLite supports them since 3.25; matview refresh queries port.
- **Date arithmetic.** SQLite uses `date()` / `datetime()` functions. Already dialect-aware in `common/sql/dialect.py` per Phase P.

### X.4 — App 1: YAML editor (the institution-shape helper)

**What.** Per `docs/x_2_design_thoughts.md`: a web-based editor for the L2 institution YAML. Top-of-page force-directed view of the L2 + click-to-filter + filter toggles + cards for editing each L2 entity. Hitting save on a card PUTs the entity, server applies + cascades; affected scope reloads via `HX-Trigger: l2-cascade-reload`.

**Why.** Hand-editing the L2 YAML is the integrator's primary friction. The validator is strict (good) but the feedback loop is "edit YAML → run schema apply → maybe see error → fix → repeat." A live editor with the L2 force-directed map + form-based entity editing collapses that loop.

**Persona match.** The integrator. Local iteration loop, paired with X.3 SQLite for "edit YAML, see it apply against local data, see the dashboard render in App 2."

**Architecture (per design doc):**
- Same Starlette process as App 2 OR separate process — TBD when X.4 starts. Same-process for the dev tool (X.4 era); split when phase.2 auth lands (App 1 has writes, needs different auth posture than App 2's read-only).
- REST shape: `/l2_shape` (force-directed view), `/l2_shape/accounts`, `/l2_shape/rails`, `/l2_shape/chains`, `/l2_shape/transfer_templates`, `/l2_shape/theme` — CRUD per entity type.
- Cascade mechanism: PUT entity, server returns 200 + new body. When the change rippled (rename rewrites references, etc.) the response ALSO emits `HX-Trigger: l2-cascade-reload`. Client-side the L2-shape view + force-directed canvas listen for that event and `hx-get` themselves. No client-side cascade computation.
- Force-directed visual is the **shared primitive** with App 2 — already in `common/tree/visuals.py::ForceGraph` from the X.2 spike. App 1's editor canvas + App 2's dashboard visual + App 1 ETL coverage overlay are three uses of the same renderer.

**Scope (placeholder — fill in detail after X.2 lands):**

- [ ] **X.4.a — Read-only L2 viewer.** `/l2_shape` GETs the L2, renders the force-directed map. Click a node → filter to the connected subgraph. Filter toggles for entity categories. Reset filters button. No edits yet — viewer first to confirm the visualization carries the L2 model.
- [ ] **X.4.b — Per-entity card view.** GET `/l2_shape/accounts/:id` returns one entity's card (read-only). All fields visible. **Discipline (pre-X.6):** card field labels + helper text come from `common/l2/primitives.py` field docstrings, NOT hand-written in the editor template. Same source X.6's mkdocstrings expansion will eventually consume — keeps editor + docs aligned by construction.
- [ ] **X.4.c — Edit + PUT + cascade.** Card flips to edit mode, save PUTs. Server applies + recomputes references + writes back the YAML. Response carries `HX-Trigger: l2-cascade-reload` when the change rippled.
- [ ] **X.4.d — Live validation feedback.** PUT validates against the existing strict L2 validator; failure returns 400 + the validator error inline as a swap fragment under the form.
- [ ] **X.4.e — Force-directed integration.** Editor canvas at the top stays in sync with edits via the same cascade-reload event.
- [ ] **X.4.f — CLI integration.** `quicksight-gen edit -c config.yaml --l2 path/to/L2.yaml` opens the local server + browser tab.
- [ ] **X.4.g — Layer 2 e2e.** Playwright tests covering the edit-cascade flow + force-directed canvas updates.
- [ ] **X.4.h — Hot reload / rebuild button.** Integrator's loop is "edit YAML → see it in App 2." Today: change YAML → re-run `data apply --execute` → restart App 2. Painful. App 1 surfaces a "Rebuild" button that: (1) kills the App 2 process, (2) re-runs `data apply -c config.yaml --execute` (whichever backing DB the config.yaml points at — SQLite for the integrator-local persona, Postgres or Oracle when the operator runs against a remote DB), (3) restarts App 2, (4) reloads App 2's open tab once it's back up. UX shape will be obvious once the editor + ETL pieces land — defer detailed spec to when X.4 starts. Note for that future moment: the user can keep reading the YAML in App 1 while the rebuild runs, so the perceived downtime is small.

### X.5 — App 1 ETL helper

**What.** Per `docs/x_2_design_thoughts.md`: data-loading helper inside App 1. Two paths:
1. Build up SQL load steps (saved to `etl.yaml`, NOT `config.yaml` — preserves the V.1.b allowlist boundary).
2. Run the synthetic data generator (existing `data apply` pipeline, exposed via the App 1 UI).

Plus: load repeatedly up to a defined date for time-travel simulations of business-over-time. View the force-directed L2 with data-coverage overlay (color/saturation by whether rows exist for each L2 primitive).

**Why.** Closes the loop for the ETL engineer persona: "did I load all the data I expected to?" The data-coverage overlay on the force-directed view is the integrator's answer to "what's covered?"

**Scope (placeholder — fill in detail after X.4 lands):**

- [ ] **X.5.a — `etl.yaml` schema + loader.** New file alongside `config.yaml`, separate strict allowlist. Holds list of SQL load steps with `--end-date` parameter support.
- [ ] **X.5.b — `data apply --end-date <ISO date>`.** Trivial in `emit_full_seed`: skip records past the cutoff. Punt on `--density-factor` for perf testing — note in PLAN, ship when needed.
- [ ] **X.5.c — App 1 ETL UI.** SQL step builder cards, run button, progress feedback, link to view results in App 2.
- [ ] **X.5.d — Force-directed coverage overlay.** Color/saturation by row-count per L2 primitive. Shared force-directed visual from X.2 + a second data fetcher that returns coverage stats.
- [ ] **X.5.e — Time-travel re-loads.** Run loader repeatedly with advancing `--end-date` to simulate business-over-time. UI exposes the date stepper.

### X.6 — Model-driven docs (drift reduction)

**What.** Drive the documentation site from the same data model that drives the renderers. Today the L2 entity reference, visual reference, dataset reference, and per-sheet walkthroughs are hand-written in `docs/` Markdown — each is a place documentation can drift from code. Phase X.6 collapses those into `common/l2/primitives.py`, `common/tree/visuals.py`, `DatasetContract`, and the tree's `Sheet.description` / `Visual.subtitle` strings as the single source of truth, with mkdocs-macros + mkdocstrings rendering them into the docs site at build time.

**Why.** Documentation drift is the failure mode this codebase has hit repeatedly (X.1.h ETL hallucination; persona-leak sweeps in Q.4 / Q.5; CLI-invocation drift caught by X.1.h.B). Each fix has been a one-off audit + handwritten correction. Auto-generation makes drift structurally impossible for the surfaces it covers — when the field docstring changes, the docs page changes the same commit.

**Scope:**

- [ ] **X.6.a — mkdocstrings expansion.** Auto-generate L2 entity reference (`common/l2/primitives.py` — Account, Rail, Chain, TransferTemplate, etc.) and visual reference (`common/tree/visuals.py` — KPI, Table, BarChart, Sankey, ForceGraph). Per-class page with docstring + field table. Replaces today's hand-written `docs/reference/l2-spec.md` + per-visual handbook callouts.
- [ ] **X.6.b — Custom mkdocs-macros plugin: tree → walkthrough scaffolds.** Reads sheet/visual descriptions from each app's tree (`apps/<app>/app.py` builds the tree; the plugin walks it). Emits per-sheet walkthrough scaffold with the sheet's own `description` as the lede + each visual's `subtitle` as a section. Hand-written prose can extend the scaffold but the model-derived parts can't drift.
- [ ] **X.6.c — Auto dataset reference.** `DatasetContract` lists columns + types + (often) shape. Generates per-dataset reference page. Replaces today's hand-written column lists in `docs/data-contract/`.
- [ ] **X.6.d — Auto config reference.** Config dataclass + `etl.yaml` schema (X.5.a) + L2 schema validators → reference pages for each config file the user touches. Field docstrings + valid-value enums → docs.
- [ ] **X.6.e — Live-embed App 2 fragments in mkdocs pages.** Because X.2.i mounts `/docs` in App 2's Starlette process, mkdocs pages can include `<iframe src="/dashboards/.../sheets/.../visuals/...">` fragments that render live. Doc walkthrough shows the actual visual it describes against the demo data, not a screenshot that goes stale. Use sparingly (page-load weight) — per-app handbook overview is the natural home.
- [ ] **X.6.f — Migrate hand-written walkthroughs.** Sweep `docs/walkthroughs/` to use the X.6.b scaffolds. Hand-written content lives in extension blocks; model-derived content comes from the tree. Captures the prose that's still useful while killing the drift surface.
- [ ] **X.6.g — README + handbook positioning sweep** (folded from former X.9). The 4 apps × 2 dialects × 3 DBs surface needs a fresh top-of-funnel pitch. README + handbook home pages should communicate what the tool now does — not what it did pre-X.2.
- [ ] **X.6.h — Drift CI gate.** pytest test that fails if any docs page references a model attribute (field, class, enum value) that no longer exists. Same shape as X.1.h.B's CLI invocation checker — extracts model references from docs, asserts they resolve. Catches the regression class even when the auto-generation is bypassed for hand-written prose.

- [ ] **X.6.i — Source-of-truth discipline sweep.** X.2.g + X.4.b only call out sheet description + visual subtitle + L2 entity field labels. Audit every analyst-facing string on tree primitives: parameter labels, calc field display names, drill action labels, dataset display names, theme token names, etc. Anything that appears in BOTH the rendered surface and the docs is a candidate for "lives on the tree, docs reads from it." Avoid the long-tail cleanup list that would otherwise hit at X.6 time.

- [ ] **X.6.j — Self-host handbook guide.** New handbook page covering the App 2 deployment story end-to-end — Dockerfile recipe (multi-stage: tailwind build → wheel install → uvicorn entry), env var contract (`QS_GEN_*` vars consumed at boot), reverse-proxy notes, ALB / phase.2 OIDC pointers. Operators going from local-iteration → production-self-host should not have to grep the codebase. Companion: a working Dockerfile in the repo at `deploy/Dockerfile` (referenced from the guide).

**Threads from X.2-X.5 already in place** (so X.6 has source material to consume):
- X.2.g — sheet `description` + visual `subtitle` are the docs source of truth from day one.
- X.4.b — L2 entity card labels come from `common/l2/primitives.py` field docstrings, not hand-written in the editor.

### X.7 — Cloud CI cost optimization
- Note there's an issue with auto creating quicksight datasources if they need to traverse a VPC endpoint. The work is parked on branch hotfix-v8.7.4-vpc-connection-arn.
Out of active development iterations — manage cloud spend deliberately. Two-tier CI: a fast loop on every push that touches no AWS, and a gated full-e2e tier triggered by tag pushes (release gate, auto-fired), manual `workflow_dispatch`, or a weekly cron.

- [ ] **X.7.a — Baseline the spend.** ~5 min in AWS Cost Explorer for the last 30 days; record the per-resource line items in this entry so X.7.b/c are sized against actual numbers. Without this we're guessing whether the right answer is "stop Oracle when idle" or "tear everything down between runs."
  - Answer: I think start/stop is fine for now, keeps the connect strings pretty static

- [ ] **X.7.b — Fast loop on every push:main.** pytest + pyright + Docker-PG + Docker-Oracle (both dialects already containerized) for everything that doesn't need QuickSight: unit + integration + contract + docs build + audit PDF generation. No AWS resources touched. This is the per-commit feedback loop.

- [ ] **X.7.c — Gated full e2e on three triggers.** Combined behavior: everything from X.7.b + start AWS RDS (PG + Oracle, both pre-existing) + warm + deploy QS + run true e2e + stop RDS + (if a tag) push to TestPyPI then prod approve. Trigger sources: (1) **on tag push** — release pipeline auto-fires it as the gate before TestPyPI publish (no manual step; failing e2e blocks the release); (2) **manual workflow_dispatch** — operator on-demand verification; (3) **weekly cron** — catches AWS breaking us between releases. Use `start-db-instance` / `stop-db-instance` cycling instead of provision-and-terminate (RDS billing pauses when stopped except storage; ~5 min start vs ~10-20 min provision). Aurora Serverless v2 already scales to 0.5 ACU min — verify whether scale-to-0 is configurable. The DB should be clean at start anyway, so cycling pre-existing instances is equivalent to fresh provision for our purposes.
  - Answer: Scale to zero is 100% doable, I'd just recommend start /stopping oracle. I'll reconfigure the scaling once we're here. For the start/stop I'll just need to know the additional permission grants.
  - **Concurrency redesign — fold in observed races (May 2026).** The current `e2e.yml` has two structural defects worth fixing as part of the X.7 redesign rather than separately: (1) **within-run race** — `e2e-pg-api` and `e2e-pg-browser` run in parallel within a single workflow run and both `schema apply` against the same `spec_example_*` tables, racing on DROP/CREATE. Caused the X.1.b L2FT cascade test to see an empty matview at assertion time. Fix: make `e2e-pg-browser` `needs: e2e-pg-api` (sequential within a run) OR give each job its own L2 instance prefix so the schemas don't collide. (2) **cross-run cancellation** — concurrency group `e2e-pg` with `cancel-in-progress: false` interacts badly with rapid push+dispatch sequences (observed 1-second cancellation when a push:main run's cleanup overlapped a queued workflow_dispatch). Fix: move concurrency to the workflow level (one run at a time, period) + simplify the per-job concurrency groups. Both are CI-shape changes that fit X.7's scope.

### X.8 - Ask for configuring the row counts and date range for the data seeding

### X.9 — _(folded into X.6.g)_

The README + handbook positioning sweep is now X.6.g, since it shares the model-driven-docs concern.

---

## Phase Y — SQL-level parameter pushdown (QuickSight + App2 convergence)

**Why.** Today QS runs every visual as a Direct Query that pulls the *entire* dataset SQL result set, then narrows in QS's process via analysis-level FilterGroups + calc fields. Datasets are parameter-blind — the database has no idea the analyst's slider is set to 5 — so DB indexes never apply to the analyst's filter, and a 1k-row matview ships in full per visual. App2 (built the database-native way) uses bind variables that prune at SQL-execute time. Result: two implementations of one filter intent, drifting each other, AND a perf hit on QS that's only growing as the matviews grow.

**Architectural shift.** The dataset SQL becomes the single source of truth — `<<$paramName>>` substitution for QS (literal at query time via `MappedDataSetParameters`); `:param_<name>` bind vars for App2 (one-line preprocessor in `_sql_executor`); analysis-level FilterGroups + calc fields go away (or stay only as decorative chrome where useful). What we lose: the QS Filters pane "active filters" list + per-visual filter icon — neither persona uses these today; the slider/dropdown control widget (the actual analyst input) is unchanged. What we gain: one SQL truth → drift gone; database does the filtering → orders-of-magnitude fewer rows on the wire; the calc field workarounds (window functions evaluated in QS engine) become real columns the database computes once.

**Branch.** `phase-y-sql-pushdown` off `main` (current main, includes Phase X work merged through this point). Phase X's in-flight work (X.2.g.3 dataset `app2_sql=` blocks, etc.) gets *replaced* by Phase Y's convergent path — when Y lands, Phase X's branch rebases on top and drops the now-redundant `app2_sql=` plumbing.

### Y.1 — Spike: prove the mechanism on Volume Anomalies

Single sheet, single slider (`pInvAnomaliesSigma`), no calc fields. **PG-only spike** — SQLite + Oracle dialect coverage lands at Y.7 once the mechanism is proven; the spike is about verifying the QS substitution + App2 bind translation round-trip on the most-used dialect, not about full dialect spread. End-state: QS sees `<<$pInvAnomaliesSigma>>` substituted to the literal value; PG `pg_stat_statements` shows the WHERE clause hitting the database; row counts to QS drop by orders of magnitude; App2 still works via the bind preprocessor.

#### Spike-time finding — visual-scoped filters need the companion-dataset workaround

Reading the Investigation app's σ wiring before touching code surfaced a design constraint not in the PLAN's premise: σ is `SELECTED_VISUALS`-scoped (KPI + Table only) — the distribution chart **deliberately stays unfiltered** so the analyst can see the full population shape and read "where my threshold sits" against the bucket bars. Pushing σ into the dataset SQL with `<<$pInvAnomaliesSigma>>` would filter every visual reading that dataset, including the distribution chart — a visual regression.

**The convention going forward:** filters fall into three scope categories, and the SQL-pushdown decision differs per category.

| Scope            | Pushdown shape                                                                                                          |
|------------------|-------------------------------------------------------------------------------------------------------------------------|
| `scope_sheet(...)` (every visual) | Clean pushdown into the dataset SQL via `<<$paramName>>` — one SQL, every visual filters identically. Most filters fall here. |
| `scope(fg, [v1, v2])` (SELECTED_VISUALS) | **Companion-dataset pattern**: filtered visuals point at the parameter-bearing dataset; unfiltered visuals point at a separate dataset reading the same source (no parameter). Both wrap the same matview; the second dataset has no `<<$pName>>`.   |
| Cross-sheet      | TBD — will surface in Y.2's L1 sweep.                                                                                   |

The σ case is the spike's load-bearing example of the SELECTED_VISUALS pattern. Y.1 ships with the companion dataset built; Y.2 audits each existing FilterGroup for scope before pushdown and applies the pattern as needed.

#### Steps

- [x] **Y.1.a — Branch off main as `phase-y-sql-pushdown`.**
- [x] **Y.1.b — Convert `pInvAnomaliesSigma` to a dataset parameter in `build_volume_anomalies_dataset`.** Dataset declares the parameter with default value; SQL gains `WHERE 1=1 AND z_score >= <<$pInvAnomaliesSigma>>`. KPI + Table read this dataset.
- [x] **Y.1.b.companion — Introduce `build_volume_anomalies_distribution_dataset` — companion dataset reading the same matview WITHOUT the σ parameter.** Distribution chart re-binds to this so it stays unfiltered. Validates the SELECTED_VISUALS workaround pattern Y.2 will reuse.
- [x] **Y.1.c — Add `MappedDataSetParameters` bridging analysis param → dataset param.** The analysis-level `pInvAnomaliesSigma` flows down to the dataset's parameter via the mapping.
- [x] **Y.1.d — Drop `FG_INV_ANOMALIES_SIGMA` analysis-level FilterGroup from the Investigation app.** Confirm slider widget still drives the value.
- [x] **Y.1.e — App2 SQL preprocessor — `<<$paramName>>` → `:param_paramName`.** Two regex passes (quoted form first, unquoted second) in `translate_qs_dataset_params` — runs before `rewrite_placeholders_for_dialect` in both sync + async executors. End-to-end SQLite test confirms the QS→bind round-trip filters at the database.
- [x] **Y.1.f — Drop the X.2.g.3.b `app2_sql=` block.** N/A on this branch — the X.2.g.3.b commit lives on `phase-x-2-g-investigation-app2` awaiting Y.8 rebase.
- [x] **Y.1.g — Re-lock affected hash tests + JSON-emit tests.** `test_filter_groups_in_expected_order` (FG count 12 → 11), `test_investigation_datasets_in_expected_order` (7 → 8), `test_investigation_datasets_declared_in_analysis` (added DS_INV_VOLUME_ANOMALIES_DISTRIBUTION), `test_money_trail_dataset_reads_from_matview` (index 2 → 3), `test_fanout_sheet_serializes_to_aws_json` (FilterGroups count 12 → 11). Two stale sigma tests replaced with five Y.1-shape assertions covering pushdown SQL, dataset parameter declaration, mapped bridge, and companion-dataset binding.
- [x] **Y.1.h — Deploy to Aurora PG; capture `pg_stat_statements` baseline + post-Y delta.** **VERIFIED LIVE.** Pre-Y baseline (historical): `SELECT * FROM sasquatch_pr_inv_pair_rolling_anomalies` — 6 calls × 6213 rows = 37,278 rows scanned, mean 192.5ms. Post-Y dashboard load issued THREE distinct queries (visible via `pg_stat_statements` with QS provenance comments tagging visualId): (1) Table — `... WHERE $2=$3 AND z_score >= $4`, 7 calls × 133 rows avg = 933 rows total, mean 19.6ms. (2) KPI — `SELECT COUNT(*) ... WHERE $1=$2 AND z_score >= $3`, 7 calls × 1 row = 7 rows, mean 19.8ms. (3) Distribution — `SELECT "z_bucket", COUNT(*) ... FROM "Investigation Volume Anomalies — Distribution"` — NO `WHERE` clause (companion dataset bound), 1 call × 5 rows, mean ~20ms. The `$2=$3` is the prepared-statement cache form of our `WHERE 1=1` template; `$4` is the σ literal QS substitutes via `MappedDataSetParameters`. **Companion dataset binding works — distribution chart gets no σ filter as intended.**
- [ ] **Y.1.i — Deploy to Oracle; capture `v$sqlstats` baseline + post-Y delta.** Same shape as PG. **Operator action — Oracle path not exercised in this session.**
- [x] **Y.1.j — Performance verdict.** **WIN, with receipts.** ~10× latency reduction (192.5ms → ~20ms mean). ~48× row reduction for the Table visual (6213 → ~130 rows on the wire). ~6213× row reduction for the KPI's COUNT(*) (full scan → 1 row). Distribution chart preserves its full-population view (~5 z_bucket aggregate rows post-server-side GROUP BY). The spike validates the entire Phase Y premise: dataset-SQL-level pushdown moves the work from QS's in-memory engine to the database, which is the right place for it.
- [x] **Y.1.k — Cascade dropdown probe.** **CLOSED. Verdict: QS-side limitation, not fixable from our wiring.** URL params populate widget state + analysis-level filter values, but `MappedDataSetParameters` bridges into dataset-level `<<$paramName>>` substitution do NOT fire on initial URL load. Bridge fires only on manual widget interaction. Y.1.p tried three reference shapes (B1 filter-with-param, B2 echo-column + filter, B3 calc field with `${param}` expression — LuisBorrego's community workaround) — all emit the right SQL via pg_stat_statements but `$N` binds remain at sentinel defaults until manual interaction. Y.1.p attempts reverted (commits 7aa0602 / 66524a4 / 0a3ded6); cascade restored to pre-Y.1.p OR-cascade WHERE shape that works under manual interaction. Quirks log entry 2.1 rewritten with the precise mechanism.
- [x] **Y.1.l — Spike commit + spike-time findings captured.** Branch is ready for Y.2 sweep. Findings:
    1. **SELECTED_VISUALS scope = companion dataset.** Pre-spike PLAN missed this; the σ filter is per-visual scoped (KPI + Table only, not Distribution). Solution shipped in Y.1.b.companion: a second dataset wraps the same matview without the parameter; the unfiltered visual binds to it. Y.2 audits each FilterGroup's scope before pushdown.
    2. **Cascade verdict reversed mid-spike.** Initial pass: "already proven via L2FT". User correction: L2FT metadata cascade does NOT work right in production today — likely a "layer filter violation" between dataset param + MappedDataSetParameter bridge + per-visual filter scope that the X.1.g browser e2e didn't catch (e2e validated dropdown UI shape, not data filter effect). Y.1.k REOPENED — cascade behavior under SQL pushdown is unverified, not proven. Y.2's L2FT sweep needs the live cascade behavior pinned down first.
    3. **App2 preprocessor is one regex pass.** `translate_qs_dataset_params` handles quoted (string) + unquoted (numeric) forms; the existing PG `:name` → `%(name)s` rewrite picks up the translated binds without changes.
    4. **Mechanism verified at SQLite layer.** End-to-end test in `test_html_sql_executor.py` confirms `<<$pName>>` placeholder + URL `param_pName=N` produces the right rows via real aiosqlite execution. PG + Oracle verification deferred to Y.1.h/i.
    5. **Live perf numbers (Y.1.h/j) deferred to operator deploy.** Mechanism is proven; the verdict on whether QS Direct Query actually pushes the WHERE to Postgres in the wire goes pending the next Aurora roll.

#### Y.1.m — Control-wiring fix (multi_valued type validator) — DONE

**Outcome.** L2FT cascade was originally broken because `pL2ftMetaValue` was `multi_valued=True` bound to `add_parameter_text_field` (single-string control). X.1.b swapped LinkedValues dropdown → text-field but didn't flip multi_valued. Text-field commits to multi-valued params silently revert to default.

- [x] **Y.1.m.1 — Type-system enforcement landed.** `ParameterTextField.__post_init__` rejects `parameter.multi_valued=True` at construction. Caught all 3 L2FT offenders on first run.
- [x] **Y.1.m.2 — Apps fixed.** `pL2ftMetaValue` / `pL2ftChainsMetaValue` / `pL2ftTtMetaValue` flipped to `multi_valued=False`; matching dataset `pValues` params (×4) flipped to `SINGLE_VALUED`. Manual interaction now commits cleanly.
- [x] **Y.1.m.3 — Tests updated + 1376 unit + JSON green.** Commit `582a900`.

#### Y.1.n — Cross-sheet / cross-dashboard URL-param drill re-evaluation — CLOSED (verdict: not viable for dataset-bridged params)

**Investigated via Y.1.p:** three analysis-level reference shapes attempted to wake the `MappedDataSetParameters` bridge on URL load. All failed identically — bridges fire only on manual widget interaction.

**Verdict on cross-app drills:**

- ✅ Cross-app URL stamping works for **analysis-level params** (`CategoryFilter` / `NumericRangeFilter` / `TimeRangeFilter` taking `${param}` directly).
- ❌ Cross-app URL stamping does NOT work for **dataset-bridged params** — the L2FT-cascade pattern. Bridge stays at sentinel default until manual interaction.

**Implications:**

- K.4.7's dropped cross-app drills that target dataset-bridged params (the L2FT metadata cascade specifically) **stay dropped** — no JSON shape on our end fixes this.
- K.4.7-style drills that target purely analysis-level params **could** be re-enabled (pending separate verify) — they don't depend on the bridge.

#### Y.1.p — Cascade-via-bridge attempts — REVERTED

Three reference shapes tried, all reverted:

- **B1** (`3c8af5c`, reverted in `66524a4`): replace OR-cascade WHERE with `_meta_match_value` CASE projection; analysis-level `CategoryFilter.with_parameter` on the column. SQL emitted with `_meta_match_value = $N` but `$N` stayed at sentinel default.
- **B2** (folded into B1 commits): + `_meta_key_echo` echo column + tautological filter using `pL2ftMetaKey`. Same outcome.
- **B3** (uncommitted; deployed via `berrt932m`, code discarded): replace param-as-filter-value with analysis-level `CalcField` whose expression references `${param}` (LuisBorrego's pattern). pg_stat_statements showed the calc fields pushed down as `CASE WHEN ... = $N THEN ... END IN ($M)` but `$N` still bound to sentinel on URL load.

**Diagnostic confirmed via `scripts/qs_substitution_probe.py`:** the SQL was correct in every iteration (direct PG execution returned 49 rows for the URL-1 inputs); the QS-side bind chose the sentinel default until the analyst manually committed.

**Y.1.q — quirks log + memory update done.** Entry 2.1 rewritten + worst-footgun framing updated (`d8f5c5e`).

### Y.2 — Sweep simple-filter datasets (one sub-task per dataset)

Spike-proven pattern applied across every sheet that has SQL-pushdownable filters today. Each sub-task is one dataset: convert FilterGroup → dataset parameter, drop `app2_sql=` if any, re-lock hash tests, verify deploy.

**Constraint baked in (post-Y.1.p verdict):** cross-app URL drills cannot pre-narrow a dataset-bridged-param cascade on initial load. SQL pushdown via dataset parameters works fine for **manual interaction** (Y.1's σ slider). Y.2 sweeps target the manual-interaction perf win — not the cross-app handoff. The L2FT cascade stays on its restored OR-cascade WHERE shape (works under manual interaction; URL load returns the unfiltered universe — accepted UX cost).

**Per Y.1 finding, audit each FilterGroup's scope first.** `scope_sheet(...)` filters get clean pushdown; `SELECTED_VISUALS` filters need the Y.1.b.companion pattern (companion dataset without the parameter for the unfiltered visuals); cross-sheet filters TBD.

- [x] **Y.2.a — Money Trail dataset: `pInvMoneyTrailRoot` + `pInvMoneyTrailMaxHops` + `pInvMoneyTrailMinAmount`.** Three params on one dataset.
    - [x] **Y.2.a.1 — Pushdown SQL.** `build_money_trail_dataset` now declares 3 dataset parameters (1 String + 2 Integer) bridged from analysis params via `mapped_dataset_params`. SQL gains `WHERE 1=1 AND e.root_transfer_id = <<$pInvMoneyTrailRoot>> AND e.depth <= <<$pInvMoneyTrailMaxHops>> AND e.hop_amount >= <<$pInvMoneyTrailMinAmount>>`.
    - [x] **Y.2.a.2 — Roots-companion dataset (spike-time finding).** `build_money_trail_roots_dataset` — unfiltered `SELECT DISTINCT root_transfer_id FROM <prefix>_inv_money_trail_edges`, no parameters. Required because the chain-root dropdown reads from `LinkedValues.from_column(...root_transfer_id)`; once the main dataset filters by `<<$pInvMoneyTrailRoot>>`, the dropdown's own DISTINCT-roots query inherits the WHERE (sentinel default → 0 options). Dropdown's `LinkedValues` repointed at the companion. Same shape as Y.1.b.companion / K.4.8k.
    - [x] **Y.2.a.3 — Drop the three FilterGroups.** `FG_INV_MONEY_TRAIL_{ROOT,HOPS,AMOUNT}` removed from `apps/investigation/{app,constants}.py`. `FG_INV_MONEY_TRAIL_WINDOW` (TimeRangeFilter, not parameter-bound) stays — Y.2.f's job.
    - [x] **Y.2.a.4 — Tests re-locked.** `test_filter_groups_in_expected_order` (FG count 11 → 8), `test_investigation_datasets_in_expected_order` (8 → 9 datasets), `test_investigation_datasets_declared_in_analysis` (added DS_INV_MONEY_TRAIL_ROOTS), `test_fanout_sheet_serializes_to_aws_json` (FG count 11 → 8). Three stale K.4.5 FG-shape tests replaced with five Y.2.a-shape assertions covering pushdown SQL, dataset parameter declaration, mapped bridges, companion dataset, and dropdown's companion-binding. Companion-dataset roots-only contract test added.
    - [x] **Y.2.a.5 — Drop X.2.g.3.c `app2_sql=` block.** N/A on this branch — lives on `phase-x-2-g-investigation-app2` awaiting Y.8 rebase, same as Y.1.f.
    - [x] **Y.2.a.6 — Full unit + JSON suite green.** 1376 unit + JSON tests pass.
    - [x] **Y.2.a.7 — Aurora deploy + verify.** Deployed to Aurora PG (commit `6db9f3e` — combined with Y.2.b deploy). All 4 dashboards `CREATION_SUCCESSFUL`. API e2e green (48/48). Investigation browser e2e green (28 passed, 9 skipped — pre-existing drill / cross-app skips). Hands off to user for manual `pg_stat_statements` walk + dashboard interaction.
- [x] **Y.2.b — Account Network dataset: `pInvANetworkAnchor` + `pInvANetworkMinAmount`.** Two params on one dataset.
    - [x] **Y.2.b.1 — Pushdown SQL.** `build_account_network_dataset` now declares 2 dataset parameters (1 String + 1 Integer) bridged from analysis params via `mapped_dataset_params`. SQL gains `WHERE 1=1 AND (source_display = <<$pInvANetworkAnchor>> OR target_display = <<$pInvANetworkAnchor>>) AND hop_amount >= <<$pInvANetworkMinAmount>>` — broad anchor narrow + min-amount cutoff.
    - [x] **Y.2.b.2 — No companion needed for the dropdown.** K.4.8k's `DS_INV_ANETWORK_ACCOUNTS` already plays the unfiltered-companion role for the anchor dropdown's `LinkedValues` source. The Money Trail-style chicken-and-egg problem doesn't apply.
    - [x] **Y.2.b.3 — Drop FG_INV_ANETWORK_ANCHOR + FG_INV_ANETWORK_AMOUNT.** Two of four account-network FGs go (broad anchor narrow + min-amount cutoff now in dataset SQL). The two directional FGs (`FG_INV_ANETWORK_INBOUND` / `FG_INV_ANETWORK_OUTBOUND`) stay — they partition the pre-narrowed anchor-touching set into per-Sankey directions; Y.3.b will push those into SQL CASE expressions too.
    - [x] **Y.2.b.4 — Drop is_anchor_edge calc field.** Only consumer was the now-dropped `FG_INV_ANETWORK_ANCHOR`; ds_anet's SQL pushdown means every row IS anchor-touching by construction. Y.3.b's calc-field-pushdown TODO is one item smaller.
    - [x] **Y.2.b.5 — Tests re-locked.** `test_filter_groups_in_expected_order` (FG count 8 → 6), `test_fanout_sheet_serializes_to_aws_json` (FGs 8 → 6, calc fields 5 → 4). Three stale K.4.8 FG/calc-field tests dropped (`is_anchor_edge` + anchor + amount FG shapes); five Y.2.b-shape assertions added covering pushdown SQL, dataset parameter declarations + sentinel default, mapped bridges, and the orphaned-calc-field removal.
    - [x] **Y.2.b.6 — Drop X.2.g.3.d `app2_sql=` block.** N/A on this branch — same as Y.1.f / Y.2.a.5.
    - [x] **Y.2.b.7 — Full unit + JSON suite green.** 1374 unit + JSON tests pass.
    - [x] **Y.2.b.8 — Aurora deploy + verify.** Deployed in same roll as Y.2.a (commit `6db9f3e`). Initial deploy surfaced a SQL bug — `source_display` / `target_display` are SELECT-list aliases over concat expressions, not real matview columns; PG / Oracle / SQLite all evaluate WHERE before SELECT, so the alias isn't visible to a same-query WHERE. PG raised `UndefinedColumn` at execute time; QS rendered the visuals blank with no banner. Fixed by wrapping projection in a CTE so the alias is in scope by the time WHERE runs. Re-deployed clean. **Spike-time finding logged**: the existing P.9f.e smoke verifier (`tests/integration/verify_dataset_sql.py`) caught the bug in seconds, but it was a CLI script not pytest-collected — so the e2e suite passed despite a deployed SQL exception. Wired it as a pytest test (`tests/e2e/test_dataset_sql_smoke.py`) so future Y.2.x mistakes auto-fail; 37 datasets parametrized one test each. 65 passed, 9 pre-existing skips, 0 failures on re-run.
- [x] **Y.2.c — L2FT Rails dataset: rail / status / bundle / metadata cascade.** Unblocked. The metadata cascade stays on the existing OR-cascade WHERE (works under manual interaction; URL load returns full universe — accepted per Y.1.p verdict). Other filters (rail / status / bundle) are good pushdown candidates and follow the Y.1 spike pattern.
    - [x] **Y.2.c.0 — SPIKE: prove option B (multi-valued `IN (<<$p>>)`) against deployed Aurora.** Branch `spike-y2c-multivalued-pushdown`: pushed `rail_name` into `build_postings_dataset` SQL via a `MULTI_VALUED` dataset param (`pL2ftRail`, default = all declared rails), dropped `fg-l2ft-rails-rail`, bridged via `MappedDataSetParameters`. Deployed to Aurora `database-2`, seeded `spec_example` (859 txns / 4 rails), browser-drove the Rails sheet. **Verdict: option B is SAFE.** (1) Default — table = 859 rows, no errors. (2) Narrow to `[PoolBalancing]` — drops to 284 rows (bridge fires on widget interaction, `<<$pL2ftRail>>` narrows SQL). (3) Deselect ALL rails — table reverts to 859 rows, **no SQL error, no `IN ()`**: QS falls back to the dataset param's default value. Bonus quirk: the MULTI_SELECT "Select all" toggle is `aria-disabled` when already all-selected (analyst deselects items individually). → Y.2.c proceeds with option B for all three Rails-sheet category filters.
    - [x] **Y.2.c.1 — `build_postings_dataset`: wrap projection in a subquery; push rail / status / bundle into the outer WHERE via `pL2ftRail` / `pL2ftStatus` / `pL2ftBundle` (`MULTI_VALUED` dataset params, defaults = declared rails / `transaction_status_values()` / `bundle_status_values()`).** `status` + `bundle_status` are CASE-aliases so the outer WHERE (over the subquery) is the only place they're visible; `rail_name` joins them there for symmetry. Metadata cascade (`<<$pKey>>` / `<<$pValues>>`) stays in the inner WHERE unchanged.
    - [x] **Y.2.c.2 — `app.py::_populate_rails_sheet`: drop `fg-l2ft-rails-{rail,status,bundle}` FilterGroups.** Declare the 3 analysis params (`StringParam` multi_valued, default = all values, `mapped_dataset_params=[(ds_postings, "<dataset-param>")]`) + MULTI_SELECT `ParameterDropdown(StaticValues)` inline. `_populate_param_filter_dropdown` stays — still used by Chains / Transfer Templates sheets (Y.2.d / Y.2.e).
    - [x] **Y.2.c.3 — Test fixups.** `test_postings_dataset_declares_pkey_and_pvalues_parameters` → 5 dataset params (rename to `..._declares_cascade_and_pushdown_parameters`); L2FT JSON-suite structural assertions that count FilterGroups / params adjust to the walked tree.
    - [x] **Y.2.c.4 — Full unit + JSON suite green; pyright clean; dataset-SQL smoke (`tests/e2e/test_dataset_sql_smoke.py`) passes the new postings SQL on PG/Oracle/SQLite.**
    - [x] **Y.2.c.5 — Aurora deploy + browser-verify** all 3 filters narrow independently + empty-multi-select on each reverts to "all" (no SQL error).
    - [x] **Y.2.c.6 — Quirks-log entry** (`docs/reference/quicksight-quirks.md`): MULTI_SELECT "Select all" disabled when already all-selected; emptying a MULTI_SELECT param mapped to a dataset param reverts to the dataset-param default (no `IN ()`).
    - [x] **Y.2.c.7 — Commit + tick PLAN.**
- [x] **Y.2.d — L2FT Chains dataset: parent chain + completion + metadata cascade.** Mirror the Y.2.c shape onto the Chains sheet.
    - [x] **Y.2.d.1 — `build_chain_instances_dataset`: wrap the final SELECT in a subquery; push `parent_chain_name` / `completion_status` into the outer WHERE via `pL2ftChainsChain` / `pL2ftChainsCompletion` (`MULTI_VALUED`, defaults = `declared_chain_parents()` / `chain_completion_status_values()`).** Metadata cascade on `parent_metadata` stays in the inner WHERE; move the `ORDER BY parent_posting DESC` to the outer query.
    - [x] **Y.2.d.2 — `_populate_chains_sheet`: drop `fg-l2ft-chains-{chain,completion}` FilterGroups; use `_populate_pushdown_dropdown` for both with `mapped_dataset_params=[(ds_chain_instances, "<dataset-param>")]`.**
    - [x] **Y.2.d.3 — Tests** — chain-instances params now 4 (pKey/pValues + the new pair); JSON-suite assertions for the SQL pushdown shape + bridges / absent FilterGroups.
    - [x] **Y.2.d.4 — Full unit/JSON green; pyright clean; dataset-SQL smoke passes the new chain-instances SQL.**
    - [x] **Y.2.d.5 — Aurora deploy + browser check.** Deployed `spec_example` apps; the Chains sheet (`Chain Instances` visual) renders clean — no QS error overlay from the subquery wrap or the `parent_chain_name IN ('__no_match__')` empty-instance fallback (`spec_example` declares no chains, so the dropdown is vacuous + the table empty, which is correct). Rails sheet still 859 rows post-merge. Narrowing/empty-revert behaviour not re-walked here — `spec_example` has no chains; it's the exact `_populate_pushdown_dropdown` + dataset-param shape proven in Y.2.c.0, and the dataset-SQL smoke test executes the new SQL on PG. Full narrowing walk would need `sasquatch_pr` seeded on the deploy DB (deferred to the next time that's needed).
    - [x] **Y.2.d.6 — Commit + tick PLAN.**
- [x] **Y.2.e — L2FT Transfer Templates dataset: template + completion + metadata cascade.** Mirror Y.2.c/d onto the TransferTemplates sheet — but the Template / Completion dropdowns narrow BOTH the tt-instances Table AND the tt-legs Sankey, so the analysis params bridge to BOTH datasets (replacing the pre-Y.2.e `cross_dataset='ALL_DATASETS'` CategoryFilter).
    - [x] **Y.2.e.1 — `_populate_pushdown_dropdown` takes `bridges: list[tuple[Dataset, str]]`** (was a single `(dataset, dataset_param)` pair). Rails/Chains call sites pass a 1-element list; the TT sheet passes a 2-element list (tt-instances + tt-legs).
    - [x] **Y.2.e.2 — `build_tt_instances_dataset` + `build_tt_legs_dataset`: wrap the final SELECT (and the UNION-ALL for legs) in a subquery; push `template_name` / `completion_status` into the outer WHERE via `pL2ftTtTemplate` / `pL2ftTtCompletion` (`MULTI_VALUED`).** Same `PUSHDOWN_NO_MATCH_SENTINEL` fallback for the template default (empty-templates instances). Shared `_tt_dataset_parameters(l2_instance)` builds the 4-param set for both datasets. Metadata cascade stays in the inner WHERE; `ORDER BY` moves outside.
    - [x] **Y.2.e.3 — `_populate_templates_sheet`: drop `fg-l2ft-tt-{template,completion}` FilterGroups; `_populate_pushdown_dropdown` with `bridges=[(ds_tt_instances, "<p>"), (ds_tt_legs, "<p>")]`.**
    - [x] **Y.2.e.4 — Tests** — tt-instances + tt-legs params now 4 each; JSON-suite assertions for the SQL pushdown shape + dual bridges + absent FilterGroups.
    - [x] **Y.2.e.5 — Full unit/JSON green; pyright clean; dataset-SQL smoke passes both new TT SQLs.**
    - [x] **Y.2.e.6 — Aurora deploy + browser check.** Deployed `spec_example` apps; the Transfer Templates sheet (Sankey `Multi-Leg Flow — Account → Template → Account` + Table `Template Instances`) renders clean, no QS error overlay; narrowing Template→`MerchantSettlementCycle` re-renders both visuals clean. (One transient `SpaceNeedleDBProxy` 400 on the first attempt — QS↔Aurora connection hiccup, not the SQL; cleared on re-run; the dataset-SQL smoke executes both new TT SQLs against PG.) Empty-revert behaviour is the proven `_populate_pushdown_dropdown` pattern from Y.2.c.0.
    - [x] **Y.2.e.7 — Commit + tick PLAN.**

#### Resume tomorrow (queued 2026-05-09 end-of-session)

State: `main` = v8.8.0a18 (Y.2.c). Branch `y-2-d-l2ft-chains-pushdown` carries **Y.2.d + Y.2.e** (commits `599ef53`, `989ce1e`) — unit/JSON green, PG dataset-SQL smoke green, ad-hoc deployed-dashboard browser checks green; **not merged to main yet**. AWS RDS (`database-2`) was turned **down** at end of session via `./run_tests.sh down --yes` — bring it back with `./run_tests.sh up aws` before any deploy / browser work.

- [ ] **Y.2.browser.triage — Triage the end-of-session full browser-chain run** (`./run_tests.sh up_to=browser --scenarios=sp --dialects=pg --targets=aw`, run-id `20260510T054237Z-989ce1e`, on the d+e branch). Result: unit / db / app2 / deploy / api all PASSED; **browser layer = 8 failed, 19 passed, 8 skipped, 1 xpassed, 2 errors**. Breakdown:
    - 5× L2FT dropdown tests (`test_l2ft_rails_dropdowns::test_rail_dropdown_narrows_does_not_empty`, `test_l2ft_chains_dropdowns::test_{chain,completion}_dropdown_narrows_does_not_empty`, `test_l2ft_templates_dropdowns::test_{template,completion}_dropdown_narrows_does_not_empty`) — every one died on a transient QS↔Aurora **`SpaceNeedleDBProxy` 400 "The connection attempt failed"** (`GENERIC_SQL_EXCEPTION`/`SERVICE_EXCEPTION`), not a SQL/structural error. Almost certainly `database-2` connection-exhaustion under the chain's load (db→app2→deploy→api→browser all hit it back-to-back, plus the day's earlier deploys/ad-hoc checks). **Needs a clean re-run on a fresh-started `database-2` to confirm flake-not-regression — and `Y.2.d+e MUST NOT be merged to main until this is green.**
    - `test_l1_sheet_visuals::test_l1_dashboard_structure_matches_tree` — `TreeValidator found mismatches` (NOT connection-flake; this is a structural mismatch). Pre-existing-suspect (categorized earlier as "QS silent-failure or real drift, not Y.3") — investigate.
    - `test_l1_filters::test_check_type_dropdown_exposes_options` — `wait_for_selector` timeout; possibly same QS-flake, possibly X.1.g.11 backlog.
    - `test_audit_dashboard_agreement[postgres-supersession]` — the known supersession flake.
    - 2 errors: `test_audit_dashboard_agreement[oracle-*]` — `ORA-00942: table or view does not exist`; the `aw` target is PG-only so the Oracle agreement tests have no seeded Oracle DB — expected, should be skip-not-error (consider a guard).
    Artifacts: `runs/20260510T054237Z-989ce1e/sp_pg_aw/browser/` (per-test `console.txt` / `qs_errors.txt` / `network.txt` / `screenshot.png` / `trace.zip`).
- [ ] **Y.2.merge.de — Merge `y-2-d-l2ft-chains-pushdown` → main + tag v8.8.0a19** (Y.2.d + Y.2.e — plus the branch now also carries gate.l.7, app2.cde.core, app2.cde.multivalued, the Y.2.f park, and l2ft-wiring.a). Gated on `Y.2.browser.triage` coming back clean. Rename the branch first (it's no longer just chains).
- [ ] **Y.2.gate.n — Hoist the variant-independent `unit` layer out of the per-variant chain (one-time prelude).** *(Queued 2026-05-10 — flagged by Chris: `./run_tests.sh up_to=unit` "significant pausing on `[sq_pg_lo] runner: dispatch-run [unit]`"; profiled — collect+pyright-gate ≈ 3.5s, the ~165s is per-test execution. Unit is *the* most expensive layer and it's being run **per matrix cell** for no reason.)*
    **Problem.** The chain runner (`src/quicksight_gen/_dev/runner.py`) treats `unit` (`pytest tests/unit tests/json -n auto`; pyright folded in via the conftest sessionstart gate) as a per-variant layer — artifacts land under `runs/<run-id>/<variant>/unit/`, and `cmd_up_to`'s `asyncio.gather` fan-out calls `_run_one_variant(spec)` per cell, each of which runs `_layers_through(target)` starting with `unit`. But the `unit` suite is **variant-independent** (no DB, no scenario/dialect/target dependency — byte-identical result every cell). So `./run_tests.sh up_to=db --dialects=pg --targets=lo` (4 cells) runs the 165s suite **4×**; default (13-cell) matrix → up to 13×. `--skip-cheap` (j.8, SHA cache) mitigates cells 2..N but it's opt-in.
    **Fix.** Run `unit` **once per `up_to` invocation, as a prelude before the matrix fans out**:
    - In `cmd_up_to`: after resolving the matrix cells but before the `asyncio.gather`, run the `unit` layer once (build its cmd via the existing `_layer_command`/`_layer_cmd` path; honor `--only`/`-k` and `--parallel`; respect `--skip-cheap` via `is_layer_cached_green("unit", variant=<sentinel>)` — pick a stable sentinel like `"_prelude"` for the cache key, or drop the variant arg for unit). If it fails → print the failure, write the cache marker NOT, return `EXIT_FAILURE` (don't fan out). On pass, `write_cache_marker("unit", variant=<sentinel>)`.
    - If `target == "unit"`: run the prelude and **return** — no matrix fan-out at all (unit-only is variant-irrelevant). This is the fast path Chris hit.
    - The per-variant chain (`_run_one_variant` / `_layers_through`) now starts at `db` — drop `"unit"` from the per-variant slice (e.g. `_layers_through(target)` for the per-variant part = `LAYERS[1:idx+1]`, or special-case: prelude layers = `("unit",)`, per-variant layers = the rest). Keep `LAYERS` as the canonical full ordering for chain semantics ("unit → db → …" — unit just moves to the prelude phase).
    - Artifacts: `unit` layer's `cmd.json`/`stdout.log`/`stderr.log`/`timings.json` go to `runs/<run-id>/_prelude/unit/` (top-level, not under a variant dir). Drift report (`drift: …`): handle `unit` as a run-level (not per-variant) timing — compare `runs/<id>/_prelude/unit/timings.json` against the prior run's. The `runs/<id>/timings.json` aggregate keeps a single `unit` entry.
    - `_LAYER_DEPS` for `db` doesn't need a runtime dep on `unit` (they're independent for *data*), but the **chain ordering** still means `db` only dispatches if the unit prelude passed — that's enforced by `cmd_up_to` aborting before the fan-out, same effect as today.
    **Tests** (`tests/unit/test_runner_skeleton.py` + siblings): the existing `_layers_through("unit") == ["unit"]` / per-variant-runs-unit assertions need updating. Add: (1) `up_to=db` with ≥2 cells runs the `unit` layer command **exactly once** (not per cell) — assert against the dispatch log / mock; (2) `up_to=unit` runs the prelude once and **does not** fan out the matrix (no per-variant dispatch at all); (3) prelude failure → `EXIT_FAILURE`, no `db` dispatch; (4) `--skip-cheap` skips the prelude when the SHA's unit is cached green. Refit `test_argparse_full_flag_coverage` / any `LAYERS`-shape tests.
    **Docs.** Update CLAUDE.md's "Test sequencing" section (the `up_to=<layer>` table + the "Layers: unit → db → app2 → …" line) to note `unit` is now a one-time prelude, not a per-variant layer. Update the runner `--help` epilog if it describes the per-variant layer set.
    **Verify.** `time ./run_tests.sh up_to=unit` (should be ~1× the suite, no `[sq_pg_lo]`/`[sp_pg_lo]` duplicate dispatch lines); `time ./run_tests.sh up_to=db --dialects=pg --targets=lo` (unit runs once, then sp_pg_lo + sq_pg_lo run db in parallel).
    **Out of scope (follow-ups, do NOT bundle):** (a) profiling the 165s itself (`pytest --durations=30` → session-scope the heavy fixtures: `make_test_config`, the per-app tree builders in `tests/json/`, `CliRunner`); (b) `ruff` — assessed, doesn't apply (the repo's lints are project-specific custom AST lints the `_dev` linters do; ruff can't express them, and there's no generic flake8-style lint in the test path to speed up); (c) `ty` (Astral type checker) — track it, not mature enough as of early 2026 to replace the pyright gate (incomplete strict mode, doesn't honor `# pyright: ignore` / `reportExplicitAny` / the `# typing-smell: ignore` convention → false-negative risk), and the gate is only ~2.5s anyway.
- [x] **Y.2.gate.l.7 — Fix the gate.l.1 cold-RDS start race.** (Renamed from `gate.l.5` 2026-05-10 — that number was already taken by the "no auto-stop timer" LOCKED decision below; `l.6` is "App2 + QS parallel at layer 6", so this lands at `l.7`.) The brittle `aws rds start-db-cluster ... || true; aws rds wait db-cluster-available` (20-min default) deadlocked whenever a sibling job's fire-and-forget `stop-db-cluster` left the cluster `stopping`: `start` errors on a `stopping` cluster (swallowed), then `wait` blocks 20 min on an `available` that never comes → `Waiter DBClusterAvailable failed: Max attempts exceeded`. Hit on **v8.8.0a17's E2E run, v8.8.0a18's Release run** (blocked `publish-pypi` → **v8.8.0a18 is on TestPyPI but NOT PyPI**), and **v8.8.0a18's nightly `e2e-pg-browser` cron** (run `25622227173`) — and it was cross-job within `e2e.yml` (`e2e-pg-api`'s own teardown left the cluster `stopping` as the dependent `e2e-pg-browser` started). **Fix shipped:** new composite action `.github/actions/ensure-rds-available` polls the resource's status, only issues `start-db-*` when it's exactly `stopped`, and waits out all transitional states (`stopping` / `starting` / `configuring-*` / `backing-up` / `modifying` / `maintenance` / `upgrading` / `resetting-*`); 30-min cap, no-op when the identifier input is empty. `e2e-pg-api` / `e2e-pg-browser` / `e2e-oracle-api` (e2e.yml) and `e2e-against-testpypi` (release.yml) all use it; per-job teardown removed from `e2e-pg-api` / `e2e-pg-browser` / `e2e-oracle-api` — the sole RDS stop now lives in the final `cleanup-pg` / `cleanup-oracle` jobs (which `needs:` everything + `if: always()`). The existing `e2e-pg` / `e2e-oracle` concurrency groups already cover the cross-workflow case (e2e.yml's PG jobs + release.yml's `e2e-against-testpypi` share `e2e-pg`). **Still TODO:** `gh run rerun 25620387619 --failed` to land v8.8.0a18 on PyPI — or let v8.8.0a19 (Y.2.d+e) supersede it. (On branch `y-2-d-l2ft-chains-pushdown`.)
- [x] **Y.2.app2.cde.core — `_sql_executor` honors dataset-param static defaults.** Reusable core for the App2 side of the Y.2.c/d/e (and existing Y.2.a/b) `<<$param>>` pushdowns: when a URL doesn't supply `?param_pName=...`, `apply_dataset_param_defaults` string-substitutes the QS `DatasetParameter`'s `DefaultValues` (SINGLE_VALUED → `'v'`, MULTI_VALUED → `'a','b','c'`, integer/decimal → unquoted) instead of binding `""` — so a freshly-loaded App2 page matches QS's initial-load render. Threaded through: `build_dataset` registers each dataset's params (`register_dataset_params` / `get_dataset_params` registry in `dataset_contract.py`, keyed by `visual_identifier`); `_tree_fetcher`'s `visual_index` now carries `ds_id` and passes `dataset_parameters=get_dataset_params(ds_id)` to `execute_visual_sql_async`; `execute_visual_sql` / `execute_visual_sql_async` call `apply_dataset_param_defaults` before `translate_qs_dataset_params`. URL-supplied params are LEFT for the bind path (untrusted input never string-spliced); undeclared params + empty static defaults left untouched (avoids `IN ()`). DateTime-RollingDate defaults return `None` (App2 can't evaluate QS rolling-date exprs) — that's the Y.2.f wrinkle, left for then. Tests: `tests/unit/test_html_sql_executor.py` (11 `apply_dataset_param_defaults` cases) + `tests/json/test_dataset_parameters.py::test_build_dataset_registers_params_for_app2_default_substitution`. pyright-clean.
- [x] **Y.2.app2.cde.multivalued — Multi-valued URL-param → `IN (...)` bind expansion in `_sql_executor`.** *(Split out of `.l2ft-wiring` 2026-05-10 — a reusable executor primitive, the missing half of the App2-parity story.)* Shipped: (1) server reads `request.query_params.multi_items()` → `dict[str, list[str]]`; `DataFetcher` contract is now `(VisualId, Mapping[str, list[str]])` (the `list[str]` value, not `Sequence[str]` — `str` is a `Sequence[str]`, so `Mapping[str, Sequence[str]]` would silently accept a stray `{"x": "a"}` and do `"a"[-1]`); (2) new `expand_multivalued_dataset_params` — for a placeholder whose dataset param is `MULTI_VALUED` and the URL supplies 2+ non-empty values, rewrites `<<$pRail>>` → `:param_pRail_0, :param_pRail_1, …` (so `IN (<<$pRail>>)` → `IN (:param_pRail_0, …)`) + per-value `extra_binds`; **bind expansion, never string-splices URL values** (App2 doesn't enforce `StaticValues`); 0 or 1 URL value left alone (0 → static default already applied by `apply_dataset_param_defaults`; 1 → normal `:param_pRail` bind, fine in `IN (...)`); (3) emptied multi-select (`?param_pRail=`) → `_url_has_real_value` returns False → `apply_dataset_param_defaults` falls through to the static default (QS reverts there too — Y.2.c.0); (4) shared `_prepare_sql_and_binds` pipeline in `execute_visual_sql{,_async}`: apply-defaults → expand-multivalued → translate → dialect-rewrite → collect-binds (+ `extra_binds`). `collect_bind_params` takes the LAST value for a scalar `:name` (mirrors the old `query_params.items()` last-wins). Fetcher stubs (`_db_fetcher` / `_smoke_app.stub_money_trail_fetcher`) scalarize at entry (they only read single-valued filters). Tests: 8 new `expand_multivalued_*` / SQLite IN-list end-to-end cases in `test_html_sql_executor.py` + `test_repeated_query_key_lands_as_list_in_fetcher` in `test_html_server.py` + call-site dict-shape updates across the affected unit/e2e tests. `./run_tests.sh up_to=app2 --dialects=pg --targets=lo --scenarios=sp` green; full `tests/unit tests/json tests/cli` green; pyright clean.
- [ ] **Y.2.app2.cde.l2ft-wiring — Wire L2FT into App2 + e2e.** *(`.multivalued` is the prereq — L2FT's c/d/e SQL adds multi-valued `<<$pL2ftRail>>` etc.; shipped.)*
    - [x] **a — serve.py L2FT branch + construct test.** `cli/serve.py --app l2_flow_tracing` now builds `build_all_l2_flow_tracing_datasets(cfg, instance)` → `build_l2_flow_tracing_app(cfg, l2_instance=instance)` → `make_tree_db_fetcher` → `make_app(... filter_specs=())` (mirrors the investigation/executives branch — `if executives / elif investigation / else l2_flow_tracing`). `"l2_flow_tracing"` added to the `--app` `click.Choice`. New `tests/unit/test_html_l2_flow_tracing_wiring.py` (4 cases: 6-sheet count; fetcher builds with no missing SQL; empty payload for unknown visual_id; ≥4 data visuals indexed). L2FT's KPI/Table/BarChart visuals + the `wrap_for_visual` aggregation all already supported by the generic tree fetcher.
    - [ ] **b — Auto-derive the L2FT MULTI_SELECT dropdown UI controls into the App2 filter form.** Currently `serve.py` passes `filter_specs=()` for all tree apps, so the rails/status/bundle (+ chains/templates) dropdowns render in App2's form as nothing — the dataset SQL still uses the declared-value default (Y.2.app2.cde.core), so visuals show *all* rows, but the analyst can't narrow. (Note: this is the same gap investigation/executives have today — their sigma/threshold params have no App2 UI control either. Task #650 "auto-derive App2 filter_specs from tree ParameterControl nodes" appears to not have actually landed a tree-walk → `render.py`'s `_render_filter_form` only renders the explicit `filter_specs` list + the universal date pickers; nothing walks the tree's `ParameterControl`/`ParameterDropdown` nodes. So this sub-task is: build that tree-walk — `make_filter_specs_for_sheet(sheet) -> list[FilterSpec]` that turns a MULTI_SELECT `ParameterDropdown` into a multi-select `<select>` serialising as `?param_pL2ftRail=A&param_pL2ftRail=B` — wire it in `cli/serve.py` for L2FT (and ideally retrofit investigation/exec), and pin `render.py` rendering it. Decide whether to fix #650 properly here or scope it to just L2FT first.
    - [ ] **c — `test_html2_l2ft*` browser e2e.** Rails/Chains/Templates: page renders, dropdowns present, selecting values narrows the table (mirror of `test_l2ft_rails_dropdowns.py` but against the App2 server). Add the new file(s) to the runner's `app2` layer test set (currently `test_html2_executives.py test_html2_executives_live.py test_html2_money_trail.py`).
    - [ ] **d — chain runner verify + commit.** `./run_tests.sh up_to=app2 --dialects=pg --targets=lo --scenarios=sp`.

- [ ] **Y.2.f — L1 universal date range pushdown — PARKED 2026-05-10 (decision: match QS behavior; revisit post-parity).** Pushing the L1 date filter into dataset SQL would force a behavior change or a coexisting-mechanism wart, because **dataset-level `DateTimeDatasetParameter.DefaultValues` only supports `StaticValues`, not `RollingDate`** (unlike the analysis-level `DateTimeParam`, which carries the `addDateTime(-7, 'DD', truncDate('DD', now()))` rolling-7-day default the L1 dashboard opens at). So a `<<$pL1DateStart>>` dataset param can only default to a *fixed* timestamp or a deliberately-wide "all dates" range — its `MappedDataSetParameters` bridge from the analysis param narrows it on interaction (Y.1.k: bridges don't fire on first load), but the *opening* view shifts from "last 7 days" to "all dates". Options surfaced to the user: (a) push down, accept all-dates-on-open (consistent with the Y.2.a/b/c categorical pattern, full App2-parity); (b) push down **+ keep** the analysis-level `TimeRangeFilter` so QS opens at last-7-days as today, dataset SQL is a no-op for QS but enables a future App2 L1 path — zero QS behavior change but two date-filter mechanisms coexist; (c) skip. **User: "match QuickSight's behavior for now, whatever that looks like — once we're at complete feature parity I'll chew on changes."** → **parked.** No App2 L1 path exists yet (`cli/serve.py` serves smoke/executives/investigation only), so there's nothing to converge today; the analysis-level `TimeRangeFilter` family + per-sheet date pickers stay exactly as-is. Revisit when wiring L1 into App2 (and bring the QS-DateTime-param-substitution-shape question + the `posting`-column day-granularity-inclusivity question along — both need a UI sample). The `db`-layer dataset-SQL smoke verifier doesn't currently hit any `<<$pL1Date*>>` placeholder (none added), so no `RollingDate`-eval is needed yet either.
- [ ] **Y.2.g — L1 per-sheet *categorical* filter controls** (Drift / Overdraft / Limit Breach / Today's Exceptions / Daily Statement / Transactions / Pending Aging / Unbundled Aging / Supersession Audit). The per-sheet dropdown/category controls — no rolling-default problem (categorical → `StaticValues` default works), so these follow the clean Y.2.a/b/c pattern (`<<$pName>>` placeholder + `MappedDataSetParameters` bridge, drop the analysis-level `CategoryFilter` FilterGroup). Per-sheet sub-bullet as we hit them.
- [ ] **Y.2.h — Executives datasets review.** Verify whether any Exec sheet has filters that benefit; sweep what does. Likely a thin pass.
    - [ ] **Y.2.h.1 — Re-enable `test_date_filter_narrows_every_date_sensitive_count_kpi` (xfail-marked 2026-05-08, runner gate.h.6 chain validation).** Two KPIs on Account Coverage failed: 'Total Open Accounts' (snapshot count — date filter only narrows the inner activity CTE, not the outer accounts CTE that drives the COUNT) and 'Active Accounts' (depends on a visual-pinned filter `activity_count > 0` that App2 doesn't apply yet). Both narrow correctly in QS; App2 will narrow correctly once Y.2 pushes the predicates down into dataset SQL (eliminates the visual-pinned-filter dependency for App2). Drop the `@pytest.mark.xfail` decorator after Y.2 lands and verify the chain stays green.

### Y.2.gate — Test layer chain audit + runner (HARD prereq for Y.2.c+)

**Why.** Y.2.b shipped a SQL bug that the e2e suite let through: the smoke verifier was a CLI script (`tests/integration/verify_dataset_sql.py`), not pytest-collected, so a deployed `UndefinedColumn` exception got past API + browser e2e. We patched that one gap (`tests/e2e/test_dataset_sql_smoke.py`) but the broader meta-concern stands: **there's no script that chains the test layers cheap → expensive in sequence**, so each layer needs to be invoked manually and we WILL forget. Re-running cheap tests is always cheaper than discovered-after-deploy rework.

**The chain (current understanding — confirm in the audit).**

| Layer | What | Currently auto-hooked? |
|---|---|---|
| 1 | pyright | ✓ via pytest sessionstart (M.1.9c) |
| 2 | unit + JSON tests (`tests/{unit,json}`) | ✓ via `pytest` |
| 3a | DB SQL smoke — `tests/e2e/test_dataset_sql_smoke.py` (Y.2.b hotfix) | ✓ via `pytest` behind `QS_GEN_E2E=1` |
| 3b | DB matview row-counts — `verify_demo_apply.py` | ✗ CLI only |
| 4 | deploy — `quicksight-gen json apply --execute` | ✗ manual |
| 5 | API e2e — `test_*_deployed_resources.py` etc. | ✓ via `pytest` behind `QS_GEN_E2E=1` |
| 6 | Browser e2e — `test_*_renders.py` etc. | ✓ via `pytest` behind `QS_GEN_E2E=1` + `QS_E2E_USER_ARN` |
| 7a | JS unit tests (`tests/js/`) — biome / playwright JS-side | ✗ separate runner |
| 7b | App2 e2e (`test_html2_*.py`) | ✓ via `pytest` behind `QS_GEN_E2E=1` |

The contract: invoking layer N implies layers 1..N-1 must be green. Today's scripts (`pytest`, `./run_e2e.sh`) only partially compose the chain.

#### Steps

- [x] **Y.2.gate.a — Audit pass (user-led, Claude assists).** Walk every test file + script and place each one on the layer table above. Surface (1) what's pytest-collected vs. CLI-only, (2) what's gated behind which env vars, (3) what preconditions each layer assumes (e.g., DB smoke needs fresh `demo apply`; deploy needs DB schema), (4) any layers I've missed (e.g., docs-build smoke, mkdocs-strict, biome). **Initial draft landed at `docs/audits/y_2_gate_test_layer_chain_audit.md`** for user review/iteration. When locked, moves to `src/quicksight_gen/docs/reference/test-layer-chain.md`. The audit doc is the spec for Y.2.gate.b–g.
- [ ] **Y.2.gate.b — Design the runner.** Spike (b.0) returned. Substrate locked: pytest-as-orchestrator + ~400 LOC Python wrapper at `quicksight_gen/_dev/runner.py`, surfaced via `./run_tests.sh` bash shim. Each design item below tracks against that substrate.
    - [x] **Y.2.gate.b.0 — SPIKE: runner-implementation language evaluation (LOCKED 2026-05-07).** Output: `docs/audits/y_2_gate_b_0_runner_lang_spike.md`. Compared 6 candidates (pure bash + jq, pure Python, `just`, pytest-as-orchestrator + thin Python wrapper, `nox`/`tox`/`mise`, Click subcommand) against the §7.3 constraint set. **Recommendation accepted**: pytest-as-orchestrator + ~400 LOC Python wrapper at `quicksight_gen/_dev/runner.py`, surfaced via `./run_tests.sh` bash shim. Locks audit §7.3.
    - [x] **Y.2.gate.b.1 — Variant axis catalog (LOCKED 2026-05-07).** Axes: **Dialect** (PG / Oracle / SQLite), **L2 instance** (`spec_example` / `sasquatch_pr` / future), **Fuzz seed** (first-class, default 1/run, opt-in `--fuzz-seeds=N`), **Browser e2e target** (`qs | app2` at merged 6/7). Under the locked substrate: each axis becomes a pytest fixture parametrization or marker (`@pytest.mark.layer3a`, `--variant=pg`, `--fuzz-seed=N`). The orchestrator's job is to dispatch the right (markers × variants × seeds) cross-product per layer; pytest does the per-cell selection.
    - [ ] **Y.2.gate.b.2 — Container-backed isolation for local-DB variants** (PG arm LOCKED 2026-05-07; Oracle + SQLite arms pending).
        - [x] **b.2.design — LOCKED 2026-05-07.** Per-variant Docker containers via `testcontainers-python`. Under the locked substrate: each container is a session-scoped pytest fixture parametrized over dialect; the orchestrator dispatches per-variant pytest subprocesses, and within each subprocess the fixture brings up its container. Aurora live cell reserved for layer 4+. Audit §7.5. **Architecture revision (2026-05-07):** during impl, found cleaner to drive container lifecycle from the runner (orchestrator) rather than a pytest fixture — module-level `_CFG = load_config(...)` in `tests/e2e/test_dataset_sql_smoke.py` makes pytest-fixture-based override fragile. Runner spins up container, threads `QS_GEN_DEMO_DATABASE_URL` env to the subprocess, tears down after.
        - [x] **b.2.impl — Plumbing: variant axis + container lifecycle + per-variant env (LOCKED 2026-05-07).** `testcontainers[postgres]>=3.7` in dev deps (Oracle's container is multi-GB; pull when needed). `KNOWN_VARIANTS = ("default", "local-pg")`; `resolve_variants` parses `--variants=<csv>` (fail-loud on unknown). `setup_variant`/`teardown_variant` manage container lifecycle: `local-pg` spins up a `postgres:17-alpine` container via testcontainers-python, returns its connection URL as `QS_GEN_DEMO_DATABASE_URL`. `dispatch_layer` accepts `variant_env` kwarg; `cmd_up_to` sets it up once at start of chain, threads to DB-touching layers only (`DB_TOUCHING_LAYERS = ("db", "deploy", "api", "browser")` — unit doesn't need or want it). Cache markers are variant-aware (per-SHA × per-layer × per-variant filename). Multi-variant fan-out (running both `default` + `local-pg` in one invocation) explicitly errors → `c.6.async` lands that. **Live-verified**: `./run_tests.sh up_to=db --variants=local-pg` spins up container (`runner: variant-env [QS_GEN_DEMO_DATABASE_URL]=postgresql+psycopg2://test:test@localhost:60455/test`), tears down clean. 12 new unit tests; 128/128 unit pass; pyright clean.
        - [x] **b.2.impl.schema — Variant schema-apply step (LOCKED 2026-05-07; PG arm only).** `seed_variant(name, env_overrides)` in `runner.py` runs three CLI subprocesses against the variant's container URL — `quicksight-gen schema apply --execute -c <cfg>` + `data apply --execute -c <cfg>` + `data refresh --execute -c <cfg>` — in dependency order (schema creates tables + matviews; data populates source tables; refresh populates matviews from source tables). `cmd_up_to` calls it inside its existing try/finally so a seed failure still hits `teardown_variant`; gated on "any DB-touching layer in chain" so unit-only chains skip the ~30s seed cost. Cfg discovery: `QS_GEN_CONFIG` env override > `run/config.postgres.yaml`; `run/config.yaml` is intentionally skipped because it may be Oracle-flavored. L2 instance honors `QS_GEN_TEST_L2_INSTANCE` env; absent that, the CLI defaults to bundled `spec_example`. **URL normalization quirk** — testcontainers-python returns SQLAlchemy-style URLs (`postgresql+psycopg2://...`) but `connect_demo_db` uses psycopg3, which rejects the `+psycopg2` driver suffix with `missing "=" after "..."` from libpq's conninfo parser. `_normalize_pg_url` strips the suffix; idempotent on already-clean URLs. **Live-verified** end-to-end: `./run_tests.sh up_to=db --variants=local-pg` spins up `postgres:17-alpine`, normalizes URL to `postgresql://test:test@localhost:60517/test`, runs all three seed steps, then dispatches db layer (37/37 dataset SQL smoke tests pass in 2.23s), then tears down clean. 14 new unit tests (9 `seed_variant` + cfg discovery + 2 URL normalization + 4 `cmd_up_to` wiring); 144/144 unit pass; pyright clean. **Scope note**: this is the PG arm only — `b.2.impl.oracle` + `b.2.impl.sqlite` track the other two dialects.
        - [x] **b.2.impl.oracle — local-oracle variant (LOCKED 2026-05-08).** Mirrors local-pg: `"local-oracle"` in `KNOWN_VARIANTS`; `setup_variant` arm spins up `gvenzl/oracle-free:23-faststart` testcontainer (~20-30s startup vs. multi-min on :slim — the heavier image's pre-warm dominates the test-loop economics); `seed_variant` Oracle arm points at `run/config.oracle.yaml` via the new `_resolve_seed_config_for_local_oracle`. Same 3 CLI subprocess pattern (schema apply + data apply + data refresh) + same env-threading + try/finally lifecycle. **URL normalization**: NOT needed — `oracle_dsn()` in `common/db.py` already accepts both the SQLAlchemy form (`oracle+oracledb://...`) the testcontainer returns and the native `user/pass@host:port/SVC` form the cfg uses. `_resolve_seed_config` factored to a generic helper taking the candidates tuple; PG + Oracle are thin wrappers. `testcontainers[postgres,oracle]>=3.7` extra in dev deps. **`QS_GEN_CONFIG` threaded into layer subprocess env**: `cmd_up_to` now resolves the variant's cfg via `_resolve_seed_config_for_variant(variant)` after `setup_variant`, injects it into `variant_env`, so DB-touching tests (e.g. `tests/e2e/test_dataset_sql_smoke.py::_load_cfg`) load the dialect-matching cfg automatically. Without this, the smoke test loaded the default PG cfg with the Oracle env URL → `psycopg.ProgrammingError: invalid connection option "oracle+oracledb://"`. **Per-layer subprocess capture** (paired): `dispatch_layer` switched from `subprocess.run` (no output captured) to `subprocess.Popen` + threaded tee; persists `cmd.json` + `stdout.log` + `stderr.log` per layer. Failures in CI / hands-off runs now leave a complete trail in `runs/<id>/<layer>/`. Recursion guard added: refuses to spawn pytest from inside pytest with the real `Popen` class — fail-loud message names the fix (mock `Popen` via `_fake_popen_factory` or `_layer_command` via monkeypatch). **Live-verified** end-to-end on 2026-05-08: `./run_tests.sh up_to db --variants local-oracle` — Oracle container starts (~25s), 3 seed steps run green, db layer dispatches 37/37 dataset SQL smoke tests pass in 0.40s against the live container, container tears down clean. Total wall-clock ~3 min (image pull + start + seed + tests). 1974 unit pass; pyright clean. **Acceptance met** (PG arm parity); `./run_tests.sh up_to app2 --variants local-oracle` extension lands implicitly — `app2` is in `DB_TOUCHING_LAYERS` so the same env threading already applies. **Followup carried**: `b.2.impl.sqlite` still BLOCKED on X.3.a-d (SQLite dialect plumbing).
        - [x] **b.2.impl.sqlite — local-sqlite variant (no Docker) (LOCKED 2026-05-08).** Lands as the third dialect arm: `"local-sqlite"` in `KNOWN_VARIANTS`. Different shape from local-pg / local-oracle — no testcontainer, no Docker. `setup_variant("local-sqlite")` calls `_setup_local_sqlite()`: creates a temp dir via `tempfile.mkdtemp(prefix="qs-gen-sqlite-")`, generates an empty SQLite file + a minimal per-invocation cfg (`dialect: sqlite` + `demo_database_url: sqlite:///<tmpdb>` + placeholder `aws_account_id` / `aws_region`), returns BOTH env overrides (`QS_GEN_DEMO_DATABASE_URL` + `QS_GEN_CONFIG`) so layer subprocesses load the right cfg + connect to the right file. Teardown handle is a custom `_SqliteHandle` with a duck-typed `.stop()` method that unlinks both files (mirrors the testcontainer `.stop()` shape `teardown_variant` already calls). `seed_variant` arm reads cfg path from `env_overrides[QS_GEN_CONFIG]` (no `run/config.sqlite.yaml` — by-design ephemeral per-invocation). Fast-feedback path (~21s wall: 18s unit + 2s db on local SQLite, including in-memory schema + 864 transactions + matview refresh). **Three failures surfaced + fixed during live verify:** (1) `tests/integration/verify_dataset_sql.py` used `with conn.cursor() as cur:` — sqlite3.Cursor doesn't implement the context-manager protocol; switched to manual try/finally + `cur.close()` (works on all 3 dialects). (2) `common/sheets/app_info.py::_liveness_sql` had Oracle's `USER_TABLES` for the non-PG branch; added explicit SQLite branch reading `sqlite_master WHERE type='table'`. (3) Two bigger gaps in L2FT dataset SQL needed dialect helpers — added `json_value(col, path_expr, dialect)` (PG/Oracle: `JSON_VALUE(...)`; SQLite: `json_extract(...)` via JSON1) + `greatest(*args, dialect)` (PG/Oracle: `GREATEST(...)`; SQLite: `MAX(...)` overloaded as scalar with 2+ args). Swept `metadata_filter_clause` (4 call sites) + `build_meta_values_dataset` + `_dead_metadata_check_fragments` (4 GREATEST sites across `build_chain_orphans_dataset` / `build_chain_instances_dataset` / `build_unified_l2_exceptions_dataset`). **Live-verified 2026-05-08**: `./run_tests.sh up_to db --variants local-pg,local-oracle,local-sqlite` boots all 3 in parallel via `c.6.async` fan-out (~30s wall — Oracle is the bottleneck), 1979 unit + 37 db green per variant, full three-dialect parity at the db layer.
    - [ ] **Y.2.gate.b.3 — App2-as-early-gate chain placement** (DESIGN LOCKED 2026-05-07; IMPL split into two pieces; layer is variant-aware so dialect spread is automatic).
        - [x] **b.3.design — LOCKED 2026-05-07.** Layer 3.7 = App2 against local Docker. At merged 6/7: App2 + QS run in parallel with fast-fail. Under the locked substrate: orchestrator's `asyncio.gather` over `[target=app2, target=qs]` at layer 6/7 with `return_exceptions=False` (first failure cancels siblings). Audit §7.10. Strategic ownership in X.2.j.
        - [x] **b.3.impl.layer — Add app2 to LAYERS + dispatch test_html2 against variant DB (LOCKED 2026-05-07).** Inserted `"app2"` between `"db"` and `"deploy"` in `runner.LAYERS`. Added to `DB_TOUCHING_LAYERS` so the variant URL flows through. `_LAYER_DEPS["app2"] = frozenset({"docker"})` — needs Docker for the variant container, NO AWS (App2 is local-only by design — audit §7.10 LOCKED). `_layer_command` arm runs `pytest tests/e2e/test_html2_*.py -q` with `QS_GEN_E2E=1` env. Three test files: `test_html2_executives.py` (stub fetcher), `test_html2_executives_live.py` (live DB fetcher), `test_html2_money_trail.py` (stub). The live test honors `QS_GEN_DEMO_DATABASE_URL` env via `connect_demo_db(cfg)`, so the variant URL flows naturally. **Variant-aware by construction**: once added to `DB_TOUCHING_LAYERS`, the layer automatically supports `--variants=local-pg|local-oracle|local-sqlite` — per-dialect arms just become verification (see b.2.impl.oracle / b.2.impl.sqlite acceptance updates). 6 new unit tests + 4 existing tests updated for new LAYERS shape; 149/149 unit pass; pyright clean. **Live-verified** end-to-end: `./run_tests.sh up_to=app2 --variants=local-pg` spins up postgres:17-alpine, seeds via b.2.impl.schema (~30s), runs unit (15.91s OK) → db (2.27s OK) → app2 (103s; 6 pass + 3 fail in money_trail), tears down clean. Wall-clock total: ~150s. **Pre-existing failures surfaced**: 3 money_trail tests fail identically against operator's Aurora — pre-existing breakage that lagged because nightly cron only. Triage tracked at `X.2.h.6`. **c.11 gap surfaced**: trace artifacts didn't land under `runs/<id>/browser/` — c.11's hook is QS-path-only; App2 path needs separate wrap. Tracked at `Y.2.gate.c.11.app2`.
        - [ ] **b.3.impl.gather — asyncio.gather at merged 6/7 (target=app2|qs).** Separate concern from b.3.impl.layer: at the AWS-browser layer (layers 6+7 merge per audit §7.7), run `target=app2` + `target=qs` in parallel via `asyncio.gather(*tasks, return_exceptions=False)` for fast-fail. App2 at 6/7 is the parity-against-deploy cell (different from layer 3.7 which is against local Docker). Likely overlaps with `c.6.async` (cross-variant gather); decide ownership when both are ready to land. Lower priority than `b.3.impl.layer` — layer 3.7 is the fast-feedback gate that earns its keep on every iteration; the 6/7 gather is a deploy-time optimization.
    - [x] **Y.2.gate.b.4 — Per-run output isolation shape (LOCKED 2026-05-07).** All artifacts under `runs/<run-id>/...`; run-id = `<utc-ts>-<short-sha>[-dirty]`. Under the locked substrate: orchestrator creates the dir; passes its path as env var (`QS_GEN_RUN_DIR`) to every pytest subprocess; conftest fixtures route screenshots / failure manifests / browser traces / L2 YAML copies under it. Last N=20 auto-pruned. `runs/` gitignored.
    - [x] **Y.2.gate.b.5 — Timing + hash capture/diff design (LOCKED 2026-05-07).** Single-source-of-drift in `runs/<run-id>/{timings,hashes}.json`. Under the locked substrate: per-test timings via `pytest_runtest_protocol` hook in conftest writing to `$QS_GEN_RUN_DIR/timings.json`; per-variant hash captures via session-scope fixture; orchestrator merges per-variant JSON files post-run and runs the drift-diff. ⚠ threshold ±50%. Hash-lock collapses into this loop (lands with `Y.2.gate.c.13`).
    - [x] **Y.2.gate.b.6 — CI artifact upload (LOCKED 2026-05-07).** Per-CI-job `timings.json` upload. Under the locked substrate: GH workflows wrap `./run_tests.sh ...` and upload the `runs/<run-id>/` dir; same shape as existing `coverage-data-py3.13` / `e2e-pg-api-top-queries-${run_id}` artifacts.
    - [x] **Y.2.gate.b.7 — Stop-on-first-failure semantics (LOCKED).** No layer N+1 if N failed. Per-variant within a layer = roll-up (one variant failing doesn't cancel its siblings; the layer fails as a whole if any cell red). Under the locked substrate: orchestrator awaits all per-variant tasks at layer N; if any returned non-zero, no `dispatch_layer(N+1)` call. App2 + QS at 6/7 are the exception (fast-fail siblings, per b.3).
    - [x] **Y.2.gate.b.8 — Skip-if-already-green policy (LOCKED 2026-05-07; design + impl).**
        - [x] **b.8.design — LOCKED.** Default re-run; opt-in `--skip-cheap`. Under the locked substrate: orchestrator's pre-dispatch check reads `.run_tests_cache/<sha>.json`; on `--skip-cheap`, layers 1-2 skip if green for the current SHA.
        - [x] **b.8.impl — Cache helpers + cmd_up_to wiring (LOCKED 2026-05-07).** Marker file at `.run_tests_cache/<sha>.<layer>.json` (per-SHA per-layer). `write_cache_marker` records green passes; `is_layer_cached_green` reads + sanity-checks. `SKIPPABLE_LAYERS = ("unit", "db")` is the contract — heavy layers (deploy/api/browser) are per-run by nature and explicitly excluded. Defensive: dirty SHA / no git / SHA mismatch / non-skippable layer all degrade to "run normally". `.run_tests_cache/` gitignored. 9 unit tests; live-verified: clean-SHA second run with `--skip-cheap` short-circuits with `runner: layer-cached [unit] skipped` (no pytest invocation).
    - [x] **Y.2.gate.b.9 — Cross-layer parallelism rules (LOCKED).** Cross-layer = sequential. Within-variant = xdist. Cross-variant within layer = `asyncio.gather`. App2 + QS at 6/7 = parallel with fast-fail.
    - [x] **Y.2.gate.b.10 — Dirty-state detection on deploy (LOCKED 2026-05-07).** Boundary at layer 4: any `up_to=<layer≥4>` invocation refuses if **tracked changes** exist. Implementation: orchestrator pre-flight runs `git diff --quiet HEAD --`; exit code 1 → dirty → refuse with code 2 (= needs-operator) and message `"refusing to deploy: tracked changes present (commit or stash, or pass --allow-dirty-deploy)"`. **Tracked-only** (not `git status --porcelain`) — untracked files are usually scratch / new-file mid-edit, not deploy-blockers. Override: `--allow-dirty-deploy` flag (also available via `QS_GEN_RUNNER_YES=1` env per the `b.14.3` destructive-op convention). The `[-dirty]` run-id suffix (per `Y.2.gate.b.4`) is the soft warning for layers 1-3; the layer-4 refusal is the hard escalation.
    - [x] **Y.2.gate.b.11 — Variant-to-layer enumeration confirmed (LOCKED 2026-05-07).** Audit doc §3 (variant axes table) is complete + canonical: rows for Python 3.13, Dialect (PG / Oracle / SQLite), L2 instance (spec_example / sasquatch_pr), Fuzz seed (today vs target). Single-source-of-truth resolution: **hard-coded Python dispatch table in `quicksight_gen/_dev/runner.py` is the runtime authority; audit §3 is the documented mirror**. Cross-check sub-task lands as part of `Y.2.gate.c`: a unit test asserts a small canonical sample of `(layer, variant)` cells exists in both places — catches "I edited one and forgot the other" drift without going full markdown-parser. (Earlier draft mentioned "parsed from audit table or hard-coded mirror — decide during implementation"; the decision lands here.)
    - [x] **Y.2.gate.b.12 — Pytest version + JSON-hook plugin confirmation (LOCKED 2026-05-07).** Custom conftest hook is sufficient; **no new dependency**. Use `pytest_runtest_makereport(item, call)` to capture `call.duration` per test (correction from earlier `pytest_runtest_protocol`-mention; that hook doesn't expose duration). `pytest_sessionstart` / `pytest_sessionfinish` for session-scope metadata. `pytest>=7.0` pinned in `[dev]` + `[e2e]` already supports both. `pytest-xdist>=3.5` already pinned. Resolution captured in spike doc §6.
    - [x] **Y.2.gate.b.13 — `_dev/` package shape lock-in (LOCKED 2026-05-07).** `src/quicksight_gen/_dev/runner.py` with `[tool.setuptools.packages.find] exclude = ["quicksight_gen._dev", "quicksight_gen._dev.*"]` added to `pyproject.toml`. Wheel-build verification step lands in `Y.2.gate.c` (CI assertion: `pip wheel . && unzip -l ...whl | grep _dev` returns no matches — mirrors `docs-portable-install` shape). Resolution captured in spike doc §6.
    - [ ] **Y.2.gate.b.14 — Runner ergonomics for Claude-agent loops** (re-opened 2026-05-07; the bash-shim shape was the easy half; the harder half is making sure Claude can loop on the runner without thrashing the operator's `proceed?` permission prompt). The runner becomes the single Claude-Code Bash-tool boundary so an always-allow rule on `./run_tests.sh*` covers every test-loop invocation; **internal sub-tools (Docker, AWS, pytest) must run as subprocesses of the runner, never as separate Bash calls Claude reaches for directly**.
        - [x] **Y.2.gate.b.14.1 — Bash shim source (LOCKED 2026-05-07).** ~12-line bash; venv check + `exec .venv/bin/python -m quicksight_gen._dev.runner "$@"`. SIGINT propagates via `exec`; no trap. No arg parsing in bash. Full source in spike doc §6.
        - [ ] **Y.2.gate.b.14.2 — Stable invocation pattern for always-allow.** Lock the canonical shape so the operator's `.claude/settings.json` (or session approve-all) can pattern-match cleanly: every invocation starts with `./run_tests.sh <verb> [...]` (no `python -m ...`, no direct `pytest ...`, no direct `aws ...`, no direct `docker ...` from Claude). Verbs: `up_to`, `up`, `down`, `status`, `sweep`. The orchestrator absorbs every sub-tool inside.
        - [x] **Y.2.gate.b.14.3 — Destructive-op opt-in within the orchestrator (LOCKED 2026-05-07).** `down` and `sweep` require `--yes` flag (or `QS_GEN_RUNNER_YES=1` env); without it, refuses with `EXIT_NEEDS_OPERATOR` (=2) and a clear message. Idempotent reads (`up_to=<layer>`, `status`, `up`) pass through. Locked by `tests/unit/test_runner_skeleton.py::test_destructive_down_refuses_without_yes` + `_sweep_`. Future destructive ops (e.g. cluster create/delete) inherit the same gate.
        - [ ] **Y.2.gate.b.14.4 — Interactive-command refusal pattern** (so Claude doesn't hang on `aws sso login` browser flow). Orchestrator detects interactive-required state (expired SSO creds, missing browser env, etc.); prints `"creds expired — type '! aws sso login' yourself, then re-invoke"`; exits with a stable non-zero code (e.g. 2 = needs-operator). Claude pattern: see exit 2, surface the message to operator, await their `!` invocation. **This refines `Y.2.gate.i.1`** — replace "refresh on ExpiredTokenException" with "detect + surface message"; `aws sso login` itself is never auto-invoked.
        - [ ] **Y.2.gate.b.14.5 — Document the always-allow pattern in CLAUDE.md.** New section under "Test sequencing": which `./run_tests.sh <verb>` invocations are safe to blanket-approve (`up_to=*`, `status`, `up`), which need the operator in the loop (`down`, `sweep`). Suggested `.claude/settings.json` snippet showing the pattern. Cross-references `feedback_aws_deploys_freely.md` (AWS deploys already pre-approved) — same shape, broader scope.
    - [ ] **Y.2.gate.b.15 — Typed `EnvVar` registry + AST lint (DESIGN LOCKED 2026-05-07).** Smell surfaced during `b.2.impl.schema`: env-var name strings spreading across `runner.py`, `config.py`, `tests/e2e/conftest.py`, `common/browser/helpers.py`, `_dev/perf.py`, `common/l2/loader.py`, `tests/conftest.py`, `_harness_html2.py`. Bare `Final` constants only catch typos; the harder dimensions — required-vs-optional + value validation — surfaced in design discussion. **Split into `b.15.spec` (registry + sweep) + `b.15.lint` (AST guard against bypass)** so the keystone lands clean before the lint locks it in.
        - [x] **b.15.spec — Typed `EnvVar` registry + sweep call sites (LOCKED 2026-05-07).** Module at `src/quicksight_gen/common/env_keys.py` (NOT `_dev/` — `_dev/` is excluded from the wheel; runtime code in `config.py` etc. needs the imports). Typed `EnvVar[T]` dataclass (frozen) per env var, declaring `name + description + coercer + optional + validator`. API: `.get_or_none() -> T | None`, `.require() -> T` (raises `EnvVarRequired` with description), `.serialize(value: T) -> str` (for set-in-subprocess-env). Validator runs in BOTH directions (get + serialize) so set-side bugs (runner forgot to mkdir before setting `QS_GEN_RUN_DIR`) AND get-side bugs (operator's stale `QS_GEN_CONFIG`) both surface at the boundary with `EnvVarInvalid` carrying name + description. Reusable validators: `must_exist` / `must_be_file` / `must_be_dir` / `positive_int` / `matches(pattern)`. Concrete map: DB URLs → no validator (psycopg/oracledb errors are already actionable); paths → `must_be_file` or `must_be_dir`; ints → `positive_int`; ARNs → `matches(IAM_ARN_RE)`; bools → `bool` coercer (any non-empty = on, mirrors existing `bool(os.environ.get(...))` semantics). Test surface: unit tests on EnvVar primitives + each validator; existing `monkeypatch.setenv` calls in tests use `EnvVar.name`. **Acceptance**: (1) every env-var access in `src/` and `tests/` goes through an `EnvVar` instance; (2) every path-shaped EnvVar carries a `must_exist`/`must_be_file`/`must_be_dir` validator; (3) failed validation raises `EnvVarInvalid` whose message names both the env var and its description.
    - [ ] **Y.2.gate.b.15.lint — AST lint family catching convention bypasses (DESIGN LOCKED 2026-05-07).** Same shape as `X.2.o.5` (custom AST lint pattern already in the codebase). Each sub-task: ~30 LOC; walks Python AST under `src/` + `tests/`; runs as a unit test in `test_typing_smells.py` (or sibling); fails loudly on convention violations with an actionable message. **Universal acceptance pattern (applies to every sub-task):** (1) lint test green; (2) deliberate-regression test (insert a bare violation in a scratch branch, lint catches it with a clear message); **(3) trim the convention prose from CLAUDE.md** — replace with a one-line cross-reference (`"Enforced by tests/unit/test_<name>.py."`). **CLAUDE.md compounding principle:** CLAUDE.md is loaded into every Claude session; every lint that lands shrinks CLAUDE.md, frees context budget, and removes a rule I have to mentally enforce. Sub-tasks ordered roughly by value-per-effort.
        - [x] **b.15.lint.envvar — Bypass of the `EnvVar` registry (LOCKED 2026-05-08).** Added `EnvVarBypassCheck` to `tests/unit/test_typing_smells.py` walking AST for `os.environ.get/[]/setdefault`, `os.getenv`, `monkeypatch.setenv/delenv` calls with `QS_GEN_/QS_E2E_` literals. Subscript form (`os.environ["QS_GEN_X"]`) caught too. Allowlist via path filter on `env_keys.py` + `test_env_keys.py`. **Surfaced 7 cfg-shaped env vars hidden by indirection** (config.py held `{"aws_account_id": "QS_GEN_AWS_ACCOUNT_ID"}` etc. — string-literal *values* in a dict, accessed via Name not Constant — bypassed the original sweep). Added registry specs for QS_GEN_AWS_ACCOUNT_ID (12-digit regex), QS_GEN_AWS_REGION (us-east-1 regex), QS_GEN_DATASOURCE_ARN (IAM ARN regex), QS_GEN_RESOURCE_PREFIX, QS_GEN_DIALECT (postgres/oracle/sqlite literal), QS_GEN_APP2_DB_POOL_SIZE; refactored config.py's env_map to hold spec instances directly. One legitimate test fixture site (test_dashboard_extract.py: `os.environ.setdefault` BEFORE load_config runs, where setdefault semantics + fake test ARN are intentional) suppressed via `# typing-smell: ignore[envvar-bypass]: <reason>`. CLAUDE.md trim: no prose to remove (env-var convention was implicit; lint failure message is the source of truth going forward). Lint test green; 1147 unit pass; pyright clean.
        - [x] **b.15.lint.why-comment — Bare `# type:ignore` / `# pyright:ignore` / `# typing-smell:ignore` without a `WHY` reason (LOCKED 2026-05-07).** Comment scan (not AST), regex `#\s*(type|pyright|typing-smell):\s*ignore(\[[^\]]+\])?` requiring `:` followed by 3+ words on the same line. Escape hatch: `# bare-suppression-ok` magic comment for the rare cases (currently zero in-tree). Added `WhyCommentCheck` to `tests/unit/test_typing_smells.py` (~95 LOC including helper). **Swept 174 sites across 44 files** (158 from initial bare grep + 16 sites where the existing inline `# Hint` comment didn't qualify as a `:`-prefixed reason). Most-common categories: `arg-type` bare-literal IDs in apps/* (32, all "bare literal pending typed-ID wrap (X.2.o.3 sweep deferred)"); `no-untyped-def` on Click decorators (~25); `no-untyped-def` on cfg/instance untyped params in audit CLI (~20); `no-untyped-def` on reportlab styles/theme params in audit/pdf.py (~30); `attr-defined`/`union-attr` on narrowed shapes (~20). Whitelisted: `tests/unit/test_typing_smells.py` (its docstring shows example syntax that would self-flag) + `tests/unit/test_env_keys.py` (defensive parity with envvar-bypass). Deliberate-regression test confirms lint catches a fresh bare suppression with the actionable error message. CLAUDE.md trim: no convention prose currently in CLAUDE.md for this rule (the lint failure message is the source of truth going forward). All 1147 unit tests + 2298 wider tests pass; pyright clean.
        - [x] **b.15.lint.determinism — Module-level `random.X()` / `random.Random()` (no seed) in seed-generating modules (LOCKED 2026-05-08).** Added `DeterminismCheck` to `tests/unit/test_typing_smells.py` (~75 LOC). Flags `random.{random,randint,choice,shuffle,uniform,sample,randrange,getrandbits,choices,triangular,...}` calls (module-level random helpers reading the unseeded global state) AND `random.Random()` with no positional arg, in scoped files: `common/l2/seed.py`, `common/l2/auto_scenario.py`, `apps/**`. Existing code already correctly uses `rng = random.Random(seed); rng.X(...)` everywhere in scope — lint lands green from the start as a regression guard, not a cleanup task. Deliberate-regression test confirms catch with actionable message: `"random.shuffle(...) reads the unseeded global random state — different runs produce different output, breaking hash-locked seeds + fuzz-seed reproducibility. Use rng = random.Random(<seed>); rng.shuffle(...) instead"`. CLAUDE.md trim: the "Generators must stay deterministic (`random.Random(42)`)" line in CLAUDE.md "Demo Data Conventions" section can drop the prose now that the lint enforces it. Effort: ~75 LOC.
        - [x] **b.15.lint.boto3-direct — `boto3.client(...)` outside the 4 known wrappers (LOCKED 2026-05-08).** `Boto3DirectCheck` added to `tests/unit/test_typing_smells.py` (~50 LOC). Flags `Call(func=Attribute(value=Name("boto3"), attr="client"))`. Scope: `src/quicksight_gen/` only (tests can use boto3.client freely). Allowlist via path filter: `common/deploy.py`, `common/cleanup.py`, `common/browser/helpers.py`, `_dev/runner.py`. **Audit's predicted 5th allowlist file (`common/datasource.py`) doesn't actually contain a boto3.client call** — production allowlist is 4. Lint lands green from the start (regression guard). CLAUDE.md trim: no prose to remove (the tagging convention is implicit; lint failure message is the source of truth going forward). Effort: ~50 LOC.
        - [x] **b.15.lint.qs-gen-prefix — Hardcoded `qs-gen-` prefix outside `Config.prefixed()` (LOCKED 2026-05-08).** `QsGenPrefixCheck` added to `tests/unit/test_typing_smells.py` (~70 LOC including the docstring-skip helper `_docstring_node_ids`). Walks `Constant(value=str)` nodes; flags any matching `r"^qs-gen[\-_]"` outside `common/config.py`. **Docstrings ignored** (the only existing hit was an example string in `common/tree/structure.py:932`'s docstring). Helper `_docstring_node_ids` collects `id()` of Constant nodes that are body[0] of Module/ClassDef/FunctionDef — reusable by future pure-string lints. Lint lands green (regression guard, not cleanup). CLAUDE.md trim: the resource-ID convention prose can drop the implementation detail "use `cfg.prefixed(name)`" since the lint now enforces it.
        - [x] **b.15.lint.no-datetime-now — `datetime.now()` / `date.today()` in determinism-sensitive modules (LOCKED 2026-05-08).** `NoDatetimeNowCheck` added to `tests/unit/test_typing_smells.py` (~70 LOC). Flags `datetime.now(...)`, `datetime.utcnow(...)`, `date.today(...)` calls in `common/l2/seed.py` + `common/l2/auto_scenario.py` + `apps/`. Allowlist via path filter: `_dev/runner.py`, `cli/audit/`, `common/sheets/app_info.py`, `common/provenance.py`, `common/pdf/audit_chrome.py`. **3 legitimate ad-hoc-fallback sites** in seed.py (default_factory + anchor) + auto_scenario.py (today_ref) suppressed per-line: each is the `if X is None: X = datetime.now(...)` shape where tests + locked seeds + CLI always pass an explicit anchor (`date(2030, 1, 1)`); the fallback exists only for hand-typed REPL invocations. Deliberate-regression confirms catch with actionable message pointing at the anchor convention. CLAUDE.md trim: none — the "Anchor pinned at date(2030, 1, 1)" line is project fact, not a rule the lint replaces. Effort: ~70 LOC.
        - [x] **b.15.lint.no-sleep — `time.sleep(...)` in `tests/e2e/` (LOCKED 2026-05-08).** `NoSleepCheck` added to `tests/unit/test_typing_smells.py` (~50 LOC). Flags `time.sleep(...)` + `asyncio.sleep(...)` calls under `tests/e2e/`. **Surfaced 6 sites:** 1 legit (`_harness_html2.py:199` — 50ms uvicorn-startup poll, `server.started` has no awaitable) suppressed with reason; 5 known flakes in `tests/e2e/test_l2ft_*.py` (chains_dropdowns:108, metadata_cascade:119+146, rails_dropdowns:117, templates_dropdowns:116) suppressed with reason pointing at follow-up `b.15.followup.l2ft-no-sleep`. Each L2FT sleep is 2-5s gating a "table not empty after dropdown pick" assertion — convertible to `wait_for_function` polls. CLAUDE.md trim: the "Filter / drill-down assertions poll for the visual state to change rather than sleeping" line stays as dev-doc framing; lint enforces the actual rule. Effort: ~50 LOC.
        - [x] **b.15.invariant.sheet-description — `Sheet`/`KPI`/`Table`/etc. require description+subtitle in `__post_init__` (LOCKED 2026-05-08).** Decided against AST lint per "Encode invariants in the type system, not in validation tests" (CLAUDE.md). **Sheet:** added `__post_init__` raising `ValueError("Sheet(name=..., sheet_id=...): description is required and must be non-blank")` when `description` is empty/whitespace. **6 visual subtypes** (KPI, Table, BarChart, LineChart, Sankey, ForceGraph): changed `subtitle: str | None = None` → `subtitle: str` (required, no default); added/extended `__post_init__` calling shared helper `_require_non_blank_subtitle(self)` raising `ValueError("<Kind>(title=...): subtitle is required and must be non-blank")` on empty/whitespace. **11 add_* helpers in Sheet** (`add_kpi`/`add_table`/`add_bar_chart`/`add_line_chart`/`add_sankey`/`add_force_graph` + control variants): kwargs changed `subtitle: str | None = None` → `subtitle: str` (required keyword-only). **Production app code:** zero changes — every apps/* call already passes a real subtitle (CLAUDE.md "every visual has a subtitle"). **Test fixtures swept:** ~80 sites across 13 test files — used safe `subtitle=None`→`subtitle="t"` + `description=""`→`description="test"` replace, then AST-driven char-aware injection for bare `add_kpi(width=..., title=..., values=...)` style calls without subtitle. Dropped `test_subtitle_optional` (asserted obsolete subtitle=None contract); replaced with `test_subtitle_required_non_blank` exercising the new ValueError path. CLAUDE.md trim: the "every visual has a subtitle" + "Coverage enforced in unit + API e2e tests" rule can drop the test-coverage clause now that the constructor catches at the buggy line. 1147 unit + 432 json tests pass; pyright clean. Effort: ~30 LOC production + ~80 fixture updates.
        - [x] **b.15.lint.tree-dataclass — `@dataclass` without `frozen=True` or `eq=False` in `common/tree/` (LOCKED 2026-05-08).** `TreeDataclassCheck` added to `tests/unit/test_typing_smells.py` (~75 LOC). Visits `ClassDef` nodes; flags both bare `@dataclass` (Name decorator) AND `@dataclass(...)` (Call) without `frozen=True` or `eq=False` in keywords. Correctly handles other-kwarg combos like `@dataclass(slots=True, frozen=True)` (passes — frozen is True). Scope: `common/tree/` only. Existing 30+ decorators all already use `eq=False` (mutable parents) or `frozen=True` (value-type leaves) — lint lands green from the start as a regression guard. Deliberate-regression confirms catch on both bare and missing-kwarg forms with actionable messages (object-ref identity vs. value-leaf framing). CLAUDE.md trim: implicit (the tree-pattern decorator convention is implicit in the L.1 design; lint failure is the source of truth going forward). Effort: ~75 LOC.
        - [x] **b.15.lint.json-indent — `json.dumps(...)` without `indent=2` in CLI / file-emit paths (LOCKED 2026-05-08).** `JsonIndentCheck` added to `tests/unit/test_typing_smells.py` (~50 LOC). Flags `json.dumps(...)` calls without `indent=` keyword, scoped to `src/quicksight_gen/cli/` + `src/quicksight_gen/common/` (NOT scripts/, NOT tests/). **2 legitimate compact-context sites suppressed per-line:** `common/html/render.py:806` (embedded HTML script payload — d3 hydration expects compact form), `common/html/server.py:357` (DEV-LOG line — compact one-line JSON is grep/jq-friendly). All other CLI/file-emit sites already use `indent=2`. Lint lands as regression guard. CLAUDE.md trim: implicit (no prose to remove). Effort: ~50 LOC.
        - [x] **b.15.lint.create-tags — boto3 `create_*` calls without `Tags=` (LOCKED 2026-05-08).** `CreateTagsCheck` added to `tests/unit/test_typing_smells.py` (~60 LOC). Walks `Call` nodes; matches `<receiver>.create_data_set` / `_analysis` / `_dashboard` / `_theme` / `_data_source` (5 known boto3 QuickSight create_* shapes). Passes when call has `Tags=` direct kwarg OR `**name` spread shape (the latter assumes Tags lives in the dict — paired with `boto3-direct` lint which gates new boto3.client() sites). Scope: `src/quicksight_gen/common/deploy.py` only — the single file with boto3 client.create_X calls today. Existing 5 sites all use `**payload` spread (the JSON files carry Tags from `build_*` helpers). Lint lands green as regression guard. Deliberate-regression confirms catch: bare `client.create_dashboard(AwsAccountId=..., DashboardId=..., Name=...)` flags; `Tags=[]` direct passes; `**payload` spread passes. **Pairs with `boto3-direct`:** new boto3 sites must extend boto3-direct allowlist first, then create-tags should extend its scope to the new file. CLAUDE.md trim: implicit (the tagging convention is currently enforced by review + by `build_*` payload helpers; lint locks in the create_* call-site discipline). Effort: ~60 LOC.
        - [ ] **b.15.lint.dict-vs-mapping — Bare `dict[K, V]` in function-parameter annotations (DEFERRED 2026-05-08).** **Decision:** punt to backlog; revisit if the convention starts drifting in production code. **Why:** highest effort in the b.15 queue (~70 LOC including the mutation-detector), lowest signal-to-noise vs. the other lints. False-positive risk is medium-high (locals that get returned, builders, defensive copies — distinguishing requires a flow-sensitive walker). The current `common/tree/` discipline around `Mapping` is healthy without enforcement, and pyright catches the actual silent-mutation bugs that matter at the call site. **Reactivation trigger:** if a bug surfaces because a `Mapping` was assumed read-only and got mutated, OR if a code review catches a fresh `dict[K, V]` parameter that should've been `Mapping`. **Original design (kept for reference):** `FunctionDef.args` annotation walker; flag `Subscript(value=Name("dict"))` only when the param is never mutated in the body (no `.update` / `[k]=v` / `.pop` / `.clear` / `.setdefault`); per-file opt-in via include list, scoped first to `common/tree/`. Effort: ~70 LOC.
        - [x] **b.15.followup.l2ft-no-sleep — Convert 5 L2FT hardcoded sleeps to polls (LOCKED 2026-05-08).** Added two helpers to `common/browser/helpers.py`: **`wait_for_table_nonzero(page, title, timeout_ms)`** — `page.wait_for_function` poll on the same JS row-counter `count_table_rows` uses, returns the count once `> 0`. **`wait_for_dropdown_options_present(page, dropdown_title, timeout_ms)`** — Python-side retry loop calling `read_dropdown_options` (the dropdown is closed between reads, so a JS `wait_for_function` poll wouldn't see options); 250ms inter-poll backoff suppressed per-line as a bounded-loop exception. Converted all 5 sites: chains/rails/templates dropdowns now call `wait_for_table_nonzero` (10s timeout, fall through to `count_table_rows` on TimeoutError so the test's > 0 assertion still surfaces the regression in the message); metadata_cascade:119 (Value-dropdown wait) calls `wait_for_dropdown_options_present`; metadata_cascade:146 (Transactions table wait) calls `wait_for_table_nonzero`. **Side benefit:** dropped the `import time` from all 4 test files. Verification deferred to next live e2e run; unit + pyright clean. Acceptance met: zero `time.sleep` suppressions in test_l2ft_*.py; helpers covered by per-line suppression where the bounded retry loop keeps an internal sleep. Effort: ~30 min as predicted.

        - [x] **b.15.followup.typed-id-wrap-apps — Wrap 32 bare-literal IDs in apps/* (LOCKED 2026-05-08).** Walked all 32 suppressions across `apps/l2_flow_tracing/app.py` (7), `apps/l1_dashboard/app.py` (9), `apps/executives/app.py` (16). Strategy: type at the source (helper-fn signatures + dataclass fields) instead of wrapping at every leaf call. **L2FT:** added `FilterGroupId` import; typed `_populate_param_filter_dropdown(fg_id, param_name)` and `_l2ft_drill(trigger)`; wrapped 7 inline literals. **L1:** added `Literal` + `FilterGroupId` imports; typed `_scope_one(fg_id)`, `_dropdown(fg_id, cross_dataset)`, `_l1_drill(trigger)`, `_DrillDest.fg_id`; wrapped 22+ inline literals (all `_dropdown` call-site fg_id kwargs + `_scope_one` positional fg_ids + drill literals). **Exec:** added `FilterGroupId` + `VisualId` imports; typed `_FG_EXEC_DATE_ACCT` constant + `_EXEC_SHEET_SPECS` first slot as `SheetId`; wrapped 13 visual_id literals + 2 inline `fg_id` literals. **Zero remaining `bare literal pending typed-ID wrap` suppressions in apps/.** 1579 unit + json tests pass; pyright clean. The existing `bare-str-id` lint covers the function-signature side; this sweep covers the call-site / dataclass-field side that the lint family forced into the open via `why-comment`. Mechanical end of the b.15 chain.
- [x] **Y.2.gate.c — Implement the runner [2026-05-09 closeout].** All sub-items LANDED across the c.5 / c.6 / c.7 / c.8 / c.9 / c.10 / c.11 / c.13 / c.14 work. The runner at `quicksight_gen/_dev/runner.py` carries the chain dispatch (c.5), per-layer artifact capture (c.10/11/12), variant fan-out (c.6.async), per-step timing + drift (c.2/3), the sweep verb (c.9), and density-tunable seed (c.13). 400-LOC budget exceeded (~3000 LOC now) — that ceiling was guidance, not contract; complexity moved INTO the runner so per-call sites stay thin.
    - [x] **Y.2.gate.c.1 — Per-run directory creation + run-id format (LOCKED 2026-05-07).** `runner.py::create_run_id()` returns `<utc-ts>-<short-sha>[-dirty]` (UTC, sortable, dirty-aware). Orchestrator `mkdir -p runs/<run-id>/` on session start. Format locked by `tests/unit/test_runner_skeleton.py::test_create_run_id_format`. Passing the path to pytest subprocesses via `QS_GEN_RUN_DIR` env var lands with `c.5` (when dispatch is real).
    - [x] **Y.2.gate.c.2 — Timings + hashes capture (LOCKED 2026-05-07).** `tests/conftest.py::pytest_runtest_makereport` hook (corrected from `pytest_runtest_protocol` per b.12 — `_makereport` exposes `report.duration` directly) writes per-test JSONL to `$QS_GEN_RUN_DIR/timings/<layer>[-<xdist_worker>].jsonl`. Hook is no-op when both env vars unset (direct `pytest tests/` invocation unchanged). Orchestrator `runner.py::collect_run_outputs(run_dir, layer_results)` aggregates per-layer JSONL + per-layer durations into `runs/<run-id>/timings.json` (with `layer_durations` / `skipped_layers` / `layer_exit_codes` / `test_durations`). `hashes.json` placeholder stub written iff missing — `c.13` populates it. Per-worker JSONL avoids xdist append contention (lands when c.6 wires xdist). Smoke verified: `./run_tests.sh up_to=unit` produces `runs/<run-id>/timings.json` with 1887 unit-test durations + per-layer wall-clocks (pyright 1.81s + unit 10.42s).
    - [x] **Y.2.gate.c.3 — Drift-diff report (LOCKED 2026-05-07).** `find_prior_run(current_run_id, runs_dir)` picks closest prior — same-SHA wins (closest signal: same code = real drift) → most-recent fallback. `compute_drift(current, prior) -> list[DriftEntry]` per-layer; `delta_pct` + `is_drift` (±50% via `DRIFT_THRESHOLD_PCT`). `report_drift(run_dir)` reads both JSONs, prints `drift: <layer> X.XXs (was Y.YYs, ±Z%)` with ⚠ for over-threshold. Wired into `cmd_up_to` between `collect_run_outputs` and `prune_old_runs`. 16 new tests; live smoke: 2× `up_to=pyright` reports same-SHA match `drift: pyright 1.84s (was 1.85s, -0.4%)`. Hashes + row-counts collapse into the same loop in `c.13`.
    - [x] **Y.2.gate.c.4 — `runs/` gitignored + auto-prune past N=20 (LOCKED 2026-05-07).** `runs/` in `.gitignore`. `runner.py::prune_old_runs(retain=N, runs_dir=...)` runs after `cmd_up_to` work, keeps last 20 by mtime. **Defensive**: only directories matching `_RUN_ID_PATTERN` are candidates — operator-parked files / scratch dirs under `runs/` are never touched (verified by `test_prune_old_runs_only_touches_run_id_pattern`). mtime-not-name sort means a touched-old-run survives over an untouched newer-by-name one. 6 tests cover the matrix; live shim verified pruning 3 from 23 → 20.
    - [x] **Y.2.gate.c.5 — Stop-on-first-failure across layers (LOCKED 2026-05-07; sequential-only cut).** `dispatch_layer(layer, run_dir)` runs the per-layer subprocess and returns `LayerResult(layer, exit_code, duration_seconds, skipped)`. `chain_through(target)` returns the chain slice. `cmd_up_to` loops the chain, exits FAILURE on first non-passing layer. Real dispatch wired for `pyright` (`.venv/bin/pyright`), `unit` (pytest tests/{unit,json,cli,docs,schema,l2} + `QS_GEN_SKIP_PYRIGHT=1`), `db` (`pytest tests/e2e/test_dataset_sql_smoke.py` + `QS_GEN_E2E=1`). `deploy`/`api`/`browser` returned as `skipped=True` stubs (cfg loading per `Y.2.gate.h.2` + variants per `b.2/b.3` need to land first). `QS_GEN_RUN_DIR` env threads through to subprocesses for c.10/c.11/c.12 conftest fixtures. Within-variant parallelism (App2+QS fast-fail at merged 6/7, fuzz-seed × dialect product) lands with `c.6`. Smoke verified: `./run_tests.sh up_to=unit` runs pyright (1.9s) + 1804 pytest tests (10.4s); exit 0.
        - [x] **Y.2.gate.c.5.deploy — Real `deploy` layer dispatch (LANDED 2026-05-08).** `_layer_command` builds `quicksight-gen json apply --execute -c <cfg> --l2 <l2> -o <run_dir>/deploy/out` from variant_env (`QS_GEN_CONFIG` + `QS_GEN_TEST_L2_INSTANCE` injected by h+i.0 + h.6); cfg path falls back to `_resolve_seed_config` for the default variant (which doesn't pre-inject the cfg). Live-validated 112.47s wall (within ≤2 min target).
        - [x] **Y.2.gate.c.5.api — Real `api` layer dispatch (LANDED 2026-05-08).** `pytest tests/e2e/ -m api -q -n auto` selects the boto3-only e2e files (every API e2e file already carries `pytestmark = [pytest.mark.e2e, pytest.mark.api]` — no test-file enumeration in the runner; tests register themselves via the mark). Live-validated 24.04s wall, 48 tests pass (within ≤3 min target).
        - [x] **Y.2.gate.c.5.browser — Real `browser` layer dispatch (LANDED 2026-05-08).** `pytest tests/e2e/ -m browser -q -n 4` (default 4 workers per existing `./run_e2e.sh`). Same mark-based test selection as `c.api`. `QS_E2E_USER_ARN` flows from h.1 derivation. Live-validated 56.96s wall, 21 passed + 16 skipped + 1 xfailed (within ≤8 min target). **Note:** the `b.3.impl.gather` merged-6/7 parallelism (target=app2 + target=qs) is still queued — c.5.browser today runs only the QS browser tier; App2 has its own `app2` layer.
        - [x] **Y.2.gate.c.5.followup — Two test xfails recorded during c.5.browser validation (2026-05-08).** (1) `test_l1_filters.py::test_date_range_filter_narrows_drift_sheet` — Sasquatch L1 dashboard render flake (task backlog #466); xfail strict=False until Y.2 SQL pushdown reshapes L1 dataset binds. (2) `test_audit_dashboard_agreement.py` — entire file skipped because module fixtures need spec_example seeded into BOTH postgres + oracle DBs in the same chain invocation; default variant only seeds ONE. Re-enable per `Y.2.gate.k.1` multi-variant CI parity, OR move into a dedicated `audit-agreement` layer outside the browser-mark set.
    - [x] **Y.2.gate.c.6 — Within-layer parallelism [2026-05-09 closeout].** Both sub-items LANDED: c.6.parallel (xdist passthrough 2026-05-07), c.6.async (cross-variant `asyncio.gather` fan-out 2026-05-08). Master ticked retroactively — the "still open" prose was stale.
        - [x] **Y.2.gate.c.6.parallel — `--parallel=N` xdist passthrough (LOCKED 2026-05-07).** `RunOptions.parallel: int = 1`; `--parallel=N` argparse flag; `_layer_command` appends `-n N` to pytest invocations for `unit` + `db` layers when N > 1 (no-op for pyright). Live smoke: `up_to=unit --parallel=4` shows 12.18s → 3.95s wall (5x on the unit layer alone).
        - [x] **Y.2.gate.c.6.async — Cross-variant `asyncio.gather` fan-out (LOCKED 2026-05-08).** When `--variants` resolves to >1 entry, `cmd_up_to` extracts the per-variant chain into `_run_one_variant(variant, run_dir, options, chain, *, terminal_prefix)` and dispatches concurrently via `asyncio.run(asyncio.gather(*[asyncio.to_thread(_run_one_variant, v, run_dir / v, options, chain, terminal_prefix=f"[{v}] ") for v in variants]))`. **Locked design (user 2026-05-08): (1) nested `runs/<id>/<variant>/<layer>/`** (easier ripgrep + per-variant artifacts can't collide); **(2) per-line terminal prefix `[<variant>] `** prepended in `_tee_stream` for live operator attribution; **(3) soft fast-fail per variant** (one variant failing doesn't kill the sibling — `_run_one_variant` returns its partial layer_results + exit_code, never raises); **(4) no concurrency cap** (default = `len(variants)`; Docker is the bottleneck). Single-variant path stays sync — un-nested run_dir for back-compat with existing tooling that reads `runs/<id>/<layer>/`. Per-variant `timings.json` writes under `runs/<id>/<variant>/`; aggregated top-level `timings.json` keys layers as `<variant>.<layer>` so `report_drift` works unchanged. **Reaper-singleton race fix**: pre-warm `testcontainers.core.container.Reaper.get_instance()` serially before fan-out (otherwise both threads race on `_create_instance` → HTTP 409 from Docker on the deterministic Ryuk container name). Refactor lifted: extracted `_spawn_with_tee(cmd, *, cwd, env, stdout_path, stderr_path, terminal_prefix)` helper from `dispatch_layer`; `seed_variant` uses it too (per-step capture under `<run_dir>/seed/<step>.{stdout,stderr}.log`). 5 new unit tests on multi-variant cmd_up_to (each variant invoked, nested run_dir, terminal_prefix threading, soft fast-fail, timings aggregation). **Live-verified**: `./run_tests.sh up_to=db --variants=local-pg,local-oracle` boots both containers in parallel (~10s PG + ~30s Oracle, total wall ~30s vs sequential ~40s), 1979 unit + 37 db green per variant, per-variant artifacts land at `runs/<id>/{local-pg,local-oracle}/{unit,db,seed}/`, top-level timings.json shows `{local-pg.unit, local-pg.db, local-oracle.unit, local-oracle.db}`.
        - [x] **Y.2.gate.c.6.xdist-safety — fix fuzz tests for parallel collection (LOCKED 2026-05-07).** Resolved without touching the failing test directly: runner now generates a random fuzz seed value once per invocation (`runner.resolve_fuzz_seed_value()` — env override > `secrets.randbits(32)`), threads it as `QS_GEN_FUZZ_SEED` to every pytest subprocess. All xdist workers in the same run inherit the same seed → parametrize collection is deterministic. Random per RUNNER INVOCATION (audit §7.11 satisfied: "default = 1 random seed per run"); operator pins via `QS_GEN_FUZZ_SEED=<N>` env for failure repro. Seed echoed in runner output: `runner: fuzz_seed=<N> (pin via QS_GEN_FUZZ_SEED env to repro)`.
        - [x] **Y.2.gate.c.6-followup — Drop pyright as a runner LAYER (LOCKED 2026-05-07; user direction).** Pyright was its own runner layer (layer 1 explicit `.venv/bin/pyright`) AND ran via repo-root `conftest.py::pytest_sessionstart` (M.1.9c contract). Bridged via `QS_GEN_SKIP_PYRIGHT=1` env. Collapsed to a single mechanism: drop the runner layer + drop the suppression env. Pyright now runs INSIDE the unit layer's pytest invocation via the conftest sessionstart hook. Trade-off: pyright duration folds into unit's wall-clock instead of being its own `timings.json` entry (acceptable; pyright is ~2s). Direct `pytest tests/` invocations preserved (M.1.9c contract intact). LAYERS = `("unit", "db", "deploy", "api", "browser")`. Smoke verified end-to-end: `up_to=unit --parallel=4` succeeds (was failing before xdist-safety fix); 1040 unit tests pass.
    - [x] **Y.2.gate.c.7 — Flag plumbing (LOCKED 2026-05-07).** `RunOptions` dataclass holds the operator-supplied flags; `_options_from_args(args) -> RunOptions` builds it from argparse. Threaded through `dispatch_layer` + `_layer_command`. Per-flag wiring: **--only=<expr>** (active) → `pytest -k <expr>` for unit/db layers. **--trace-all** (active) → `QS_GEN_TRACE_ALL=1` env passthrough; consumed by `c.11` browser fixtures when they land. **--allow-dirty-deploy** (active per `b.10`) → bypasses tracked-changes refusal at layers ≥ deploy; `QS_GEN_RUNNER_YES=1` env also bypasses (matches `b.14.3` destructive-op convention). **--variants** / **--fuzz-seeds=N** / **--skip-cheap** / **--keep-on-failure** plumbed but no-op pending their respective consumer tasks (c.6 / b.8 cache / b.14.3 lifecycle). 12 new tests; live smoke: dirty `up_to=deploy` refused with exit 2 + actionable message; `--allow-dirty-deploy` bypasses to probe gate. 1020 unit tests total (1008 + 12).
    - [x] **Y.2.gate.c.8 — Dependency probe (LOCKED 2026-05-07; no state file per §7.12).** `runner.py::probe_dependencies(layer)` runs before dispatch + aggregates ALL failures so operator sees everything missing in one pass. Per-layer required-deps map (`_LAYER_DEPS`) is the runtime authority; cross-checked vs audit §3 by `test_layer_deps_match_audit_table`. Probes: `_probe_aws` (sts:GetCallerIdentity, distinguishes ExpiredToken / NoCreds / CLI-missing / unknown), `_probe_docker` (`docker ps`, distinguishes daemon-down / CLI-missing / unknown), `_probe_qs_e2e_user_arn` (env var presence). All failures expose `kind` + operator-facing `message`; never auto-invoke `aws sso login` (b.14.4 refusal pattern). Subprocess wrapped with 10s timeout (rc=124) + missing-binary handling (rc=127). DB-conn probe deferred until cfg loading lands (`Y.2.gate.h.2`); for now the downstream pytest fixture catches DB-unreachable.
    - [x] **Y.2.gate.c.9 — General-sweep cleanup subcommand (LOCKED 2026-05-07).** `./run_tests.sh sweep` calls `runner.py::cmd_sweep`. Tag set: `Harness:e2e` (production deploys don't carry that tag — they wear `ManagedBy:quicksight-gen` + optional `L2Instance:<prefix>` — so the sweep is safe against the production resource graph). Default = dry-run (collect + print, no AWS-side mutation); `--yes` flag (or `QS_GEN_RUNNER_YES=1` env per `b.14.3`) flips to actual delete. Config-path lookup: `QS_GEN_CONFIG` env override → `run/config.yaml` → `config.yaml` → `run/config.{postgres,oracle}.yaml` (sweep only needs `aws_account_id` + `aws_region`; per-dialect cfg works fine since they share both). Helpers `_collect_resources_matching_tag` + `sweep_qs_resources_by_tag` lazy-imported from `tests/e2e/_harness_cleanup.py` via `importlib` + `sys.path` injection — `Y.2.gate.f.8` lifts those into `quicksight_gen/_dev/cleanup.py` and drops the path dance. 4 unit tests in `test_runner_skeleton.py` (argparse no-yes / argparse with-yes / dry-run no-deletes / --yes triggers sweep / QS_GEN_RUNNER_YES=1 env bypass / no-config-file → needs-operator). Live verify against AWS: dry-run found 7 real orphan datasets from prior failed e2e runs (`qs-gen-postgres-e2e_*`).
    - [x] **Y.2.gate.c.10 — Top-queries auto-capture (LOCKED 2026-05-07; per `Y.2.gate.f.4`).** `scripts/dump_top_queries.py` helpers lifted into `quicksight_gen/_dev/perf.py` (`fetch_top_queries` / `format_top_queries_markdown` / `format_skipped` / `dialect_name`) so both the standalone script (still in place until f.4 deletes it) and the new in-process fixture share the same code path. Session-scope teardown fixture `capture_top_queries` lives in `tests/e2e/conftest.py` (where `cfg` is loaded — only e2e layers hit a DB). Picks dialect from `cfg.dialect`, picks like-pattern from `QS_GEN_TEST_L2_INSTANCE` env override → `cfg.l2_instance_prefix` → `spec_example` default. Writes to `$QS_GEN_RUN_DIR/db/<dialect>/top-queries.md`. SQLite → clean skipped marker; PG → bootstraps `pg_stat_statements`; Oracle → `v$sqlstats` + LOB unwrap. Sidecar contract: connect-fail / stats-view-missing / disk-fail all swallowed; never breaks the test session. 10 unit tests on lifted helpers. Live verify (`up_to=db --parallel=2`): 37/37 pass + `runs/<id>/db/postgres/top-queries.md` landed with 50 real `pg_stat_statements` rows narrowed to `spec_example` queries.
    - [x] **Y.2.gate.c.11 — Browser trace capture (LOCKED 2026-05-07).** Hook lives in `common/browser/helpers.py::webkit_page` (the shared Playwright entry point all browser e2e tests already use — no per-test refactor needed). Adds `context.tracing.start(screenshots=True, snapshots=True, sources=True)` before yield; `_stop_and_maybe_save_trace` in finally decides save-vs-discard. Save policy: failure ALWAYS saves; clean exit only saves with `QS_GEN_TRACE_ALL=1` env (plumbed by c.7). Output: `$QS_GEN_RUN_DIR/browser/<test-id>/{trace.zip, screenshot.png, console.txt, network.txt, qs_errors.txt}` plus an extracted `trace/` sibling dir (per user feedback — zip isn't grepable; unpack alongside for direct `grep`/`ls`). Path resolver `_capture_path(filename, test_id)` makes per-failure dumps route to the run-dir per-test directory under runner mode AND keep the legacy flat `<SCREENSHOT_DIR>/_failures/<test_id>_*` naming when env unset (backwards-compat with `./run_e2e.sh` artifact uploads). 4 unit tests on path resolver + 4 Playwright-driven integration smoke tests under `tests/unit/test_browser_trace_smoke.py` (gated on `pytest.importorskip("playwright.sync_api")`) verify the four-cell matrix: env-unset×clean / env-set×clean+TRACE_ALL / env-set×clean / env-set×failure. Sidecar contract preserved (errors during stop/extract swallowed). 16 unit tests total in `test_browser_helpers.py` + 4 smoke; live-runner `up_to=unit --parallel=4` green.
    - [x] **Y.2.gate.c.11.app2-server-logs — Capture App2 uvicorn server logs in run dir (LOCKED 2026-05-07).** Three-part fix:
        1. **Harness** (`_harness_html2.py`): `_attach_app2_log_handler` opens `$QS_GEN_RUN_DIR/app2/server.log`, attaches a `FileHandler` to `uvicorn.error` + `uvicorn.access` + the new `quicksight_gen.app2.devlog` logger; `_detach_app2_log_handler` removes + closes on cleanup. **Attach point matters**: must happen AFTER `server.started` is True — `uvicorn.Server.run()` calls `logging.config.dictConfig()` on startup which would wipe handlers attached pre-start. Trade-off: uvicorn's startup banner only goes to stderr; every per-request access log + traceback after that lands in the file. `log_level="info"` only when capturing (else stays `"error"` to keep stderr quiet for direct pytest invocations). Sidecar contract: OSError on mkdir/open swallowed; failed capture ≠ test failure.
        2. **Server** (`common/html/server.py`): `log_event` POST /log handler now uses `_DEVLOG.info("DEV-LOG %s", ...)` (Python logging) instead of `print(file=sys.stderr)`. New module-level `_DEVLOG = logging.getLogger("quicksight_gen.app2.devlog")` so the harness's FileHandler picks up browser-forwarded events. Production callers without log handlers no longer see stderr output for dev-log events — acceptable tradeoff; dev_log is a debug feature and operators running it in production should configure logging properly.
        3. **E2E test** (`test_html2_executives.py::test_dev_log_events_land_in_server_log`): spins a separate App2 server with `dev_log=True` so the page emits `<meta name="dev-log">` (which activates `dev_log.js`); script POSTs `dev-log:ready` synchronously on load; test asserts `DEV-LOG` + `dev-log:ready` substrings present in the captured server.log. Skips cleanly when `QS_GEN_RUN_DIR` unset (legacy pytest invocation).
        **De-dup gotcha caught + fixed**: initial wiring attached handlers to root `uvicorn` AND `uvicorn.error`. `uvicorn.error` propagates to root by default → every error logged twice. Fix: only attach to leaf loggers (`uvicorn.error`, `uvicorn.access`, `quicksight_gen.app2.devlog`).
        **Live-verified**: `./run_tests.sh up_to=app2 --variants=local-pg` → `runs/<id>/app2/server.log` lands with uvicorn access log + lifecycle messages + the dev-log:ready event from the new test (`grep -c DEV-LOG → 1` matching `quicksight_gen.app2.devlog INFO DEV-LOG {"event": "dev-log:ready", "ua": "..."}`). 10/10 app2 + 3 skipped; pyright clean.
    - [x] **Y.2.gate.c.11.app2 — Extend trace capture to App2's html2 harness (LOCKED 2026-05-07).** Resolved by the simplest possible fix: refactor each App2 test's `with playwright_sync_api.sync_playwright() as p: browser = p.webkit.launch(...); page = browser.new_page()` block to `with webkit_page() as page:` (importing from `common/browser/helpers.py`). 10 callsites swapped across `test_html2_executives.py` (6) + `test_html2_executives_live.py` (1) + `test_html2_money_trail.py` (3); explicit `browser.close()` calls dropped (handled by the context manager). Same lifecycle now surrounds App2 tests as QS tests: tracing.start at context creation, stop+save on failure, console + network capture, screenshot on exception. **Live-verified**: re-ran `./run_tests.sh up_to=app2 --variants=local-pg` with money_trail still failing → `runs/<id>/browser/tests_e2e_test_html2_money_trail__test_layer2_initial_load_renders_sankey/{screenshot.png, trace.zip, trace/, console.txt, network.txt, qs_errors.txt}` all landed. The .txt files are empty for these particular failures (Playwright timeout, not JS error → no console/network signal); the trace.zip + extracted trace/ has the full Playwright timeline + frame snapshots — that's what unblocks `X.2.h.6`. Notes: (1) `qs_errors.txt` filename is a slight cosmetic mismatch on the App2 path (App2 has no QS overlays — file always empty); will rename to `dom_errors.txt` if and when a future task lifts the capture into a renderer-agnostic helper. (2) The shared `webkit_page` helper's `_capture_failure_qs_errors` runs harmlessly on App2 pages (selectors return empty) — also matches the X.2.m toaster `[role="alert"]` selector usefully. Trade-off accepted. 6/6 stub tests still pass post-refactor; pyright clean.
    - [x] **Y.2.gate.c.12 — L2 YAML always copied (LOCKED 2026-05-07).** Hook lives at `common/l2/loader.py::_capture_to_run_dir`, called from `load_instance` after the instance prefix parses successfully. No-op when `QS_GEN_RUN_DIR` env unset (direct `pytest` / `quicksight-gen` invocations unchanged). When set, writes raw YAML bytes to `<run-dir>/l2/<instance-prefix>.yaml`. Filename uses the YAML's own `instance:` field (not the source basename) so destinations dedupe across paths and match the spec's `<instance-or-seed>` shape — fuzz instance `fuzz_seed_0042` lands as `fuzz_seed_0042.yaml`. **Sidecar contract**: capture failures (mkdir on a path-blocker, disk full) are swallowed via `except OSError`; the load itself must not break because the sidecar can't write. Same defensive treatment retroactively applied to `tests/conftest.py::pytest_runtest_makereport` timings hook (also a sidecar). 5 unit tests + live-runner verify (`up_to=unit --parallel=4`: 1870 pass, 126 captured YAMLs landed including the static `aaaaa...` prefix-test fixture + 125 fuzz seeds).
    - [x] **Y.2.gate.c.13 — `--seed-density=N` tunable generator parameter (LOCKED 2026-05-07).** All four sub-tasks resolved: c.13.1 done (CLI + `build_full_seed_sql` density param + 4 byte-identity tests); c.13.2 deferred pending real-world drift signal from c.3; c.13.3 subsumed by c.13.1's per-density tests; c.13.4 done (CLAUDE.md doc + stale-mechanism fix). Net: `--seed-density=N` is operator-tunable, locked SQL files stay valid at default density, per-run drift detection lives separately in `runs/<run-id>/{timings,hashes}.json`.
        - [x] **c.13.1 — Plumb `--seed-density=N` (LOCKED 2026-05-07).** `density: float = 1.0` parameter on `cli/_helpers.py::build_full_seed_sql`; scales the three plant knobs (`densify_scenario` factor, `add_broken_rail_plants` count, `boost_inv_fanout_plants` multiplier) via `int(BASE * density)`. CLI surface: `quicksight-gen data apply --seed-density=N`. density=1.0 byte-identical to pre-c.13 — the 3 `_locked_seeds/*.sql` files stay valid (4 locked-seeds tests pass + 4 new density-scaling assertions: byte-identity at 1.0, more rows at 2.0, fewer at 0.5, distinct SHA256 headers across densities). Runner thread punted — `data apply` is a CLI verb (not a runner layer); operators pass `--seed-density` directly when needed. Commit `15e60fb`.
        - [x] **c.13.2 — Drop the global SHA256 lock — DEFERRED 2026-05-07; CLOSED 2026-05-09 as parking decision.** Audit finding: `tests/data/test_l2_baseline_seed.py` no longer carries a standalone SHA256 hash assertion (likely swept Q.3.a / R.5.a era). The only lock in play is `_locked_seeds/*.sql` files + `test_locked_seed_matches_fresh_emit` (byte-equality, which subsumes hash). c.13.1 preserved this lock by making `density=1.0` byte-identical. **Decision (locked):** keep `_locked_seeds/*.sql` as the default-density anchor for clean clones / fresh CI checkouts; revisit only if/when `runs/<run-id>/hashes.json` surfaces a real drift event the locked-seeds test missed. Closing the checkbox: parking is a decision, not work-in-progress — no item should sit `[ ]` indefinitely.
        - [x] **c.13.3 — Regression guard test — SUBSUMED 2026-05-07.** Per-density byte-identity is already enforced by 4 new assertions in `test_locked_seeds.py` (c.13.1) + the existing 4 `test_locked_seed_matches_fresh_emit` parametrizations. No additional checked-in golden needed.
        - [x] **c.13.4 — Document the tunable in CLAUDE.md (LOCKED 2026-05-07).** Updated Demo Data Conventions § lines 206–207: added `--seed-density=N` paragraph (1.0/2.0/0.5 examples + density flow through `build_full_seed_sql`); fixed stale lock-mechanism description (was: `tests/test_l2_baseline_seed.py` SHA256 hash assertion → now correctly: `tests/data/_locked_seeds/*.sql` byte-equality via `test_locked_seeds.py`); added cross-link to `runs/<run-id>/` per-run drift (c.2 + c.3) so the two mechanisms are clearly separated.
    - [x] **Y.2.gate.c.14 — Dispatch-table cross-check unit test (LOCKED 2026-05-07; per `Y.2.gate.b.11`).** 7 spot-check tests in `tests/unit/test_runner_skeleton.py` read `docs/audits/y_2_gate_test_layer_chain_audit.md` directly and assert correspondence with runner state: audit-doc-exists, layer-table-mentions-every-runner-layer (with explicit runner→audit-name mapping that itself fails loudly on drift), pyright-pure-static-check, browser-needs-QS_E2E_USER_ARN, deploy-needs-AWS-creds, dialect-axis-distinguishes-PG-from-Oracle, no-preconditions-on-pyright. Spot-check shape (not a markdown parser) catches one-side-only edits without forcing exhaustive coverage.
    - [x] **Y.2.gate.c.15 — `boto3-stubs` → `types-boto3` swap (LOCKED 2026-05-07; spike result: REVERTED).** Path-A spike per user direction. Findings:
        - **Baseline**: 112 pyright errors across the 6 boto3-touching files (`_dev/runner.py`, `_dev/perf.py`, `cli/_helpers.py`, `common/browser/helpers.py`, `common/datasource.py`, `common/deploy.py`). Per-file: 0/0/16/0/0/96, plus a `# pyright: ignore` already in `runner.py::cmd_sweep` for the `boto3.client("quicksight", ...)` overload-fallback issue.
        - **Swap**: `boto3-stubs[quicksight]>=1.34` → `types-boto3[quicksight]>=1.34` in `pyproject.toml::dev`. Required code change in `common/browser/helpers.py`: import path moved from `mypy_boto3_quicksight` (boto3-stubs's namespace) to `types_boto3_quicksight`.
        - **Post-swap**: 112 errors. Identical count, identical files. Drop of `# pyright: ignore` in `runner.py` resurfaced the same `client is partially unknown` overload issue — `types-boto3` carries the same `(service: str, ...) -> Unknown` fallback branch.
        - **Decision: REVERT**. Net pyright delta = 0. The 96 errors in `common/deploy.py` are about internal under-annotation (`dict` without type args, missing `client` param types) — completely unrelated to which boto3-stubs flavor is installed. Same applies to most of the 16 in `cli/_helpers.py`. The `boto3.client` overload-fallback issue is a pyright/stub-design clash that NEITHER package solves cleanly. `# pyright: ignore` stays as-is in `runner.py::cmd_sweep`.
        - **Lessons captured** (for future swap-evaluations): (1) baseline-then-diff IS the right protocol; (2) the deep pyright noise in `deploy.py` / `cli/_helpers.py` is a separate cleanup task (annotate the bare `dict` returns + helper signatures) — file under future PLAN entry, not a stub-package issue; (3) live sweep + unit suite both stayed green throughout the spike, so the runtime behavior is stub-package-agnostic.
- [x] **Y.2.gate.d — Wire into CLAUDE.md + memory [2026-05-09 branch y-2-gate-l].** CLAUDE.md `Test sequencing + git hooks` section gained the prescriptive "Never invoke `pytest tests/e2e/` directly for layered work" rule, with the Y.2.b SQL bug as the concrete cost example, plus a pre-dispatch probes paragraph capturing gate.l.3 (probes refuse before container spin-up, not after). Memory mirror lives at `feedback_test_layer_chain.md` (locked 2026-05-07; carries the additive-chain principle + the rationale).
- [x] **Y.2.gate.e — Synthetic regression [2026-05-09 branch y-2-gate-l].** Three planted-bug experiments verified live: (1) pyright violation halts at `unit` (runner rc=1, conftest sessionstart pyright output bubbled to stderr); (2) failing unit test halts at `unit` (standard pytest assertion surface); (3) SELECT-alias-in-WHERE SQL bug halts at `db` via `test_dataset_sql_smoke.py` (the f.1-lifted parametrize that catches exactly this Y.2.b-shape regression). Audit at `docs/audits/y_2_gate_e_synthetic_regression.md` carries the planted-bug shapes, captured outputs, replay incantations, and a coverage matrix mapping each bug class to its catch layer + mechanism. Side issue surfaced + filed: local-Oracle cells fail with `unrecognized arguments: -n` (pytest-xdist visibility) at the unit layer — non-blocking for gate.e (chain still halts), follow-up on the runner side.
- [x] **Y.2.gate.f — "Validate, then delete-or-fold" sweep over scripts/ + harness layer [2026-05-09 closeout].** All sub-items f.1-f.10 LANDED 2026-05-09 (per individual entries below). Final-pass cleanup on branch `y-2-gate-l`: dropped `run_e2e.sh` — pre-runner one-shot wrapper with stale `--harness` flag (test file deleted in f.9) + bare `pytest tests/e2e` invocation directly violating gate.d's "never invoke pytest directly for layered work" rule. README.md `Tests` block + repo tree listing rewritten to show `./run_tests.sh up_to=<layer>` examples; CLAUDE.md "Legacy direct invocations" block dropped. Net `scripts/` content: 1 file (`dump_top_queries.py` thin shim — kept until k.6 retires CI's direct calls; tracked in f.4).
    - [x] **Y.2.gate.f.1 — `tests/integration/verify_dataset_sql.py` → DELETED 2026-05-09.** The pytest twin (`tests/e2e/test_dataset_sql_smoke.py`) was importing `_smoke_one` from the CLI script — not a clean duplicate. Lifted the helpers (`_format_value`, `_resolve_default`, `_substitute_qs_params`, `_wrap_smoke`, `_custom_sql`, `_smoke_one`) directly into the pytest test module, deleted the CLI script, and rewired `ci.yml`'s 2 invocations (PG + Oracle) to call pytest with `QS_GEN_E2E=1` + `QS_GEN_CONFIG=/tmp/ci-<dialect>.yaml` + `QS_GEN_TEST_L2_INSTANCE=tests/l2/spec_example.yaml` env. Unit suite green (1285 passed, 73 skipped).
    - [x] **Y.2.gate.f.2 — `tests/integration/verify_demo_apply.py` → FOLDED + DELETED 2026-05-09.** New pytest test at `tests/e2e/test_demo_apply_row_counts.py` (parametrized over the 4 smoke matview suffixes); cfg-driven dialect dispatch via `connect_demo_db` (PG / Oracle / SQLite all share the same test). Dropped the legacy exact-counts arm — only `spec_example` had locked counts and CI was already calling `--smoke`; the comment said "re-lock once `demo apply --anchor` lands". CI's 2 calls (PG + Oracle) rewired to pytest with `QS_GEN_E2E=1` + cfg/L2 env. Empty `tests/integration/` directory removed.
    - [x] **Y.2.gate.f.3 — `scripts/bake_sample_output.py` → DELETED 2026-05-09.** Per user direction ("unneeded") + locked redundancy: the published wheel + docs site already give evaluators full access to the generated JSON shape via `pip install quicksight-gen && quicksight-gen json apply -c ...`. Removed `release.yml::bake-sample` job (35 lines), dropped `bake-sample` from `publish-testpypi`'s `needs:`, removed the `out-sample` artifact download in `github-release` job + the `dist/out-sample.zip` line from the release files list. Next release (v8.8.0a10+) won't ship out-sample.zip; v8.8.0a9 still has it on the GitHub Release page.
    - [x] **Y.2.gate.f.4 — `scripts/dump_top_queries.py` FOLDED into runner output 2026-05-09.** Added `_dump_top_queries_for_variant(spec, variant_env, run_dir, terminal_prefix)` to `_dev/runner.py` (sibling of `teardown_variant`). Wired into `_run_one_variant`'s `finally` — fires after every chain that touched a DB layer (same gating as the seed step). Runs on success AND failure so triage always has the perf signal. Output: `<run_dir>/<spec.name>/db-perf/top-queries.md`. Best-effort throughout: connection / query / format failures all degrade to a `format_skipped` marker via `_dev.perf.format_skipped`. SQLite skipped cleanly (no equivalent stats view). Filter narrows to the L2 instance prefix so unrelated workloads on the shared DB get dropped. Slimmed `scripts/dump_top_queries.py` to a thin CLI shim that delegates to `_dev.perf` (dropped ~150 lines of duplicate helper code; kept the script for now since `e2e.yml` still calls it directly — gate.k.6 retires those calls when CI moves to runner). Unit suite green (1285 passed).
    - [x] **Y.2.gate.f.5 — `scripts/harness_manual_deploy.py` → DELETED + `--keep-on-failure` runner flag wired 2026-05-09.** The flag was scaffolded in `RunOptions` + argparse already (since c.7) but unwired. Wired the suppression into `_run_one_variant`'s `finally`: chain failure + `--keep-on-failure` → skip `teardown_variant`, print operator-actionable guidance ("container LEFT UP; clean up later via `docker stop <name>` or `./run_tests.sh sweep`"). Default (no flag, OR chain succeeded) tears down as before. 3 unit tests in `tests/unit/test_runner_skeleton.py::test_run_one_variant_*_keep*` cover keep-on-failure-fail / success-still-tears-down / no-flag-still-tears-down. Deleted `scripts/harness_manual_deploy.py` (depended on `_harness_*` modules f.9 will delete; the runner flag covers the local-container case; QS-deploy inspection now goes through the e2e tests directly with existing skip-cleanup mechanisms). Sweep subcommand (f.8) is the general-purpose cleanup. Unit suite green (1288 passed, 73 skipped).
    - [x] **Y.2.gate.f.6 — `scripts/m2_6_verify.py` → DELETED 2026-05-09.** User direction: "should be covered by existing tests". The deploy + plant + per-invariant assertion flow is now covered by `tests/e2e/test_harness_*.py` (live AWS deploy + assert) plus the M.2c.* L1 e2e tests. Removed `scripts/m2_6_verify.py` + `m2_6_verify.sh` shell wrapper. Updated 6 stale references in `src/quicksight_gen/docs/L1_Invariants.md` (s/`m2_6_verify.py` asserts/the matview should surface/) + 1 in `customization.md` (replaced "Verify with `m2_6_verify.py`" section with "Verify with the test runner" pointing at `./run_tests.sh up_to=db --variants=sp_pg_aw`).
    - [x] **Y.2.gate.f.7 — `scripts/qs_substitution_probe.py` → DELETED 2026-05-09.** Manual triage tool (Y.1.o vintage) for QS runtime SQL behavior — `inspect` (post-deploy CustomSQL via describe_data_set), `snapshot`/`diff` (pg_stat_statements deltas around a user action). Different surface from the smoke tests but no callers, not in CI, doesn't gate anything. The fold-into-runner option (per-run pg_stat_statements diagnostic dump) overlaps with f.4's `dump_top_queries` future plan; if it lands, the probe's snapshot logic gets re-derived there. Until then, the 317 lines of niche QS runtime triage are dead weight — git history preserves the recipe if a similar incident hits.
    - [x] **Y.2.gate.f.8 — `scripts/sweep_harness_orphans.py` → DELETED 2026-05-09.** `cmd_sweep` already lives in `_dev/runner.py` (added at c.9; tagged `Harness:e2e`, default dry-run, `--yes` to actually delete). Standalone script removed; the runner subcommand replaces it. **Note:** runner's `cmd_sweep` still imports `_harness_cleanup` helpers via `sys.path.insert(REPO_ROOT/tests/e2e)` — the lift to `quicksight_gen/_dev/cleanup.py` is forced by f.9 (which deletes the `_harness_*` modules) and lands as part of that.
    - [x] **Y.2.gate.f.9 — Layer 8 (harness) DROPPED 2026-05-09.** ~5600 lines net deleted. Surfaced 4 of the 8 `test_harness_*.py` files were unit tests of harness helpers themselves (`test_harness_browser`, `test_harness_cleanup`, `test_harness_deploy`, `test_harness_failure_dump`, `test_harness_l1_assertions`, `test_harness_l2ft_assertions`, `test_harness_seed`); they only had value while the helpers existed and become dead code with the helpers gone. The 8th file (`test_harness_end_to_end.py`, 913 lines) was THE actual layer-8 lane; its planted-scenario assertions are covered by the merged 6/7 layer (`test_l1_*` / `test_inv_*` / `test_exec_*` / `test_l2ft_*`) running against per-cell variant deploys. **Forced co-work landed:** (a) lifted `sweep_qs_resources_by_tag` + `_collect_resources_matching_tag` from `tests/e2e/_harness_cleanup.py` → `quicksight_gen/_dev/cleanup.py`; rewired `cmd_sweep` to import from the new location; dropped the `sys.path` + `importlib` dance + a now-unnecessary cast (pyright happy). (b) lifted `apply_db_seed` from `_harness_seed.py` → `tests/e2e/_seed_helpers.py` (only consumer is `test_audit_dashboard_agreement.py`); `build_planted_manifest` did NOT lift — it was harness-specific. **Kept:** `_harness_html2.py` (App2 fixture infra, NOT layer-8; 3 import sites in `test_html2_*` tests). **Deleted:** 9 `_harness_*.py` modules + 8 `test_harness_*.py` files + `tests/e2e/failures/` historical-artifact directory + 3 stale doc-comment references. Updated `_install_fake_harness_cleanup` in `test_runner_skeleton.py` to monkeypatch the new `_dev.cleanup` location. Unit suite green (1288 passed, 73 skipped). Smoke-imported all `test_html2_*` + `test_audit_dashboard_agreement` to confirm no broken imports.
    - [x] **Y.2.gate.f.10 — P.9f.d alignment SUBSUMED 2026-05-09.** **Research (f.10.1)**: the variant matrix (`m.1+m.2+m.3` LANDED v8.8.0a6) covers all 4 P.9f.d cells natively as `{sp,sq}_{pg,or}_aw` across api+browser layers. Per-run capture under `runs/<run-id>/<variant>/<layer>/` (`cmd.json` + `stdout.log` + `stderr.log` + `timings.json` + app2 server logs from c.11.app2-server-logs) gives full failure-triage signal — no bespoke re-run tooling needed. **Decision (f.10.2)**: P.9f.d (task #509) marked completed; runner subsumes the manual matrix re-run. Also DELETED the two orphan `scripts/p9_deploy_verify.sh` + `scripts/p9_e2e.sh` shells (P.9f-vintage spike infrastructure, no callers, no CI, fully replaced by `./run_tests.sh up_to=browser` against the operator's external Aurora).
- [x] **Y.2.gate.h — Credential auto-discovery [2026-05-09 closeout].** All sub-items h.1-h.6 LANDED 2026-05-08/09 (per individual entries). Acceptance verified live in c.5 close-out chain run + gate.l live-status: with the operator's `auth: { aws_profile: quicksight-gen-local }` in `run/config.<dialect>.yaml`, every runner-invoked layer + lifecycle command works without env-var exports — runner injects `AWS_PROFILE` into subprocess env, derives `QS_E2E_USER_ARN` via STS+ListUsers (h.1), reads cluster + instance + region + account from cfg (h.2-h.4), loud-fails actionably on missing fields (h.5), threads default-L2 from `cfg.default_l2_instance` (h.6). Combined h+i.0 spike at `docs/audits/y_2_gate_h_i_combined_spike.md` documents the long-lived-IAM-keys decision; runbook in §6.
    - [x] **Y.2.gate.h.1 — `QS_E2E_USER_ARN` derived from AWS identity (LANDED 2026-05-08).** `_dev/runner.py::_derive_qs_user_arn(cfg)` calls STS GetCallerIdentity → QS ListUsers paginate → matches `PrincipalId == "federated/iam/<UserId>"`. Override path: `cfg.auth.quicksight_user_arn` wins (no API call). Wired into `_run_one_variant` so the env var is set before any `qs_arn`-needing layer dispatches. `_probe_qs_e2e_user_arn` accepts cfg-auth fallback (probe pre-passes if `auth.aws_profile` or `auth.quicksight_user_arn` set in cfg). New IAM user `quicksight-gen-local` created + registered as QS user; cfg yamls carry `auth: { aws_profile: quicksight-gen-local }`. Live-validated: derivation returns the correct ARN end-to-end. Memory entry `project_qs_e2e_user_arn.md` rewritten to "runner self-derives" + retained as triage runbook.
        - [x] **Y.2.gate.h.1.0 — SPIKE: QS-user-ARN derivation strategy (LOCKED 2026-05-08; SUPERSEDED by `h+i.0` combined spike — see `Y.2.gate.i.0`).** Output `docs/audits/y_2_gate_h_1_0_qs_user_arn_spike.md` (preserved for the join-key research). **Locked finding:** every QS user's `PrincipalId == "federated/iam/<UserId>"` where `<UserId>` is what `sts:GetCallerIdentity` returns — verified across IAM-user (`AIDA…`), assumed-role (`AROA…:session`), and root (account ID) identity types in account 470656905821. **Recommendation rolled into combined h+i spike:** A (ListUsers + STS UserId match) for local; cfg override for CI. See `docs/audits/y_2_gate_h_i_combined_spike.md` for the full decision (auth path + ARN derivation + cfg shape + IAM policy, locked together).
    - [x] **Y.2.gate.h.2 — DB connection strings cfg-driven 2026-05-09.** Implementation already landed: every consumer reads `cfg.demo_database_url`. For local-pg/oracle/sqlite variants the runner spins a container and injects `QS_GEN_DEMO_DATABASE_URL=<container-url>` into that variant's subprocess env (no operator action). For aw target, `cfg.demo_database_url` from `run/config.<dialect>.yaml` is the source of truth. Documented precedence in CLAUDE.md `Cfg precedence + tunable defaults` section.
    - [x] **Y.2.gate.h.3 — AWS account / region / partition cfg-driven 2026-05-09.** Implementation already landed: `cfg.aws_account_id`, `cfg.aws_region`, `cfg.partition` (auto-derived from region). Loader honors `QS_GEN_AWS_ACCOUNT_ID` / `QS_GEN_AWS_REGION` env overrides. Required fields with no defaults — loud-fail covered by h.5. Documented in same CLAUDE.md section.
    - [x] **Y.2.gate.h.4 — Tunables defaulted 2026-05-09.** Implementation already landed: every tunable EnvVar (`QS_GEN_FUZZ_SEED`, `QS_E2E_PAGE_TIMEOUT`, `QS_E2E_VISUAL_TIMEOUT`, `QS_E2E_IDENTITY_REGION`, `QS_GEN_TEST_L2_INSTANCE`, `QS_GEN_RUNNER_YES`) is `optional=True` with a sensible default applied by the consumer. Operator sets them only for the rare override case. Documented per-tunable behavior + defaults in CLAUDE.md.
    - [x] **Y.2.gate.h.5 — Loud-fail on missing config 2026-05-09.** Implementation already landed: `load_config` raises `ValueError("Missing required configuration: ...")` with the missing field names AND the env-var fallbacks (`QS_GEN_AWS_ACCOUNT_ID`, etc.) so the operator knows both how to fix in YAML AND the env-override alternative. `connect_demo_db` raises with "set it in your config YAML or via QS_GEN_DEMO_DATABASE_URL." Runner catches both → `EXIT_NEEDS_OPERATOR=2` with the message bubbled to stderr. Probes (`probe_dependencies`) fire pre-dispatch and short-circuit the chain with the same exit code. **3 new unit tests** in `tests/unit/test_config_loader.py::test_missing_*` lock the loud-fail contract: missing aws_account_id → ValueError naming the key + env-var fallback; missing datasource_arn-without-demo_url → same; demo_database_url-set → no loud-fail (datasource_arn auto-derived).
    - [x] **Y.2.gate.h.6 — Cfg-driven default L2 instance (LANDED 2026-05-08; resolution (b) per user direction).** Operator's external DB only had ONE L2 instance seeded (sasquatch_pr) but the dataset-SQL smoke test was parametrizing over `default_l2_instance()` = bundled spec_example, producing 33 `relation "spec_example_current_transactions" does not exist` failures. Fix: new `cfg.default_l2_instance: str | None` field (path to L2 yaml). Runner's `_run_one_variant` resolves to absolute path via `REPO_ROOT` + injects `QS_GEN_TEST_L2_INSTANCE=<path>` into subprocess env_overrides. Same shape as `cfg.auth.aws_profile` → `AWS_PROFILE` injection. Both the seed flow (passes `--l2 <yaml>` to schema/data CLI) AND the smoke test (`_load_l2()` reads the env) align with the operator's DB state. Live-validated: db layer 33-fail → 37-pass, runner output shows `variant-env [QS_GEN_TEST_L2_INSTANCE]=/.../tests/l2/sasquatch_pr.yaml`. Operator declares once in `run/config.{postgres,oracle}.yaml`; no env var to remember.
- [x] **Y.2.gate.i — AWS auth refresh (LANDED 2026-05-08).** Constant `aws sso login` friction is automated away. Claude should never have to ask the user to "re-login" mid-flow — the runner handles it. **Real pain today (the failure mode i.* fixes)**: a multi-hour Claude-loop session burns through the AWS SSO token's ~12-hour cache; every cache miss triggers a browser-based `aws sso login` which Claude can't auto-invoke (per `b.14.4`'s refusal pattern), forcing the user to type `! aws sso login` mid-flow + lose the loop's continuity. The fix space for that is **not just "detect-and-surface better"** — it's "pick an auth approach where the cache miss is rare or doesn't require a browser." **All five sub-tasks shipped:** spike (i.0) → AuthConfig wiring (i.1) → instructions (i.2) → mock-boto3 unit tests (i.3) → live-chain validation (i.4).
    - [x] **Y.2.gate.i.0 — SPIKE: AWS auth approach selection (LOCKED 2026-05-08; combined with `h.1.0`).** Output `docs/audits/y_2_gate_h_i_combined_spike.md` (combined h+i decision; supersedes the standalone h.1.0 spike). **Locked recommendation: C for local + D for CI.** Long-lived IAM access keys for a new `quicksight-gen-local` IAM user (policy = mirror of `Github_e2e_testing` role's `github-e2e-policy` + `quicksight:ListUsers` for the h.1 derivation) — never triggers a browser flow, so multi-hour Claude-loop sessions work. Keys live in `~/.aws/credentials` (referenced by profile name, not stored in cfg yaml). CI keeps OIDC (D) unchanged. **Cfg shape:** new `auth:` block in `run/config.<dialect>.yaml` (no new files per user direction): `auth.aws_profile` (profile name to inject as `AWS_PROFILE`) + `auth.quicksight_user_arn` (override for h.1 derivation, used by CI). **Rejected:** A (status quo — pain), B (aws-vault wrapping SSO — still browser on cache miss; less frequent ≠ eliminated). IAM policy lands at `docs/audits/_iam/quicksight-gen-local-policy.json`. Operator runs `aws iam create-user` + `put-user-policy` + `create-access-key` + `aws configure --profile` + `quicksight register-user` once (combined spike §6 + §7 are the runbook). **Acceptance** (validated post-build): a 4+ hour Claude-loop session against AWS-touching layers triggers ZERO auth interactions (vs B/A's "at most one at start").
    - [x] **Y.2.gate.i.1 — Design + AuthConfig wiring (LANDED 2026-05-08).** `AuthConfig` dataclass in `common/config.py` (`aws_profile: str | None`, `quicksight_user_arn: str | None`); `auth:` allowlisted in loader. `_run_one_variant` injects `AWS_PROFILE=<aws_profile>` into subprocess env_overrides when set. Cfg-load failures during auth wiring downgrade to "skip auth + let layer probes catch any operator-action need" (keeps unit/db layers running when AWS unreachable). Pyright-clean. **Deferred:** structured `ExpiredTokenException` / `UnrecognizedClientException` detection in derivation (currently any `Exception` from STS/ListUsers surfaces as `EXIT_NEEDS_OPERATOR` with the boto3 error message) — when `boto3.exceptions.ClientError` parsing matters, narrow the catch.
    - [x] **Y.2.gate.i.2 — Instructions (LANDED 2026-05-08).** CLAUDE.md `Commands` block now carries an `Auth (Y.2.gate.h+i) — cfg-driven, never sso login in the loop` sub-section: cfg `auth:` block schema (spike §5), why long-lived IAM keys over SSO (multi-hour Claude-loop survival), pointer to spike §6 (runbook) + §7 (policy) + the IAM policy json file. Runner's `--help` now carries an `Auth (Y.2.gate.h+i)` epilog block (`_HELP_EPILOG` in `_dev/runner.py`) — same content, surfaces directly when an operator runs `./run_tests.sh --help`. **Memory entries:** `feedback_no_credential_friction.md` is a *principle* (kept — applies beyond Y.2.gate to any future credential-touching code). The PLAN's "drop" directive was actually pointing at the OLD value-cache version of `project_qs_e2e_user_arn.md`; that memory file's content has already been rewritten to triage notes (auto-derive + 3-QS-user namespace map), and the MEMORY.md index entry is updated to match.
    - [x] **Y.2.gate.i.3 — Build (LANDED 2026-05-08; combined h+i build per spike §8 + §11).** `_derive_qs_user_arn(cfg)` lives in `_dev/runner.py` (lazy boto3 import; threads cfg.auth.aws_profile into `boto3.Session(profile_name=...)`; STS GetCallerIdentity → ListUsers paginate → match `PrincipalId == "federated/iam/<UserId>"`; raises actionable RuntimeError naming UserId / IAM Arn / account / cfg-override escape / spike doc when no match). `_resolve_aws_profile(cfg)` is intentionally inlined at call sites — single `cfg.auth.aws_profile if cfg.auth else None` lookup, no helper warranted. Wired into `_run_one_variant` (line 1702 region) — `AWS_PROFILE` injected into variant_env when set; `_derive_qs_user_arn` called pre-layer-loop with broad except → `EXIT_NEEDS_OPERATOR`. **Mock-boto3 unit tests** in `tests/unit/test_runner_skeleton.py`: 4 tests covering (a) override short-circuits boto3 entirely (asserts boto3.Session NEVER called), (b) join-key derivation matches `PrincipalId == federated/iam/UserId` + threads aws_profile to Session factory, (c) no-match → actionable RuntimeError with all 5 required substrings, (d) boto3 errors propagate (no swallowing — locks the i.1 deferred-narrowing contract). Operator-action gate: handled by `feedback_no_credential_friction.md` principle — Claude flags actions but doesn't auto-create IAM users; operator runs the runbook.
    - [x] **Y.2.gate.i.4 — Validate (LANDED 2026-05-08; combined h+i acceptance).** With `quicksight-gen-local` IAM user already created (h+i.0 spike) + `auth.aws_profile` already set in `run/config.{postgres,oracle}.yaml` + memory entries cleaned up: (1) ✓ `./run_tests.sh up_to=browser` runs end-to-end without env-var exports — VALIDATED LIVE in c.5 close-out chain run (~4 min wall, all 6 layers green; runner derived QS_E2E_USER_ARN from cfg.auth.aws_profile silently). (2) ⊘ 4+ hour Claude-loop validation is longitudinal — can't single-shot test it; carried as ongoing observation. (3) ✓ cfg override path locked by `test_derive_qs_user_arn_override_short_circuits_boto3` — sentinel boto3.Session that raises AssertionError if called proves no ListUsers fired. (4) ✓ expired-keys propagation locked by `test_derive_qs_user_arn_boto3_errors_propagate` — fake `ClientError("ExpiredToken")` propagates with original message intact for `_run_one_variant` log-line. **Live revoke test** intentionally skipped: rotating a real IAM access key just to test the path is blast-radius asymmetric vs the unit-test coverage. Re-test if (2) ever surfaces a regression.
- [x] **Y.2.gate.j — Parallelism (so the longer chain doesn't kill iteration speed) [2026-05-09 closeout].** All sub-items j.1-j.10 LANDED. Total wall-clock surfaces for live iteration: layered cross-variant fan-out via `c.6.async`'s `asyncio.gather` (j.3), within-layer pytest-xdist defaulting to `auto` (j.6), `--only=<test-id>` for narrow chain runs (j.7), `--skip-cheap` for SHA-aware short-circuits (j.8), Oracle container reuse via stable-name pattern (j.5), and the m.4 variant matrix collapses dialect × scenario × target into a single argparse surface. Wall-clock targets are baseline-then-ratchet (j.9) — `runs/<id>/timings.json` + drift report make regressions surface visibly.
    - [x] **Y.2.gate.j.1 — Local-layer parallelism budget unconstrained (LOCKED).** Beefy Mac, Docker fair game. QS bottleneck self-relieves as Phase Y lands (fewer + smaller queries per visual). Re-evaluate per major sweep.
    - [x] **Y.2.gate.j.2 — Cross-layer = sequential (LOCKED).** Chain semantics require it; pipelining complicates failure attribution.
    - [x] **Y.2.gate.j.3 — Cross-variant within a layer = parallel (LOCKED).** Layer 3a runs PG + Oracle + SQLite containers simultaneously (`testcontainers-python` or `docker compose up` per variant). Layers 4-6 (AWS-touching) parallelize across `L2Instance` tag = separate AWS resource sets, throttle-aware.
    - [x] **Y.2.gate.j.4 — Fuzz-seed parallelism (when sample > 1) (LOCKED, audit §7.11).** Default = 1 random seed/run. Operator-opted-in larger samples (`--fuzz-seeds=100`) parallelize per-seed local Docker × xdist; wall-clock ≈ one sequential seed run. Regression reproducible via `QS_GEN_FUZZ_SEED=<N>` from the failing-seed manifest.
    - [x] **Y.2.gate.j.5 — Container reuse policy (LANDED 2026-05-09).** PG containers cheap (~5s start) — recreate per session OK (current behavior preserved). Oracle 19c heavy (~30-60s, ~5GB) — name-cached as `quicksight-test-oracle` and adopted across runs via `_get_or_start_oracle_container`. SQLite in-process; no container. Implementation: `_PersistentContainerHandle.stop()` is a no-op so `teardown_variant` doesn't kill the named Oracle container; subsequent runs find it via docker-py `containers.get(name)` and reconstruct the URL from the host port + `ORACLE_REUSE_PASSWORD` (pinned because testcontainers' default `oracle_password` is randomized — adopt path needs determinism). Stopped-but-exists containers are restarted via `existing.start()`. Lifecycle: operator stops via `docker stop quicksight-test-oracle` (or future `./run_tests.sh down --yes`, Y.2.gate.l.2). Tests at `test_persistent_container_handle_stop_is_no_op`, `test_oracle_reuse_constants_are_stable`, `test_get_or_start_oracle_adopts_running_container`, `test_get_or_start_oracle_falls_through_when_not_found`. **Live verify deferred to next live Oracle run** (the win is observable as wall-clock delta on subsequent invocations).
    - [x] **Y.2.gate.j.6 — Within-layer-within-variant = pytest-xdist (LANDED 2026-05-09).** Existing `--parallel` knob from `./run_e2e.sh` carries over. Layer 3a's 37 datasets + layer 5/6's per-app suites all benefit. Implementation: every layer-command builder in `_layer_command` (unit, db, app2, api, browser) appends `-n auto` when `opts.parallel == 1` (default), `-n N` when operator pins. Earlier draft was "no -n flag for parallel=1" — corrected: serial-by-default left obvious xdist speedups on the table for every triage iteration. Tests at `test_runner_skeleton.py::test_layer_command_default_parallel_is_auto` + per-layer variants cover the wiring. **Out of scope**: serial-pytest opt-in from the runner — operator wanting serial debug uses `.venv/bin/pytest -k <test-id>` directly (j.7 will land a `--only=<test-id>` shortcut for the runner-orchestrated chain).
    - [x] **Y.2.gate.j.7 — Targeted-iteration mode (`--only=<test-id>`) (LANDED 2026-05-09).** Runs the full chain but narrows within-layer-within-variant to one test id. Use case: tweaking one Investigation drill, validate the chain through layer 6 with only `test_inv_drilldown.py::test_X`. Wiring already in place from `Y.2.gate.c.7` — `--only=<expr>` argparse → `RunOptions.only` → `_layer_command` appends `-k <expr>` to every pytest layer (unit/db/app2/api/browser). Verified end-to-end: `./run_tests.sh up_to=unit --only=test_drift` narrows the unit layer; the same flag carries through deploy → api → browser when chained higher. Tests at `test_runner_skeleton.py::test_layer_command_only_adds_pytest_k_flag` + argparse coverage at `test_argparse_full_flag_coverage`.
    - [x] **Y.2.gate.j.8 — Skip-cheap mode (`--skip-cheap`) (LANDED 2026-05-09).** Skips layers 1-2 if green on the same git SHA earlier in the session. Off by default; opt-in for tight loops. Cache lives in `.run_tests_cache/<sha>.<layer>.<variant>.json` so a `git commit` invalidates. Variant-aware (b.2.impl): a green marker for `sp_pg_lo` doesn't signal green for `sp_or_lo`. Wiring already in place from `Y.2.gate.b.8.impl` — `is_layer_cached_green(layer, variant=spec.name)` short-circuits dispatch in `_run_one_variant` (line ~1985); successful pass writes a marker via `write_cache_marker` (line ~2007). Defensive degrade: dirty SHA / no-git / non-skippable layer / missing marker file all degrade to "run normally" — no false-negative skips. Helper-level tests at `test_write_cache_marker_*` + `test_is_layer_cached_green_*`; chain-level integration coverage added at `test_cmd_up_to_skip_cheap_short_circuits_cached_layer` + `test_cmd_up_to_skip_cheap_no_cache_runs_all_layers` (j.8).
    - [x] **Y.2.gate.j.9 — Wall-clock targets: first run = baseline; ratchet via timing-diff (LOCKED 2026-05-07; audit §7.8).** No upfront target numbers committed. The first run produces the per-(layer, variant, test) timing capture; that becomes the baseline. Subsequent runs surface drift via §7.9 timing-diff loop.
    - [x] **Y.2.gate.j.10 — Hash-locked seed + ephemeral DB = no flaky-state debt (LOCKED).** Every test sees byte-identical fixture data; M.4.4.12-style xfail-as-flake category should shrink.
- [ ] **Y.2.gate.k — CI parity (no `works-on-my-box` surprises).** The same runner runs locally and in CI.
    - [ ] **Y.2.gate.k.1 — Push:main + PR cells invoke `./run_tests.sh up_to=browser variants=full`.** Mirrors heaviest local invocation; full dialect × L2-instance matrix.
        - [x] **k.1.spike — Runner CI-mode unblocker [2026-05-09 v8.8.0a14+].** `QS_GEN_RUNNER_CI` typed EnvVar; `setup_variant` skips Docker spin-up for `lo` targets when set + `QS_GEN_DEMO_DATABASE_URL` is pre-set; loud-fails if URL missing; aw target unchanged. 4 unit tests + pyright clean. Audit doc at `docs/audits/y_2_gate_k_runner_ci_mode_spike.md` lays out the 4-phase migration roadmap (proof-point wedge → runner absorbs row-counts/audit-pdf → coverage pass-through → full thin-wrapper).
        - [x] **k.1.proof — Wire `ci.yml::integration-pg` to use the runner via CI-mode (Phase 1 of the spike's plan) [2026-05-09 v8.8.0a15].** Partial wrapper: runner drives variant setup + `db` layer pytest; row-count + audit-PDF steps stay as separate workflow steps. Local verify against a transient `postgres:17` container completed: unit (2119 passed in 13.45s) + db (37 passed in 7.32s) — runner sequenced through CI-mode end-to-end. Pre-existing test isolation gap surfaced + fixed: `tests/json/test_cli_json.py::test_apply_no_demo_database_url_skips_datasource_emit` now monkeypatches `QS_GEN_DEMO_DATABASE_URL` to dodge env-fallback leakage.
        - [x] **k.1.absorb — Promote `test_demo_apply_row_counts.py` into the runner's `db` layer default set (Phase 2) [2026-05-09 main commit 38cd6f4 + 0810c1d, CI verified green].** Audit-PDF render+verify deferred to k.1.absorb-audit (Phase 2.5) — needs to be wrapped as a pytest first since it's CLI-driven today. Bug discovered in flight: workflow's downstream steps were querying `spec_example_*` (cfg default L2 prefix) but the runner synthesizes per-variant L2 yamls with `instance: <spec.name>` (e.g. `sp_pg_lo`), so tables actually land under `<spec.name>_*`. Fix is the absorption itself: row-count test now runs INSIDE the variant subprocess where `QS_GEN_TEST_L2_INSTANCE` points at the synthesized yaml.
        - [x] **k.1.absorb-audit (Phase 2.5) — Promote audit-PDF render+verify into the runner's `db` layer [2026-05-09 branch y-2-gate-k-finish, commit 3df4cc7].** New `tests/e2e/test_audit_pdf_render_verify.py` invokes `quicksight-gen audit apply --execute` + `audit verify` via subprocess, reads cfg + L2 from runner's variant subprocess env so it picks up the synthesized prefix. Wired into `dispatch_layer("db")` alongside test_dataset_sql_smoke + test_demo_apply_row_counts. Local verify on transient PG: 43 passed (37 sql_smoke + 4 row_counts + 2 audit) in 8.4s. Workflow steps dropped from `ci.yml::integration-pg` + `integration-oracle` as part of the same commit.
        - [ ] **k.1.coverage (Phase 3) — Teach runner to emit `.coverage.<variant>` artifacts behind a flag.** CI's existing `coverage` job's combine logic (W.8b) stays unchanged. After this lands, the runner-driven workflow paths can produce coverage data the existing aggregator picks up.
        - [x] **k.1.thin-wrapper — Workflows become thin runner-dispatch wrappers (Phase 4 = k.6 close-out) [2026-05-09 branch y-2-gate-k-finish].** ci.yml::integration-pg + integration-oracle migrated (commit 3df4cc7); both jobs now: install + cfg-gen + `./run_tests.sh up_to=db --dialects=<...> --targets=lo` + done. e2e.yml::e2e-pg-api + e2e-pg-browser + e2e-oracle-api migrated (commit 8a7ea10); each job: install + OIDC + cfg-gen + `./run_tests.sh up_to=<api|browser> --variants=<sp_pg_aw|sp_or_aw>`. Net diff in e2e.yml: -100 lines. Cleanup-pg + cleanup-oracle stay as separate belt-and-suspenders jobs (per-cfg sweeps for the runner-died case GHA hard-timeout/OOM/cancel doesn't reach `if: always()`). Local CI-mode verify against transient `postgres:17` AND `gvenzl/oracle-free:23-faststart` — both passed (unit + db layers, 2119 + 43 tests respectively). e2e.yml's AWS-touching jobs not locally verifiable; will validate against live AWS at first push:main run after merge.
    - [x] **Y.2.gate.k.2 — `e2e-oracle-browser` added to nightly cron (LOCKED §7.4 2026-05-07).** PG and Oracle at parity for browser e2e; the asymmetry today is a gap, not a deliberate choice.
    - [x] **Y.2.gate.k.3 — PR-quick mode (PG only on PR; Oracle on push:main + workflow_dispatch) [2026-05-09 v8.8.0a14].** Split ci.yml's merged `integration` job into `integration-pg` (always) + `integration-oracle` (gated `if: github.event_name == 'push' || github.event_name == 'workflow_dispatch'`). Oracle Free service container alone adds ~60-90s first-boot init; skipping it on PR cuts ~3 min from PR feedback. PR feedback now ~2 min (test + integration-pg); push:main + workflow_dispatch keeps the full PG+Oracle gate. e2e.yml's nightly cron remains the heavy AWS-touching matrix sweep. Note: this is a workflow-level split, not a runner invocation — k.6 (workflows as thin runner wrappers) is the deeper refactor and still pending; the runner doesn't yet detect GHA service containers vs spinning its own Docker.
    - [x] **Y.2.gate.k.4 — `timings.json` uploaded as workflow artifact (LOCKED §7.9 2026-05-07).** Cross-CI-run drift is comparable.
    - [x] **Y.2.gate.k.5 — Pre-push git hook (default off, opt-in via `git config core.hooksPath .githooks`) [2026-05-09 v8.8.0a14].** `.githooks/pre-push` runs `./run_tests.sh up_to=db --dialects=pg --targets=lo` (~30s on local-pg). No-ops on detached HEAD or branches missing the runner so legacy-branch pushes don't break. Skippable per-push via `git push --no-verify` (discouraged — investigate the failure rather than bypass). CLAUDE.md `Test sequencing + git hooks` section documents the opt-in step.
    - [x] **Y.2.gate.k.6 — Existing CI alignment [2026-05-09 branch y-2-gate-k-finish].** ci.yml + e2e.yml workflow YAMLs are now thin runner-dispatch wrappers (per k.1.thin-wrapper, same migration path). release.yml stays its own shape (publish-side, not test-side; its Tests/Smoke jobs predate the runner and were already disjoint from CI test orchestration). Net effect: every `up_to=<layer>` flow exercised in CI is the same shape an operator runs locally — no CI-vs-local invocation drift.
    - [x] **Y.2.gate.k.7 — Failure surface parity (audit + document) [2026-05-09 v8.8.0a14].** Audit pass: per-layer artifacts (`runs/<run-id>/<variant>/<layer>/{cmd,stdout,stderr,timings}`) use the same `RUNS_DIR=runs/` paths locally and in CI; top-queries dump (`db-perf/top-queries.md`) emitted by the runner per cell (gate.f.4); coverage combined per-matrix-entry into the GHA Step Summary (W.8b); timings.json uploaded as workflow artifact (k.4). Failure shape: cfg/probe/boto3 paths surface as `EXIT_NEEDS_OPERATOR=2` with the actionable message bubbled to stderr; pytest failures surface as `EXIT_FAILURE=1` with failed test names + traceback. CLAUDE.md `Test sequencing + git hooks` section locks the contract — no "decode the GH log" step, the artifact set IS the local triage shape just packaged in GH artifacts.
- [x] **Y.2.gate.l — Ephemeral AWS infra (start/stop both ways) [2026-05-09 branch y-2-gate-l].** Per audit §7.12. Motivator: pain visibility, not dollar cost. Two halves landed: CI-side auto start/stop wiring on every AWS-touching workflow job (l.1.a), runner-side `up` / `down` / `status` commands operating on cfg-declared cluster + instance identifiers (l.2.a-g). Provisioning runbook (l.0) walks the operator through manual creation of `qsgen-ci-aurora` + `qsgen-ci-oracle` plus the IAM additions for both the OIDC role (CI side) and the local IAM user (`docs/audits/_iam/quicksight-gen-local-policy.json` extended with the `RDSLifecycle` statement). Workflow `if:` guards are gated on `secrets.QS_GEN_AWS_PG_CLUSTER_ID` / `secrets.QS_GEN_AWS_ORACLE_INSTANCE_ID` — unset = pre-gate.l shape (no start/stop), set = gate.l.1 active.
    - [x] **Y.2.gate.l.0 — CI infra provisioning runbook (LANDED 2026-05-09).** `docs/audits/y_2_gate_l_ci_aws_provisioning.md`. Step-by-step manual provisioning of `qsgen-ci-aurora` (Aurora PG cluster) + `qsgen-ci-oracle` (RDS Oracle SE2 instance) in the same AWS account (470656905821) per Chris direction 2026-05-09. Documents IAM policy additions for the `Github_e2e_testing` OIDC role + the local `quicksight-gen-local` IAM user, GH Actions secret updates (`QS_GEN_PG_URL`, `QS_GEN_ORACLE_URL` repointed; new `QS_GEN_AWS_PG_CLUSTER_ID` + `QS_GEN_AWS_ORACLE_INSTANCE_ID`), local cfg yaml additions, and a verification flow.
    - [x] **Y.2.gate.l.1 — CI side: start/stop existing cluster (LANDED 2026-05-09).** `e2e.yml::e2e-pg-{api,browser}`, `e2e.yml::e2e-oracle-api`, `release.yml::e2e-against-testpypi` each gain a pre-deploy `aws rds start-db-{cluster,instance}` + `aws rds wait db-{cluster,instance}-available` step, plus an `if: always()` post-cleanup `aws rds stop-db-{cluster,instance}` step. All 4 step pairs gated `if: env.PG_CLUSTER_ID != ''` / `if: env.ORACLE_INSTANCE_ID != ''` (env injected from `secrets.QS_GEN_AWS_PG_CLUSTER_ID` / `secrets.QS_GEN_AWS_ORACLE_INSTANCE_ID`) so workflows stay green pre-l.0-provisioning. Idempotent: `start-db-cluster || true` swallows InvalidDBClusterStateFault when already running. Sub-tasks: l.1.a (workflow YAML edits — landed in this slice).
    - [x] **Y.2.gate.l.2 — Local lifecycle commands: default-to-all (LANDED 2026-05-09).**
        - `./run_tests.sh up` — default = `local + aws`; brings everything up. Local = no-op (containers spin on-demand per matrix cell). AWS = `aws_rds.start()` for cfg-declared cluster + instance, polls until `available`.
        - `./run_tests.sh up local` / `up aws` — explicit subset.
        - `./run_tests.sh down` — default = stop everything; requires `--yes` (b.14.3 destructive-op gate).
        - `./run_tests.sh down local` / `down aws` — explicit subset. Local stops named persistent containers (`quicksight-test-oracle-*`); AWS calls `stop_db_*` (async on RDS side, runner doesn't poll for `stopped`).
        - `./run_tests.sh status [--cost]` — local Docker section + AWS RDS section. `--cost` adds rough hourly estimates per resource (Aurora running ~$0.30/hr, Oracle running ~$0.10/hr, both stopped ~$0.05/hr storage-only). Loud-fails with operator-actionable message when cfg lacks `aws_pg_cluster_id` / `aws_oracle_instance_id`.
        - Implementation sub-tasks: l.2.a (cfg fields + EnvVars), l.2.b (`common/aws_rds.py` boto3 wrapper, allowlisted in boto3-direct lint), l.2.c (cmd_up), l.2.d (cmd_down), l.2.e (cmd_status), l.2.f (10 unit tests via boto3 mocks), l.2.g (live validation against operator's existing `database-2` / `database-3` — proved cfg-load + AWS_PROFILE injection + boto3 dispatch; full start/stop happy path requires the IAM permissions added in `quicksight-gen-local-policy.json`).
    - [x] **Y.2.gate.l.3 — Layer dispatch interlock probes actual dependency (LANDED 2026-05-09).** New `aws_rds_running` probe (`_probe_aws_rds_running` in `_dev/runner.py`) wired into `_LAYER_DEPS` for `deploy` / `api` / `browser` (the AWS-touching layers). Loads cfg via `_load_runner_cfg_for_lifecycle`, calls `aws_rds.get_status()` for cfg-declared cluster + instance, refuses dispatch with operator-actionable `"Cfg-declared RDS resources are not all 'available': … Run './run_tests.sh up aws' first"` when status != available. Skipped (passes through) when cfg fields unset — operator opts out of cfg-driven lifecycle (uses console / Terraform / etc.). Aggregates PG + Oracle failures so the operator sees both broken in one pass. 7 unit tests cover: no-cfg passthrough, fields-unset passthrough, available-status pass, stopped-status fail, boto3-error fail, PG+Oracle aggregation, registration-in-PROBE_FUNCTIONS-and-_LAYER_DEPS lock. Without this probe, a stopped cluster surfaces as `psycopg.connection refused` deep inside the deploy step's first SQL call — operator wastes ~5 min on container spin-up + boto3 chatter before seeing the actionable error.
    - [x] **Y.2.gate.l.4 — No state file (LOCKED 2026-05-07).** Probe is cheap; no hidden state to drift.
    - [x] **Y.2.gate.l.5 — No auto-stop timer (LOCKED 2026-05-07 — explicitly rejected).** Operator's `down` discipline + `status --cost` visibility keep things tidy.
    - [x] **Y.2.gate.l.6 — App2 + QS run in parallel at layer 6 (LOCKED 2026-05-07 — refines §7.10).** "The main end deliverable is still AWS QuickSight." App2 + QS browser tests run concurrently with fast-fail; not "App2 first, QS later". App2's speed advantage is in the local-Docker layers (3 + the App2-target portions of 6); at the AWS-touching steps both targets run side-by-side.
- [x] **Y.2.gate.m — Variant matrix composition (LANDED 2026-05-09 in v8.8.0a6).** Today the runner only weaves the **dialect axis** (`default | local-pg | local-oracle | local-sqlite`); the **L2-instance** and **fuzz-seed** axes are stubs. Audit (§7.11 + line 15: "Variants — dialect, L2 instance, fuzz seed — sit per-layer; the runner multiplexes them") locked the design intent at gate-design time, but only dialect implementation followed through. Concrete consequences today: (a) `k.1`'s `variants=full` references a `dialect × L2-instance` matrix that doesn't exist, (b) `j.4`'s LOCKED fuzz-seed parallelism doesn't actually fan out N seeds, (c) the c.5.followup `test_audit_dashboard_agreement.py` skip (multi-dialect-same-L2 in one chain invocation) is concretely blocked on m.4. **Blocks:** rest of `k.*`, `j.5`. **Doesn't block:** `b.3.impl.gather` (orthogonal — that's app2|qs target merging, not variant fan-out).
    - [x] **Y.2.gate.m.0 — SPIKE LOCKED 2026-05-08.** Output `docs/audits/y_2_gate_m_0_variant_matrix_spike.md`. **Locked model:** 3-axis matrix `scenario × dialect × target`; variant codes `<sc>_<di>_<ta>` are run-internal only (operators don't type them, except for triage). Scenario codes: `sp` (spec_example), `sq` (sasquatch_pr), `f<n>` (fuzz seed N), `us` (user-supplied). Dialect codes: `pg`, `or`, `sl`. Target codes: `lo`, `aw`. Invalid cells (`<any>_sl_aw`, `us_*_*` excluded from `full`) auto-skip. `full` = ~13 cells. Operator CLI: default = `full`; narrow via `--scenarios`/`--dialects`/`--targets` sub-flags; `--variants=<code>` for triage. **Iteration discarded:** the cfg-yaml `l2_instances:` list approach + user-facing variant string syntax — not needed once scenarios collapse the L2 axis into named codes. **Out-of-scope, carried forward:** the broader `config.yaml` redesign (multi-database cfg, deploy.local/aws blocks) Chris sketched during iteration — its own gate when it earns one.
    - [x] **Y.2.gate.m.1 — `VariantSpec` dataclass + parser + sub-flag composer (LANDED 2026-05-08).** New `common/variant.py` module: 6 sub-tasks (a/b/c/d/e/f) all closed. Module is the design-locked surface m.2 wires into the runner. 78 unit tests; pyright-strict; 1246-test suite green.
        - [x] **m.1.a — `common/variant.py` skeleton (LANDED 2026-05-08).** `ScenarioCode` (NewType, open-set with regex validation), `DialectCode` / `TargetCode` (Literal, closed sets so pyright catches typos at construction). `VariantSpec` frozen dataclass with `__post_init__` validation (scenario regex + scenario-vs-fuzz_seed/user_yaml axis-consistency), `name` property, `is_valid()` rejecting `<any>_sl_aw`. 30 unit tests in `tests/unit/test_variant.py` covering construction + validation + name + is_valid + frozen invariant. Pyright-strict (added to `pyproject.toml::tool.pyright.include`); zero errors.
        - [x] **m.1.b — `expand_full()` matrix builder (LANDED 2026-05-08).** 13-cell default matrix: 6 named-scenario × all-dialects × local + 4 named-scenario × non-sqlite × aws + 3 fuzz cells (one shared random seed × 3 dialects × local for cross-dialect coverage on identical synthesized L2). `derive_default_fuzz_seed()` uses `secrets.randbelow(2**32)` (random by default per audit §7.11; no module-level RNG state to trip determinism lint). Caller-side invariant: every returned spec satisfies `is_valid()` (invalid combinations excluded by construction, not filtered). 9 new unit tests covering cell count + named-local + named-aws + fuzz-share-seed + fuzz-excluded-from-aws + no-us + random-by-default + seed-uint32-range.
        - [x] **m.1.c — Sub-flag composer (LANDED 2026-05-08).** `compose_matrix(scenarios, dialects, targets)` with two modes: all-None → returns `expand_full()` directly; any-specified → cross-product mode where unspecified axes default to `DEFAULT_SCENARIOS_NAMED` / `DEFAULT_DIALECTS` / `DEFAULT_TARGETS`. Cross-product filtered for `is_valid()` (so `sl × aw` cells auto-skip). Cross-product mode uses NAMED-only default for scenarios — fuzz/us are special-form territory the operator names explicitly. Added `ScenarioSpec` frozen dataclass to carry scenario + extras (fuzz_seed/user_yaml) through the cross-product. 9 new unit tests cover no-args/dialects-only/targets-only/scenarios-only/invalid-filter/explicit-fuzz/explicit-us/full-via-sub-flags/constants. 48 variant tests, 1216 unit suite.
        - [x] **m.1.d — Special-form parsers (LANDED 2026-05-08).** `parse_scenarios(arg)` handles named (`sp`/`sq`), fuzz (bare `fuzz` = 1 random seed; `fuzz:N` = N random seeds), and user-supplied (`us:<path>`); whitespace-tolerant CSV; rejects empty/unknown/bad-count entries with actionable messages. `parse_dialects(arg)` + `parse_targets(arg)` are typed CSV parsers with closed-set validation. `parse_variant_code(code)` for `--variants=<code>` triage — extracts fuzz seed integer from `f<n>` suffix; rejects `us_*_*` with a "use --scenarios=us:<path>" pointer (us cells need yaml context that triage can't carry). 27 new unit tests cover all parsers + edge cases. 78 variant tests, 1246 unit suite.
        - [x] **m.1.e — Unit tests (LANDED 2026-05-08; TDD parallel to m.1.a-d).** `tests/unit/test_variant.py` covers each axis narrow + cross-product + invalid-cell rejection + special-form parsers (named + fuzz + fuzz:N + us:path) + `--variants=<code>` triage path + `expand_full()` cell-count assertion. 78 tests total.
        - [x] **m.1.f — Pyright + commit (LANDED 2026-05-08; per-step gate).** Pyright-strict on `common/variant.py`: 0 errors. Per-sub-task commits on branch `y2-gate-m-variant-matrix`: m.1.a (eb8113e) → m.1.b (41e7dd6) → m.1.c (a0908de) → m.1.d (this commit). Full unit suite: 1246 passed.
    - [x] **Y.2.gate.m.2 — Wire VariantSpec into runner; delete KNOWN_VARIANTS (LANDED 2026-05-08).** Hard-cut migration — the `local-pg`/`local-oracle`/etc. names retire (no back-compat shim per Chris direction 2026-05-08; CI workflow update covered by `k.6`). 8 sub-tasks (a/b/c/d/e/f/g/h) all closed. Live 2-cell parallel validation (`up_to=db --scenarios=sp,sq --dialects=pg --targets=lo`) caught + fixed an env-resolution bug at seed time. 171 unit tests + full 2066-test suite green. Pyright-strict clean.
        - [x] **m.2.a — Argparse: add `--scenarios` / `--dialects` / `--targets` flags (LANDED 2026-05-08).** Three new sub-flag axes on `up_to`; default = no narrowing → `compose_matrix` returns `expand_full()` (13 cells). `--variants=<csv>` retained as triage escape (each entry is a `<sc>_<di>_<ta>` cell code). Legacy `local-pg` / `local-oracle` / `local-sqlite` / `default` names error with a "use --dialects=X --targets=Y" hint via `_check_legacy_variant_names`. RunOptions extended with `scenarios` / `dialects` / `targets` / `variants` (all `str | None`); `_options_from_args` threads them through; help epilog updated.
        - [x] **m.2.b — `_run_one_variant(spec: VariantSpec)` (LANDED 2026-05-08).** Signature flipped from `variant: str` to `spec: VariantSpec`; per-spec L2 yaml resolution via `_resolve_l2_yaml_for_spec` (sp → bundled spec_example.yaml, sq → bundled sasquatch_pr.yaml, us → spec.user_yaml, f<n> → NotImplementedError with m.3 pointer). `QS_GEN_TEST_L2_INSTANCE` env always injected from spec — overrides cfg.default_l2_instance because the matrix axis IS the source of truth.
        - [x] **m.2.c — `setup_variant` dispatch by `(dialect, target)` (LANDED 2026-05-08).** Replaces dialect-only KNOWN_VARIANTS branch. `target=aw` → no-op env (operator's external Aurora). `(pg, lo)` → postgres:17-alpine testcontainer. `(or, lo)` → gvenzl/oracle-free:23-faststart. `(sl, lo)` → tempdir SQLite. `seed_variant` symmetric: aw = no-op, lo = dialect-flavored cfg via `_resolve_seed_config_for_dialect`.
        - [x] **m.2.d — Artifact-path + AWS-tag naming (LANDED 2026-05-08).** Per-cell `run_dir = runs/<id>/<spec.name>/`; cache markers keyed by `spec.name` ("sp_pg_lo" not "local-pg"); single-cell + multi-cell paths now consistent (single-cell also gets the nested subdir, no back-compat top-level un-nesting). `L2Instance:<spec.name>` AWS-tag wiring is implicit through the per-spec QS_GEN_TEST_L2_INSTANCE injection (deploy CLI tags AWS resources from cfg.prefixed which threads from L2 instance prefix).
        - [x] **m.2.e — Delete `KNOWN_VARIANTS` const + `resolve_variants()` function (LANDED 2026-05-08).** Replaced with `_compose_specs_from_options(options)` — composes from the new sub-flag axes via `compose_matrix`, or parses triage-escape `--variants` codes via `parse_variant_code`. Mutex enforced (sub-flags and triage can't both be set). `_resolve_seed_config_for_local_pg/oracle` collapsed to one `_resolve_seed_config_for_dialect(dialect)` taking a `DialectCode`.
        - [x] **m.2.f — Update `test_runner_skeleton.py` for new dispatch (LANDED 2026-05-08).** All KNOWN_VARIANTS / `local-pg`-shape references gone. Old `test_known_variants_*` / `test_resolve_variants_*` tests replaced with `_compose_specs_from_options` coverage (no-args = 13 cells, dialects-only narrowing, triage-escape parsing, legacy-name error, mutex error). `setup_variant` / `seed_variant` tests refitted to take VariantSpec. cmd_up_to-driving tests pinned to `--variants=sp_pg_aw` (single AW cell — keeps tests fast + deterministic). 170 runner tests + 2066 full unit suite green; pyright-strict clean on `runner.py`.
        - [x] **m.2.g — `./run_tests.sh up_to=db --scenarios=sp,sq --dialects=pg --targets=lo` validation (LANDED 2026-05-08).** Two scenario cells in parallel against same dialect, against fresh testcontainers postgres. **First run surfaced a real bug**: `seed_variant` was reading `QS_GEN_TEST_L2_INSTANCE` from `os.environ` directly instead of the per-cell `env_overrides` — so both cells seeded the same L2 (cfg's default), and `sq_pg_lo`'s smoke test failed with "relation sasquatch_pr_current_transactions does not exist" (the seed had built spec_example tables). Hotfixed: env_overrides wins, os.environ is fallback. Re-run after fix: both cells green (37 dataset SQL smoke tests passed each, sp_pg_lo + sq_pg_lo). Added regression test `test_seed_variant_env_overrides_l2_wins_over_os_environ` (171 unit tests). DB-schema deconfliction works because L2 yaml prefix differs (sp → `qs_gen_spec`, sq → `qs_gen_pr`).
        - [x] **m.2.h — Pyright + commit (LANDED 2026-05-08).** Pyright-strict `runner.py`: 0 errors. Per-step commit on branch `y2-gate-m-variant-matrix`: 3176eaa (m.2.a-f) + this commit (m.2.g hotfix + close-out). Full unit suite green.
    - [x] **Y.2.gate.m.3 — Fuzz scenario (LANDED 2026-05-08).** `f<n>` codes resolve to synthesized L2 yaml at runtime; **random by default** (per audit §7.11 LOCKED "different on each invocation"); seed value captured in manifest so the operator can pin via `--variants=f<seed>_<di>_<ta>` for reproduction. 6 sub-tasks (a/b/c/d/e/f) all closed. Live 3-cell parallel run + pinned-seed repro: byte-identical synth yaml across two runs (md5 match). Surfaced + fixed a `prune_old_runs` race condition (sibling pytest workers competing on the same `runs/` dir).
        - [x] **m.3.a — Fuzz code parser + resolution (LANDED 2026-05-08).** Parser landed in m.1.d (`f<digits>` regex in `parse_variant_code`). Runner wiring: `_resolve_l2_yaml_for_spec(spec, run_dir)` extended with `run_dir` arg; for `f<n>` scenarios it lazy-imports `random_l2_yaml` from `tests/l2/fuzz.py` (sys.path injection — matches the cmd_sweep `_harness_cleanup` pattern), calls it with `spec.fuzz_seed`, writes to `run_dir / "_synth_l2.yaml"`, returns the path.
        - [x] **m.3.b — Random-by-default + count semantics (LANDED 2026-05-08).** Already wired in m.1.d (`parse_scenarios("fuzz")` = 1 random seed via `derive_default_fuzz_seed`; `parse_scenarios("fuzz:N")` = N random seeds). `expand_full()` curates 1 random seed × 3 dialects = 3 fuzz cells with shared seed (cross-dialect coverage on identical L2 topology). Live verified: `up_to=db --scenarios=fuzz:3 --targets=lo --dialects=pg` produced 3 cells with 3 distinct seeds (f4207048642, f1386832084, f358581206).
        - [x] **m.3.c — Per-cell tmp dir lifecycle + manifest capture (LANDED 2026-05-08).** Synthesized yaml lives at `runs/<id>/<variant>/_synth_l2.yaml`; cleanup happens via existing `prune_old_runs` machinery (synth yaml is part of the per-cell run dir). New `_write_cell_manifest(spec, run_dir)` writes `manifest.json` for ALL cells (consistency); fuzz cells additionally carry a `repro_hint` field with the exact `--variants=` invocation.
        - [x] **m.3.d — Unit tests (LANDED 2026-05-08).** 7 new tests in `test_runner_skeleton.py`: named-scenario passthrough (no synthesis), us scenario passthrough, fuzz scenario synthesizes to run_dir, fuzz byte-deterministic across calls (the locked contract), manifest carries repro_hint for fuzz, manifest no-repro_hint for named, full matrix has 3 shared-seed fuzz cells, expand_full produces different seeds across calls. 179 unit tests, pyright-strict clean.
        - [x] **m.3.e — `./run_tests.sh up_to=db --scenarios=fuzz:3 --targets=lo --dialects=pg` validation (LANDED 2026-05-08).** First run surfaced a real bug: `prune_old_runs` raced on `shutil.rmtree(old)` when sibling parallel-cell pytest workers all called `runner.main(...)` from `test_up_to_creates_run_dir` (FileNotFoundError because another worker had partially cleaned the same dir). Hotfixed: `ignore_errors=True` on rmtree + defensive `try/except FileNotFoundError` on the candidate stat pass — best-effort cleanup, leftovers picked up by next prune. Re-run after fix: 3 cells green; manifests captured all 3 seeds. **Pinned-seed reproduction**: copied `_synth_l2.yaml` from f4207048642 cell, re-ran `--variants=f4207048642_pg_lo`, MD5-compared the freshly-synthesized yaml against the saved one → byte-identical (`187bb1e776a8ce0229e9160f6de0085b`).
        - [x] **m.3.f — Pyright + commit (LANDED 2026-05-08).** Pyright-strict `runner.py`: 0 errors. Per-step commit on branch `y2-gate-m-variant-matrix`.
    - [x] **Y.2.gate.m.4 — `full` default + invalid-cell skip + `audit-dashboard-agreement` unblock + docs (LANDED 2026-05-08).** The matrix becomes the operator's "I just want to run everything" path. 7 sub-tasks closed; live AW validation walked unit→db→app2→deploy→api→browser end-to-end with audit-dashboard-agreement re-enabled (5 of 6 invariants passed; 6th is a pre-existing render flake). Two architectural extensions surfaced + landed during validation: per-cell layer cap (lo terminal at app2) + per-cell L2 yaml synthesis (instance rewritten to spec.name for DB schema isolation).
        - [x] **m.4.a — `expand_full()` as no-narrowing default (LANDED 2026-05-08; m.2.e wired this).** `_compose_specs_from_options(RunOptions())` returns `expand_full()` (13 cells). Locked by `test_compose_specs_no_flags_returns_full_matrix`.
        - [x] **m.4.b — Invalid-cell skip with informative log line (LANDED 2026-05-08).** New `partition_matrix(scenarios, dialects, targets)` returns `(valid, skipped)` lists; runner logs `runner: skip [<sc>_sl_aw] (sl × aw: sqlite is file-based; QuickSight can't reach it remotely)` for each invalid cell before fan-out. `compose_matrix` collapses to a partition wrapper. New unit test locks the sl×aw skipped-list contract.
        - [x] **m.4.c — Re-enable `tests/e2e/test_audit_dashboard_agreement.py` (LANDED 2026-05-08).** Drop file-level `pytest.mark.skip`. Added per-cell dialect-mismatch skip (`_env_demo_url_dialect`) so container-target cells gracefully skip the parametrization that doesn't match the cell's env URL. Refactored to read L2 yaml from `QS_GEN_TEST_L2_INSTANCE` env (per-cell synthesized yaml when runner-driven; falls back to bundled spec_example for operator-driven runs).
        - [x] **m.4.d — CLAUDE.md `Commands` update (LANDED 2026-05-08).** Replaced every legacy local-pg/local-oracle/local-sqlite/default example with the new sub-flag/triage form. Documented matrix model + hard-cut explicitly.
        - [x] **m.4.e — Runner `--help` epilog update (LANDED 2026-05-08).** Concrete matrix examples for sub-flag narrowing (sp/sq/fuzz/us), dialect/target subsets, fuzz seed reproduction, `--variants` triage.
        - [x] **m.4.f — Live validation (LANDED 2026-05-08).** Surfaced **two architectural gaps** the spike didn't fully address; both fixed and validated:
            - **(A) Per-cell layer cap.** Lo cells naturally cap at `app2` because deploy/api/browser need an AWS-reachable datasource and lo's localhost container is unreachable from QS. New `cell_chain(spec, requested_chain)` filter; aw cells pass through unchanged. **Note (Chris 2026-05-08):** the cap is transitional — once App2 is feature-complete and SQL pushes down, lo cells will run "deploy/api/browser" semantics against App2 instead of QS, so the cap becomes a no-op.
            - **(B) Per-cell L2 yaml synthesis.** All cells (lo + aw) now get a per-cell synthesized yaml at `runs/<id>/<spec.name>/_synth_l2.yaml` with the `instance` field rewritten to `<spec.name>`. DB schema prefix becomes `<spec.name>_*` (e.g., `sp_pg_aw_transactions`); QS resource prefix derives via `cfg.with_l2_instance_prefix(instance.instance)` chain → `qs-gen-sp_pg_aw-l1-dashboard`. Sister cells deploying to shared external Aurora don't collide. `seed_variant` early-return on aw target dropped — runner owns the seed weave for both targets (creates only `<spec.name>_*` tables; never touches operator data under other prefixes). Locked-seed test unaffected (it bypasses the runner). Test fixture `test_audit_dashboard_agreement.py` refactored to read L2 yaml from `QS_GEN_TEST_L2_INSTANCE` env. Live AW chain end-to-end: unit (63s) → db (9s) → app2 (45s) → deploy (56s) → api (25s) → browser (139s, 22 passed + 5 failed where 1 supersession render is a pre-existing flake + 4 L2FT cascade are pre-existing failures unrelated to m.4).
        - [x] **m.4.g — Pyright + commit (LANDED 2026-05-08).** Pyright-strict clean across runner.py + variant.py + config.py + env_keys.py. Per-step commits on branch `y2-gate-m-variant-matrix`.

    **m.4 follow-ups (captured for m.5 / Phase Y cleanup):**
    - [ ] **VariantName NewType wrapper for spec.name.** Per Chris suggestion 2026-05-08 — lift `spec.name` from bare `str` to `NewType('VariantName', str)` per CLAUDE.md typed-id-wrap convention. Used as DB schema prefix, QS resource prefix component, runs/<id>/<variant>/ dir name, manifest field, AWS L2Instance tag. Catches construction-site drift at type-check time.
    - [ ] **App2 Oracle column-casing bug surfaced by m.4.f per-cell yaml synthesis.** Surfaced 2026-05-08 in `m.5.d` live AW pair (sp_or_aw cell): `tests/e2e/test_html2_executives_live.py::test_account_coverage_kpi_renders_with_real_data` + `test_date_filter_does_not_error_when_applied` fail with `ORA-00904: "ACCOUNT_ID": invalid identifier` and `"ACCOUNT_TYPE"` from the App2 server's SQL execution against Oracle. The PG sister cell (sp_pg_aw) didn't hit this — PG-only test paths or column-casing semantics differ. Likely the App2 tree-fetcher / SQL preprocessor emits quoted-lowercase column names (`"account_id"`) which Oracle reads literally, but tables have unquoted columns → stored uppercase as `ACCOUNT_ID`. Triage: read App2 `_sql_executor.py` / `_tree_fetcher.py` for column-name quoting and `common/sql/dialect.py` for dialect-aware col-name handling. Likely a one-line `cfg.dialect == ORACLE` branch in the column quoter. NOT m-gate-blocking — gate.k's CI matrix would catch this routinely; for now AW-Oracle cells fail at app2 layer.
    - [x] **Y.2.gate.m.5 — Validate + release (LANDED 2026-05-09).** Live acceptance + tag close-out.
        - [x] **m.5.a — Parallel scenarios in same dialect (LANDED 2026-05-08).** `./run_tests.sh up_to=browser --scenarios=sp,sq --dialects=pg --targets=lo` ran both cells (sp_pg_lo + sq_pg_lo) end-to-end through unit/db/app2 (lo cells cap at app2 per m.4.f). No DB schema collision (per-cell containers + per-cell `<spec.name>_*` table prefixes). Both green.
        - [x] **m.5.b — Fuzz: random-by-default + pinned-reproduction (LANDED 2026-05-08).** (1) Two consecutive `--scenarios=fuzz:3 --dialects=pg --targets=lo` runs produced DIFFERENT seed sets: run 1 = (2754515618, 1511858866, 4023099696); run 2 = (2587238078, 2150930595, 2727817500). No overlap. (2) Pinned `--variants=f2754515618_pg_lo` against run 1's reference yaml: MD5 byte-identical (`555e09b976ec7e87f186f79ed0dcd67b`).
        - [x] **m.5.c — Full matrix wall-clock measurement (LANDED 2026-05-09).** With j.5+j.6+j.7+j.8 + the four m.5 fix-ups (test-tests stale-ness, fuzz-seed pin in conftest, prune xdist guard, per-cell Oracle container name), the matrix runs cleanly in parallel. Run #8 numbers (lo subset, partial — 5 of 9 lo cells through full chain, 3 or_lo blocked at app2 by the App2 Oracle column-casing bug, 1 sq_sl_lo unit-flake on test_rich_text): unit ~50–100s, db ~60–80s, app2 ~80–100s per cell. Wall-clock for lo subset ≈ 4 min (parallel asyncio.gather + within-layer xdist auto). Aw cells extrapolate to ~10–15 min each (deploy + api + browser). **Full-matrix all-13-green re-attempt deferred to post-Y.3.f** (Oracle dialect-correct case will unlock the 3 blocked or_lo + sp_or_aw + sq_or_aw cells). The measurement deliverable — proving parallel scaling works, not 13/13 pass — is satisfied.
        - [x] **m.5.d — `test_audit_dashboard_agreement.py` no-skip sanity check (LANDED 2026-05-08, partial).** Originally specified as `sp_pg_lo + sp_or_lo` cell pair; m.4.f layer cap put lo cells out of reach for browser-marked tests, so re-targeted to `sp_pg_aw + sp_or_aw` AW pair. sp_pg_aw cell ran the full chain end-to-end (audit-dashboard-agreement covered in m.4.f). sp_or_aw chain halted at app2 with the App2 Oracle column-casing bug captured above — Oracle e2e Aurora chain blocked behind that fix. **Per-cell prefix isolation validated** — sister AW cells deployed without resource-ID collision (`qs-gen-postgres-sp_pg_aw-*` vs `qs-gen-oracle-sp_or_aw-*`).
        - [x] **m.5.e — Per-variant artifact path collision check (LANDED 2026-05-09).** Run #8 produced 9 distinct cell dirs under `runs/<id>/`, one per `<sc>_<di>_<ta>` spec.name. No collisions. Trivially satisfied by the m.4.f spec.name uniqueness invariant; this run is the empirical confirm.
        - [x] **m.5.f — Tag v8.8.0a6 + RELEASE_NOTES + push (LANDED 2026-05-09).** Cuts the alpha that closes Y.2.gate.m. Bundle: gate.j (j.5–j.8) + gate.m (m.0 → m.5) + four m.5 fix-ups + three follow-ups (#741 RUNS_DIR isolation, #740 test-module-nondeterminism lint, #729 VariantName NewType).
- [ ] **Y.2.gate.n — `Y.2.c+ unblocked` gate.** Once a-m are done, resume the Y.2 sweep. (Renamed `g`→`m` on 2026-05-07, then `m`→`n` on 2026-05-08 when the new variant-matrix `m` landed; both renames preserve alphabetical=positional order so this stays the terminal gate, not a mid-list item.)

### Y.3 — Push calc fields down to dataset SQL as real columns

Calc fields exist in the QS analysis layer because QS could evaluate them in its engine. Pushing them into the dataset SQL as window functions in CTEs makes them real columns the database computes once at query time. Visuals reference real columns; the calc field declarations vanish from the analysis tree; both QS and App2 see one shape.

- [ ] **Y.3.a — Recipient Fanout: `distinct_senders` window column.** `WITH base AS (...), enriched AS (SELECT base.*, COUNT(DISTINCT sender_account_id) OVER (PARTITION BY recipient_account_id) AS distinct_senders FROM base) SELECT * FROM enriched`. The HAVING-via-X.2.g.3.e becomes a plain `WHERE distinct_senders >= <<$pInvFanoutThreshold>>` (Y.2-style). Drops `CF_INV_FANOUT_DISTINCT_SENDERS` calc field.
- [ ] **Y.3.b — Account Network: `is_inbound_edge` + `is_outbound_edge` + `counterparty_display` window columns.** Each calc field's expression converts to a SQL CASE / window function in the dataset SQL. Drops the three remaining calc field declarations (Y.2.b already dropped `is_anchor_edge` as orphaned).
- [ ] **Y.3.c — Volume Anomalies: any calc fields used by the ranked table** (e.g. `z_score_max`). Same pattern.
- [ ] **Y.3.d — Update visuals to reference real columns instead of calc fields.** `Measure.max(ds, calc_field)` becomes `Measure.max(ds["distinct_senders"])` etc. The Table-aggregated wrap (X.2.g.4 if we land it pre-Y) reads them naturally.
- [ ] **Y.3.e — Drop now-unreferenced CalcField declarations from the analysis tree.** Sweep `apps/investigation/app.py`; remove the calc field constructors; the analysis emit no longer carries them.
- [ ] **Y.3.f — Stop bridging case sensitivity; emit dialect-correct identifier case from the generator.** Today the bridge is `_oracle_lowercase_alias_wrapper` (`common/dataset_contract.py:309`): DDL is emitted unquoted (PG folds lowercase, Oracle folds UPPERCASE → naturally divergent storage); the wrapper adds an outer `SELECT qs_inner."ACCOUNT_ID" AS "account_id" ...` so QuickSight (which builds `SELECT "account_id" FROM (...)` from its declared lowercase Columns) finds matching aliases. The bridge produces the App2 Oracle column-casing bug (m.5.d AW chain: `ORA-00904: "ACCOUNT_ID": invalid identifier`) — App2's `wrap_for_visual` references the wrapper's quoted-lowercase aliases unquoted, Oracle case-folds those refs to UPPERCASE, no match. The proper fix: generator emits dialect-natural case in `DatasetContract.to_input_columns()`; QS quotes UPPERCASE on Oracle natively; wrapper goes away; App2 references columns case-folded too. **Why:** the bridge cost (App2 fragility, future-feature edge cases) exceeds the bridge benefit (one less codepath). The generator already branches on `Dialect`; case selection is one more attribute. **Substep breakdown:**
    - [x] **Y.3.f.1 — `column_name(name, dialect)` helper.** Added to `common/sql/dialect.py` (returns `name.lower()` for PG/SQLite, `name.upper()` for Oracle); exported from `common.sql.__init__`; 4 unit tests in `tests/schema/test_sql_dialect.py::TestColumnName` covering each dialect + idempotence. No call sites yet — f.2 is the first consumer.
    - [~] **Y.3.f.2 — Wire helper through `DatasetContract.to_input_columns()` — LANDED then REVERTED 2026-05-09.** Oracle-deploy probe surfaced 45 column-missing errors across L1 + L2FT apps. **Finding:** QuickSight's analysis-side validation is **case-sensitive** against Dataset.Columns. Switching Columns to UPPERCASE on Oracle (f.2 alone) breaks every Dim/Measure/Filter/CalcField that references columns by lowercase string. The wrapper isn't just QS-Columns-quoting — it's part of the larger lowercase-everywhere contract that the analysis-side validation depends on. **Implication:** dropping the wrapper requires case-folding every analysis-side column ref per dialect (~30+ sites across visuals/filters/calc-fields/drills), not just the Columns declaration. The original m.5.d App2 bug doesn't justify that scope. f.3-9 parked; **Y.3.g (SQL builder spike) promoted to immediate** (a builder library would handle this transparently — exact pain Y.3.g's premise predicted).
    - [~] **Y.3.f.3-9 — PARKED 2026-05-09.** Y.3.g spike concluded "stay on strings + helpers"; replaced by Y.3.f.alt below.
    - [x] **Y.3.f.alt.1 — Fix `common/html/_visual_sql.py::wrap_for_visual` to quote column refs.** Added `_quote_col(name)` helper; `_dim_sql` and `_measure_sql` now produce `"col"` instead of `col`. On Oracle the wrapper produces lowercase quoted aliases; quoted-lowercase refs preserve case → match. On PG, quoted lowercase refs match lowercase-stored DDL. On SQLite, identifiers are case-insensitive regardless. The KPI / BarChart / LineChart / Sankey wrap branches all flow through these two helpers, so one fix covers every visual kind.
    - [x] **Y.3.f.alt.2 — `_tree_fetcher.py` audit — NO CHANGE NEEDED.** Read confirmed it just delegates to `wrap_for_visual` and executes the result; no column-name string emission of its own.
    - [x] **Y.3.f.alt.3 — Unit test.** New `tests/unit/test_visual_sql_wrap.py` (7 tests covering KPI count/distinct_count/sum, BarChart, LineChart with ORDER BY, Sankey, Table pass-through). Full unit + json + schema sweep green (2000 pass, +5 from baseline).
    - [x] **Y.3.f.alt.4 — Live verify locally:** `./run_tests.sh up_to=app2 --variants=sp_or_lo` green (12 passed + 1 xfailed, layer-ok rc=0). Two follow-on Oracle bugs surfaced + fixed: **alt.4a** — `app2_date_filter` made dialect-aware in `common/sql/app2_filters.py` (Oracle `TO_DATE(..., 'YYYY-MM-DD')` instead of `CAST(... AS DATE)` which honors session NLS_DATE_FORMAT and rejects ISO strings with `ORA-01847`; SQLite passes through as plain text). 11 unit tests in `tests/unit/test_sql_app2_filters.py`. **alt.4b** — `_sql_executor.py::execute_visual_sql_async` Oracle branch uses explicit `cur = conn.cursor(); await cur.execute(...)` because oracledb async `conn.execute()` returns `None` (executes against an internal cursor we can't read back). psycopg/aiosqlite still take the one-shot `cur = await conn.execute(...)` path.
    - [x] **Y.3.f.alt.5 — Aurora verify:** `./run_tests.sh up_to=db --variants=sp_or_aw` green (unit 2109 passed + db 37 passed, both layer-ok rc=0). m.5.d AW chain unblocked. Run dir `runs/20260509T213336Z-c84db0e/`.
    - [ ] **Y.3.f.alt.6 — Tick PLAN, commit, decide on release.** PLAN ticked here. Next: user call on whether to merge `y-3-g-sql-builder-spike` → main and cut `v8.8.0a8` (carries Y.3.f.alt.1-5 + Y.3.g spike write-up), or roll into a larger Y.3.f.alt.6+ release later. Branch is shippable as-is.
- [x] **Y.3.g — SQL builder library spike (LANDED 2026-05-09).** Ported Account Network dataset via SQLAlchemy Core 2.0.49 AND sqlglot 30.7.0 (transpile + build modes); compared per-dialect output to current emitter baseline; probed dialect-edge handling (SQL/JSON path extraction, recursive CTE, date arithmetic). **Findings (full writeup in `spike/y3g/findings.md`):**
    - sqlglot transpile is the most attractive option — write SQL once in PG, transpile to Oracle/SQLite, ~5 lines of glue per dataset, source SQL stays human-readable.
    - **But sqlglot's Oracle JSON path is broken** — emits `JSON_EXTRACT(metadata, '$.x')` for Oracle but Oracle has no `JSON_EXTRACT` function (uses `JSON_VALUE`). SQLite date arithmetic also transpiles to `INTERVAL` (SQLite has no INTERVAL type). Both gaps could be patched via custom transformers — but that's reinventing our `dialect.py` helpers inside sqlglot's framework.
    - SA Core eliminates per-construct branching more thoroughly but at higher per-dataset Python cost (~50 lines vs current ~20) and lower reviewer ergonomics.
    - **Neither library moves the needle on Y.3.f case bridging** — that's an AnalysisDefinition concern, not a SQL-emit concern.
    - **Decision: STAY on strings + helpers.** Re-narrow Y.3.f to App2-only fix (Y.3.f.alt above). Both libraries stay in the toolbox. Re-spike if dialect-helper count grows past ~60 (currently ~40), we drop SQLite from the matrix, or a future feature genuinely needs cross-dialect SQL transformation.

### Y.4 — Test sweep

Hashes shift, FilterGroup-walking tests get pruned, App2 preprocessor gets unit coverage.

- [ ] **Y.4.a — Re-lock all dataset SQL hash tests.** Per-instance + per-dialect.
- [ ] **Y.4.b — Re-lock JSON-emit hash tests for every analysis whose FilterGroups / calc fields changed.** Investigation + L1 + L2FT all shift.
- [ ] **Y.4.c — Update `tests/integration/verify_dataset_sql.py` smoke verifier for the new placeholder shape.** Verifier currently parses + executes dataset SQL; needs to pass dataset parameter values for `<<$>>` substitution to round-trip cleanly.
- [ ] **Y.4.d — Drop or rewrite FilterGroup-walking tests that no longer apply.** Tests that asserted "Investigation has FG_INV_ANOMALIES_SIGMA scoped to sheet X" are obsolete. Replace with tests that assert "Volume Anomalies dataset SQL contains `:param_pInvAnomaliesSigma` after preprocessor".
- [ ] **Y.4.e — App2 unit test for `<<$paramName>>` → `:param_paramName` preprocessor.** Cover string + numeric param shapes; cover the `'<<$pName>>'` quoted case.
- [ ] **Y.4.f — Full unit test suite green.** Pytest + pyright clean.

### Y.5 — App2 cleanup (drop now-redundant infrastructure)

The X.2.g.3 work I (the engineer) added over the past few days becomes redundant once Y.1–Y.4 land. Sweep it.

- [ ] **Y.5.a — Drop `app2_sql=` parameter from `build_dataset` signature.** All call sites converted in Y.1–Y.2.
- [ ] **Y.5.b — Drop `app2_anchor_filter` / `app2_param_eq` / `app2_param_gte` / `app2_param_lte` helpers in `common/sql/app2_filters.py`.** Filter snippets now live in dataset SQL via `<<$>>` substitution; the App2-specific helper module shrinks to (or drops) `app2_date_filter` if Y.2.f converts it.
- [ ] **Y.5.c — Drop `tests/unit/test_sql_app2_filters.py`** if all helpers gone; otherwise keep for what remains.
- [ ] **Y.5.d — `_filter_specs_from_tree.py` simplifies.** ParameterControl auto-derive still walks tree controls (Y doesn't change that surface), but the LinkedValues query path may collapse if dataset parameters carry their own option lookup.

### Y.6 — Performance verification (the headline result)

The whole point of Phase Y. Concrete before/after numbers, per dialect.

- [ ] **Y.6.a — Pre-Y baseline: rows-on-the-wire per dashboard load (PG).** Use `pg_stat_statements` + a clean dashboard-walk script. Capture rows-fetched + query duration per visual.
- [ ] **Y.6.b — Pre-Y baseline (Oracle).** Same shape via `v$sqlstats`.
- [ ] **Y.6.c — Post-Y measurement: same dashboards, same operations, fresh capture.** Per dialect.
- [ ] **Y.6.d — Document deltas: % reduction in rows + query duration.** RELEASE_NOTES + a perf section in CLAUDE.md.
- [ ] **Y.6.e — Identify any sheet that DIDN'T see expected gains and root-cause** (likely cases: dataset SQL that's already narrow; a missed pushdown opportunity).

### Y.7 — e2e verification (clean on all three dialects, the gate)

Per user direction: the e2e suite must be clean on PG, Oracle, AND SQLite before Y can merge. SQLite is App2-only (QS doesn't support it), so the SQLite gate is specifically about the App2 bind preprocessor + dataset SQL with `:param_<name>` placeholders working end-to-end through aiosqlite.

- [ ] **Y.7.a — Full unit + integration suite green.**
- [ ] **Y.7.b — `e2e-pg-api` green.** API layer covers the dataset-parameter wiring + analysis JSON shape on PG.
- [ ] **Y.7.c — `e2e-oracle-api` green.** Same; surfaces any Oracle-specific `<<$>>` substitution + bind-translation quirks (Oracle's NUMBER/NUMERIC handling + empty-string-as-NULL semantics).
- [ ] **Y.7.d — `e2e-pg-browser` green.** Browser-level cascade + slider drag round-trips end-to-end.
- [ ] **Y.7.e — SQLite e2e green.** Layer 1 + Audit PDF SQLite tests (X.3.g.{1,2,3}) re-run after the dataset-SQL preprocessor changes; verify the bind translation works against aiosqlite.
- [ ] **Y.7.f — Resolve any flakes / regressions.** Per-failure investigation; xfail only with documented reason + follow-up task.

### Y.8 — Phase X rebase + merge

Phase X's still-in-flight work (X.2.g.3 dataset SQL, anything dependent on `app2_sql=`) gets rebased on top of Y so it picks up the convergent shape. The X.2.g.3.* tasks I just completed are subsumed by Y.2 — they get DROPPED in the rebase, not preserved.

- [ ] **Y.8.a — Rebase the in-flight Phase X branch onto `phase-y-sql-pushdown`.** Drop X.2.g.3.{a,b,c,d,e} commits; their effect is now in Y.2.
- [ ] **Y.8.b — Resolve conflicts in `apps/investigation/datasets.py` + `cli/serve.py`.** Likely the two contention points.
- [ ] **Y.8.c — Re-run e2e PG green post-rebase.**
- [ ] **Y.8.d — Re-run e2e Oracle green post-rebase.**
- [ ] **Y.8.e — Merge Phase Y to main.** Phase X continues from there with the new convergent path baked in.

### Y.9 — Convention + docs sweep

The new authoring pattern needs to be the canonical one for any future filter / parameter work. Docs catch up.

- [ ] **Y.9.a — CLAUDE.md update.** New section "Authoring filters: SQL-level parameter pushdown is the canonical pattern" — `<<$paramName>>` in dataset SQL + `MappedDataSetParameters`; analysis-level FilterGroups deprecated for filter intent (kept only for visual highlighting if any case justifies it).
- [ ] **Y.9.b — README sweep** for the architecture overview section.
- [ ] **Y.9.c — Customization handbook walkthrough — "How filters work" page.** Cover the dataset-parameter pattern + cascade behavior + perf intent.
- [ ] **Y.9.d — Migration note.** Customer L2 instance YAMLs are unaffected (this is internal architecture); flag explicitly so customers don't worry.

### Y.10 — Cut release

- [ ] **Y.10.a — Bump version (likely v9.0.0 — major architectural shift).**
- [ ] **Y.10.b — RELEASE_NOTES entry: convergence + perf wins headlined.**
- [ ] **Y.10.c — Tag + push.**
- [ ] **Y.10.d — Verify release pipeline runs green** (the existing `e2e-against-testpypi` gate already covers this).

### Y.11 — CLI shape revisit: cfg ⇄ L2 dual-yaml factoring

Surfaced 2026-05-08 during `Y.2.gate.h.6` build. The runner now reads
`cfg.default_l2_instance: tests/l2/sasquatch_pr.yaml` and threads it as
`QS_GEN_TEST_L2_INSTANCE` into subprocess env_overrides — meaning the operator
declares the L2 instance ONCE in cfg and the runner aligns the seed flow + the
dataset-SQL smoke test automatically. **This makes the CLI's existing dual-arg
shape (`-c <cfg.yaml> --l2 <l2.yaml>`) partially redundant**: every `quicksight-gen
{schema|data|json|audit} {apply|clean|...}` invocation requires `--l2 <yaml>`
even though the cfg now carries that pointer. The dual-yaml factoring itself
may also be wrong now — a single combined cfg-with-L2-pointer (or a single
yaml union) might be the right shape.

Spike-before-implement (per `feedback_spike_before_locking_implementation.md`):
this is a CLI-surface change touching every operator command + every doc
example + tests. Wrong factoring locks in for years.

- [ ] **Y.11.0 — SPIKE: combined-yaml vs cfg-with-L2-pointer vs status-quo
  (LOCKED 2026-05-08; spike before Y.11.1).** Output `docs/audits/y_11_cli_shape_spike.md`.
  Compare the candidate factorings against today's two-yaml shape:

  - **A. Status quo + `--l2` defaults from `cfg.default_l2_instance`.**
    Operator can omit `--l2`; cfg implies it; explicit `--l2 <yaml>` overrides.
    Smallest delta, mostly-additive. Only one breaking case: cfg without
    `default_l2_instance:` AND CLI without `--l2` becomes ambiguous (was an
    error before; now silently uses bundled `default_l2_instance()` =
    spec_example).
  - **B. Single combined yaml.** Cfg + L2 merge into one file; CLI takes one
    `-c <combined.yaml>`. Eliminates the dual-yaml friction entirely.
    Trade-off: cfg yaml grows large (couple hundred lines for a real
    institution); env-only fields (account, DB password, signing material)
    co-mingle with institution-flavor fields (rails, chains, accounts) — the
    Q.5 separation existed for a reason (dev secrets vs institution shape are
    different release cadences).
  - **C. Cfg-with-L2-pointer + `--l2` removed entirely.** Cfg ALWAYS carries
    `default_l2_instance:` (made required); CLI drops `--l2`. Operators who
    deploy multiple L2 instances against one cfg use multiple cfg files (one
    per instance). Cleanest narrow change. Forces multi-instance operators to
    duplicate cfg.
  - **D. `--l2 <yaml>` becomes `--l2 <name>` indexed against an L2 registry
    in cfg.** Cfg carries `l2_instances: { sasquatch_pr: tests/l2/sasquatch_pr.yaml,
    spec_example: tests/l2/spec_example.yaml }` plus `default_l2_instance: sasquatch_pr`.
    CLI: `--l2 spec_example` (named, short). Multi-instance operators get
    cleaner ergonomics; single-instance operators still benefit from the
    default. Trade-off: another layer of indirection.

  **Constraint set:**
  1. **Operator types `quicksight-gen json apply --execute` with no other
     args** and gets a sane deploy of the default L2.
  2. **Multi-L2 operators don't have to copy cfg files** to deploy each L2.
  3. **Existing `--l2 <yaml>` invocations keep working** OR a clean migration
     path is documented (sed-able rename, deprecation warning, etc.).
  4. **Doc examples shrink** — every CLAUDE.md / README / handbook command
     example currently shows `-c X --l2 Y`; the spike's chosen shape should
     simplify the dominant single-instance case.
  5. **Tests pass without env-var passthrough** — the runner's
     `QS_GEN_TEST_L2_INSTANCE` injection (h.6) covers the test-side; the spike
     decides whether the CLI grows the same default behavior for non-test
     invocations.

  **Likely outcome (to validate in spike):** A or D. A is the smallest delta;
  D is the cleanest if multi-L2-per-cfg becomes common.

- [ ] **Y.11.1 — Implement per spike result.** Updates touch `cli/json.py`,
  `cli/schema.py`, `cli/data.py`, `cli/audit.py`, `cli/_helpers.py::resolve_l2_for_demo`,
  every CLAUDE.md / README / handbook example, every test that invokes
  `runner.invoke([...,"--l2",...])`, and every CI workflow YAML that uses
  `--l2`. Migration warning for at least one minor version.
- [ ] **Y.11.2 — Sweep memory entries + docs for stale `--l2 <yaml>` references.**
- [ ] **Y.11.3 — Update CLAUDE.md "Commands" block** to show the new shape as
  canonical; keep the explicit `--l2` form as the "multi-instance / explicit
  override" sub-pattern.
- [ ] **Y.11.4 — Bump version to v9.x.0 (breaking CLI change) + RELEASE_NOTES
  entry highlighting the simplification + migration recipe.**

---

## Sustainment & minor features

Backlog beyond Phase X. Promote to a numbered phase entry when scope justifies it.

### L2 model gaps

- **Multiple dashboards from one L2 instance** (shared prefix + naming).
- **PR dashboard → generic L2-validation dashboard** (re-skinning of L2FT for a different validation persona).

### Dashboard polish

- **Executives Transaction Volume + Money Moved — metadata grouping** (was Q.1.c.6). Needs L2-instance-aware metadata key dropdowns (cascading Key + Value like L2FT Rails sheet) plus a dataset pivot to expose metadata as a dim. Bigger than a punch-list item; queue as its own sub-phase.

### Post-X.2 App 2 polish (queued — not part of phase X scope)

- **Mobile / responsive.** Tailwind handles the layout primitives but no explicit mobile-first design pass. Promote when there's a customer story. Note: dashboards are dense by nature; mobile may always be a worse experience than desktop, regardless of effort.
- **Per-table CSV / XLSX export.** Operators expect "export to spreadsheet" on tables (QS has it). Lower priority than feature parity — punt unless it's a small agent task. The audit PDF already covers the "regulator-ready snapshot" case; spreadsheet export is for analyst self-serve.

### Audit / data evaluation

- **Postgres dataset evaluator** — given a connection, evaluate whether all exception cases are present; report stats on the CLI.

### Tech debt

- **Encode more invariants in the type system.** K.2 did this for drill-param shape compatibility; Phase L's tree primitives close another big chunk. What remains after L is the candidate list for the next round.

### Known platform limitations — do not re-attempt without new evidence

- **QS URL-parameter control sync** — K.4.7 cross-app drills dropped. URL fragment sets the parameter store but doesn't push values into bound controls. Re-entry conditions: AWS fix, custom embedded app via `setParameters()` SDK, or a new URL form that triggers control sync. See `PLAN_ARCHIVE.md` for full re-entry details.
- **QS dropdown click target is the middle grey bar** — `ParameterDropDownControl` only opens on the inner grey bar; clicking the visible edge does nothing. Suggest before investigating "unresponsive dropdown" reports.
- **QS silent-fail mode** — datasets healthy + describe-cleanly, every visual on every sheet shows the spinner forever. See CLAUDE.md → Operational Footguns for the diagnostic ladder.
