# QuickSight Generator — Active Plan

## Phase BS - Studio + Dashboards rethink (scaffolding)

Full design in `SPEC.md`. Trace: `docs/bs_design_thoughts.md`. Four-phase
decomposition: **BS** (scaffolding — this) → BT (ETL Support surface) →
BU (L2 plants + Training) → BV (dogfood cap-stone). BS is internal scope:
nav cleanup, deploy-model shift, `_kv` static-collapse audit. No new
user-facing surfaces beyond the nav reshape. No design-review cold-read
needed for BS itself (deferred to BT).

- [x] BS.0 - Locks (decisions before BS.1 fires) **— LOCKED 2026-05-29.**
  - **Lock 1 — Navigation contract.** Top nav always present on App2. Studio is the only toggle, sourced from `cfg.yaml::studio_enabled: bool`. **Resolved:** default `true` (operators opt OUT for prod by setting `studio_enabled: false`). Field location in `common/config.py` is implementation detail, deferred to BS.2.
  - **Lock 2 — Studio modes flat into top-nav.** Studio container intermediate goes away. Top-nav order: `L2 Editor <hr> ETL Support <hr> Training <hr> L1 Dashboard <hr> L2 Dashboard <hr> Investigation <hr> Executives <hr> Docs`. **Resolved:** divider style is `<hr>` vertical separator (styled-thin, between every entry — not just role-group boundaries). Studio entries hide when `studio_enabled=false`.
  - **Lock 3 — D6 framing is static-collapse, not dynamic-expansion.** With `_kv` carrying runtime variation, emit layer becomes *more* static (one canonical SQL × N `_kv` fixtures vs N SQL × N L2s). Test surface shrinks. The audit (BS.1) is deep-work; land thorough inventory even if conversion itself slips. See SPEC.md::D6 reframe.
  - **Lock 4 — D4.arch deploy-model shift in scope.** Drop `upstream → demo_db → matview` intermediate copy. New flow: `truncate(demo_db) → ETL hook (writes to demo_db) → matview refresh`. Touches `cli/data.py`, runner seed flow, existing `etl_hook` plumbing. Spike-before-implementing per `[[feedback_spike_before_locking_implementation]]`.
  - **Lock 5 — BS scope boundary.** Phase ships when: shared top nav lands, deploy-model shift lands, `_kv` audit ships with explicit recommendation. **Excluded:** any ETL Support page implementation (BT scope), any new Training surfaces (BU scope), any new dogfood claim (BV scope).
  - **Lock 6 — BS.5 conditional in-phase.** **Resolved:** if BS.1's audit produces a clear P0 list with low-friction conversions, BS.5 fires up-front (we want them while context is fresh). If audit is mixed or P0 items carry conversion cost, BS.5 defers to BT. Decision lands at BS.1 exit.
- [x] BS.1 - `_config_kv` static-collapse audit (early spike) **— COMPLETE 2026-05-29.** Walked 4 apps' datasets.py + schema.py matviews via 5 parallel research agents. Output: `docs/audits/bs_6_kv_static_collapse_audit.md`. Headline: Exec / L1 / Investigation dataset layers all already-canonical (28 builders, all P2); L2FT is the action (10 P0 of 13); schema.py has 2 P0 matviews + 12 P2 (data-only). `_v_config_chain_children` is the single highest-leverage new view (unlocks 7 paths: 2 matviews + 3 L2FT + 2 immediate-win via existing views). BS.5 fires in-phase per Lock 6 with focused scope. Metadata-kv decomposition shelved indefinitely (Oracle 19c JSON_VALUE constraint not lifting soon).
- [x] BS.2 - D1 cfg `studio_enabled` toggle + always-on top nav **— COMPLETE 2026-05-29.** `Config.studio_enabled: bool = True` field + loader (3 tests). Shared chrome wrapper `emit_top_nav(*, entries, active_href)` + `TopNavEntry` dataclass in `common/html/render.py` (8 tests). `divide-x` Tailwind utility yields the `<hr>` separator per BS.0 Lock 2 without polluting DOM. Per-callsite migration of existing chrome blocks lands in BS.3 (collapse Studio container + reroute every page through the wrapper).
- [x] BS.3 - D2 flat top-level nav, Studio container collapse **— COMPLETE 2026-05-29.**
  - **Part 1 — DONE (commit 78d6c7a0):** Dashboards landing + per-dashboard view + per-sheet view all route through `emit_top_nav` via the new `build_top_nav_entries` helper. `_PAGE_SHELL` gains `{nav}` slot. server.py derives `studio_enabled = studio_routes is not None`.
  - **Part 2 — DONE (commit 9c148875):** `cli/studio.py` refuses mount when `cfg.studio_enabled=False` per BS.0 Lock 1 (operator running `recon-gen studio` against a Studio-disabled cfg gets a ClickException with the dashboards-CLI hint).
  - **Part 2b — DONE (commit 39f3d41c).** Inline `← landing` / `→ diagram` / `→ data` / `→ dashboards` link strips stripped from `_render_home_page` / `_render_diagram_page` / `_render_data_page` in `_studio_routes.py`. Closes BF.11 (dual-nav-shape) — Studio pages no longer render a duplicate nav.
  - **Part 3 — DONE 2026-05-29.** Threaded `top_nav_fn: Callable[[str], str] | None` through `make_studio_routes` + each route handler computes `top_nav_html` via the closure for its active_href. The 3 main Studio renderers (home, diagram, data) accept `top_nav_html: str = ""` and inject right after `<body>` (before page-local headers). `_html_serve.py` builds the closure from the dashboards mapping + docs_dir (when `embed_docs` is on) and passes it into the factory invocation. Embedded diagram (iframe inside home) suppresses the nav to avoid double-stacking. `StudioRoutesFactory` type alias relaxed to `Callable[..., ...]` to permit kwarg forwarding. 4 new tests in `test_studio_home_route.py` cover (a) home/diagram/data render the shared nav with top_nav_fn supplied, (b) `?embed=1` diagram suppresses it, (c) without top_nav_fn the pages still render (unit-test surface stays valid). Editor + training renderers keep their existing "← back to Studio" context-preserving links (in-flow editing surface, separate UX axis from the global top-nav).
  - **BF.11** closed by part 2b — flip BF.11 in Phase BF as part of next commit.
- [x] BS.4 - D4.arch deploy-model shift **— COMPLETE 2026-05-29.** Spike doc landed at `docs/audits/bs_4_arch_shift_spike.md` finding the legacy upstream-pull branch was already a self-contained ~280-LOC block (one function + helpers + cfg field + dataclass + loader + tests; no scattered coupling). Per `[[feedback_no_compat_shims]]` (pre-stable default): **deleted** rather than kept-as-opt-in. Implementation: (1) removed `step_2_pull` + `_PULL_BATCH_SIZE` + `_connect_etl_source` + `_pull_table` + `_row_coercer_for` + `_dialect_from_url` + `_insert_paramstyle_sql` from `common/l2/deploy_pipeline.py`; (2) removed `EtlDatasourceConfig` dataclass + `Config.etl_datasource` field + `_CONFIG_ALLOWED_KEYS` entry + the entire loader block from `common/config.py`; (3) **reordered orchestrator** — `run_deploy_pipeline` now runs step 2 wipe BEFORE step 1 etl_hook (the hook writes directly into a clean demo_db); (4) `DeploySummary` dropped `step2_pull_*` fields; (5) updated `tg_cache.patched_config` (etl_hook is the sole toggled field — etl_datasource pair removed); (6) cleaned remaining docstring/comment references in `schema.py`, `studio_state.py`, `_studio_routes.py`. **Halt-contract change:** pre-BS.4 etl_hook failure left demo_db untouched; post-BS.4 the wipe always runs first, so a failed etl_hook leaves demo_db in whatever partial state the hook produced. Operators are recommended to wrap their hook in a transaction for atomic rollback. Tests touched: deleted entire 332-line `step_2_pull` section from `test_deploy_pipeline.py` (kept `_apply_demo_schema_only` helper inlined); deleted `tests/e2e/test_deploy_pipeline_pg_to_sqlite.py` outright (the cross-dialect upstream-copy was its only purpose); dropped etl_datasource references from `test_config_loader.py` (3 tests retired, 2 reshaped) + `test_tg_cache.py` (3 tests reshaped); cleaned `_studio_deploy_helpers.py::make_studio_cfg` + `test_studio_deploy_browser.py`. Spike estimated 45-60 min for implementation; actual ~75 min including doc-sweep + halt-contract test reshape. 3812 tier tests pass (unit + json + data + schema), 85 skipped.
- [x] BS.5 - Dynamic SQL conversion per BS.1's P0 recommendation **— COMPLETE 2026-05-29.** Authored `_v_config_chain_children` view (relational walk over `_config_kv` mirroring `_v_config_rails`; handles both bare-string + mapping ChainChildSpec emits via COALESCE + LEFT-JOIN on field rows). Converted 7 emit paths to source from projection views: `_fan_in_disagreement` + `_multi_xor_violation` matview bodies (static-collapse — body identical regardless of L2 chain count); `_declared_chains_cte` + `_declared_rail_names_cte` + `_declared_limit_schedules_cte` L2FT helpers switched to `(cfg)` signature, SELECT from `_v_config_chain_children` / `_v_config_rails` / `_v_config_limit_schedules` (downstream consumers `build_chains_dataset` + `build_chain_instances_dataset` + `build_exc_chain_orphans_dataset` + `build_exc_unmatched_rail_name_dataset` + `build_exc_dead_limit_schedules_dataset` + `build_unified_l2_exceptions_dataset` + `build_tt_instances_dataset` + `build_tt_legs_dataset` all collapse together). Fixed pre-existing semantic_lock-builder bug: `_build_fresh_semantic_lock_sqlite` was passing `l2_json="{}"` which silently masked production-honest limit_breach + stuck_pending + stuck_unbundled violations (the AW projection views already existed but had nothing to read against). Re-locked spec_example + sasquatch_pr semantic locks. **Discovered baseline noise needing operator triage** (separate from BS, per `[[feedback_production_honest_invariants]]`): spec_example baseline now shows 185 limit_breach + 119 stuck_unbundled + 2 stuck_pending violations production has been seeing all along; sasquatch_pr shows 2 + 127 + 2. Tests touched: tests/unit/test_aw1_config_table.py (+2 BS.5 tests), tests/schema/test_l2_schema.py (3 string-walk tests → static-collapse + view-shape gates), tests/json/test_l2_flow_tracing.py (3 tests reshaped), 4 spine `_fresh_db` helpers populate kv with full L2.
- [x] BS.6 - uv extras collapse — 8 → 3 (`prod` + `dev` + `e2e`) **— COMPLETE 2026-05-29.** Merged `deploy + serve + demo + demo-oracle + audit` → `prod`; merged `docs` → `dev`; kept `e2e`. Updated pyproject.toml + uv.lock + 4 GHA workflows (ci, e2e, release, pages) + CLAUDE.md + install.md. ci.yml + release.yml in parity per `[[feedback_ci_release_workflow_parity]]`. 2865 unit tests pass; `uv sync --all-extras` resolves cleanly.

## Phase BT - ETL Support surface (provisional)

Builds on BS.4's deploy-model shift. First new operator surface. **Provisional** — replan at BT.0 against post-BS state before any BT.1+ work fires. SPEC.md::Phase BT for the design. Agent-based design cold-read fires before BT exits.

- [x] BT.0 - **REPLAN — COMPLETE 2026-05-30.** Output: `docs/audits/bt_0_replan.md`. Headline: Phase BT fires as-spec'd, no SPEC changes needed; BS exit state satisfies every BT.2-BT.5 prerequisite. Locked sub-decisions: (1) BT.3 invokes `run_deploy_pipeline` with `test_generator.enabled=False` for pure-ETL runs (generator overlay stays a Training-mode opt-in); (2) BT.1 extends existing `make_studio_routes` rather than adding new factory plumbing; (3) `_v_config_transfer_templates` projection view bundles with BT.2 (the only BS.1 carryover with a real BT need); other BS.1 deferrals stay deferred; (4) BT.5 lands a new `common/l2/contract.py::derive_column_contracts(L2Instance) → ColumnContracts` pure function — no DB-side work, typed primitives carry enough; (5) BT.4 ships **link-only** in v1 (no pre-fill of NEW entity creation forms — defer to follow-on if cold-read flags friction); (6) BT.6 cold-read persona = "first-time ETL Engineer for a midsize credit union." Phase BT total estimate: ~14-18h focused work. Suggested sequencing: BT.0.5 → BT.1 + BT.5 (parallel) → BT.2 → BT.3 → BT.4 → BT.6.
- [x] BT.0.5 - **Agent-driven design mockup session — COMPLETE 2026-05-30.** Output: `docs/audits/bt_design_mockups.md` (LOCKED, single-cycle). Comprehensive first-pass briefing landed all 4 wireframes + sequence diagram + open-questions list; operator review pass 1 flipped one open question (Probe date-range axis → day-range picker for backfill scenarios) + locked the agent's lean on the other 4. Out-of-scope follow-up captured: Studio /l2 editor Deploy-button cleanup (queued in Backlog). Total: ~80 min including operator review. Validates the BU.0.5 / BV.5 pattern — see `[[feedback_agent_driven_design_works]]`.
- [x] BT.1 - `/studio/etl` flat-nav entry + landing page — **COMPLETE 2026-05-30**. Extends `make_studio_routes` with `Route("/etl/", etl_landing)` + new `_render_etl_landing_page` helper; nav entry from BS.3's `build_top_nav_entries` (already pointing at `/etl/`) goes live automatically. Landing renders the same chrome (top-nav + Studio header) + a 3-card grid (Probe / Run / Triage) per BT.0.5 design mockup §5. Cards link to `/etl/probe` / `/etl/run` / `/etl/triage` (404 until BT.2/3/4 ship) with a "coming in BT.N" hint per card so the operator isn't surprised. 6 new unit tests green; total unit suite 2889 pass / 75 skipped.
- [x] BT.2 - D4.surface #1: L2-slice probe — **COMPLETE 2026-05-30**. `_v_config_transfer_templates` projection view ships (BS.1 carryover bundle per BT.0 lock 3 — scalar projection only; array fields stay Python-side). New `common/l2/probe.py` module with async `fetch_probe_rows` + pure-Python `evaluate_predicate` (✓ / ✗ / — per BT.5 predicate against a row). `/etl/probe` route + `_render_etl_probe_page` ship: 3-radio kind picker + name dropdown + date-range form (defaults to last 7 days per BT.0.5 lock 1); side-by-side contract panel (left, from BT.5) + observed rows panel (right, posting-DESC + per-row predicate-fit count) when a name is picked; empty-state nudge before picking. 28 new unit tests green (2 view + 18 probe + 8 route); pyright clean.
- [x] BT.3 - D4.surface #2: ETL execution + coverage report — **COMPLETE 2026-05-30**. `/etl/run` GET renders Run button + last-run status banner + per-step event log + Rails/Templates/Chains coverage cards (reused `coverage_for`) + new Metadata card via new `metadata_coverage_per_template` helper in `common/l2/coverage.py` (per-template required-keys landing tally — unions `transfer_key` ∪ each leg-rail's `metadata_keys`, dedupes). POST `/etl/run` invokes `run_deploy_pipeline` with `test_generator.enabled=False` per BT.0 lock 1 (via `dataclasses.replace`), stores summary in closure state, 303 → GET. Empty-state when no run AND no data; halt-banner branch when last summary halted; no-DB-pool branch for unit surface. BT.1's Run landing card drops its "coming in BT.3" hint. 9 new tests (6 route + 3 metadata-helper); full unit suite 2926 pass / 75 skipped; pyright clean.
- [x] BT.4 - D4.surface #3: Exception triage + handoff — **COMPLETE 2026-05-30**. New `common/l2/triage.py` with `detect_gaps(pool, prefix, instance, contracts)` returning a typed `Gap` tuple covering four gap kinds: (1) unmatched rail_name, (2) unmatched template_name, (3) missing LimitSchedule for (parent_role, rail) firing in rows, (4) missing required metadata key on rows tagged with the template. Each `Gap` carries diagnosis prose + `GapEvidence` (row count, sample id, extras) + `link_target` (link-only v1 per BT.0 lock 5 — points at editor list page OR template's `editor_path` for metadata gaps). `/etl/triage` route renders one decision card per gap with deep-link CTA or success affirmation when empty. BT.1's Triage landing card drops its "coming in BT.4" hint. 15 new tests (9 detector across all four gap kinds + 6 route shape including in-process render with seeded pool). Full unit suite 2941 pass / 75 skipped; pyright clean.
- [x] BT.5 - Per-column-pair contract derivation in `common/l2/contract.py` — **COMPLETE 2026-05-30**. Pure function `derive_column_contracts(L2Instance) → ColumnContracts` ships with 4 contract kinds (`RailContract` / `TemplateContract` / `ChainEdgeContract` / `LimitContract`), each carrying a typed `RowSelector` + `ColumnPredicate` set + symmetric editor-deep-link path. Variable-direction single-leg rails correctly skip the static `amount_direction` predicate (closure resolved at posting time by the containing template). 25/25 unit tests green (24 minimal + 1 sasquatch_pr live-fixture smoke). Full unit suite remains green (2883 pass). Pyright strict clean. Editor-path symmetry pinned by `_studio_editor_routes._entity_id` cross-call test.
- [x] BT.6 - Agent-based design cold-read against the implemented surface — **COMPLETE 2026-05-30**. Output: `docs/audits/bt_cold_read.md` (789 lines). Persona-driven cold-read with **iterative screenshot loop** (operator's preferred pattern per `[[feedback_cold_read_iterative_screenshots]]` — much stronger than source-only). Drove Studio against sasquatch_pr.yaml + sqlite demo (~12,844 rows); captured 13 screenshots across all 4 pages + happy/sad states for Probe (rail/template/chain) + post-Run state + L2 editor home. Agent reacted in two rounds; hand-amendment corrected one transient HTTP 500 catch to the actual window-mismatch finding. **Headlines:** 5 P1 (window-mismatch between Probe + Run is the new #1), 8 P2, 8 P3 (transient 500 catalogued). Operator triaged inline 2026-05-30 — 18 of 20 items shipping in Phase BTa, 2 P3 deferred, 2 to backlog. v1 source-only draft preserved at `bt_cold_read_v1_source_only.md` for comparison.

## Phase BTa - ETL Support v2: address cold-read recommendations (provisional)

- [x] BTa.0 - **REPLAN — COMPLETE 2026-05-30.** Output: `docs/audits/bta_0_replan.md`. Locked four cross-cutting design decisions: (1) **Side-panel pattern** = slide-out drawer from right edge, ~30-35% viewport width, hx-get content, dismissable via X / Escape / outside-click, ARIA role=complementary + focus trap; new helper `_studio_side_panel.py`. (2) **Numbered landing + tutorial** = 3-numbered cards with arrows + dismissable "First time here?" banner with inline collapsible 5-step checklist; dismissal persists in `localStorage` per-deployment. (3) **Triage group-by** = 4 accordion sections per gap kind (default-collapsed), sub-sort by row count DESC, per-card volume badge in title, per-kind color/icon stripe (accessible — shape + label too), columnar sample-row mini-table replaces JSON dump. (4) **Back-breadcrumb** = query-string carryover `?from=/etl/triage` (NOT Referer header — doesn't survive POSTs); validated to start with `/etl/` to prevent open-redirect; preserved through save-redirect for one-click "save then go back." Also names rejected variants per lock.
- [x] BTa.0.5 - **Agent-driven design mockup session — COMPLETE 2026-05-30.** Output: `docs/audits/bta_design_mockups.md` (1169 lines, over soft cap because four cross-cutting locks pulled cross-references into every section). Covers all 6 deliverable sections: (a) numbered landing + dismissable "First time here?" banner with inline 5-step checklist; (b) side-panel chrome with 3 trigger shapes (per-field `?` / top-nav `[?]` glossary / entity diagram link) + helper-module signature sketch; (c) Triage 4-accordion group-by with volume badges + per-kind color stripes + columnar sample table + per-section "What does the L2 declare?" sub-panel; (d) Run polish — rename `Run ETL` → `↻ Refresh Data` (no modal per operator), hook attribution distinguishing bundled-vs-operator hooks, per-stage timings + log levels, transient flash + browser-tab title pulse + opt-in audible bell, metadata math fix + "Show failures only" toggle; (e) Probe polish with one-sentence radio definitions, searchable name dropdown + ✓/✗ status badges + row counts, date quick-pick chips defaulting to "All time" per BTa.2 P1.1, Chain Expected → side-panel diagram link; (f) Sticky `← Back to Triage/Probe/Run` breadcrumb on L2 editor pages when `?from=/etl/...` is present. **7 open questions in §7 (agent has default leans for each — fall-through to defaults if operator skips review).** Operator review pending → BTa.1.
- [ ] BTa.1 - **Side-panel infrastructure** (foundation for BTa.4/5 diagrams + glossary). New `_studio_side_panel.py` helper or Tailwind drawer pattern; hx-get fragment-driven, dismissable, accessible. Combines P3.7 (chain arrow diagram) + workflow.3 (glossary popover). Operator's note: "side panel with additional help text will reduce the need for a separate doc site." Estimated 2-3h.
- [ ] BTa.2 - **P1 cluster — trust killers.** (1) Probe default window → "All time" (operator: simpler than max(posting) inference; shrink only if needed). (2) Rename "Run ETL" → "Refresh Data" (operator: rejects modal + confirm — friction-heavy). (3) Post-run report shows hook command + exit code + stdout/stderr tail. (4) Triage CTAs deep-link to specific entity (`/l2_shape/rail/<name>/edit` not `/l2_shape/rail/`). (5) Referer-based "← back to Triage" breadcrumb on L2 Editor when arriving via CTA — only if cheap. Estimated 3-4h.
- [ ] BTa.3 - **Landing rework + tutorial.** Numbered "1. Refresh Data → 2. Triage gaps → 3. Probe & fix" cards with arrows (replaces equal-cards layout). First-time tutorial banner ("First time here? Walk the loop") — collapsible inline 5-step checklist. Estimated 2-3h.
- [ ] BTa.4 - **Triage rework.** Group cards by gap kind (collapsible accordion); strip per-card `declared_rails:` block (move to group header or side panel via BTa.1); volume badge in card title (`Unmatched rail_name • 256 rows`); columnar sample-row view (not JSON dump); distinct color/icon per gap kind (accessible — not red+green-only). Transient HTTP 500 retry-on-lock-busy OR friendlier "this slice is recomputing" message. Estimated 4-5h.
- [ ] BTa.5 - **Probe polish.** One-line definitions next to Rail / Transfer Template / Chain radios. Name dropdown gets search box + ✓/✗ status badges. Date quick-pick chips ("Last 7d / 30d / 90d / All time"). Chain Expected gets the arrow diagram in BTa.1's side panel (parent → child visual). Operator caveat: re-check information density at phase exit. Estimated 3-4h.
- [ ] BTa.6 - **Run polish.** "Show failures only" toggle on Coverage cards (collapses green entries). Fix Metadata roll-up math (denominator disagreement). Per-stage timings + log level (info/warn/error). Transient flash on coverage refresh + browser-tab bell/sound for long runs (operator: "we all know we're multi-tasking"). Estimated 2-3h.
- [ ] BTa.7 - **Cold-read v3.** Same iterative-screenshot pattern (per `[[feedback_cold_read_iterative_screenshots]]`). Persona: same first-time ETL Engineer; carry over the v2 findings as known prior context. Output: `docs/audits/bta_cold_read.md`. Operator iteration + sign-off. Estimated 60-90 min.

**Phase BTa total estimate:** ~14-18 hours focused work. Sequencing: BTa.0 → BTa.0.5 → BTa.1 (side-panel infra blocks BTa.4/5 diagrams) → BTa.2/3 in parallel → BTa.4/5/6 (can run in parallel after BTa.1) → BTa.7.

**Deferred to Backlog (not in BTa):**
- P3.1 qsgen-sqlite hover-tip → wait for cfg.yaml readdress phase
- P3.2 Probe empty-state copy wording — defer
- workflow.4 Snapshot/restore around Run — challenging non-SQLite; revisit later
- workflow.2 Reverse-link from dashboards → ETL Support — touches dashboard chrome (broader scope than BTa); deferred 2026-05-30 per operator. Worth doing eventually ("would GREATLY help someone learn this" per the cold-read) but not bundled with this phase.

## Phase BX - L2 Editor cold-read + address cycle (provisional)

Same cold-read → triage → design → implement → re-cold-read pattern that BT/BTa established, applied to the L2 Editor surface (the `/` home + `/l2_shape/<kind>/{list, new, edit}` for the six entity kinds + the three singleton pages for instance / theme / persona). Unlike BT (where the design intent was operator-driven from SPEC), the L2 Editor is **discovery-first** — we haven't pointed a first-time persona at it yet, so we don't know what's broken until BX.0.5 fires. BX.1+ implementation cells get enumerated by BX.0.7's REPLAN after operator triage.

**Relationship to in-flight Phase BF:** BF.0-5 + BF.10 address known L2-editor defects (BB.2 sub-form completeness, structured pickers for textareas, AI.2.e subtype banner). BX.0.6 triage may obsolete some of these (if the cold-read says they're fine or finds bigger fish) or keep them in flight. Reconciliation lock in BX.0.7.

- [x] BX.0 - **REPLAN — COMPLETE 2026-05-30.** Output: `docs/audits/bx_0_replan.md`. Locked: (a) two-persona cold-read (ETL Engineer bounce-and-fix + L2 author from scratch); (b) full route inventory across the 6 entity kinds + 3 singletons + diagram surface; (c) field-richness ranking driving screenshot priority (rail → transfer_template → chain → limit_schedule → account_template → account); (d) BF reconciliation rules (BF.1-4 likely subsumed by BX.0.7's design pass, BF.5 + BF.10 keep regardless, BF.0 + BF.6 hold pending BX.0.7); (e) sequencing recommendation — fire BX.0.5b (author flow) now since it's independent of BTa; defer BX.0.5a until BTa.2.4 lands so we cold-read the FIXED bounce flow not the bad-deep-link flow.
- [ ] BX.0.5a - **Cold-read v1a: ETL Engineer bounce-and-fix.** Iterative screenshot loop per `[[feedback_cold_read_iterative_screenshots]]`. Drive Studio against sasquatch_pr.yaml + sqlite demo. Scenario: operator arrives via a Triage CTA's deep link (BTa.2.4 will fix the link target to a specific entity edit page) — they need to make ONE edit and bounce back. Pages: the specific entity edit page they landed on + back-breadcrumb path. Output: `docs/audits/bx_cold_read_v1a_bounce.md`. Estimated 60-90 min.
- [x] BX.0.5b - **Cold-read v1b: L2 author building from scratch — COMPLETE 2026-05-30.** Output: `docs/audits/bx_cold_read_v1b_author.md` (455 lines) + 30 screenshots in `bx_cold_read_screenshots_v1b/`. Iterative two-round screenshot loop: 19 initial captures (home + 2 diagram + 6 list pages + 3 singletons + 8 create-form pages) + 10 follow-ups (fill+submit rail, edit existing entities, composite-key URLs, markdown preview, persona fill, delete-in-use, orphan-role create). **Headlines: 5 P1 / 8 P2 / 10 P3.** Top P1 finds: (1) silent-accept of orphan-role account create (most severe integrity gap — no validation, redirect to `/`); (2) delete-in-use rail has no confirm + no reference-check + no warning; (3) Instance singleton is raw YAML textarea (BF-pending; "a consultant won't survive this"); (4) composite-key URLs (`Parent::Child`, `ParentRole::Rail::Direction`) are rename-fragile + URL-encoding-hostile; (5) no dependency-order guidance + no "what's complete" signal on the home/list surfaces. **Genuine wins:** rail subtype-picker page (`14_new_rail_picker`) is the gold standard form-page; limit_schedule's Direction helper copy is excellent; markdown preview on description fields actually works (promote it across all entity description fields). Cleanup done: orphan `test-flobber-1` account removed from local `run/sasquatch_pr.yaml` (fixture under `tests/l2/` is unchanged — `run/` is gitignored). Pending: operator triage → BX.0.6.
- [ ] BX.0.6 - **Operator triage of cold-read v1.** Operator inline-comments on the recommendations same way they did on `bt_cold_read.md`. Output: triage decisions inline in v1 doc.
- [ ] BX.0.7 - **REPLAN with the triage.** Slot green-lit recommendations into BX.1-N cells. Decide BF reconciliation (collapse BF.1-6 + BF.10 into BX cells if the cold-read covered the same ground, OR keep BF in flight if cold-read missed them, OR cut BF entries the cold-read says aren't worth doing). Output: enumerated BX.1-N + updated BF status. Estimated 30-60 min.
- [ ] BX.0.8 - **Agent-driven design mockups** (only the design-heavy fixes flagged in BX.0.7). Skip trivial copy-tweaks. Output: `docs/audits/bx_design_mockups.md`. Estimated 60-90 min.
- [ ] BX.1..N - **Implementation cells** — enumerated by BX.0.7's triage. Cannot list in advance; expect 4-8 cells covering the cold-read's P1 + P2.
- [ ] BX.M - **Cold-read v2.** Same iterative-screenshot pattern, same persona. Carry over v1 findings as known prior context. Output: `docs/audits/bx_cold_read_v2.md`. Operator iteration + sign-off → phase exit.

**Phase BX total estimate:** highly uncertain until BX.0.5 lands. Floor: ~6-8h (discovery + replan + minimal fixes + re-cold-read if surface is mostly clean). Ceiling: ~25-30h (if cold-read surfaces ~20 issues like BT did).

**Sequencing vs BTa:** BX.0 + BX.0.5 can run in parallel with BTa implementation (different file surface — BTa is ETL Support pages, BX cold-read is L2 Editor pages). BX.0.7 onwards waits until BTa has stabilized (to avoid two simultaneous editor-touching phases).

## Phase BU - L2 plant generation + Training mode (provisional)

**Provisional** — replan at BU.0. Could run in parallel with BT after BS; sequencing decided at BU.0. SPEC.md::Phase BU for the design.

- [ ] BU.0 - **REPLAN.** Re-read SPEC.md::Phase BU against post-BS state. Decide sequencing vs BT (sequential or parallel). Inventory whether BT.3's coverage-report shape can be reused for BU.5's plant-presence reports. Lock BU.1's audit scope.
- [ ] BU.0.5 - **Agent-driven design mockup session.** Hand off SPEC.md::Phase BU + BU.0 lock list; agent produces mocks for the Training landing, plant picker, and before/after dashboard tour (BU.5's load-bearing UX). Reuse BT's mockup format if BT shipped first. Cycle with operator critique. Output: `docs/audits/bu_design_mockups.md`.
- [ ] BU.1 - D5 plant inventory audit. Output: `docs/audits/bs_5_l2_plant_inventory.md`.
- [ ] BU.2 - Expose existing plants under `/studio/training`.
- [ ] BU.3 - Add missing plants per BU.1's audit.
- [ ] BU.4 - Training landing + sub-nav (D2's deferred sub-nav question lands here).
- [ ] BU.5 - Per-invariant "before/after dashboard" tour: pick a plant, see the dashboard sheet that surfaces it, no-plant vs with-plant side-by-side.
- [ ] BU.6 - Agent-based design cold-read. Output: `docs/audits/bu_cold_read.md`. Operator iteration + sign-off after.

## Phase BV - Dogfood cap-stone (provisional)

The full L2 + ETL + Training round-trip. Glues everything under one e2e test. **Provisional** — replan at BV.0 to confirm BT + BU shipped the parts BV.4 needs. SPEC.md::Phase BV for the design.

- [ ] BV.0 - **REPLAN.** Re-read SPEC.md::Phase BV against post-BT + post-BU state. Inventory the parts BV.4 (the HUGE one) actually depends on. Decide e2e test infra: extend `tests/e2e/test_studio_dogfood_browser.py` or new file? AWS-deploy required or App2-only?
- [ ] BV.1 - D3.1 L2 audit redux + per-primitive curriculum doc (the "what's persona for?" deliverable).
- [ ] BV.2 - D3.2 ETL round-trip claim: test data generator hooked as ETL hook; BT.3's coverage report green per fuzz seed.
- [ ] BV.3 - D3.3 Training round-trip claim: every plant kind surfaces correctly per fuzz seed.
- [ ] BV.4 - **The HUGE one.** Take an L2 yaml → recreate in the editor → deploy via Training mode (all defaults) → assert dashboards match existing test deploys. Browser-layer e2e, parametrized over the fuzz pool.
- [ ] BV.5 - Agent-based design cold-read against the dogfood + the HUGE test. Output: `docs/audits/bv_cold_read.md`. Operator iteration + sign-off.

## Phase BW - Docs posture (deferred / follow-on)

**Provisional + may not fire.** SPEC.md::Phase BW. Only schedule if an operator asks for the "your deployment overview" page; otherwise D7 ticks as "static-default locked, follow-on deferred" at BV exit.

- [ ] BW.0 - **REPLAN.** Confirm operator demand for the `_kv`-templated docs page exists before scheduling BW.1+. If no demand, tick BW.0 as deferred-pending-demand and close Phase BW without further work.
- [ ] BW.0.5 - **Agent-driven design mockup session** (only if BW.0 said yes). Mock the deployment-overview page reading from `_kv`.
- [ ] BW.1 - D7 lock + minimal implementation (static-default, per SPEC).
- [ ] BW.2 - "Your deployment overview" page reading from `_kv`, if BW.0 said yes.

## Phase AI

Per `feedback_build_verbs_not_skip`: when an editor verb's underlying UI is missing, BUILD the UI (and the verb that wires to it), don't skip the test param.
The dashboards-match assertion is the user-facing acceptance: "the editor produces an L2 that drives identical dashboards".
- [ ] AI.0 - Locks (decisions before AI.1 fires, 2026-05-19).
  - **Scope split for missing editor surfaces** (Lock 1, confirmed 2026-05-19). If the AI.1 audit reveals a non-trivial editor UI gap (no widget for a primitive that some test-input L2 uses), AI.2 BUILDS the missing UI inline as a sub-task per entity kind. Phase ships only when the editor can recreate the full corpus. No "scope to current surface; defer gaps" escape hatch — the dogfood claim has zero acceptable noise floor.
  - **L2Instance equivalence granularity.** Compare via parsed `L2Instance` dataclass equality, not byte-equality on the YAML file. Editor-emitted YAML may diff in formatting (field ordering, comment placement, indentation) but the L2Instance struct MUST match. Use existing `tests/unit/test_l2_loader.py`-style structural asserts.
  - **Dashboard equivalence granularity** (Lock 2, confirmed 2026-05-19). Per-sheet, per-visual: visual titles + table row content (rows + cell text) + KPI numeric values. Skip DOM byte-equality + screenshot diffs. Hermetic comparison: same L2 + seed + anchor must yield identical `DashboardDriver.visual_titles()` + `table_rows()` + `kpi_value()` outputs. Filter unstable fields (analysis_id / sheet_internal_id / wall-clock timestamps) from the comparison dict.
  - **Test layer = browser** (Lock 3, confirmed 2026-05-19). New file `tests/e2e/test_studio_dogfood.py` gated behind `QS_GEN_E2E=1`. Runs under `./run_tests.sh up_to=browser`. Marker: `@pytest.mark.browser` (existing convention).
  - **Transport: hybrid** (Lock 3 amendment, decided 2026-05-21 after the AI.1 audit revealed the editor surface is large — rail subtype picker, multi-selects, `multi_select_groups`, chain per-child checkboxes, 3 YAML-block singletons). `StudioEditorDriver` is one verb protocol with two transports: (a) `StudioBrowserEditorDriver` (Playwright) drives ONE full pass on the deterministic `spec_example` for real form-render+submit fidelity; (b) `StudioHttpEditorDriver` (httpx form-POST against a running studio server) drives `sasquatch_pr` + the fuzz-sampled bulk — fast, deterministic, fuzz-scalable. Rationale: the unit route tests (`test_studio_editor_routes.py`) already prove server-side coerce→create→serialize→save end-to-end, so HTTP covers the same fidelity; Playwright's marginal value (a form that renders-but-mis-submits) is captured by the one spec_example browser pass rather than a full-corpus×fuzz browser rebuild.
  - **Determinism + reproducibility.** Anchor `date(2030, 1, 1)`; `RECON_GEN_FUZZ_SEED` pinned per CI cell. Editor mutations are exact sequences (no waits-for-element ambiguity); fail loudly if a mutation widget reports unexpected state. Studio cfg is ephemeral (tmpdir-rooted) per test invocation so studio's `.studio-state.yaml` doesn't pollute across runs.
  - **No mutation of source yamls.** Dogfood'd YAML writes to `tmp/dogfood_<instance_name>.yaml` (test-scoped tmp dir). The shipping `tests/l2/spec_example.yaml` + `tests/l2/sasquatch_pr.yaml` are reference; the test asserts dogfood matches reference.
  - **Fuzz axis sample size.** Locked-input seeds: 5 per CI cell. Override via `QS_GEN_AI_FUZZ_SAMPLE_N=N` env var (default 5, runner pins this to a known-good value for the deterministic suite; ad-hoc local testing can crank it). The fuzz pool itself is `tests/l2/fuzz.py::random_l2_yaml(seed)` — already produces validator-passing L2 instances. Add an opt-in nightly run that bumps the sample to 100+ once the deterministic 5-seed sample is green.
  - **Editor save-to-yaml route.** The editor needs a single POST route that serializes the current in-memory L2Instance to a yaml file (file path supplied or returned). If no such route exists, AI.2 builds it (preferred path: `POST /l2/export?path=<dest>` returns 204 after writing). Save-on-mutate (every edit re-flushes to disk) is the current behavior; AI confirms or adjusts as needed.
- [x] AI.1 - Editor surface coverage audit. Walk every entity kind in the test-input corpus (spec_example + sasquatch + 5 fuzz seeds) and inventory whether each kind has an "add/edit/delete" widget in the Studio editor. Cover:
  - Account (with all optional fields: name, role, parent_role, expected_eod_balance, description)
  - AccountTemplate (with instance_id_template / instance_name_template)
  - SingleLegRail (with origin variants, metadata_keys, leg_role, leg_direction)
  - TwoLegRail (source_role, destination_role, source_origin, destination_origin)
  - AggregatingRail (cadence, bundles_activity)
  - TransferTemplate (leg_rails, transfer_key, completion, leg_rail_xor_groups)
  - Chain (parent, children with ChainChildSpec including fan_in + expected_parent_count + mixed-cardinality)
  - LimitSchedule (per-rail + per-account_type caps)
    Produce `docs/audits/ai_1_editor_surface_audit.md` listing per-entity gaps + "needed widget" punch list. This is the discovery step — without it, AI.2 has no driver-verb scope. Run the audit BEFORE locking AI.2 effort.
- [ ] AI.2 - StudioEditorDriver verbs + missing UI builds. Extend the App2Driver protocol (or subclass it as `StudioEditorDriver` in `tests/e2e/_drivers/`) with editor verbs keyed off the AI.1 audit. Per Lock 1, build any missing editor UI inline as AI.2.x sub-tasks. Driver shape:
  - `create_account(id, role, scope, **opts)` + similar for other entity kinds
  - `set_template_leg_rail_xor_groups(template, groups)` (AB.3 surface)
  - `create_chain(parent, children: list[ChainChildSpec])` (AB.6 surface — mixed-cardinality)
  - `save_l2_to_path(path)` — invoke editor's serialize-to-yaml route, write to disk
  - Bulk-create helper `create_l2(reference: L2Instance)` that walks reference entities in dependency order and creates each via the verb-per-entity-kind path
  - [x] AI.2.a - AI.2.a Fix create-path field drops (rail cadence/amount_typical_range/firings_typical_per_period + chain per-child fan_in/epc)
  - [x] AI.2.b - AI.2.b TransferTemplate transfer_key field (FieldSpec + create wiring)
  - [x] AI.2.c - AI.2.c Top-level L2 editor for description + role_business_day_offsets (new singleton kind)
  - [ ] AI.2.d - AI.2.d StudioEditorDriver verbs + create_l2(reference) bulk helper
    - [x] AI.2.d.1 - AI.2.d.1 Protocol + HTTP transport + create_l2 bulk — **DONE via Phase BB** (commits c1939621 + 934b274e). Dogfood round-trip un-skipped + passes for spec_example AND sasquatch_pr through HTTP form-POSTs with full validate() on every save.
      - [x] AI.2.d.1.a - Defer-validation bilateral. **Resolved via Phase BB** (surgical reconciler form-pairing; no validator split, no defer-validation cheat). Catalog: `docs/audits/bb_0_a_validator_circulars.md`. Server-side composite handler (BB.1) + Reconciler picker UI (BB.2) + driver wiring (BB.3) + dogfood un-skip (BB.4).
    - [ ] AI.2.d.2 - AI.2.d.2 Playwright transport (spec_example pass). **Piece 1 LANDED (2026-05-25)**: `tests/e2e/test_studio_dogfood_browser.py` + `tests/e2e/_drivers/studio_browser_editor.py` + `tests/e2e/_harness_studio_editor.py`. Real WebKit + uvicorn + real `make_studio_routes` app + click-through navigation through real HTML forms. Two operator-fidelity constraints honored: (a) discovery-only — every typed value operator-fresh or read from rendered DOM; (b) no URL editing past initial entry — every navigation is link-click or form-submit. No Playwright in the test body per X.2.q's no-playwright-leak lint — driver verbs only. 1 smoke variant (account create + list verify) green at 2:16. **Pieces 2-3 remaining**: extend driver verbs for rail (multi-select roles), reconciler create-new sub-form (BB.2 inline JS), XOR groups; then full `create_l2(reference)` walk for spec_example with AI.4/AI.5 equivalence on the saved YAML.
  - [x] AI.2.e - AI.2.e Route diagram edit/add affordance to dedicated screens (drop inline-on-diagram editing)
- [x] AI.3 - Test harness — `tests/unit/test_studio_editor_driver.py` carries the dogfood harness (NOT moved to `tests/e2e/`: the Starlette `TestClient` path needs no docker, no real server, and runs in 1.5s — keeping it at the unit layer guards every push with no e2e cost). Parametrized over `spec_example` + `sasquatch_pr` + 5 fuzz seeds (default; override via `RECON_GEN_AI_FUZZ_SAMPLE_N`). 7 dogfood variants, all green. The browser/Playwright pass (AI.2.d.2) layers form-render fidelity on top of this same L2-equivalence claim.
- [x] AI.4 - L2Instance equivalence assertion. `_assert_l2_structurally_equal` (factored from the BB.4 test body in AI.6) compares parsed `L2Instance` dataclasses per Lock 1: tuple-order differences in entity collections (BB.3 reconcilers landing at first-occupant rail position; aggregator-rail-as-reconciler shifts) AND description trailing-whitespace differences get normalized via `_by_identifier` / `_normalize_descriptions`. Failures surface as Python `assert` diffs on the specific entity collection. Used by AI.3's 7 variants.
- [x] AI.5 - Dashboard data-equivalence assertion (SQLite-only, matview-row level). Per L2 (reference + dogfood): build in-memory SQLite → emit_schema → populate `<prefix>_config_kv` → plants-only seed (skips 90-day baseline for sub-second runtime; every L1 + Investigation matview still exercises its SELECT shape against the planted scenario) → refresh_matviews_sql. Then SELECT * from each of 18 dashboard-consumed matviews + assert byte-identical row sets between the two L2s. Skipped step 4 (instantiating App2 server) per the simpler proof shape: dataset SQL is downstream-pure (a function of L2), so AI.4's L2 structural equality + AI.5's matview row equality together imply dashboard data equality. 14 dogfood variants (7 AI.4 + 7 AI.5: 2 named fixtures + 5 fuzz seeds each), all green, ~3.25s total runtime.
- [x] AI.6 - Fuzz axis wiring. 5-seed pool (`_DEFAULT_FUZZ_SEEDS`) parametrized in `test_studio_editor_driver.py::test_http_driver_rebuilds_fuzz_l2_structurally`; deterministic per seed (same seed → same `random_l2_yaml` output → same dogfood claim). `RECON_GEN_AI_FUZZ_SAMPLE_N` registered in `common/env_keys.py` (typed registry) overrides the pool size. Failed seeds re-runnable via `pytest -k fuzz_NNNNNNNN`. **The axis IMMEDIATELY EARNED ITS KEEP**: seed 11 surfaced a real wave-ordering bug in the studio driver — `max_unbundled_age` edit was running BEFORE the wave-5 aggregator `bundles_activity` reorder, leaving a fuzz-generated bundled rail unbundled at edit time. Driver fix: move wave 5 reorder pass before the deferred-max_unbundled_age edits. 0 hand-authored fixture covered that shape.
- [ ] AI.7 - Re-verify + commit. Run `./run_tests.sh up_to=browser --scenarios=sp --dialects=sl --targets=lo` locally to confirm the 5-seed deterministic dogfood suite passes; gate CI on it via the existing browser-job pipeline (`e2e.yml` or analog). Phase history one-liner: "AI — Studio editor dogfood: ANY L2 yaml (spec_example + sasquatch_pr + fuzz-sampled) rebuilt via browser-driven editor matches reference structurally + in dashboard output (SQLite/App2 only)".
- [x] AI.8 - **Phantom framing — closed 2026-05-25**. My initial AI.8 claim ("sub-pages don't link to home") was wrong: every editor sub-page renders `<a class="nav-link" href="/">← landing</a>` (or `← back to Studio`) in the chrome header (`_render_list_page` line 2745, `_render_create_page` line 2521, edit page line 2443, singleton line 2664). The Playwright timeout that triggered the AI.8 log was my driver calling `goto_home()` on the HOME PAGE itself (chained after a 303 redirect already landed at `/`); home doesn't link to itself. Driver fix: use `goto_home()` from sub-pages only (which works — they have the chrome link); browser test piece 2c switched from `go_back()` workaround to `goto_home()` (real operator clicks the chrome link). No editor UI change needed.
- [x] AI.9 - **Runtime defense-in-depth on RoleExpression fields (2026-05-25)**. Initial framing was wrong: `RoleExpression` is already strict `tuple[Identifier, ...]` (not a union); the smell was a test passing bare `Identifier` and the runtime silently iterating it as a `str` subclass char-by-char. Fix shipped via `_coerce_role_expression` helper + `__post_init__` on `TwoLegRail` (source_role + destination_role) + `SingleLegRail` (leg_role). Accepts tuple (unchanged), bare str/Identifier (→ 1-tuple), iterable (→ tuple of Identifier), and raises `TypeError` on None. Frozen-dataclass setattr via `object.__setattr__`. 4 new tests in `tests/unit/test_l2_primitives.py` pin the contract. The strict tuple type hint stays — typecheck enforcement remains the first line of defense; the runtime guard catches typecheck-bypass cases (tests, dynamic constructors) with a clear error instead of a mysterious char-iteration explosion downstream. Pyright-strict-on-tests/ would catch the same smell at compile time; backlogged as BE.7 (sibling to BE's cross-corpus lint theme).
- [x] AI.10 - **xor_groups form-pairing landed via textarea (2026-05-25)**. `_render_reconciler_section` now emits a `leg_rail_xor_groups_text` textarea (one group per line, comma-separated rail names). Server's create handler parses the textarea + rewrites into the BB.3 wire shape (`leg_rail_xor_groups__present` + `leg_rail_xor_groups_<i>` per group) before `_apply_xor_groups_update_to_tt` consumes it. Browser driver fills the textarea when given `partial_xor_groups`. Operator-facing UX: optional, leave blank for the common case; required only when attaching Variable rail to TT with existing Variables (SPEC C1). MVP textarea-style affordance; a richer per-rail-checkbox UI could land later but the textarea handles the validator-mandated case end-to-end. Piece 3 now reaches wave 5/6; remaining blocker is the browser `_edit` verb (separate piece-4 work).
- [x] AI.11 - **Fuzzer XOR/Variable injection lifted to unconditional (2026-05-25)**. `_maybe_inject_xor_template` was firing at ~50% per seed; bumped to always-on so every fuzz seed generates a Variable-direction-rail-plus-closing-TT-plus-xor-groups shape. The injector already exists (AB.3.5.fuzz) — just lifted the probability gate. Module docstring updated to reflect the new contract. Every fuzz seed now exercises the operator path Variable-direction rails require — same shape AI.10 needed. Base random-rail generator still picks Debit/Credit only (Variable rails require template-bound context; the injector synthesizes the pair atomically). 113 fuzz tests + 14 HTTP dogfood variants + 2772-test full unit sweep all green with the extended output. Will surface more UI form-pairing gaps as the fuzz axis hits the same shape AI.10/AI.15 want.
- [ ] AI.12 - **WebKit fill-on-hidden quirk surfaced by AI.2.d.2 piece 3 (2026-05-25)**: in the BB.2 create-new sub-form, my driver fills `reconciler_new_destination_role` with `"InternalRole_01"` (correct via diagnostic), but the form serializes the input as `"InternalRole_010"` — extra trailing `0`. Hypothesis (user "probably a to string issue"): subsequent iteration steps fill the TT-kind block's `reconciler_new_expected_net` (with value `"0"`) using `force=True` while that input is inside a `[hidden]` parent (aggregator-kind path doesn't reveal it); WebKit may be appending the `0` to a different input under `force=True`. **FIXED 2026-05-25**: visibility gate in `_apply_form_data` skips fills on inputs inside `[hidden]` parents; surfaces AI.13 next.
- [x] AI.13 - **BB.2 aggregator-two-leg expected_net field added (2026-05-25)**. `_render_reconciler_section`'s aggregator-two-leg sub-block now renders `<input name="reconciler_new_expected_net">` alongside source_role + destination_role. Per BB.0's form-pairing principle, every validator-required field is now exposed. Also added the BB.2 inline JS `setBlockHidden(b, hide)` helper that toggles `disabled` on contained inputs in lock-step with `[hidden]` — required because `reconciler_new_expected_net` now appears in BOTH the TT-kind block AND the aggregator-two-leg block; without disabling hidden inputs, both would submit and `FormData.get()` would return whichever came first in document order. Init pass calls `setBlockHidden` at first render too so the initial-hidden blocks don't pollute the POST.
- [ ] AI.14 - **BB.2 form completeness + structured pickers** → **moved to Phase BF** (2026-05-25 per user: "let's plan that as a phase BF together with the missing UI parts"). The three sub-pieces (BB.2 sub-form completeness, free-form textarea → structured pickers, driver alignment) land in BF.2 / BF.3+BF.4 / BF.5 respectively, alongside the AI.2.e part-2 subtype-requirements banner (BF.1). Driver-side post-create-backfill workaround stays skipped per the same call — BF ships the real fix. AI.14 closes when BF.5 lands.
- [x] AH.8 - Docs builds: isolate into run-folder sandbox (kills xdist flakes)
- [ ] AH.9 - Tailwind output.css scan-scoping — stop whole-repo prose pollution
- [ ] AH.10 - demo-publish on-release refresh broken (trigger + runner-user)
- [ ] AI.0 - Studio editor dogfood: any L2 yaml rebuilt via browser-driven editor

## Phase BH - Fix v11.21.0 cold-read defects (turn BG red→green)

**Trigger (2026-05-25, user)**: "post BG.7, we're going to need to plan to fix all these issues with these new tests." BG.2-BG.6 are honest gates; some are designed to RED on current production code (BG.4's fanout cartesian-inflation gate explicitly so). Phase BH walks the cold-read findings + the gates' reproductions, fixes each at the right layer (visual binding, dataset SQL, sheet narrative, format spec), and confirms each BG.X cell turns green.

Sequencing rationale: BG ships the gates first, then BH fixes the bugs against them. The reverse ("fix first, then write tests") risks shipping fixes that don't actually verify — BG's identity+delta gate is the verification.

Per-finding cells map to the v11.21.0 triage doc + BG.X surfaces:

- [x] BH.0 - **Spike: re-run every BG.X probe against sasquatch_pr-backed pg + sqlite, snapshot the current red/green state**. Output: per-finding table — "finding #N still red? / green? / still presentation?" — at `docs/audits/bh_0_finding_state_snapshot.md`. Drives BH.1-BH.N priority order. **Done (2026-05-25)**: snapshot at `docs/audits/bh_0_finding_state_snapshot.md`. **3 confirmed RED bugs**: #3 (316 CustomerDDA + 85 ACHOrigSettlement rows with negative Opening — cardholder invariant violation, worst=-$1.37M), #7 (fanout cartesian inflation $280K over-count = $22.08M vs $21.80M deduped, 1.0129×), `plant` (7 transactions with `rail_name='ach'` not declared on sasquatch_pr L2). **2 narrative**: #8 (predicate-mismatch subtitle, 49.3% gap by design), #11 (units mismatch rename, 12.6× ratio). **2 presentation**: #9 (stuck_unbundled=83.2%), #10 (32 rails legend). **4 needs-browser**: #2, #12, #13, #14. **7 needs-visual** (copy/format): #15-#21. **3 GREEN on current seed**: #1 (all 3,928 rows satisfy formula), #5, #6. **1 N/A**: #4 (no KPI by that name in current code). Priority order: BH.7 first (BG.4 already red), then BH.3 + BH.22 (the other reds), then narrative + browser-inspection cells.
- [x] BH.1 - **#1 Daily Statement Drift formula gap** — root cause found 2026-05-25 (user shared unsanitized cold-read): matview's `net_flow` formula at `schema.py:2502-2504` uses `SUM(CASE WHEN Credit THEN amount_money ELSE -amount_money END)` which assumed v5's unsigned `amount_money`. v6 made `amount_money` already-signed → the `-amount_money` for Debit rows over-flips → `net_flow = credits + abs(debits)` = gross magnitude. **Done (2026-05-25)**: dropped the CASE → `SUM(tx.amount_money) AS net_flow`. One-line in `schema.py:2498`. Verified on `cust-0011-snb 2026-02-24`: matview drift went from -$46,986.60 → $0.00. **Aggregate-level verification across all 3,928 matview rows**: 0 narrative-formula violations (drift = closing − (opening + net_flow)) AND 0 net_flow-sign violations (net_flow = debits + credits). **BG.2 App2 leg now PASSES on sasquatch sqlite** — all 5 KPI identity assertions + narrative-formula gate + day1≠day2 delta assertion all green. Findings #1 + #2-shape on App2 + #14 all resolved by the BH.14 + BH.24.1 + BH.1 chain.
- [x] BH.2 - **#2 Daily Statement Business-Day picker non-functional** — **DONE 2026-05-25 (resolved as part of BH.14 → BH.24.1 → BH.1 cascade; user 2026-05-25: "that WAS detected in app2, no quicksight needed" + "this is the hidden input problem")**. Same class as **AI.12 WebKit fill-on-hidden quirk** — both are "hidden input drives the wire, events/state get fragile around it" pattern. The cold-read flagged "byte-identical KPIs regardless of picked day"; BG.2's delta block tests exactly that. Before the cascade, BG.2 couldn't reach the delta assertion (parser tripped on 3-decimal currency → identity tripped on 100× cents-double-divide). After BH.14 + BH.24.1 + BH.1, BG.2 PASSES end-to-end on App2 against sasquatch sqlite — including the delta assertion. **Picker confirmed binding correctly on App2**: network capture showed `param_pL1DsBalanceDate=2026-05-25` reaching the server (the hidden input's value did make it through despite the AI.12-shape fragility); SQL pushdown returns distinct values per day; KPI deltas surface correctly. QS-side wiring (analysis param + MappedDataSetParameters + dataset param) is the SAME shared contract — verified by reading `app.py:2118-2129`. QS deployment-time verification deferred to BH.23 phase exit (when a QS deploy is available).
- [x] BH.3 - **#3 Negative Opening Balance on cardholder class** — **CLOSED 2026-05-25 (resolved by BH cascade per user "if we got both of them with our BH fixes, I'm good to move on")**. Cold-read showed the bug on BOTH pool + cardholder examples; the BH.14 + BH.24.1 + BH.1 cascade is class-agnostic and addresses both. Per-fix coverage: **BH.14** — all currency KPIs render with 2 decimals (no more 3-decimal noise the cold-read flagged on these specific values). **BH.24.1** — App2 KPIs no longer 100× off across the entire L1/Inv/Exec/L2FT surface. **BH.1** — matview `drift = closing - (opening + net_flow)` holds correctly across all 3,928 sasquatch rows (formula class-agnostic — same SQL hits pool + cardholder identically). If the cold-read's L2 also declares balance-≥-0 semantics on a restricted class (sasquatch's CustomerDDA does not), real data-side violations get surfaced by the L1 `<prefix>_overdraft` matview the dashboard already binds. Final re-cold-read after BH cascade ships in BH.23 phase exit.
- [x] BH.4 - **#4 Latest Snapshot Drift SUM-cancellation** — **CLOSED 2026-05-25 (resolved by BH cascade per user "BH.4 is very likely the x100 problem")**. No KPI by this exact name in current code (BH.0 verified) — the closest current bindings are "Largest Leaf/Parent Drift Day" on the Drift Timelines sheet using `MAX(abs_drift)` not `SUM(signed_drift)`. The cold-read's complex of symptoms ("headline = 0 while underlying detail oscillates -$50K to +$60K", "Largest Parent Drift Day = 58,221.979" with 3-decimal noise) all carry the BH.14 + BH.24.1 + BH.1 cascade signatures: 3-decimal currency noise (BH.14), pre-BH.24.1 100× off (BH.24.1), wrong matview drift values (BH.1). After cascade: 3-decimal noise gone, magnitudes correct, drift column = `closing − (opening + signed_net)` consistently. If the cold-read's specific "Latest Snapshot Drift" KPI ever existed in an older deploy + needs revival, the binding would be `MAX(ABS(drift))` rather than `SUM(signed_drift)` per the cold-read's own correct hypothesis.
- [x] BH.5 - **#5 Volume Anomalies KPI=0 vs populated bins** — **DONE 2026-05-25 (option b)**. Renamed KPI "Flagged Pair-Windows" → "Flagged at current σ" + expanded subtitle making the threshold-relative reading explicit ("Zero is expected at the default σ when the population sits below the bar"). BG.4 test references updated. Closes operator-readability misread; the seed-side σ retuning is a separate demo-richness question.
- [x] BH.6 - **#6 Leaf Drift Timelines flat constant** — **CLOSED 2026-05-25 (resolved-by-cascade + verified GREEN by BH.0 probe)**. BH.0 probe against sasquatch.sqlite: 10 leaf-timeline days / 3 distinct per-day Σ values (`{1500, 75, 3075}`) — variance gate (≥2 distinct sums when ≥2 days exist) passes cleanly. The cold-read's "flat at $15 across 30+ days" shape doesn't reproduce on current data. Cold-read was likely against an older deploy (or a thin seed); pre-cascade the 100× bug may have masked the actual values. Post-BH.24.1, BG.3's variance gate trips RED if the constant-line shape ever returns. Closes as "not present on current code; gate catches regression."
- [x] BH.7 - **#7 Recipient Fanout cartesian inflation** — **DONE 2026-05-25**. Window-function fix in `apps/investigation/datasets.py` outer SELECT: replaced bare `j.amount` projection with `j.amount / COUNT(*) OVER (PARTITION BY j.recipient_account_id, j.transfer_id) AS amount`. The divide pre-inflates each row to inflow/sender_count, so SUM across sender rows = inflow_per_pair (correct), not inflow × M (inflated). Works on PG/Oracle/SQLite (sqlite window-functions since 3.25, AO.1.impl targets ≥3.38). **Verified on sasquatch.sqlite**: SUM(amount) went from $22,078,499.69 (inflated, $280K over-count) → $21,797,950.68 (inflation-free truth). BG.4's `test_bg4_recipient_fanout_kpis_match_inflows_only_truth[app2]` now PASSES end-to-end. Test's ground-truth computation updated to be invariant to BH.7's dividing (multiplies amount × rows_per_pair to reconstruct per-pair inflow regardless of whether the dataset projects raw or pre-divided values).
- [x] BH.8 - **#8 Total Transactions vs App Info gap** — **DONE 2026-05-25**. Expanded `Total Transactions` KPI subtitle in `executives/app.py:429` to spell out the predicate ("Count of distinct Posted transfers... multi-leg transfers count ONCE per transfer_id... Expect this to differ from the per-leg / all-status row count the App Info sheet reports"). BG.5's contract gate confirms KPI value still matches dataset SUM(transfer_count). Twin "All legs, all statuses" KPI not added — the subtitle disambiguation alone closes the operator-readability question without adding visual clutter.
- [ ] BH.9 - **#9 Today's Exceptions one-bar dominance** — add a click-to-drill-into-check_type left-click drill action, OR log-scale Y-axis, OR top-N + Other bucket. Presentation, not BG scope; tracked here for completeness.
- [ ] BH.10 - **#10 Executives legend overwhelm** — top-N + Other rail bucketing at the dataset SQL layer (limits wire bytes too). Presentation; not BG scope.
- [x] BH.11 - **#11 L2 Exceptions units mismatch** — **DONE 2026-05-25**. Renamed KPI "Open L2 Violations" → "Distinct Exception Types Open" in `l2_flow_tracing/app.py:1267`; expanded subtitle to make the unit-shift explicit. Renamed detail-table `count` column display_name "Occurrences" → "Violations per Type" in `l2_flow_tracing/datasets.py:407`. Both halves' contracts (KPI = COUNT(rows), table column = SUM(occurrences)) preserved; the rename makes the kpi-vs-table magnitude difference legible instead of confusing.
- [ ] BH.12 - **#12 Internal Overdraft KPI=0** — investigate `.count()` resolution (COUNT vs COUNT DISTINCT) between QS + App2; verify KPI + table bind the same dataset. BG.3's `test_bg3_overdraft_kpi_matches_matview_count` is the gate.
- [ ] BH.13 - **#13 Pending Aging triple disagreement** — unify the chart + KPI + table on one dataset; if separate datasets are required, add explicit subtitles ("All ageing" vs "Open ageing"). BG.6's triple-identity gate.
- [ ] BH.14 - **#14 3-decimal currency standardization** — audit `currency=True` callsites + verify the format string emits `$#,##0.00` not `$#,##0.000`. Parser-layer gate (`_kpi_parse.parse_currency_kpi`) is the trip point; sweep until clean.
- [ ] BH.15 - **#15 Empty-state discipline** — **DEFERRED** for design (not in pre-release cold-read window). Extract L2FT Transfer Templates' empty-state banner into a `common/tree/` helper + apply to Daily Statement / Money Trail / Account Network / Recipient Fanout. Will revisit after next cold-read identifies which sheets are highest priority.
- [x] BH.16 - **#16 L1 getting-started typo + leaked seed-config** — **CLOSED NOT-IN-SOURCE (2026-05-25)**. Strings `CELIBERATE` / "90-day window" / "1.5 ledgers" / "Part I/II" don't exist in `src/recon_gen/`. Cold-read prose came from operator-owned L2 yaml description fields, not generator code. No generator fix to apply; operator content is operator-owned.
- [x] BH.17 - **#17 Executives `(vL1.5.21*)` build-note** — **CLOSED NOT-IN-SOURCE (2026-05-25)**. String `vL1.5.21` doesn't exist in `src/recon_gen/`. Same shape as BH.16 — operator-owned content.
- [x] BH.18 - **#18 App Info Matview Status only 2 matviews** — **CLOSED ALREADY-RESOLVED (2026-05-25)**. Current code already passes 12 matview specs from `l1_matview_specs(cfg)` (see `apps/l1_dashboard/datasets.py:44-75`). Cold-read's "1-2 of 2" must have been against an older deploy where the spec list was thinner. No further fix.
- [ ] BH.19 - **#19 Daily Statement KPI tile text-clipping** — **DEFERRED** (needs browser-render inspection + responsive-CSS work; not in pre-release window). Size-to-content + abbreviation on narrow widths.
- [x] BH.20 - **#20 Unbundled Aging KPI label rendering** — **DONE 2026-05-25**. Title changed `"Stuck Unbundled — $ Exposure"` → `"Stuck Unbundled Exposure"` in `apps/l1_dashboard/app.py:1267`. The `$` prefix lives on the value side via `currency=True` formatting; the title no longer carries a redundant literal `$` glyph that read as concatenated.
- [x] BH.21 - **#21 App Info dialect token leak in prod** — **DONE 2026-05-25**. `common/sheets/app_info.py:346` now appends `" (dev build)"` to the `dialect:` line when `cfg.dialect is Dialect.SQLITE`. PG / Oracle deploys render bare `dialect: postgres` / `dialect: oracle` (those ARE prod dialects); sqlite gets the explicit dev-build disambiguator so cold-read agents don't mistake a local capture for a prod deploy.
- [x] BH.22 - **Plant-coverage `ach` mystery rail** — **DONE 2026-05-25 (minimum-viable)**. Replaced all 6 `rail_name="ach"` hardcodes across `common/spine/{account_simulation,drift,anomaly,chain_parent_disagreement,inv_fanout,money_trail}.py` with `rail_name="_spine_plant"` — leading-underscore sentinel self-documents the source as synthetic spine-plant emission (not real ETL). Plant-coverage agents reading the dashboard now see `_spine_plant` rows and know they're synthetic, not a partial-name leak. Full BH.22 (thread an actual L2-declared rail through plant_adapter at adapt-time) is a follow-up; for the cold-read window the sentinel rename closes the misread.
- [ ] BH.24 - **NEW (2026-05-25, surfaced by BG.7 browser run)**: **App2 KPI 100× cents-vs-dollars double-conversion** — pervasive: 27 `cents_to_dollars_sql` × 27 `currency=True` KPI bindings on L1 alone, plus 23 more across Inv/Exec/L2FT. **Option C chosen 2026-05-25** ("the more I think of it, the more this smells like a typing issue too" + "biggest complaint in the cold read better we fix the pervasive problem now"): contract-declared storage shape, with type-system reinforcement via existing `Cents` (`common/money.py`).

  Root cause: `_measure_sql` (`common/html/_visual_sql.py:100-105`) divides currency measures by 100 assuming AO.1.impl's "matview is BIGINT cents" storage convention. Production datasets (L1/Inv/Exec/L2FT) pre-convert via `cents_to_dollars_sql` in their CustomSql — projected column is already dollars. App2 then divides again → 100× off. QS happens to render correctly because QS just formats (no divide).

  Verified via BG.2 sasquatch-sqlite probe: rendered Opening Balance = -$11,993.10 vs matview Opening = -$1,199,309.68 (exact 100× ratio).

  - [x] BH.24.1 - **DatasetContract storage-shape foundation**. **Done (2026-05-25)**: `Storage` enum (`DOLLARS` default, `CENTS` opt-in) + `currency: bool` + `storage: Storage` fields on `ColumnSpec`. `_measure_sql` (`common/html/_visual_sql.py`) gets new `_wants_cents_divide` helper that gates on `storage=CENTS`. `_apply_cents_to_dollars` (`_tree_fetcher.py`) gets new `_resolve_money_columns` requiring BOTH `currency=True` on visual AND `storage=CENTS` on contract. Backwards-compat: no contract or contract w/o annotation → pre-BH.24.1 heuristic. Verification: BG.2 App2 leg against sasquatch.sqlite — Opening Balance KPI was -$11,993.10 (100× off matview -$1,199,309.68); after fix the identity assertion passes (rendered matches matview). Test now correctly trips on the NEXT layered assertion — the strengthened narrative-formula gate catching v5→v6 net_flow bug (matview drift -$37,480.76 ≠ formula-from-independent-SUM $300.00). **The pervasive 100× bug is gone across the entire L1/Inv/Exec/L2FT surface** in one architectural change.
  - [ ] BH.24.2 - **Sweep + annotate every currency column on every contract** — **DEFERRED 2026-05-25 (housekeeping)**. ~50 ColumnSpec callsites across `apps/{l1_dashboard,investigation,executives,l2_flow_tracing}/datasets.py`. Production behavior is ALREADY correct via the contract-default `storage=DOLLARS` (matches today's pre-converted-in-SQL pattern); the annotation is for documentation hygiene + safety when a future migration introduces raw-cents projections. Defer until BH.24.4 lands (the Cents-typed read boundary will benefit from explicit `storage=CENTS` annotations on the AO.1 storage-shape columns).
  - [x] BH.24.3 - **Verify systemic fix** — **DONE 2026-05-25 (via BG cascade)**. BG.2 + BG.4 PASS end-to-end on sasquatch sqlite App2 leg — direct evidence the 100× systemic bug is fixed across the L1 + Investigation surfaces. BG.3 / BG.5 / BG.6 to be re-fired in the next cold-read iteration window (they were not RED before the cascade; pre-BH.24.1 they would have surfaced different shapes of the same bug). `bh0_probe.py` snapshot remains current (BH.7 + BH.1 are the only fix-shape-changing deltas vs the snapshot; both are already documented in their own cells).
  - [ ] BH.24.4 - **Type-system reinforcement** (per user 2026-05-25 "smells like a typing issue too") — **DEFERRED to a follow-up cell post-BH** (architectural shift that touches every read-boundary in the codebase). Leverage existing `Cents` (`common/money.py`) at the read boundary: for columns declared `storage=CENTS`, auto-wrap raw cursor values as `Cents.from_db(raw)` before they reach the renderer pipeline. Renderers consume `Cents` and project via `.to_dollars()`. Double-divide becomes a pyright-time type error: `Decimal / 100` doesn't yield `Cents`, so chained converts don't typecheck. Big enough to warrant its own phase; BH.24.6 already makes the BG.7-class bug RAISE LOUDLY at runtime (the runtime gate is in place; pyright gate is the next-level reinforcement).
  - [ ] BH.24.5 - **Migration sweep + cleanup** — **DEFERRED** (blocked on BH.24.4). Once Cents-typed read boundaries exist, audit whether the SQL-side `cents_to_dollars_sql` wraps can be dropped. If so, drop the wraps from datasets, mark every currency column `storage=CENTS`, single canonical conversion path. Otherwise document the dual-storage pattern as the new convention.
  - [x] BH.24.6 - **Remove the backwards-compat fallback before phase exit** — **DONE 2026-05-25**. Audited callers: only `tests/unit/test_html_tree_fetcher.py` calls `register_sql` standalone, and ALL its identifiers already have matching module-level `register_contract` calls (zero risk). Removed `_safe_get_contract` helper; replaced both `_wants_cents_divide(column, contract=None)` and `_resolve_money_columns(ds_id=None)` fallbacks with raise-on-missing. `_tree_fetcher.py` call site now uses `get_contract(ds_id)` directly (raises KeyError if dataset unregistered — the actionable signal). Updated `tests/unit/test_visual_sql_wrap.py` to pass a stub `_CENTS_CONTRACT` to the 4 currency=True wrap-shape tests. Full unit suite green (2,840 passed); BG.2 + BG.4 PASS on sasquatch sqlite post-removal. **The "silent fall back to old heuristic" escape hatch is gone** — future double-convert-class bugs structurally raise instead of misbehaving.
- [ ] BH.23 - **Phase exit** — every BG.X cell green; final cold-read pass on the deployed dashboards; archive BG + BH to PLAN_ARCHIVE.md. **Pre-conditions met as of v11.22.0**: BG.2 + BG.4 PASS on sasquatch sqlite; BH cascade (BH.1 + BH.7 + BH.14 + BH.24.1 + BH.24.6 + 11 narrative/closed cells) shipped. **Blocked on**: (a) next cold-read against deployed v11.22.0 (user is running one); (b) BH.9 + BH.10 presentation cells per next-cold-read priority; (c) BH.12 + BH.13 browser-render-dependent verification.

## Phase BF - Editor form completeness + structured pickers

**Sequenced after Phase AM** (user 2026-05-25): AM lands the theme-driven surface vocabulary BF builds on — `bg-surface-alt` for picker empty-states, the `_studio_assets/tw_classes.py` helper module new field-shape helpers extend, and the `field_input_classes()` / `field_row_classes()` patterns the new pickers reuse. Starting BF before AM closes risks reinventing styling conventions AM is mid-flight on.

**Why this phase exists.** Three pre-existing gaps that all hit the same set of forms and share the same fix shape (replace string surface with a structured picker; expose every domain field):

1. **AI.2.e part-2** — operators authoring a rail today see a generic per-kind intro paragraph + `*` markers on required fields. They reverse-engineer the subtype-specific rule set ("single-leg needs a reconciler, Variable rails must attach to a TT…") from validator errors after a failed submit. A subtype-requirements banner up-front would put the rule set on the page before the form is filled. (Folded out of AM.1 to keep AM strictly visual-chrome.)
2. **AI.14(a) BB.2 sub-form completeness** — the create-new sub-form for aggregator-rail / TT reconcilers only renders validator-required minimums. Optional fields (`metadata_keys`, `amount_typical_range`, `firings_typical_per_period`, `description`, `posted_requirements`, `max_pending_age` / `max_unbundled_age`, `bundles_activity`) don't render → an operator's create-new'd reconciler can never match a reference that sets those. `tests/e2e/test_studio_dogfood_browser.py::test_browser_full_create_l2_structural_equality` (piece 3) stays skipped as a result.
3. **AI.14(b) free-form textareas → structured pickers** — `leg_rail_xor_groups_text` (one group per line, comma-separated rail names) is typo-prone + has no autocomplete / visual hint at which rails are eligible. Same anti-pattern on `metadata_keys`, `posted_requirements`, `metadata_value_examples`. Each gets a structured per-{rail,role,key}-checkbox-group picker — typo-proof + the domain auto-completes itself.

The phase closes when `test_browser_full_create_l2_structural_equality` flips from skipped to passing — proving an operator can rebuild any L2 yaml end-to-end through the editor with no driver-side backfill hacks.

**BF.0 Locks** (open for sign-off):

- **L1 — Form-pairing principle scales to fields, not just entities.** BB.0 said "every validator-required entity must be authorable atomically with its dependencies." BF extends: every field the YAML loader accepts on an entity must be exposed in every form where that entity is being authored — including the BB.2 create-new sub-form. Hidden-because-optional is the anti-pattern.
- **L2 — Free-form textareas anti-pattern for typed-identifier domains.** When a field's value space is enumerable from L2 state (rail names, role names, metadata keys declared on a sibling entity), the surface MUST be a structured picker — never a free-form textarea. AB.3.7's `multi_select_groups` field-kind is the canonical template; new pickers extend the existing `_FIELD_SPECS_BY_KIND` machinery.
- **L3 — Theme-driven surface vocabulary.** Pickers use AM's `bg-surface-alt` for disabled / empty placeholder states; no `bg-slate-50` or palette-bypass colors. The `--color-surface-alt` token IS the theme-driven escape hatch.
- **L4 — Banner is additive, not a replacement.** The subtype-requirements banner sits on top of the existing per-kind intro paragraph + `*` field markers — not in place of either. Intro stays generic ("what is a Rail"); banner is subtype-specific ("what does THIS subtype need as a checklist"). Goal is layered discoverability, not consolidation.
- **L5 — Coverage scope: rail + transfer_template only.** account / account_template / chain / limit_schedule create-pages already expose every field via the main `_FIELD_SPECS_BY_KIND` walk; the gap is specifically the BB.2 create-new sub-form for the two reconciler-shaped kinds. Wider coverage deferred unless the BF.6 verify pass surfaces another shape.

- [ ] BF.0 - **Spike** — produce a per-field migration table at `docs/audits/bf_0_field_completeness_spike.md`. Walk `_FIELD_SPECS_BY_KIND["rail"]` + `_FIELD_SPECS_BY_KIND["transfer_template"]`; for each field list (a) current shape on the main create page, (b) current shape on the BB.2 create-new sub-form, (c) proposed shape post-BF. Same exercise for the four free-form textareas — current → proposed picker shape + which sibling field provides the option universe. Sign off on the L1-L5 locks above before BF.1 fires.
- [ ] BF.1 - **Subtype-requirements banner on rail form** (AI.2.e part-2). Per-subtype prose dict (`_RAIL_SUBTYPE_REQUIREMENTS_BANNER`) keyed on `two_leg` / `single_leg`; renders above the field list on `/l2_shape/rail/new?subtype=<...>` + the rail edit page. Single-leg banner names: reconciler attachment requirement (per SPEC §S3 + C3), Variable-direction TT-only constraint. Two-leg banner names: source_role + destination_role + expected_net auto-balance contract. Uses AM-landed bg-link-tint surface for hover-style emphasis without competing with the per-kind intro card.
- [ ] BF.2 - **BB.2 sub-form completeness** (AI.14(a)). Widen `_render_reconciler_section`'s create-new TT-kind + aggregator-kind sub-blocks to render every field on `TransferTemplate` / `Rail` respectively, not just validator-required minimums. Reuse `_render_field` over the relevant `_FIELD_SPECS_BY_KIND` slice with the `reconciler_new_` prefix; the existing `_create_new_reconciler_with_rail` payload-reader already namespaces by prefix so the server side just gets more fields.
- [ ] BF.3 - **Structured picker for `leg_rail_xor_groups`** (AI.14(b) part 1). Replace `leg_rail_xor_groups_text` textarea with a per-rail-checkbox-group picker (mirror the existing `multi_select_groups` field-kind, hoisted to the rail create form via the new picker shape). Option universe = the rail's sibling `leg_rails` (chicken-egg: TT must have ≥2 rails saved before the picker is meaningful — same staged-edit pattern as the existing `_render_multi_select_groups_field` empty-state handles).
- [ ] BF.4 - **Structured pickers for the remaining textareas** (AI.14(b) part 2). Audit + structurize `metadata_keys` (option universe = keys declared elsewhere in the L2 + a small fixed set of canonical names), `posted_requirements` (enum-like), `metadata_value_examples` (key + value list shape — nested picker). Each lands as a new field-kind in `_FIELD_SPECS_BY_KIND` with a paired `_render_<kind>_field` + `_coerce_form` branch.
- [ ] BF.5 - **Driver alignment** (AI.14(c)). Once BB.2 exposes every field, drop the post-create-edit backfill in `StudioBrowserEditorDriver`. The `create_form_data` payload reaches the form natively; `test_browser_full_create_l2_structural_equality` (piece 3) flips from skipped to active in the same change.
- [x] BF.7 - **Persona singleton: yaml-block → structured form** (landed 2026-05-25). `_render_singleton_page("persona")` now dispatches to `_render_persona_form` which decomposes `DemoPersona` into per-field controls: `institution` as 4 labeled positional inputs (name / acronym / region / legacy_entity), `stakeholders` / `merchants` / `flavor` as repeating text-input lists (1 trailing empty slot for add-row; blank rows filtered on save), `gl_accounts` as a grid of `code` / `name` / `note` rows. POST handler routes form data via `_persona_form_to_dict` → yaml.safe_dump → existing `singleton_save_l2` (server validation unchanged). Updated singleton intro prose to explain the structured form. Test updated to POST via form-data dict.
- [x] BF.8 - **Theme singleton: yaml-block → structured form** (landed 2026-05-25). `_render_singleton_page("theme")` dispatches to `_render_theme_form` which decomposes `ThemePreset` into per-field controls. New `_render_color_picker_row` helper renders a paired `<input type="color">` + hex text input + a preview chip — bound JS keeps all three in sync. For bg/fg color pairs (accent/accent_fg, danger/danger_fg, etc.) the preview chip shows the actual contrast (text in fg colour on bg colour) so contrast issues surface visually. UI palette grouped into 4 named subsections with intro prose explaining each role (surfaces+text / brand / state colours / chart axis chips). POST handler routes via `_theme_form_to_dict` → yaml → `singleton_save_l2`.
- [x] BF.9 - **Description preview/edit toggle** (landed 2026-05-25). Added `preview_markdown: bool = False` to `FieldSpec`; flipped True on all 6 description fields (account / account_template / rail / transfer_template / chain / limit_schedule). When `True`, `_render_field`'s textarea branch wraps the textarea in Edit/Preview tabs with a 5-line inline JS toggle. The Preview tab `hx-post`s the current textarea content to `/preview/markdown` (new editor route) which lazy-imports Python-Markdown + returns rendered HTML (extensions: fenced_code + tables). Demo-mode strips the route since the editor form doesn't render there either. Pre-existing `test_get_new_form_returns_full_page_with_intro_prose` updated to scope its `hx-post not in body` assertion to the `<form>` open-tag only (the field-internal preview-tab HTMX is additive UI sugar, not the form submit).
- [x] BF.11 - **Studio top-nav redesign discussion** **— CLOSED 2026-05-29 (BS.3 part 2b).** Dual-nav-shape (home/data render one chrome; standalone diagram renders another diverging in label prefix + link inventory + forward/back arrows) was resolved by Phase BS's flat top-nav design. The inline `← landing` / `→ diagram` / `→ data` / `→ dashboards` link strips in `_render_home_page` / `_render_diagram_page` / `_render_data_page` removed. The shared top-nav (BS.2 + BS.3 part 1) is now the single source of nav truth — landed on the dashboards-listing + per-dashboard + per-sheet pages; full injection into Studio page templates is BS.3 part 3 follow-on. Open sub-asks deferred to BS.3 part 3 / BT (Deploy-on-every-Studio-page is in scope; deployment_name shipped as a chip; iframe vs /panel embedding is a separate question).
- [ ] BF.10 - **Composite-scalar fields: field-level validation + inline help** (per BF.0 L2 user comment 2026-05-25). `amount_typical_range` / `firings_typical_per_period` / `max_pending_age` / `max_unbundled_age` (Duration) stay as `kind="text"` (composite scalar with optional discriminator — out of L2's textarea carve-out scope). User signed off on keeping these as text inputs IF we add field-level format validation + a more explicit inline help/example. Today the spec.helper carries a verbose example but the validator only fires on POST. Add: (a) a small example chip next to the input (e.g. `e.g. 50, 500` for amount_typical_range), (b) optional inline JS that runs the same regex the validator uses + surfaces a per-field warning before submit. Server-side validator is the truth source either way.
- [ ] BF.6 - **Verify + close.** Full unit suite (~2800 tests including new picker unit-tests on `_FIELD_SPECS_BY_KIND` shape) + 4/4 studio browser dogfood tests (including the newly-active structural-equality test) + HTTP dogfood 14 variants. Update `feedback_browser_drivers_user_facing_locators` if any new picker surface needs a locator hook. Tick BF.0-5 + BF.7-10, archive Phase BF to PLAN_ARCHIVE.md.

## Phase BK - v11.22.1 cold-read second-pass + deferrals

Cold-read at `docs/audits/v11_22_1_feedback.md`. Closed in v11.22.3 small-fix batch (2026-05-26): #1 Daily Statement signed-labels, #10 Drift Timelines parent-vs-leaf scope explainer, #14 Overdraft scope label, #17 Open/Active KPI disambiguation, #18 Avg-Daily-Volume integer format (new `Measure.decimals=` kwarg), #19 strip internal-phase-ref language from exec subtitles. Remaining items below survived the small-fix pass — they need design work, fresh data, or operator clarification.

- [ ] BK.1 - **#3 $0.01 customer-DDA closing balance plant**. Cold-read flagged a planted leaf-class account hovering ~$0.01 — suggests the closing balance plant on that role is a magnitude smaller than intended. **Needs operator input**: confirm whether the $0.01 figure is the planted target or a 100× residue from the pre-BH.24 cents-vs-dollars bug. Re-test on a fresh sasquatch deploy under v11.22.3 (cents-vs-dollars cleaned in BH.24 + drift-formula fixed in BH.1 — most likely already resolved). Defer-resolution gate: (a) repro under v11.22.3; (b) if reproduces, identify which spine generator plants it.
- [x] BK.2 - **#5 Drift KPI conditional formatting FIXED 2026-05-29**. Built `KPIValueZeroIndicator` typed primitive — single-value KPIs get an icon-first signal (CHECKMARK / X) plus parallel color channel (green-700 / red-700). Icon is the load-bearing channel (per user: red/green colorblind users in scope). QS rendering via `KPIConditionalFormatting.ConditionalFormattingOptions[].PrimaryValue.Icon.CustomCondition`; App2 parity via `state_icon` (✓/✗) + `state_color` (`text-success`/`text-danger`) payload fields + `bootstrap.js::renderKPI` glyph prefix + color-class swap. Wired on all 5 drift KPIs (Daily Statement Posting Drift + Drift sheet Largest Leaf/Parent Drift + Drift Timelines Largest Leaf/Parent Drift Day). Three layered QS shape gotchas surfaced via the deploy-probe spike, all undiscoverable from boto3 SDK alone — (1) Color must be uppercase hex per `^#[A-F0-9]{6}$`; (2) Expression refs column name not field_id ("Didn't find field <uuid>"); (3) Expression must wrap the column in the aggregation function ("Aggregation Function can not be null"). All three pinned in `tests/unit/test_tree.py::TestKPIVisual::test_bk_2_value_zero_indicator_emits_qs_conditional_formatting` + documented verbatim in `docs/reference/quicksight-quirks.md::§5.0`. Verified end-to-end on AWS via `run_tests up_to=deploy --dialects=pg --targets=aw` — 4 analyses + 4 dashboards CREATION_SUCCESSFUL on both spec_example and sasquatch_pr.
- [x] BK.3 - **#6, #7 carryovers — Today's Exceptions one-bar dominance + Executives stacked-by-rail_name legend/outlier**. Both are presentation issues (one category swallows the chart). Two candidate fixes: (a) log-scale Y axis, (b) "Top 5 + Other" rollup at the dataset layer. Pair with BH.9/BH.10 if those carryovers haven't been retired yet. Open design question — neither is a wire bug.
- [x] BK.4 - **#8 Investigation Money Trail / Account Network blank-by-default**. Standing carryover — sheets need a default-pick (top transfer? planted scenario row?) so they load with content. Design Q: what's the "right" default that doesn't bias the analyst toward a specific case.
- [x] BK.5 - **#9 L2 Exceptions distinct-types KPI vs only ~5 visible bars CLOSED 2026-05-29 (probe + pin; already resolved upstream)**. Probed on local App2 / sasquatch_pr: KPI = 3 ("Distinct Exception Types Open"); BarChart = 3 bars (Chain Orphans, Dead Limit Schedules, Unmatched Rail Name). Math consistent. The cold-read F9 was a CARRYOVER from v11.22.1 — pre-BL.1 the KPI used `.count()` which rendered DISTINCT on QS via the CategoricalMeasureField(COUNT) quirk and equal-counted (= every row) on App2; that produced the divergent KPI=41 / 5-bars shape the cold-read saw. BL.1 flipped the binding to `.distinct_count()` explicitly and BH.11 renamed the KPI title to make the unit clear. Both renderers now agree by construction. Pinned by new `test_bk_5_distinct_types_kpi_matches_barchart_category_count` (KPI.kind == "distinct_count" over `check_type`; BarChart.category[0] dim == `check_type` on the same dataset) — a future refactor that re-flips the KPI back to `.count()` chasing some other quirk fails at unit time. No production code change required this round.
- [ ] BK.6 - **#12 Limit Breach KPI=0 vs 5 rows**. Probed under sasquatch (1/1 correct binding) — does not repro on the bundled L2. Cold-read scenario must be operator-specific. **Needs operator input**: which L2 yaml + which filter combination reproduces the gap on a v11.22.3 deploy.
- [x] BK.7 - **#13 Chains Required Total FIXED 2026-05-29**. Probed: the column `required_total` on `chain-instances-ds` is `SUM(CASE WHEN required='Required' THEN 1 ELSE 0 END)` from the declared-chains CTE, grouped per parent — i.e., **count of L2-declared Required child chains** for this parent. NOT count of transfer legs. Per `_declared_chains_cte` (Z.A grammar): a chain with 1 child marks that child Required (singleton "always fires"); a chain with N children marks them all Optional (XOR siblings — exactly one fires by design). Sasquatch_pr confirms: ACHOriginationDailySweep (1 child) → required_total=1, CustomerInboundACH (2 XOR siblings) → required_total=0. The cold-read's "should be ≥2 for multi-leg chains" conflates "transfer leg" with "chain child". Math is correct; labels were ambiguous. Fix: added `display_name="Required Children Declared"` to `required_total` and `display_name="Required Children Fired"` to `required_fired` on `CHAIN_INSTANCES_CONTRACT`. Subtitle on the Chain Instances table now leads with the disambiguation ("count CHILD CHAINS the L2 declares (not legs of a multi-leg transfer — a Debit+Credit pair is one transfer with two legs, not two children)") and explains why a 0/0 row is normal for XOR-sibling parents. 114/114 L2FT JSON tests green.
- [ ] BK.8 - **#15 cust-019 / Customer 18 off-by-one probe**. Cannot repro in sasquatch (which uses `SNB Customer #0011` naming) or auto-scenario fallback (which binds `cust-{n:03d}` ↔ `Customer {n}` with same `n`). The shape suggests the operator's L2 yaml specifies static Account entries with a typo'd display name. **Needs operator input**: share the L2 yaml row for `cust-019` so we can confirm data-side vs generator-side.
- [x] BK.9 - **#16 Executives Net Money Moved sign-aware FIXED 2026-05-29**. `KPIValueSignIndicator` sibling of the BK.2 zero indicator — ▲ green (ARROW_UP) when value ≥ 0 (inflow), ▼ red (ARROW_DOWN) when < 0 (outflow). Same QS wire pattern, same App2 plumbing, same three shape gotchas applied. Zero treated as inflow (operator-friendly default for "balanced book" KPIs). `KPI.__post_init__` raises if both indicators set on the same KPI — they'd emit competing ConditionalFormattingOptions. Wired on Exec "Net Money Moved" KPI. Pinned by `test_bk_9_value_sign_indicator_emits_qs_conditional_formatting` + `test_bk_9_value_sign_indicator_blocks_mixed_indicators`. Verified on AWS by the same v5 deploy probe that pinned BK.2's 5 drift KPIs.
- [x] BK.10 - **#20 Empty-state discipline**. Several sheets show blank visuals when filters narrow to zero rows. Convention candidates: empty-state banner ("No rows match these filters; try widening the date range"), or pre-loaded counter ("0 of 12,345 rows match"). Cross-sheet design pass — needs a convention before rolling out.

## Phase BQ - BK third-pass: presentation + defaults + empty-state discipline

Three open design items from `docs/audits/v11_22_1_feedback.md` cold-read, locked 2026-05-29 via user decisions:

- **BK.3 (#6+#7)** — one-bar dominance on Today's Exceptions + Exec stacked-by-rail_name outlier. User picked **both**: top-5 + Other rollup on Today's Exceptions (dataset layer; CPA-readable), log-scale Y on the Exec chart (preserves all categories for trend-spotting).
- **BK.4 (#8)** — Investigation Money Trail / Account Network blank-by-default. User picked **largest planted violation** as the default-pick: scenario auto-resolves the highest-magnitude transfer/account on first load. Empty-state caveat needed when no plants exist (covered by BK.10's discipline).
- **BK.10 (#20)** — empty-state discipline across all sheets. BO.3 already shipped the Sankey empty-state pattern. User picked **extend to Table + Bar + KPI + QS parity audit** — propagate the same banner shape, distinguish "0 rows match" from legit "value=0" KPIs, verify QS shows its native empty-state overlay on every visual type.

**Done when:** all three items ticked, unit + json + e2e green, swept to PLAN_ARCHIVE.

- [x] BQ.1 - BK.10 FIXED 2026-05-29: extended BO.3 empty-state to renderTable + renderBarChart + renderLineChart + renderForceGraph. Each fires the same Tailwind-styled centered banner when its data shape is empty (`total_rows===0` for Table; no categories OR no numeric series values for Bar / Line; no nodes/links for ForceGraph). Pinned by `test_table_renders_zero_rows_without_crashing` (updated) + `test_table_empty_rows_renders_empty_state_banner` (new) + `test_barchart_handles_empty_categories_without_crashing` (updated) + `test_barchart_empty_categories_renders_empty_state_banner` (new) + `test_linechart_handles_empty_series_without_crashing` (updated) + `test_linechart_empty_renders_empty_state_banner` (new). Mirrors BO.3 Sankey shape — same Tailwind utility classes + `role="status"` for accessibility.
- [x] BQ.2 - BK.10 FIXED 2026-05-29: KPI empty-state via `shape_kpi(...)` `empty: True` sentinel. Set when `rows` is zero-length OR every row's first column is None (the SUM/MAX/MIN-over-empty-input case). COUNT KPIs returning 1 row with value=0 still render "0" as a legit count. Pinned by `test_kpi_empty_flag_renders_empty_state_banner` + `test_kpi_value_zero_not_treated_as_empty`.
- [x] BQ.3 - BK.10 FIXED 2026-05-29: QS parity confirmed by reading `common/browser/helpers.py::visual_is_empty` — QS mounts `[data-automation-id="visual-overlay-title"][data-automation-context="No data"]` on every visual type (table / Sankey / chart / KPI). Native parity by construction; no gaps to document in the quirks log.
- [x] BQ.4 - BK.3 FIXED 2026-05-29: Today's Exceptions one-bar dominance — applied `log_scale=True` to the check_type chart (5 categories already, top-N would be a no-op). Subtitle notes the log Y axis.
- [x] BQ.5 - BK.3 FIXED 2026-05-29: log_scale knob on BarChart tree primitive. Six new model classes (`AxisLogarithmicScale`, `AxisLinearScale`, `AxisScale`, `NumericAxisOptions`, `AxisDataOptions`, `AxisDisplayOptions`) match the AWS schema's 5-level nesting verbatim. App2 parity via d3 `scaleLog(base=10, domain=[1, maxVal])`. Wired on L1 Today's Exceptions + Exec stacked Period-Total (Volume + Money). Pinned by 2 unit tests + 1 JS Playwright test. AWS deploy probe PASSED on first iteration (no QS shape-gotcha iterations like BK.2's 5).
- [x] BQ.6 - BK.3 FIXED 2026-05-29: top-20 + Other rollup on Exec `rail_name`. `DENSE_RANK() OVER (ORDER BY rail_gross_cents DESC)` CTE + `CASE WHEN rank ≤ 20 THEN rail_name ELSE 'Other' END` outer GROUP BY in `build_transaction_summary_dataset`. Caps legend at 21 entries + preserves long-tail visibility as the "Other" bucket. Verified totals match exactly on sasquatch_pr.sqlite (6714 transfers, $17.4M gross both ways). Portable across PG 17 / Oracle 19c / SQLite 3.38+.
- [ ] BQ.7 - BK.4: Investigation default-pick = largest planted violation. Compute the highest-magnitude transfer_id / account_id at dataset-build time from the scenario context; wire as the analysis-param default for the Investigation pickers. Empty-state from BQ.1 covers the "no plants" path. Test: unit test pins the default-pick computation; e2e test asserts the picker preselects on first load.

## Phase BN - sasquatch + AWS variant browser-layer flakes (BM.5 fallout)

**Trigger** (user 2026-05-28 during BM.5 final verification): the sp_pg_aw browser cell PASSED end-to-end with the BM core + 5 root-cause fixes, proving the BM unification works under live AWS PG load. BUT the sasquatch variants (sq_pg_aw, sq_or_aw) + the Oracle/AWS cell (sp_or_aw) continued to fail with shapes that don't trace back to BM-the-mechanism. Per `project_phases_ab_bm_tech_debt_cleanup`: file as a separate cleanup phase rather than block the BM ship.

**Known failure shapes (BM.5 run #6, commit `c874591d`):**

- **sq_pg_aw browser, 7 tests fail:** Limit Breach Detail empty BEFORE any pick on both [qs] + [app2] (matview HAS rows per anchor SQL, visual renders zero — sasquatch's auto_scenario plant at days_ago=4 should land inside the BM default 7-day window, so cust1 mapping or densify_scenario adding rows outside-window is the suspect); same 2-test pair for `test_l1_dropdown_pickers_inverse_excludes_anchor`; L2FT Rails inverse [qs]; L2FT Templates Completion both [qs] + [app2].
- **sp_or_aw browser, rerun storm (50+ R chars):** Oracle + sasquatch under AWS load — connection-pool churn / lock-timeout interaction / matview-refresh timing. Need timings + per-test error shape captured.
- **sq_or_aw browser:** never finished (chain killed mid-run). Repro likely shares shapes with sq_pg_aw + sp_or_aw.

**Why this isn't a BM regression:** sp_pg_aw (spec_example PG/AWS) passes end-to-end with the same BM code. Differential is sasquatch's seed shape AND/OR Oracle/AWS scaling — independent of BM-the-mechanism. Seed-shape side wants `auto_scenario` adjustments (plants on anchor day, not days_ago=N?); Oracle/AWS side wants pool-size tuning + retry-backoff review.

**Done when:** sq_pg_aw + sq_or_aw + sp_or_aw browser layers green on `./run_tests.sh up_to=browser` (no xfail-to-mute per `feedback_no_xfail_to_sweep_under_rug`); sweep to PLAN_ARCHIVE.

- [ ] BN.0 - Triage spike: snapshot per-failure repro shape against sq_pg_aw, sq_or_aw, sp_or_aw. Output: `docs/audits/bn_0_sq_aw_flake_snapshot.md` with per-test root-cause hypothesis + reproduction recipe. Prereq for BN.1+ fixes.
## Phase PLAN - Phase PLAN
- [ ] PLAN.md - BS.5 — _v_config_chain_children + 7-path conversion

# Backlog (not yet phased)

- **Studio /l2 editor — remove Deploy button.** Surfaced 2026-05-30 during BT.0.5 review. The per-entity edit form (`/l2_shape/<kind>/<entity>`) carries the Deploy-changes button in the page header, but per-entity edit context has no deploy semantics — Deploy belongs on the diagram / data / ETL pages where the operator has full-cfg context. Touch `_studio_editor_routes.py`'s edit-page templates to drop the deploy strip. Cleanup; small.

- **BC.12 deferred: `l2_yaml_raw` / `cfg_yaml_raw` opaque-provenance kv rows.**
  Surfaced 2026-05-24 during BC.12 integration. The original design
  said the kv would hold both per-field decompositions AND opaque-raw
  rows (`key='l2_yaml_raw'`, value=<full JSON>) so the operator could
  SELECT the original yaml back from the DB. **Deferred** because the
  Oracle chunked literal shape (`TO_CLOB(c1) || TO_CLOB(c2) || ...`,
  required to dodge ORA-01704's ~4000-char string-literal cap)
  collides with `batch_oracle_inserts`'s quote-aware coalescer —
  the batcher counts quotes per row to detect VALUES tuple boundaries,
  and the chunked TO_CLOB form has multiple quoted segments per row
  that confuse the count.

  Operators retain access via the `--l2 <path>` source file (the yaml
  on disk is the canonical artifact); matview consumption goes through
  the typed projection views. The provenance feature is the loss.

  Fix shape (when prioritized): either (a) teach `batch_oracle_inserts`
  to handle multi-quoted-segment VALUES tuples correctly, OR (b) emit
  the provenance rows as single-row INSERTs that skip the batcher
  entirely (~1KB SQL per deploy event; negligible cost).

- **`no-raw-str-args` AST lint — extend BC.1's D8 family to bare `str` parameters.**
  Surfaced 2026-05-24 by user during BC.0 sign-off. The codebase already has
  typed string newtypes (`SheetId`, `VisualId`, `FilterGroupId`, `ParameterName`,
  `DashboardId` in `common/ids.py`; `AccountId`, `RailName`, etc.) but a sweep
  for bare `str` parameters across `src/recon_gen/**` would surface every
  callsite that slipped through. Same shape as D8 (`no-raw-temporal-args`):
  AST-walk function/method signatures, flag any param annotated `str` (or
  `str | None`), require migration to a NewType wrapper OR a
  `# typing-smell: ignore[raw-str-arg]: WHY` escape.

  Expected to be a BIG migration — `str` is everywhere. Whitelist surface
  needs care:
  - SQL fragments + column names (low value to wrap; they're already
    SQL-injected territory if the caller is wrong).
  - User-facing display strings (titles, descriptions, error messages —
    these are content, not identifiers).
  - Dataclass field annotations stay unaffected (point values, not policy).
  - Stdlib-facing seams (Click string opts, env-var reads, file paths via
    `os.PathLike[str]`).
  - The `# typing-smell: ignore[raw-str-arg]: WHY` escape with required
    WHY.

  Staged like D8 — disabled during the migration, enabled at end. Probably
  a dedicated phase rather than a sub-task: scale similar to a full
  Phase BC × N.

  Sequenced after BC + BD land (those validate the AST-lint + named-
  constructor pattern is workable on a smaller migration before scaling).

- **BB.2.b — Reconciler picker: inline "create new" sub-form.** BB.2
  shipped attach-existing only (operator picks from existing TTs /
  aggregating Rails). The PLAN's BB.2 spec also called for an inline
  "create new" sub-form so the operator can author the reconciler
  alongside the rail in one atomic save (single picker selection +
  the new reconciler's required fields nested in the rail form).
  Deferred because attach-existing was sufficient to unblock BB.3 /
  BB.4 dogfood (the reference L2's reconcilers always exist; the
  driver test never needs create-new). Operator-UX nice-to-have:
  without this, the operator must create the reconciler separately
  first, then create the rail. Surfaced 2026-05-24.

- **Studio / Dashboards rethink under the post-AW DB-projected
  L2/cfg.** Surfaced 2026-05-23 after AW completed. AW lifted L2 + cfg
  yaml into `<prefix>_config` as DB-resident JSON; matviews now JOIN to
  it for per-L2 values. That changes a bunch of Studio/Dashboard
  questions that deserve evaluation as a unit, not piecemeal:
  - **Studio editing model.** Studio currently writes the L2 yaml file
    only; deploy regenerates the schema + populates the config table.
    Options: (a) yaml stays the only source of truth; deploy projects;
    (b) Studio also UPDATEs `<prefix>_config` on save for "live-reflect"
    semantics; (c) Studio writes the DB; exports back to yaml on
    operator request. AW makes (b) + (c) feasible; whether they're
    desirable is the open question.
  - **"Deployed-vs-edited" Studio view.** Studio could show the
    DB-resident `<prefix>_config` row alongside the WIP yaml edit — a
    "what's deployed vs what you're authoring" diff surface. Useful for
    "this rail's cap was 5000 in prod; my edit sets it to 7000."
  - **Dashboard pickers from L2 yaml** (the originally-queued item):
    pickers that show L2-DECLARED values (vs dataset-derived which
    shows whatever's in the data); pickers that JOIN to L2 metadata
    (descriptions, types, classifications); pickers that survive
    deploys without re-emitting JSON. Caveat: requires changing
    dashboard JSON's filter shape from `StaticValues` to
    `LinkToDataSetColumn`.
  - **Cross-tabular L2 context in dashboards.** Per-rail
    descriptions / per-account roles surfaced in tooltips, drill
    contexts, etc. — directly JOIN to `<prefix>_config.l2_yaml` from
    the dataset SQL rather than baking at emit time.
  - **Customer ETL access.** Operators / customer pipelines may want
    SQL access to "the same L2 values the matview uses" — DB-resident
    is friendlier than parsing yaml.
  - **Auditability.** `<prefix>_config` could carry timestamps for
    "when did the config last replace?" — Studio could surface "L2
    last updated 3 hours ago" as a deploy-state indicator.
  Scope this as its own evaluation phase (likely audit + spike +
  decision per surface); each Studio/Dashboard surface gets a "use
  DB / don't use DB / hybrid" call. Sized after picking a driver
  goal (analyst-friendliness vs operator ergonomics vs Studio
  iteration speed).



- **Q.6 — CLI shape revisit: cfg ⇄ L2 dual-yaml factoring.** Surfaced 2026-05-08 during `Y.2.gate.h.6`. The runner reads `cfg.default_l2_instance` and threads `QS_GEN_TEST_L2_INSTANCE` to subprocesses, making the CLI's dual-arg shape (`-c <cfg.yaml> --l2 <l2.yaml>`) partially redundant. Spike-before-implement (per `feedback_spike_before_locking_implementation`); CLI-surface change touches every operator command + doc example + tests.
  - **Q.6.0 SPIKE: combined-yaml vs cfg-with-L2-pointer vs status-quo** (LOCKED 2026-05-08; deferred from PLAN 2026-05-19). Output `docs/audits/y_11_cli_shape_spike.md`. Four candidates: **(A)** status quo + `--l2` defaults from `cfg.default_l2_instance` (smallest delta, mostly additive); **(B)** single combined yaml (eliminates dual-yaml friction but env-only fields co-mingle with institution-flavor — Q.5 separation existed for a reason); **(C)** cfg-with-L2-pointer + `--l2` removed entirely (forces multi-instance operators to duplicate cfg); **(D)** `--l2 <name>` indexed against an `l2_instances:` registry in cfg + `default_l2_instance:` (named ergonomics, one indirection layer). Constraints: (1) no-args `json apply --execute` deploys the default L2; (2) multi-L2 operators don't copy cfg files; (3) existing `--l2 <yaml>` keeps working or has a documented migration; (4) doc examples shrink; (5) tests pass without env-var passthrough. Likely outcome: A or D — A smallest delta, D cleanest if multi-L2-per-cfg becomes common.
  - **Q.6.1–Q.6.3 implement per spike result.** Updates `cli/{json,schema,data,audit}.py`, `cli/_helpers.py::resolve_l2_for_demo`, every CLAUDE.md / README / handbook example, every `runner.invoke([..., "--l2", ...])` test, every CI workflow YAML that uses `--l2`. Migration warning ≥1 minor version. Sweep memory entries + docs for stale `--l2 <yaml>` refs. Update CLAUDE.md "Commands" block to show the new shape as canonical; keep explicit `--l2` form as the multi-instance/override sub-pattern.
  - **Q.6.4 bump version (breaking CLI change — post-v9.0.0) + RELEASE_NOTES** (deferred from PLAN 2026-05-19) — entry highlighting the simplification + migration recipe.
- **Dashboards-local L1 dashboard render errors (surfaced 2026-05-10, X.2.g.4 territory, NOT a Y.2.g regression).** With the Y.2.g.2.d pool-lifespan fix landed, `dashboards --app l1_dashboard` starts cleanly + the drift KPI fetches data from the live matview, but other L1 visuals throw render errors in Dashboards (smoke + drift KPI work, broader rendering doesn't). Per-visual coverage in `_tree_fetcher` / `wrap_for_visual` — investigation/L2FT shipped via X.2.g.{2,3} with the same pattern, so the gap is L1-specific visual kinds the renderer hasn't grown arms for yet. Triage: capture the failing visual_ids + renderer error, extend `wrap_for_visual` with the missing arms, mirror the Investigation/L2FT shape. Out of Y.2.g scope (Dashboards visual coverage ≠ pushdown SQL); on the X.2.g roadmap. *(deferred from PLAN 2026-05-19)*
- **Ephemeral AWS infra arc** *(consolidated; six related items, deferred 2026-05-19/05-20 per `[[feedback_ephemeral_aws_infra]]`)*. Six sub-tasks, sequence matters:
  1. **Cleanup-scope bugs (3)** — CI/release cleanup currently targets the wrong scope (sweeps `database-2` + QS resources from other runs). Harmless to release-correctness, leaks resources.
  2. **IAM widen** — `rds-start-stop` inline policy → also allow Create/Delete on `recon-gen-local` + `Github_e2e_testing` (DBCluster + DBInstance + DBSubnetGroup + `iam:PassRole` for monitoring + `ec2:` SG/subnet describe+authorize).
  3. **CI fresh-DB-per-run** — create the DB at run start, apply locked seed, drop at end. No snapshots (seeds churn). Runner `up`/`down` lifecycle.
  4. **Local scripts** — same pattern locally; ephemeral per-run DB pointed at the one standing QS account.
  5. **Per-run cleanup verification** — `tests/e2e/test_cleanup_completeness.py` asserts deploy→teardown leaves no DB row or QS resource tagged with the run's `deployment_name`.
  6. **Verify + close** — confirm idle RDS = ~$0 between runs in Cost Explorer.
- **X.10 — Runner: intra-cell layer DAG (deploy starts right after seed).** The per-cell chain `unit → seed_variant → db → app2 → deploy → api → browser` runs strictly serially today, but `db` / `app2` / `deploy` only depend on `seed_variant` — they're siblings. `deploy` is the long pole (~2 min QS async creation); `db` (~45 s) + `app2` (~30 s) fit inside it. Fan `{db, app2, deploy}` with `asyncio.gather` after seed, then gather `{api, browser}` after `deploy` → ~75 s saved per `aw`-target cell (~6 min off a full-matrix run). The `asyncio` plumbing exists (`Y.2.gate.c.6.async`). Sub-tasks: (a) `cell_chain` returns a `{layer: frozenset[deps]}` DAG; (b) `_run_one_variant` topo-sorts + gathers siblings; (c) failure semantics for in-flight `deploy` (boto3 isn't cleanly cancellable — let it finish, report `db` failure, skip downstream); (d) unit tests for the DAG dispatch (topo/sibling-gather/truncation/failure-skips-downstream, mocked layer dispatch); (e) live wall-clock check, pyright, commit; update CLAUDE.md "Commands" chain description.
- **AA.0 Dashboard UX + exception literacy** *(COMPLETE — shipped v10.1.0a1; full plan archived in `PLAN_ARCHIVE.md` → "Phase AA")*. Six follow-ups still queued from that work:
  - **AA.A.10 (stretch) Tree-walk picker→column derivation.** `PickerSpec.column` still hand-mapped; the tree carries the wiring formally (`ParameterControl.parameter → Parameter.mapped_dataset_params → (dataset, dataset_param_name) → SQL <<$p>> site → column`). Either parse the dataset SQL or annotate `DataSetParameter` with a `narrows_column` field at construction. Spike before locking annotation-vs-parse.
  - **AA.A.l2ft rails-inverse.4 Type-encode the `table_rows()` invariant.** `table_rows()` for narrowing-assertion sites is a smell — picker-row-survival is about SQL row count, not DOM visibility. Deprecate `len(table_rows())` for assertion use, or rename to `dom_visible_rows()`.
  - **AA.A.daterange.3 Structural refactor: single DATE_RANGE control.** Replace each sheet's `(DateTimePickerControl from + to + TimeRangeFilter)` triplet with one `FilterDateTimePickerControl(Type="DATE_RANGE")`. Closes "from > to" footgun; aligns L1 / L2FT / Exec with Investigation. **Wall:** L1's multi-dataset-per-sheet model needs a sharing mechanism (Investigation's filter-bound widget binds to ONE filter on ONE dataset). Options: (a) consolidate L1 datasets, (b) one widget per dataset per sheet, (c) QS mechanism driving multiple parameter-bound filters via intermediates. Spike before locking.
  - **AA.A.daterange.4 App2 renderer for widget-bound DATE_RANGE.** Already proven for Investigation; extends to the new L1/L2FT/Exec range controls. Follows .3.
  - **AA.A.daterange.5 Test infra.** `apply_anchor_to_pickers` becomes "set the range to span anchor's date ±1 day" instead of separate from/to. Single picker spec. Follows .3.
- **Model-driven docs (drift reduction).** Headline carried forward; design TBD.
- **Mobile / responsive.** Tailwind handles the layout primitives but no explicit mobile-first design pass. Promote when there's a customer story. Note: dashboards are dense by nature; mobile may always be a worse experience than desktop, regardless of effort.
- **Per-table CSV / XLSX export.** Operators expect "export to spreadsheet" on tables (QS has it). Lower priority than feature parity — punt unless it's a small agent task. The audit PDF already covers the "regulator-ready snapshot" case; spreadsheet export is for analyst self-serve.
- **Drop `_oracle_lowercase_alias_wrapper`; emit dialect-natural identifier case from the generator** (was Y.3.f, parked 2026-05-09). DDL is emitted unquoted (PG folds lowercase, Oracle UPPERCASE → divergent storage); `_oracle_lowercase_alias_wrapper` (`common/dataset_contract.py`) bolts an outer `SELECT qs_inner."ACCOUNT_ID" AS "account_id" ...` so QuickSight (which builds `SELECT "account_id" FROM (...)` from its declared lowercase Columns) finds matching aliases. The proper fix — generator emits dialect-natural case in `DatasetContract.to_input_columns()`, QS quotes UPPERCASE on Oracle natively, wrapper gone — is bigger than it looks: QuickSight's analysis-side validation is *case-sensitive* against `Dataset.Columns` (Y.3.f.2's reverted Oracle-deploy probe surfaced 45 column-missing errors), so it requires case-folding ~30+ analysis-side column refs per dialect (visuals / filters / calc-fields / drills), not just the Columns declaration. The original App2 Oracle column-casing bug it would have fixed was instead fixed narrowly by Y.3.f.alt (`wrap_for_visual` quotes its column refs). Re-spike if the dialect-helper count grows past ~60, or if SQLite gets dropped from the matrix. See `project_qs_analysis_validates_columns_case_sensitive` memory.
- **App2 date picker shows resolved default** — added 2026-05-27 (deferred BL.2.C polish). When the analysis declares a `default_universal_date_range`, the bind layer narrows correctly, but the Flatpickr widget shows "All dates" placeholder — slightly misleading UX. Two paths: (a) pre-fill the visible input's `value` attribute with the default range string (Flatpickr reads it as defaultDate in range mode); (b) emit a `<small>` hint next to the picker showing the resolved range. Skip unless cold-read flags it.
